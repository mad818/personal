name: GitHub Advanced Security

on:
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: "19 4 * * 1"

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  codeql:
    name: CodeQL (${{ matrix.language }})
    runs-on: ubuntu-latest
    continue-on-error: true
    strategy:
      fail-fast: false
      matrix:
        language: ["javascript-typescript", "rust"]

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        if: matrix.language == 'javascript-typescript'
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - name: Install JS dependencies
        if: matrix.language == 'javascript-typescript'
        run: npm ci

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}

      - name: Build JS/TS
        if: matrix.language == 'javascript-typescript'
        run: npm run type-check

      - name: Install Linux deps for Rust/Tauri scan
        if: matrix.language == 'rust'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            curl \
            file \
            libglib2.0-dev \
            libgtk-3-dev \
            libsoup-3.0-dev \
            libwebkit2gtk-4.1-dev \
            libjavascriptcoregtk-4.1-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            libssl-dev \
            libxdo-dev \
            patchelf \
            pkg-config

      - name: Configure pkg-config path
        if: matrix.language == 'rust'
        run: echo "PKG_CONFIG_PATH=/usr/lib/x86_64-linux-gnu/pkgconfig" >> "$GITHUB_ENV"

      - name: Prepare placeholder frontend bundle for Rust scan
        if: matrix.language == 'rust'
        run: |
          mkdir -p .next/standalone
          printf '<!doctype html><title>Nexus CodeQL placeholder</title>\n' > .next/standalone/index.html

      - name: Ensure Tauri icons for Rust scan
        if: matrix.language == 'rust'
        run: node scripts/ensure-tauri-icon.mjs

      - name: Build Rust (Tauri)
        if: matrix.language == 'rust'
        run: |
          cd desktop/src-tauri
          if [ -f Cargo.lock ]; then
            cargo build --locked
          else
            cargo build
          fi

      - name: Perform CodeQL Analysis
        if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false
        continue-on-error: true
        uses: github/codeql-action/analyze@v3

  dependency-review:
    # Enable by setting repository variable ENABLE_DEPENDENCY_REVIEW=true
    if: github.event_name == 'pull_request' && vars.ENABLE_DEPENDENCY_REVIEW == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Dependency Review
        uses: actions/dependency-review-action@v4
