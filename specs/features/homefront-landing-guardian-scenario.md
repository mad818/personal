# Homefront Landing Guardian Scenario

## Goal

Make the public Homefront hero explain the project through a concrete guardian scenario instead of abstract command-room decoration: existing cameras, future patrol drones, local AI classification, and operator-approved response.

## Scope

- Replace the first-viewport hero copy with perimeter/guardian language.
- Replace the abstract command-map hero background with a self-contained animated property scene.
- Show camera coverage, drone patrol motion, event classification, and response stages.
- Keep `/hq` and the private RPG lane out of public positioning.
- Avoid new remote media, autonomous harm, facial recognition, citizen scoring, or forceful action claims.

## Acceptance

- `/` renders the new "Homefront Guards The Perimeter" hero.
- The hero backdrop exposes guardian motion, camera/drone proof, and response-ladder test hooks.
- The response ladder reads as progressive and operator-approved.
- Reduced-motion mode keeps the scene mounted without layout overflow.
- `npm run type-check`, `npm run verify`, `npm run build`, route health, and browser console proof pass before the task is closed.
