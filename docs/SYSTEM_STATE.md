# Nexus Prime System State

This file is the canonical source of current reality for Nexus.

Use it for:

- latest shipped tranche
- active blockers
- release posture
- known environment issues
- the active Next Up queue

Do not use it for:

- long historical batch logs
- generalized engineering rules
- stable product intent

## Latest Shipped

- ADAPTIVE-CAPABILITY-ASSURANCE-2026-08-03 is complete as Nexus's verified-outcome learning and capability-truth layer. All 13 canonical assistant capabilities now expose versioned information and action contracts, six honest readiness states, evidence decay, known weaknesses, approval posture, verification and recovery semantics, efficiency evidence, and the strongest safe next action. Completed runs create bounded prompt-free receipts in an ignored local store; repeated current failures can propose a deduplicated lesson, but only evidence-backed operator approval can place it into future context. Protected assurance and compatibility APIs fail honestly, and one shared operator panel is reachable from COMMAND, Skills, and Field Manual. Missing forecast and scheduler execution paths now render unavailable instead of offering absent actions, while the current RECON status route is restored. Focused contract/API/reinforcement/UI proof, canonical verification, the optimized 41-page production build, performance budgets, authenticated Edge QA, publication and path safety, quality, and both JavaScript/TypeScript and Rust CodeQL pass on PR 70. The local QA receipt and browser artifacts remain ignored or removed; no prompt/answer persistence, autonomous permission, new provider, dependency, deployment, phone/PWA work, or unrelated redesign entered scope.
- README-PROJECT-SHOWCASE-2026-07-30 is complete as the strongest truthful public showcase of Nexus Prime. The rebuilt README leads with operator outcomes, a privacy-reviewed 1540x580 production capture of the Field Manual cross-surface capability audit, the existing eight-surface route-art tour, five specialist agents, local-first trust boundaries, verified startup, local/BYOK AI options, architecture, desktop support, and contributor checks. The final image contains no credentials, private vault/device state, local paths, phone setup, or unavailable-data banner; its SHA-256 is `BC8A3624302C593974786796F3A3A919E6A641FF0B075607C13E96D07B660E03`. The one-off capture workflow and script are absent from the final tree. All README targets resolve; publication safety, TypeScript, the optimized 39-page production build, focused documentation/security/runtime checks, and canonical `npm run verify` pass. PR 69 was squash-merged to `main` as `cd0654ec`, preserving the final scope behind the exact title `docs: publish Nexus Prime showcase and retention baseline`.
- REPOSITORY-RECORD-CLEANUP-2026-08-02 is complete as a dependency-aware tracked-evidence cleanup. Forty-four recurring timestamped reports are collapsed into seven new stable `*-latest.json` artifacts plus the existing CP2 latest artifact, reducing `docs/metrics` from 62 to 25 tracked files; the new retention validator prevents recurring snapshot accumulation. Executable `tests/e2e`, current task authority, task-specific evidence, runtime histories, the SBOM, the historical `35373eab` RPG-retirement commit, and archive quarantine required by current validators remain intact. Metrics retention, archive quarantine, dependency security/risk, infrastructure hardening, desktop trust, phone-contract, script reachability, documentation, publication, TypeScript, production build, canonical verification, and final remote GitHub gates pass. No application source, executable test, desktop runtime, phone/PWA implementation, provider behavior, secret, or private data changed in this cleanup.
- AURORA-SHELL-RELEASE-CANDIDATE-CLOSURE is locally complete as the canonical Homefront Aurora shell across all eight GA routes. The redesign consolidates a slim global rail, one dominant route workplane, proportional wide-desktop layouts, restrained obsidian/ice-cyan glass, bounded entry motion, and one reachable `TrustOperationsRail`; the obsolete duplicate `TrustPostureStrip` is retired. `DESIGN.md` remains the token source and deterministically reproduces the tracked CSS and runtime outputs. Focused design, smoothness, accessibility, TypeScript, lint, exact-scope formatting, publication, and security gates pass; canonical `npm run verify` passed; the optimized 39-page production build, performance budget, and secure-start check pass; owned operational smoke and authenticated release smoke across `/hq`, `/command`, `/intel`, `/alpha`, `/cyber`, `/recon`, `/vault`, and `/resources` pass with port cleanup. The repository-wide formatter's final rerun reports only 23 excluded CRLF/status-only paths with no Git content diff; those paths remain untouched and unstaged. Mario authenticated into the local runtime, confirmed that the interface displayed correctly, and explicitly accepted that manual confirmation as this release's visual gate. Automated Browser DOM, screenshot, console, network, keyboard, focus, reduced-motion, and multi-viewport evidence was unavailable because local-URL policy rejected page reads, so none of it is claimed as passed. Phone/PWA acceptance remains deferred, and the wider product release posture remains unchanged.
- OPERATIONAL-STARTUP-AND-SKILL-LINKAGE is locally complete as the supported local readiness and skill-routing spine. One tracked `NexusPrime.bat` delegates to `npm run operational:start`, which validates Node/npm majors, dependencies, the single ignored `.env.local` token posture, runtime mode, and port; reuses only an exact Homefront `/api/health` response; opens HQ only after verified health; and exits with a specific recovery action for missing prerequisites, foreign ports, child exit, or bounded timeout. `--check --json`, `--no-open`, and `--smoke` provide non-secret inspection, headless operation, and repeatable start/health/owned-cleanup proof. Windows cleanup uses a fixed managed stdin handshake so `start-runtime.mjs` stops its direct Next.js child without external process-tree authority. Development and post-build standalone smokes reached health with boot IDs, acknowledged managed shutdown, and independently released ports 3190 and 3191. The four July project skills now resolve through a typed registry, root instructions, the production workflow router, Company Map source paths, and per-agent live capability injection. Focused operational, skill-intake, dependency-graph, instruction, Company Map, script-reachability, publication, security, documentation, TypeScript, lint, formatting, and diff gates pass. Canonical `npm run verify` passed with exit code 0 in 236.5 seconds; the optimized Next.js production build, post-build performance budget, and `secure:start -- --check` also pass. No package, phone/PWA path, provider call, secret output, deployment, or unrelated redesign file was added to this lane; publication evidence remains separately recorded during finalization.
- GITHUB-X-SOURCE-CAPABILITY-CLOSURE is locally complete across all 83 tracked source matrices. The deterministic closure report now records 524 implemented/adapted capabilities, 293 explicit security/legal/license/free-local/product-purpose exclusions, and zero pending or open matrices. Safe project-native tranches add or finish workflow contracts and scheduling, encrypted VAULT paths/history/receipts, design-token export, local usage analytics and episodic-memory benchmarking, typed orchestration, project graph communities/explanations/paths/export, academic and 30-day research contracts, passive OSINT casefiles, bounded public-profile lookup, SEC Companyfacts extraction, four evidence-separated financial research lenses, visible bull/bear synthesis, and manual deterministic risk sizing. Unsafe mass identity profiling, proxy-chain evasion, automatic person dossiers, autonomous financial execution, transcript scraping, fabricated backtests, unbounded workers, paid-only services, incompatible runtimes, and unrelated product shells remain exact exclusions. The retired-product gate now scans the complete active application tree (`app`, `components`, `lib`, `public`, and `store`) and preserves only command/chat HQ focus. Benefits: every imported idea now has a truthful disposition and reachable proof, missing data remains unavailable instead of simulated, the source ledger cannot silently drift open again, and the obsolete product surface cannot re-enter active runtime unnoticed.
- AZURE-OPENAI-CREDIT-LANE is locally complete as an explicit advanced Nexus provider backed by Mario's configured Microsoft Foundry deployment. The protected `/api/ai` route now validates Azure OpenAI endpoint hosts and paths, pins every request to the configured deployment, authenticates server-side with the `api-key` header, and reuses existing token caps, rate limits, Privacy Shield, agent tools, health, status, diagnostics, and Settings readiness contracts. Ollama remains the free/local default; Azure requires the existing paid-provider opt-in plus connected network mode, and phone-token sessions remain local-only. The ignored root `.env.local` is the only local env file and is structurally ready without exposing or tracking its credential. Benefits: eligible Azure credit can fund operator-selected high-capability inference without adding another AI subsystem or weakening local-first defaults. Focused Azure, settings, local-inference, script-reachability, publication, security-boundary, privacy, TypeScript, lint, formatting, diff, and handoff checks pass. Exact staged-scope canonical verification passed in run `2026-07-28T04-07-45Z-a4669637dd` with 18 files, zero overlap, and successful cleanup. A live smoke attempt reached the protected Nexus route, but this Codex sandbox blocked outbound Azure HTTPS with `connect EACCES`, so no remote success or credit consumption is claimed. The unrelated redesign remains untouched and unstaged.
- VAULTWARDEN-LOCAL-SEALED-VAULT is locally complete as an original browser-local privacy capability informed by the current `dani-garcia/vaultwarden` `main` branch and release `1.36.0`. Source truth now distinguishes the compatible Vaultwarden server from cryptography performed by official clients. The existing VAULT Archive chamber now exposes Sealed notes with a strict versioned PBKDF2-SHA-256/AES-GCM-256 envelope, 600,000 iterations, fresh 16-byte salt and 12-byte IV on every seal, authenticated schema context, bounded validation, no persisted plaintext/passphrase/key, and the full create/unlock/add/edit/delete/rekey/manual-auto-lock/export/import/destroy lifecycle. Destructive actions use a labeled accessible approval surface; imported envelopes remain locked and cannot replace existing browser data without approval. Benefits: persisted private-note content is encrypted locally, randomized resealing avoids deterministic envelopes, five-minute and hidden-document locks narrow the plaintext session window, malformed backups fail closed, and encrypted exports provide the only explicit recovery artifact. The UI states the limits: this is not a password manager, sync service, compatible client, recovery system, or audited equivalent and cannot protect plaintext from compromised same-origin code while unlocked. No upstream AGPL code, assets, schemas, protocol, server, account, credential, sharing, or network runtime was imported. Focused real-crypto/static, source-parity, Company Map, accessibility, TypeScript, lint, reachability, publication, instruction, formatting, and diff checks pass. Exact staged-scope canonical verification passed in run `2026-07-27T09-09-23Z-2f83c5af81` with 16 files, zero overlap, and successful cleanup. The unrelated redesign remains untouched and unstaged.
- MAUTIC-CAMPAIGN-DRAFT-BUILDER is locally complete as an original, bounded adaptation of the current `mautic/mautic` `7.x` Campaign Builder pattern. Workflow Forge now includes a human-gated Campaign Draft Studio and the full scoped editing lifecycle: node add/edit/type/duplicate/remove, drag ordering, visibly named left/right controls, deterministic edge rebuilding, dirty-state feedback, validated save, current-draft clone/export, and saved-state run gating. New built-in workflows merge into existing local state by ID without overwriting operator definitions, and bounded server parsing rejects malformed, oversized, duplicate, dangling, or approval-less human-gated campaign graphs. Benefits: campaign planning becomes visual and repeatable, existing installations receive the template safely, mouse and keyboard paths are equivalent, and malformed drafts cannot become trusted state. The GPL-3.0 source remains pattern evidence only: no upstream code, assets, schemas, styles, text, CRM, contacts, profiling, messaging, tracking, scheduler, provider, or external campaign activation was imported. Focused static/runtime, source-parity, Company Map, accessibility, TypeScript, lint, reachability, publication, instruction, formatting, and diff checks pass. Exact staged-scope canonical verification passed in run `2026-07-27T08-36-22Z-5367d27094` with 21 files, zero overlap, and successful cleanup; the final documented snapshot is included in the exact-scope verification recorded with this shipment. The unrelated redesign remains untouched and unstaged.
- OPENDIRECTORY-GO-TO-MARKET-SKILL-ATLAS is locally complete as a guarded project-native adaptation of the current `Varnan-Tech/OpenDirectory` portfolio. Connected primary evidence replaces the obsolete general-directory description and three-capability matrix with the current 62-skill founder/GTM catalog across eight categories. Fifty-eight safe workflows now resolve into complete visual, content, launch, market-intelligence, respectful outreach, buyer-research, developer-communication, and opportunity-research operating contracts with unique purpose, prerequisites, inputs, ordered workflow, guardrails, acceptance checks, exact source links, and honest native/source/connector/host/dependency availability. The existing Skill Library exposes responsive search and filters plus complete selected detail; bounded read-only list/resolve tools give agents the same contracts. Cold-email guessing/enrichment, maintainer lead harvesting, authenticated-session login-bypass scraping, and competing `CLAUDE.md` generation remain exact exclusions, while the upstream CLI, skills.sh, plugin, marketplace, desktop ZIP, dependencies, prompts, code, assets, and templates were not imported. Benefits: launch and marketing work is discoverable and repeatable, prerequisites cannot masquerade as connected execution, and every source read, render, provider, file, account, message, ad, post, PR, dependency, purchase, or publication action stays on a separately protected path. Focused static/runtime, Company Map, source-parity, script-reachability, tool-security, instruction, publication, TypeScript, lint, scoped formatting, diff, and exact-scope canonical isolated verification pass. The unrelated redesign remains untouched and unstaged.
- MATT-POCOCK-ENGINEERING-FLOWS is locally complete as a project-native adaptation of the current `mattpocock/skills` portfolio. Primary evidence corrects the stale 20-item matrix to the current 22-skill contract: 13 explicit user-invoked orchestrators and nine model-invoked reusable disciplines. Nexus now owns those 13 explicit entrypoints plus bounded prototype, domain-modeling, and intent-preserving merge-conflict skills; six existing production-engineering disciplines provide the interview, diagnosis, source-research, TDD, interface-design, and two-axis review behavior without duplication. User-only metadata prevents accidental orchestration, the exact dependency graph is acyclic, local tasks/specs/context stay authoritative, disposable experiments cannot enter production, and external tracker writes, global/plugin mutation, subagents, Git history actions, and publication require separate authority. Benefits: operators gain memorable alignment, triage, architecture, spec, ticket, implementation, teaching, and handoff flows while Nexus retains one tested engineering discipline layer and no mutable upstream install. All 16 skill-creator validations plus focused inventory, instruction, hidden-channel/dependency, source-parity, script-reachability, publication, documentation, TypeScript, lint, diff, and exact-scope canonical isolated verification pass. The unrelated redesign remains untouched and unstaged.
- ADDY-PRODUCTION-SKILL-LIFECYCLE is locally complete as a guarded project-native adaptation of the current `addyosmani/agent-skills` portfolio. Primary evidence replaces the stale six-capability matrix with the current 24-skill lifecycle: one router plus define, plan, build, verify, review, and ship workflows. All 24 are tracked under the approved, security-scanned project-skill root with precise trigger metadata, explicit authority boundaries, ordered process, stop conditions, evidence-based verification, and human-facing metadata. Root instructions route relevant future work through a meta-skill whose exact 23 dependencies are validated as acyclic; the procedures guide only work already authorized by the operator and cannot grant tools, subagents, provider/network calls, writes, Git publication, deployment, or secret access. Host-specific installers and competing upstream session hooks remain product-purpose exclusions. Benefits: requirements, slicing, implementation, tests, browser proof, debugging, review, simplification, security, performance, migration, documentation, observability, and release work now share repeatable local contracts without a mutable plugin install or legacy `.claude` authority. All 24 skill-creator checks plus focused catalog, instruction, hidden-channel/dependency, source-parity, script-reachability, publication, documentation, TypeScript, lint, diff, and exact-scope canonical isolated verification pass. The unrelated redesign remains untouched and unstaged.

## Active Blockers

- MAINLINE-RELEASE-AND-REPOSITORY-SURFACE-HYGIENE is merged through PRs 69, 70, 72, and the documentation-only PR 73. The implementation baseline is `ae0eab3c` with tree `cf995c58`; the post-merge record is `84f5521`. Exact-tree canonical verification, the optimized 41-page build, performance, secure startup, Quality, Path Safety, verify-security, and both CodeQL languages pass. Manual GitHub inspection confirms `desktop`, `public`, and `tests/e2e` carry the current RC1 baseline label, and Dependabot, code scanning, and secret scanning each report zero open alerts. Repository settings now enforce squash-only merges, PR-title/PR-body squash messages, and automatic merged-branch deletion. Only separate approval for the `v1.0.0-rc.1` tag/release and the final audit remain; no history rewrite occurred.
- AURORA-WHOLE-PRODUCT-VISUAL-AND-FETCH-RELIABILITY is implementation-complete and automated-release-complete but still awaits Mario's visual nuance confirmation for this new pass. The shared shell now adapts the supplied near-black editorial/liquid-glass reference across all GA routes, and eight active feed proxies plus their clients distinguish total upstream failure from verified empty data while retaining prior verified state. Focused feed, type, lint, design, motion, accessibility, performance, publication, security, exact formatting, optimized 39-page build, authenticated eight-route smoke, explicit eight-feed non-2xx failure smoke, and owned-port cleanup pass. Exact staged-scope canonical verification passed in run `2026-07-29T12-02-19Z-173b49c271` with 29 files, zero overlap, and successful cleanup. Automated Browser DOM/screenshot evidence is unavailable under the current local-URL policy and is not claimed; the prior Aurora release's manual confirmation does not count as acceptance of this new visual layer.
- This Codex shell currently cannot reach GitHub over port 443, so `npm run git:safe -- push` failed before this tranche could publish. Use `npm run repo:sync:status` to inspect ahead/dirty state without network, then run `npm run git:safe -- push` from normal PowerShell when GitHub connectivity is available; do not claim the branch is synced until that succeeds.
- Phone/PWA acceptance still requires a physical phone or iPad on the same trusted network even though the receipt engine, assistant proof bridge, live status panel, capture-status artifact, direct-HQ QR handoff, one-command local preflight, no-network repo sync status, and latest-capture report are now implemented: run `npm run repo:sync:status`, run `npm run ops:preflight`, scan the `Scan direct HQ` QR or open the printed LAN HQ URL, log in with `NEXUS_TOKEN`, let the Free Local Readiness panel create the mobile receipt, send `ping`, ask one local Ollama prompt, watch live receipt proof turn ready, install the PWA, rerun capture, then run `npm run phone:acceptance:report` until `acceptanceReady: true`.
- The wide readiness rollup is not release-candidate ready yet because `docs/metrics/readiness-rollup-latest.json` links a blocked phone acceptance artifact, release diagnostics used local fallback with routes down, Docker is unavailable, and the local Dependabot audit lacks a current exported alert payload.
- Local Docker proof is still blocked because Docker is not currently available on this machine.
- The target-runtime gate now reads repo-root `.env.local`, but the current project-local config still does not define `NEXUS_RELEASE_BASE_URL`, so the combined `FD2`/`FD3` proof remains blocked until the real Coolify host is added there.

## Release Posture

- Current stage: late internal beta / release-candidate hardening for the web lane.
- Current active priority: obtain separate approval for the `v1.0.0-rc.1` tag and GitHub release, publish only after that approval, and complete the final release audit. Phone/PWA acceptance remains deferred.
- Current wide rollup status: blocked, not release-candidate ready, with sanitized evidence in `docs/metrics/readiness-rollup-latest.json`.
- Highest-value milestone: first web deployment proof, not another major feature tranche.
- First release remains web-only and GA-scope only: `/hq`, `/command`, `/intel`, `/alpha`, `/cyber`, `/recon`, `/vault`, `/resources`.
- Desktop remains the second release wave after the web lane is proven in staged hosting.

## Known Environment Issues

- The in-app Browser can attach to a local `127.0.0.1` Nexus tab while still rejecting DOM, screenshot, console, and network reads under local-URL policy. For the Aurora closure only, Mario explicitly accepted authenticated manual visual confirmation as the release visual gate; automated Browser evidence remains unavailable, is not represented as passed, and should be rerun in a policy-permitted environment for future visual changes.
- `handoff:pull` now uses `scripts/git-with-acl-repair.ps1` so the known `.git` DENY ACL is removed before Git runs. Normal PowerShell previously proved GitHub connectivity and push, but this Codex shell currently cannot reach GitHub port 443; inside the Codex sandbox, `npm run repo:sync:health` may still show the sandbox-applied DENY between tool calls, so use `npm run git:safe -- <git args>` for Git operations and retry publish from normal PowerShell when network connectivity is available.
- The original checkout at `<repo-root>` is back on `main` and is now the canonical local coding/runtime folder again. The previous dirty root state is preserved on `codex/root-preserve-before-main-restore-2026-04-21`, and the former dedicated `main` worktree now lives on `codex/mainline-runtime-archive-2026-04-21` for rollback/reference.
- The merged `main` runtime launcher prefers `.next/standalone` and mirrors `public/` plus `.next/static/` into it before boot; if a local Next build does not emit standalone output or a `BUILD_ID`, `scripts/start-runtime.mjs` now falls back to a repo-root Next runtime so local review on `http://localhost:3100` can still become healthy.
- In this shell harness, `npm run runtime:launch:3100` can time out even when the managed runtime is already healthy; check `http://127.0.0.1:3100/api/health` plus `.nexus-runtime.pid` before assuming the launch failed.
- Direct background dev-server launch may be blocked by shell policy in Codex; if `runtime:launch:3100` is killed by a tool timeout, rerun `npm run dev` or `npm run runtime:launch:3100` from normal PowerShell for live phone/iPad acceptance.
- Local browser automation can still hit Windows `spawn EPERM` at either the Playwright worker or Edge launch layer after a successful prior run; this reproduced on 2026-05-02 during `npm run hq:e2e` after build/audit/live routes passed and again during the operational-lights hydrated-browser check. When it happens, record the blocker, keep the managed `3100` runtime/source proof, and rerun the browser suites from a fresh shell before using the result as a product regression signal.
- The canonical root-main review flow is now `npm run build` followed by `npm run runtime:launch:3100` from `<repo-root>`; any merged-main visual change still needs that rebuild/relaunch cycle before the IAB reflects it.
- Docker CLI is unavailable on this machine, so `FD2` cannot be proven locally here yet.
- Target-runtime proof now auto-loads repo-root `.env.local` for `NEXUS_RELEASE_BASE_URL` and `NEXUS_TOKEN`, so `npm run launch:gate:target` no longer requires inline env export when the local operator file is populated.
- The repo-local deployment docs intentionally use placeholder hostnames such as `your-host.example`, `target-host.example`, and `<staging-host>`; no real staging hostname is committed under `docs/`, `scripts/`, `.github/`, or env examples, and the current `.env.local` still needs the operator's real Coolify domain.
- The preserved-branch candidate now has a fresh `.nexus-release-boundary.json` capture on `codex/preserve-main-2026-04-11`, and `npm run release:boundary` plus `npm run launch:gate` both pass again on that captured state.
- Local release proof surfaced and closed two deployment-gate regressions during this session: the forecast and scheduler-efficiency runtime-eval endpoints now have explicit route-policy coverage, and their protected `run` handlers now emit direct no-store protected responses.
- Some local shells still do not expose a working `vitest` binary even though repo-level verification and browser harnesses are green.
- Authenticated Playwright suites should still run sequentially on Windows because they share the `.next-e2e` dist dir, but `type-check` no longer depends on that transient folder and `clean-next` now retries removal before failing.
- In the current Codex sandbox, npm package fetches for the MW4 PixiJS/GSAP dependency set fail with registry `EACCES`; install `pixi.js`, `@pixi/react`, `gsap`, and `@gsap/react` from a network-enabled shell before converting the shipped Canvas/Web Animations fallback into a bundled Pixi/GSAP runtime.

## VX1 Verification Checklist

- Targeted proof: `npm exec -- vitest run __tests__/voiceLab.test.ts __tests__/projectArchitecture.test.ts __tests__/aiAnonymization.test.ts __tests__/vulnerabilityReview.test.ts __tests__/assistantCanonicalRegistry.test.ts __tests__/exactSessionLinks.test.ts`
- Baseline proof: `npm run type-check`
- Surface proof: `npm run hq:e2e`, `npm run route:e2e`, and `npm run tabs:e2e` run sequentially on Windows
- Voice Lab truth: `/resources?view=voice-lab&voiceProject=...` must reopen the exact project, browser speech must remain usable when the local runtime is down, and runtime-backed render/export must never leave stale success state behind after degradation
- Architecture truth: `/api/project?section=graph|ownership|hotspots|security` must stay local-only, normalize repo-relative paths correctly, and keep coupling plus sink/secret heuristics readable inside the compact Resources workbench
- Privacy-shield truth: `/api/ai` anonymization must default to cloud-bound sensitive turns only, keep local providers raw unless explicitly forced, and always de-anonymize operator-visible output before it reaches HQ/CYBER/RECON surfaces
- Vuln-review truth: `workflowId=vuln-review` must keep filing durable `/cyber` artifacts into VAULT compiled pages with exact reopen and a visible repair-lane handoff
- AI-exposure-review truth: `workflowId=ai-exposure-review` must stay passive-first and advisory-only, file durable `/recon` or `/cyber` artifacts into VAULT compiled pages with exact reopen, and never widen into active scanning, endpoint probing, or exploit guidance

## Next Up

- Active queue source: `tasks/todo.md`, `docs/plans/unfinished-ideas-triage-2026-05-07.md`, and `docs/repo-hygiene/local-change-set-map-2026-05-08.md` now define the current completion order; current priority is project security, vulnerability protection, dependency auditability, infrastructure validation, and optimization.
- Security infrastructure hardening is now shipped locally with latest proof in `docs/metrics/infra-hardening-latest.json`: keep using `npm run infra:hardening:audit` for sanitized evidence and `npm run verify` for the full hardening gate. The next security move is GitHub Dependabot metadata classification, then one minimal dependency upgrade batch at a time with `npm run verify` after each batch.
- Free local phone acceptance is intentionally deferred until Mario has time to focus on it. Keep the existing implementation stable, but do not reopen the checklist, QR, PWA, LAN, or mobile-proof lane from the active queue.
- Local AI offline operations retain authenticated desktop proof, visible receipt support, and `npm run offline:local:report`; any remaining phone/browser validation is deferred with the phone lane.
- Git permission recovery is closed for the working flow: use `npm run git:safe -- <git args>` when the Codex sandbox shows `.git` ACL friction, and keep branch cleanup review-gated.
- Dependency-security auditing now has stable current evidence in `docs/metrics/dependabot-security-audit-latest.json` and `docs/metrics/dependency-risk-posture-latest.json`. The local package posture is ready with all three consumer install-hook packages reviewed; the audit remains open because no current GitHub alert export was supplied to this local run. Archived manifests remain quarantined, and historical timestamped evidence stays available through Git rather than the active tree.
- Homefront vehicle/drone readiness is complete for the current no-hardware phase; future work should wait for real hardware or new simulated scenario requirements, and must remain passive/readiness-only with no flight-critical control path.
- AI operator workflow gaps are closed locally; future assistant work should build on `lib/assistantOperatorWorkflow.ts`, the shared chat action model, and the existing `ProposedEditPanel` approval surface instead of adding another mutation path.
- Release engineering stays blocked externally, but the exact FD2/CP2 diagnostics and rollback runbook now exists; the next release move requires the real staged Coolify/VPS hostname, token, and Docker proof before any FD2 completion claim.
- Cleanup follow-through: retire the older replay worktree queue (`m2`/`m3`/`m4` and the temporary UXA3 review worktree) only after there is no remaining need for side-by-side rollback or visual-reference inspection.
- Mainline replay status: `main` now contains the M2/M3/M4 stack plus UXA3 and the S1 runtime/security replay, so no more clean-replay slices are required before normal product work resumes.
- FD2 remains the next external release gate; staged Coolify/VPS proof is still blocked on the real staged hostname being present in repo-root `.env.local`.
- Historical replay reference remains in `docs/plans/m2-fresh-mainline-replay-stack-2026-04-18.md`; the older `m2`/`m3`/`m4` and `uxs45` replay worktrees now serve as rollback/reference material only until they are explicitly retired.
- The successful replay order remains the historical record for this migration: shared shell and taste primitives first, HQ plus route-interior completion second, then trust/auth/protected-action substrate third, followed by UXA3 plus the S1 runtime/security replay.
- UXS4 is now a shipped visual contract, so follow-on UI work should preserve `docs/NEXUS_TASTE_CONTRACT.md` plus `lib/nexusTasteContract.ts` as the taste source of truth unless a future tranche explicitly replaces them.
- Project-local rule remains in force until staging is real: upstream repos are idea sources only, and implementation still lands inside Nexus routes, contracts, state, and docs under `<repo-root>`.
- Repo-boundary rule now remains a shipped contract for the GitHub-repo lane: public GitHub repos only, explicit operator action only, metadata-first only, durable output to VAULT only, and no private repo support, token UX, GitHub write flows, or background operator loops.
- VX1 ship guard: keep Voice Lab, project architecture, privacy shield, and vuln-review inside their current Nexus seams and contracts unless a new tranche explicitly widens them; no new top-level tabs, no provider bypasses, and no remote code-upload service.
- FD2 through FD5 and A7b remain blocked exactly as they are; the shipped UX tranches do not change staged-host, deployment, or forecast-execution posture.
- [x] XR1 — Comprehensive assimilation program: keep Nexus native to its existing shell, routes, governance posture, and trust boundaries while mapping the current external source set into three sequenced subprograms instead of vendoring third-party products
- [x] MR1 — Market Research Workbench: adapt the DefiLeo trader-journaling cue plus reference inputs such as `public-apis`, `ai-engineering-from-scratch`, and the existing `timesfm_companion` contract into governed thesis review, loss review, and emotion-aware market continuity across `/hq`, `/alpha`, and `/vault` without adding autonomous trading or a new top-level route
- [x] MO1 — Memory + OSINT Casefile Loop: adapt `claude-obsidian`, `D4rk_Intel-OSINT-Investigative-Toolkit`, and `public-apis` as native patterns for VAULT memory stewardship plus passive-first RECON/CYBER case progression without bundling third-party binaries, widening the shell into a tool catalog, or relaxing the current advisory boundary
- [x] VR1 — Vehicle + Radar Readiness: adapt `PLFM_RADAR` and reference-only learning patterns from `ai-engineering-from-scratch` into `/vehicle` readiness, onboarding, artifact packaging, and future radar session continuity without adding RF control, flight-critical behavior, or new public routes
- [x] XR2 — Keep the next tranche project-local while polishing reuse, metadata stewardship, exact durable-artifact reopen, and continuity handoffs for the shipped XR1 surfaces without adding new tabs, routes, or external repo dependencies
- [x] MR2 — Market review continuity polish: populate source metadata, reuse strongest prior reviews directly in ALPHA, and reopen exact prior reviews in VAULT without widening into forecast execution
- [x] MR3 — Market Postmortem Coach: extend the existing market-review lane with durable repeat / avoid / next-rule lesson capture across `/hq`, `/alpha`, and `/vault` so exact reopen preserves operator discipline without widening into forecast execution or a new dashboard
- [x] MO2 — Memory + OSINT stewardship polish: reuse strongest prior casefiles, file explicit source/evidence metadata, deepen VAULT cues, and reopen exact casefiles while keeping RECON and CYBER passive-first
- [x] VR2 — Vehicle + radar continuity polish: keep `nexus-vehicle-session-v1` backward compatible while improving radar-readiness guidance, imported bundle review, and exact VAULT handoffs from the existing vehicle artifact lane
- [x] RI1 — RECON Repo Intel: ship a protected public-GitHub metadata brief under `/api/repo-intel`, expose it as a compact RECON OSINT module and `recon-repo-intel` exact-session focus, wire an `analyze_repo` tool into the active agent runtime, and stage GitHub/dependency/competitor repo assessment through HQ without widening the shell into a new tab or code-ingestion surface
- [x] RI2 — GitHub Repo Assimilation Completion: finish the public-safe repo loop by adding explicit repo assimilation through RECON and HQ, exposing an `assimilate_repo` tool, filing durable repo-assimilation briefs into VAULT compiled pages, and upgrading ORBIT handoff to use the saved assimilation brief instead of only the lighter RI1 metadata profile
- [x] RI3 — Public Repo Compare Matrix: add explicit 2–3 repo comparison inside the existing RECON repo-intel lane, expose `compare_repos`, support `/compare-repos`, file durable `repo-compare` briefs into VAULT, and hand ORBIT the saved comparison brief instead of only single-repo intel
- [x] DR1 — NOVA Deep Research Engine: expose a real bounded `deep_research` tool through `/api/tools`, align `/deepresearch` guidance and INTEL/NOVA staging to that runtime, and keep explicit deep-research briefs filing durable `research-workflow` pages into VAULT without widening into a new research product surface
- [x] PC1 — Persona Council Completion: finish the older multi-persona HQ lane by routing Council mode through the existing AI/runtime spine, preserving single-persona prompts for normal dispatch, rendering trustworthy side-by-side council answers with explicit pick-or-merge actions, and adding the missing contract coverage so the feature stops being presentation-only scaffolding
- [x] SO1 — Switch Operator + Provider Resilience Completion: add explicit `/operator` one-shot orchestration, provider-health exact-session focus, shared COMMAND/HQ resilience posture, and actual-provider runtime reporting without inventing scheduler automation or a second provider-routing system
- [x] DU1 — Dynamic UI Contract Completion: finish the existing rules-and-alerts lane by making signal evaluation shell-wide, making dismissals event-aware instead of permanent, rendering the missing top-rail header indicator contract, and proving the behavior with dedicated dynamic-UI coverage before promoting the lane beyond experimental status
- [x] RG1 — Reasoning RAG + VAULT Article Index Completion: keep keyword routing as the cheap fast path, add bounded async reasoning fallback for low-confidence HQ retrieval, enrich saved VAULT clips with claim/entity indexes through the existing AI spine, and reuse those cues across HQ, VAULT, and the memory spine without adding a vector DB, a new public route, or an always-on background process
- [x] VG2 — VAULT Archive Compounding, Capture, and Weekly Synthesis: add local semantic cross-linking and backlink health, explicit dismissible `Save to VAULT` capture prompts for qualifying HQ answers, explicit `/weekly` archive synthesis that files durable `vault-weekly` briefs, and richer Obsidian-style export with frontmatter and wikilinks without widening Nexus into autosave, schedulers, or external graph services
- [x] UX2 — Visual Redesign Across the Core App: redesign the shared shell, HQ flagship composition, and all core tabs into a denser, graphics-heavy, premium operator system without changing routes, exact-session behavior, or assistant workflows
- [x] UX4 — Monochrome Tactical War-Room Redesign: pivot the shell and HQ from the older brass sanctum language into a unified monochrome tactical room with image-led route plates, flatter command-header framing, sharper functional surface naming, and one coherent war-room grammar across the core tabs without changing routes, workflow IDs, or continuity contracts
- [x] UX5 — Creative Work-First Tactical Redesign: absorbed into the shipped UX6 Satellite Ops redesign so the older box-removal and work-first HQ goals now live inside one bespoke whole-app visual system instead of a separate intermediate pass
- [x] UX6 — Bespoke Satellite Ops Redesign: replace the visible UX5 shell objects entirely with a bespoke Satellite Ops command environment built from route-specific operations plates, a new command header, reactive tactical motion, a custom HQ operating table, and a whole-app rail-based visual system that no longer reads like reused shell primitives
- [x] UX7 — Full Satellite Ops Migration and Interactive Experience: finish the redesign as a true full migration by replacing the remaining legacy route module/card wrappers with bespoke workplane/rail/continuity primitives, rebuilding HQ’s 3D scene into a real Satellite Ops command room, and adding richer stateful interaction so no obvious sanctum-era or boxed-dashboard objects remain in first-view route compositions
- [x] VX1 — Local Voice, Privacy Shield, and Repo Security Intelligence: keep Resources Voice Lab trustworthy across runtime-backed and browser-only paths, harden the local Impact graph/ownership/hotspot/security workbench, prove cloud-bound privacy shield behavior inside `/api/ai`, and ship the defensive `vuln-review` workflow with durable VAULT continuity
- [ ] FD2 — Blocked on the real staged Coolify hostname in repo-root `.env.local`; once present, stand up the first Coolify/VPS staging deployment from the repo-root `Dockerfile` with conservative web-self-hosted defaults and the preserved candidate branch `codex/preserve-main-2026-04-11`
- [ ] FD3 — Blocked on FD2 host config; run `npm run launch:gate:target` against that exact host after repo-root `.env.local` carries the real staged `NEXUS_RELEASE_BASE_URL` and local `NEXUS_TOKEN`
- [ ] FD4 — Blocked on staged-host proof; capture remote `/api/diagnostics`, GA route/manual smoke, and the focused A6 CYBER/SKILLS/HQ/COMMAND staging pass on the staged host
- [ ] FD5 — Blocked on staged-host proof; document the first production rollback target and promotion checklist before going live
- [x] A6 — Add shared governance metadata (risk tier, approval requirement, domain tags, operator-only/automation posture) and cyber pack baseline across skills, workflow packs, assistant capabilities, readiness lanes, and CYBER support rails
- [ ] A7b — Blocked forecast execution lane: compare a TimesFM companion provider against the native baseline only after decision-lift measurement and staging proof are stable

## Compatibility Tranche

- `CLAUDE.md`, `tasks/todo.md`, `tasks/lessons.md`, and `docs/AGENT_HANDOFF.md` remain in the repo for compatibility only.
- Historical execution detail remains in `docs/plans/`, `docs/ideas/`, and git history.
