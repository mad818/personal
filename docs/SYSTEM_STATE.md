# Nexus Prime System State

This file is the canonical source of current reality for Nexus.

Use it for:

- latest shipped tranche
- active blockers
- release posture
- known environment issues
- the active Next Up queue

Do not use it for:

- long historical batch logs
- generalized engineering rules
- stable product intent

## Latest Shipped

- NEXUS-COMPANY-MAP is complete as a Nexus-native translation of the supplied “AI company” skill chart. The existing `/skills?view=library` Agency Role Library exposes seven departments across command, engineering, design, research, marketing/social, finance/business, and legal/trust; every department routes through MAX and the existing specialist bench, stages a bounded prompt into HQ, and can copy a portable ChatGPT brief without persistence or execution. Eighteen capability sources carry explicit Nexus-native, Codex-skill, MCP-tool, reference, or translation-required posture. Graphify remains optional knowledge-graph tooling; Last30Days is a preflight-gated NOVA research/marketing source; Emil Kowalski remains one review-first Design source; Frontend Slides supports review-first presentation work; Matt Pocock's current engineering workflows are selectively reviewed with merged PR #505 tracked separately as an in-progress deep-module reference; and David Ondrej's operations, engineering, and research skills are reviewed individually. Duplicate URL assertions prevent repeated links from creating duplicate sources. Scoped repo analyses and honest `in_progress` parity matrices cover the external sources. No new durable agent, route, provider, dependency, hook, API key, browser-session access, paid service, auto-install, deployment, server mutation, automatic publishing, or professional legal/finance claim was added. Proof: focused static/runtime checks, source parity, type-check, lint, full verify, and diff checks passed; rendered localhost QA remains blocked by the in-app browser policy.
- NO-MISTAKES-RELEASE-GATE is complete as the first Nexus-native tranche from `kunchenguid/no-mistakes` `v1.34.0`. `npm run release:gate -- --intent "..."` now requires explicit operator intent, runs ordered diff/handoff/operator-preflight/full-verify checks, inspects local Git readiness, supports `--quick` and `--json`, and reports honest local, remote, and manual blockers without printing or persisting the intent. Focused static/runtime validators are wired into `npm run verify` and the pre-push hook. Nexus does not install the upstream Go daemon, add a Git proxy remote, create worktrees, call external coding agents, auto-fix code, push, open PRs, monitor or mutate CI, rebase, merge, start services, or write evidence. The exhaustive source-parity matrix remains honestly `in_progress` with disposable-worktree isolation, safe mechanical auto-fixes, and durable local evidence pending. Proof: focused release-gate/runtime/source-parity checks, `npx tsc --noEmit`, canonical handoff checks, full verification, and diff checks passed.
- LYRA-PROMPT-FORGE is complete as a named capability inside the existing five-agent architecture. `/skills?view=prompts&focus=skills-prompt-forge` now provides the 4-D optimization method, deterministic AUTO/BASIC/DETAIL modes, Nexus/ChatGPT/Claude/Gemini/Other target shaping, two-or-three-question DETAIL clarification, strict structured output, assumptions and usage guidance, and copy-only results. Rough prompts, answers, and outputs stay component-local and are never written to persisted Zustand state, URLs, VAULT, agent memories, unfinished sessions, or run artifacts. JANSKY routes explicit prompt-optimization requests into the governed exact session without becoming a sixth agent, and the orphaned prompt-recipe registry is restored to live capability context with a verify gate. Proof: focused LYRA/runtime/recipe/taxonomy checks, `npx tsc --noEmit`, lint, production build, full verify, and diff check passed. Rendered localhost browser interaction remains blocked by the in-app browser policy.
- FEYNMAN-HUGGING-FACE-INSPECTION is complete as a bounded public Hub inspection tranche. `lib/huggingFaceInspection.ts` normalizes public model/dataset references, reads public metadata and access posture, returns capped top-level files plus dataset split/schema information, and allows only explicit allowlisted small text-file reads under a streaming 64 KiB limit. The protected `huggingface_inspect` network tool is advertised only for explicit Hugging Face intent; relevant repository URLs add one normalized direct-evidence receipt to Feynman's first parallel collection wave inside the existing eight-read budget. Nexus does not use Hugging Face tokens, attempt gated/private access, run inference/training, clone repositories, or download model weights/binaries. The `hugging-face-inspection` parity row is adapted; Feynman remains honestly `in_progress` with 7 useful pending capabilities. Proof: red focused gate, focused runtime/static/Feynman/source-parity checks, type-check, lint, security boundaries, full verify, and diff check passed.
- FEYNMAN-PROGRESSIVE-RESEARCH is complete as a bounded source-quality upgrade to every Feynman workflow. `lib/feynmanProgressiveResearch.ts` now runs the paper lane and four varied web lanes concurrently, uses structured recency/domain qualifiers, prioritizes authoritative/paper/repository URLs inside an eight-read budget, assesses five-discovered/three-direct/two-high-confidence evidence thresholds, and runs at most one distinct three-query refinement wave using identified gaps and discovered terminology. All work uses settled parallel results so failed lanes do not cancel successful evidence; weak final coverage degrades the Researcher stage and remains visible in reports, synthesis prompts, continuity notebooks, and provenance sidecars. The `parallel-progressive-research` source-parity row is adapted; Feynman remains honestly `in_progress` with 7 useful pending capabilities. Proof: two focused red/green gates, focused Feynman/runtime/source-parity checks, type-check, lint, security boundaries, full verify, and diff check passed.
- FEYNMAN-RESEARCH-CONTINUITY is complete as the next bounded source-parity tranche. Every existing `feynman_research` run now creates a collision-safe local session under `agent-workspace/feynman/sessions/`, emits chronological Researcher/Writer/Verifier/Reviewer notebook events, and finalizes a plan, report, evidence ledger, claim audit, reviewer findings, separate provenance sidecar, self-contained HTML preview, local dependency-free PDF, and session manifest. The existing `feynman_outputs` tool now lists, searches, resumes, and exports only real sessions and VAULT outputs; protected local-only `/api/feynman/artifacts` serves only fixed artifact kinds from validated generated session IDs. Continuity failures degrade silently so the primary research answer still returns. The five continuity/export source-parity rows are implemented/adapted while the parent Feynman parity task remains open. Proof: red focused gate, focused continuity/runtime checks, source parity, type-check, lint, security boundaries, full verify, and diff check passed.
- CODEWHALE-OBLITERATUS-NATIVE-ASSIMILATION is complete under the Nexus-aligned source contract. `lib/runtimeAuthority.ts` now gives the active agent runtime one machine-checkable authority hierarchy, protected free/local and approval invariants, bounded harness profiles, honest interrupted/stale lifecycle semantics, and an evidence-first completion contract; `lib/agent.ts` injects that contract into every active run and records structured continuity receipts on agent-run artifacts. `lib/modelSafetyEvaluation.ts` and protected `/api/model-lab` now provide validated, derived, local-only passive safety evaluation with refusal consistency, policy robustness, coherence, harmless helpfulness, leakage pressure, stability, and usefulness while explicitly disabling model mutation, steering-vector application, safeguard removal, remote telemetry, model uploads, and remote code execution. No CodeWhale runtime or OBLITERATUS AGPL code is vendored. Exhaustive `codewhale.json` and `obliteratus.json` parity matrices are complete. The same pass repaired the earlier MasterDnsVPN runtime fixture to use a reserved documentation IP so publication safety remains clean. Proof: red focused gate, focused source/runtime checks, source parity, network integration runtime check, publication safety, type-check, lint, full verify, and diff check passed.
- ESPECTRE-MASTERDNSVPN-NATIVE-ASSIMILATION is complete under the Nexus-aligned source contract. The existing `/iot` sensor desk now includes a consent-aware ESPectre WiFi-sensing viewer backed by protected normalized telemetry ingest, multi-sensor readiness, simulation, threshold/detector/filter/traffic posture, calibration, and review-required command queues that operator-managed bridges can poll and acknowledge; Nexus stores no raw CSI and keeps the GPL runtime external. The existing Escape legal IP-privacy guard now includes a sanitized MasterDnsVPN readiness panel that validates explicit authorization, delegated-domain/resolver posture, AES/ChaCha20, and an already-running loopback listener. Nexus never creates, launches, contacts, or disguises traffic through a DNS tunnel; rejects XOR; does not treat MasterDnsVPN as an IP hider; and keeps Tailscale/VPN/legal proxy as the supported public-link routes. Exhaustive `espectre.json` and `masterdnsvpn.json` parity matrices are complete. Proof: red static gate, red runtime behavior gate, focused integration/runtime/source-parity checks, type-check, lint, full verify, and diff check passed. The managed `3100` runtime reported healthy, but the in-app browser security policy refused the local target, so live browser rendering proof remains unavailable in this environment.
- LOCAL-ACCELERATION-PLANE is complete under the Nexus-aligned assimilation contract with evidence-backed `100%` Nexus-owned implementation, `100%` offline operational lifecycle, `100%` aligned integration acceptance, and `100%` source capability parity. `lib/localAcceleration.ts` and protected `/api/local-acceleration` expose bounded loopback/tailnet TurboVec and TurboQuant controls; VAULT compiled pages auto-index and semantically retrieve with keyword fallback; and `app/api/ai/route.ts` explicitly routes a separate local `turboquant` vLLM provider. The loopback-only companion bridge uses the exact TurboVec stable-ID contract when available and keeps semantic retrieval operational without dependencies through a clearly reported exact-search local vector fallback plus deterministic private hash embeddings. It also provides native allowlists, collision-checked IDs, concurrent search, private persistence/rebuild state, a tested standard-library HTTP backend, automatic Python discovery, sanitized receipts, and double-gated fixed TurboQuant `proof.py`/`benchmark.py` commands with `shell=False`. The TurboQuant checkout audit pins exact reviewed commit `7ac9b8d165a3f7d5e6df33b0450bc1f88ec0d4d5`, rejects dirty/untracked/ignored checkout content, and records README-advertised scripts absent from that source. `npm run local:acceleration:acceptance:require-complete` proves the required Nexus integration and passes without reproducing either optional upstream project; `npm run local:acceleration:acceptance:require-upstream-runtime` separately fails closed until real TurboVec and TurboQuant runtimes are installed and proven. Latest artifact: status `complete`, optional upstream readiness `0%`/`unavailable`, no required blockers, and three transparent optional environment gaps. No GPL TurboQuant code is vendored into Nexus.
- SOURCE-PARITY-CONTRACT is now the completion boundary for GitHub/X assimilation: `docs/ideas/source-assimilation-contract.md` defines exhaustive primary-source inventory, `docs/ideas/source-parity/*.json` records source version/license/evidence plus implemented/adapted/excluded/pending capability rows, and `npm run source:parity:check` rejects unproved implementation claims, undocumented exclusions, and complete matrices with pending work. Feynman has been corrected from complete to source parity in progress against `companion-inc/feynman` `v0.2.58`. Its secure native foundation, continuity/export tranche, parallel progressive-research strategy, and bounded Hugging Face inspection are shipped, but 7 useful source capabilities remain open: complete paper inspection, workflow-specific contracts, paper-code audit, approved local replication execution, Docker-isolated experiments, measured autoresearch, and enabled recurring watches. Command coverage and planning templates no longer count as full behavioral parity.
- SECURE-RUNTIME-GATE is now complete as the default production startup boundary: `npm run secure:init` creates or preserves a strong ignored local token without printing it; `npm run secure:start` defaults to loopback, isolated networking, paid APIs blocked, high-risk tools blocked, and approval-required high-risk writes; the explicit tailnet/private-network profile requires confirmation before broad binding; and `--check` plus `--full-verify` provide fast or exhaustive startup proof without a development fallback or false Tailscale-only claim. Runtime shutdown signals now propagate to the child server. Proof: weak-token and unconfirmed-tailnet blocks, token initialization/preservation, local and confirmed-tailnet checks, live health/shutdown proof, `npm run secure:start:check`, `npm run verify`, and final production build passed.
- LOCAL-RECOVERY-BUNDLE is now an offline disaster-recovery lane for Nexus-owned private local state: `npm run local:recovery -- create` snapshots only allowlisted Escape state, private Escape assets, and phone acceptance receipts under ignored `.nexus/backups/`; `list` summarizes snapshots; `verify` rejects malformed, missing, extra, modified, symlinked, unsafe, or disallowed bundle content; and `restore` remains a verification-backed dry run unless the operator supplies `--apply --confirm=RESTORE_LOCAL_STATE`, with an additional `--overwrite` requirement for conflicts. Every manifest records relative paths, byte counts, and SHA-256 hashes without printing private file contents. The real host currently reports no snapshots because this workspace has no allowlisted local-state files yet, while isolated fixtures prove the complete create/list/ID-and-path verify/tamper/dry-run/confirmation/conflict/overwrite lifecycle. This pass adds no cloud service, subscription, automatic restore, deletion, retention scheduler, arbitrary workspace backup, env/token/key/git read, UI, public route, provider, dependency, or ARPG change. Proof: the validator failed before the runtime existed; `npm run local:recovery:check`, real list/JSON list, security scan, full verify, production build, and diff check passed.
- DESKTOP-SBOM-GENERATOR is now local, deterministic, and integrated with desktop release readiness: `npm run desktop:sbom` generates the canonical offline CycloneDX 1.5 artifact at `docs/metrics/desktop-sbom.cdx.json` exclusively from committed npm and Cargo lockfiles. The current artifact inventories 1,316 components (841 npm and 475 Cargo) and records a deterministic combined lock digest. `npm run desktop:sbom:check` verifies generator safety, artifact shape, ecosystem coverage, and freshness without network access, installs, lockfile changes, packaging, signing, publishing, secret reads, or product UI changes. Desktop trust-chain status now records the canonical SBOM. Proof: the validator failed before the generator existed, then failed before the canonical artifact existed; SBOM generation/check, trust-chain check, publication safety, security scan, type-check, full verify, production build, and diff check now pass.
- ESCAPE-LEGAL-PRIVACY-ROUTE is now local inside `/resources?view=escape`: `specs/features/escape-legal-privacy-route.md` defines the legal VPN/proxy/Tailscale exit-node boundary, `lib/legalPrivacyRoute.ts` defines the route options and readiness posture, and `components/resources/SecureLinkOpenPanel.tsx` now has a session-only selector for No privacy route, VPN, Tailscale exit node, and Legal proxy plus explicit confirmation before public HTTPS tiles unlock. Private/local/Tailscale links still use the existing safe no-referrer open path, while public links remain locked unless the operator selects and confirms an authorized route. This does not build a VPN, open proxy, relay, tunnel, IP lookup service, anonymity guarantee, DRM/paywall/geo-restriction bypass, public route, dependency, or ARPG change. Proof: red validator failed on missing helper first, then `npm run escape:privacy-route:check`, `npm run type-check`, `npm run lint`, `npm run publication:safety:check`, `npm run privacy:shield:check`, `npm run verify`, `npm run build`, and compiled artifact checks passed.
- PRIVACY-SHIELD-FULL-RUNTIME is now local: `specs/features/privacy-shield-full-runtime.md` defines the full runtime slice, `lib/privacyShieldServer.ts` now protects `messages`, `system`, `tools`, and `tool_choice` before any non-local provider dispatch, and `/api/ai` forwards only the protected payload. Protected `POST /api/privacy-shield/preview` previews sanitized content without provider calls, `lib/privacyShieldClient.ts` and `store/useStore.ts` carry policy/field posture, and COMMAND mounts `components/command/PrivacyShieldPreviewPanel.tsx` inside the existing provider-health disclosure. Route policy marks the preview API as protected `local_only`; headers now expose `X-Anonymization-Policy` and `X-Anonymization-Fields`. This does not create a VPN/proxy/IP hider or make anonymity claims; it is deterministic local redaction/blocking before cloud-bound AI dispatch. Proof: red validator failed on the missing preview API route first, then `node scripts/validate-privacy-shield-preview-cli.mjs`, `npm run privacy:shield:preview -- --check`, `npm run privacy:shield:check`, `npm run type-check`, `npm run publication:safety:check`, `npm run security-scan`, `npm run verify`, `npm run build`, and compiled artifact checks passed.
- PRIVACY-SHIELD-PREVIEW-CLI is now local: `specs/features/privacy-shield-preview-cli.md` defines the no-network preview slice, `scripts/privacy-shield-preview.mjs` adds `npm run privacy:shield:preview`, and `scripts/validate-privacy-shield-preview-cli.mjs` adds `npm run privacy:shield:check` wired into `npm run verify`. The command previews the same runtime shield classes for credential, internal-host, protected-path, and sensitive-evidence markers, reports class counts, dispatch mode, blocked reason, and sanitized preview text, and supports `--sample`, `--stdin`, `--text=`, `--json`, and `--check`. It does not call AI providers, call the network, read files except stdin, write artifacts, read `.env.local`, read tokens/cookies/auth headers/raw receipts, add proxy/VPN/IP-hiding claims, change provider routing, add public routes, install dependencies, or touch ARPG. Proof: the validator failed red on the missing runner first, then `node scripts/validate-privacy-shield-preview-cli.mjs`, `npm run privacy:shield:preview -- --sample`, `npm run privacy:shield:preview -- --sample --json`, `npm run privacy:shield:preview -- --check`, `npm run privacy:shield:check`, `npm run security-scan`, `npm run verify`, `git diff --check`, and `npm run build` passed.
- PHONE-ACCEPTANCE-GUIDE-CLI is now local: `specs/features/phone-acceptance-guide-cli.md` defines the read-only guide slice, `scripts/phone-acceptance-guide.mjs` adds `npm run phone:acceptance:guide`, and `scripts/validate-phone-acceptance-guide-cli.mjs` adds `npm run phone:acceptance:guide:check` wired through `phone:acceptance:receipts:check` and therefore `npm run verify`. The command reads the latest sanitized `phone-local-acceptance-*.json` artifact, prints desktop and phone/iPad HQ URL candidates from local network interfaces, labels each phone proof item as done/missing/unknown, and prints the post-phone command sequence for capture, report, local-AI report, and first-three closure. It also shows the manual capture fallback only as an operator-run fallback after the real phone/iPad actions are complete. This pass does not call the network, launch a runtime, call receipt/provider APIs, write files, read `.env.local`, read raw receipt storage, read or print token values, simulate proof, change UI/product routes, or touch ARPG. Phone/iPad acceptance remains manual until the real device proof is captured. Proof: `npm run phone:acceptance:guide`, `npm run phone:acceptance:guide:check`, `npm run phone:acceptance:receipts:check`, `npm run ops:first-three`, `npx tsc --noEmit`, `npm run verify`, and `npm run build` passed.
- FIRST-THREE-OPERATIONAL-CLOSURE is now local: `specs/features/first-three-operational-closure.md` defines the no-network closure slice, `scripts/first-three-operational-closure.mjs` adds `npm run ops:first-three`, and `scripts/validate-first-three-operational-closure.mjs` adds `npm run ops:first-three:check` wired into `npm run verify`. The command reads the latest sanitized Dependabot audit, phone acceptance, and readiness-rollup artifacts plus root package metadata, then reports each first-three lane with status, blocker class, proof source, and exact next action. Current status is honestly `blocked_or_manual`: `postcss` is still `8.4.39` and needs the normal-PowerShell `postcss@8.5.10` install because this Codex shell cannot reach the npm registry; phone acceptance is blocked by runtime/physical phone/iPad proof; local AI offline proof is blocked by missing phone-side Ollama receipt; and the larger GitHub/X/YouTube pool is already mapped through `docs/ideas/external-links-mapping.md`, `docs/plans/nexus-ideas-assimilation-master-backlog.md`, `docs/plans/unfinished-ideas-triage-2026-05-07.md`, and the Resources source ledger. This pass does not install packages, call the network or GitHub, launch a runtime, simulate phone/iPad proof, read secrets/env/raw receipts, write artifacts by default, change UI/product routes, or touch ARPG. Proof: `npm run ops:first-three`, `npm run ops:first-three:check`, `node scripts/first-three-operational-closure.mjs --check`, `npm run offline:local:report`, `npm run phone:acceptance:report`, `npx tsc --noEmit`, `npm run verify`, and `npm run build` passed.
- DESKTOP-TRUST-CHAIN-STATUS is now local for CP2.3: `specs/features/desktop-trust-chain-status.md` defines the no-network desktop trust-chain status slice, `scripts/desktop-trust-chain-status.mjs` adds `npm run desktop:trust-chain`, and `scripts/validate-desktop-trust-chain-status.mjs` adds `npm run desktop:trust-chain:check` wired into `npm run verify`. The command writes timestamped `docs/metrics/desktop-trust-chain-*.json` records with checksum verification posture, signing posture from committed Tauri config only, SBOM/dependency-inventory posture, blockers, and next actions. The canonical SBOM is now recorded; the current verdict remains `releaseReady: false` because `desktop/dist`, its checksum manifest, and configured signing strategy are still missing. This pass does not sign artifacts, read certificates/private keys/tokens/env values, call the network, install dependencies, run Tauri packaging, change UI/product behavior, or touch RPG work. Proof: `npm run desktop:trust-chain`, `npm run desktop:trust-chain:check`, `npx tsc --noEmit`, `npm run verify`, and `npm run build` passed.
- DEPENDABOT-NEXT-RUNTIME-PATCH-HELPER is now local: `specs/features/dependabot-next-runtime-patch-helper.md` defines the no-network next-patch helper, `scripts/dependabot-next-runtime-patch.mjs` reads the latest sanitized Dependabot audit artifact, skips already-patched direct npm runtime alerts, and prints the next actionable direct patch with the exact normal-PowerShell install command plus proof commands. `npm run dependabot:next-runtime-patch` currently identifies `postcss` alert 85 as the next active direct runtime patch, with current lock `8.4.39` and patched floor `8.5.10`; `npm run dependabot:next-runtime-patch:check` keeps the helper covered through the existing Dependabot validator. The actual `postcss` package update is still pending because this Codex shell cannot fetch `https://registry.npmjs.org/postcss` (`EACCES`), so finish it from normal PowerShell with the printed command. This helper performs no package upgrades, does not run `npm audit fix --force`, and does not touch archive packages, Cargo alerts, UI/product surfaces, auth/provider code, public routes, or raw Dependabot exports. Proof: `npm run dependabot:next-runtime-patch`, `npm run dependabot:next-runtime-patch:check`, `npm run dependabot:audit:check`, `npx tsc --noEmit`, `npm run validate:infra-hardening`, `npm run verify`, and `npm run build` passed.
- MEDIA-ESCAPE-BOOK-LIBRARY is now local inside `/resources?view=escape`: `specs/features/media-escape-book-library.md` defines the book-library slice, `lib/subscriptionEscape.ts` extends the protected media model from movies/music to movies/music/books, and `lib/subscriptionEscapeStore.ts` normalizes saved `book` media plus book secure-link categories. `components/resources/MediaEscapeLibrary.tsx` now has book counts, Add book, book filters, author/series-friendly fields, and a Books shelf; `components/resources/MediaIntakeReviewPanel.tsx` can review `Author - Book Title (Year).epub`-style filenames; `components/resources/SecureLinkOpenPanel.tsx` can classify reading links; and `components/resources/SubscriptionEscapeConsole.tsx` shows the operator-supplied `https://fmhy.net/` source bookmark with a lawful-use note. This pass does not scrape, download, torrent, bypass DRM/paywalls, create a piracy workflow, expose media publicly, add paid APIs, add cloud storage, or add a new tab. Proof: `npx tsc --noEmit`, `npm run verify`, and `npm run build` passed.
- DEPENDABOT-NEXT-RUNTIME-PATCH is now local: the active root app `next` dependency is patched from `^15.5.15` to `^15.5.18`, `package-lock.json` now resolves `next`, `@next/env`, and the optional `@next/swc-*` packages to `15.5.18`, and local `node_modules` reports `Next.js v15.5.18`. Latest dependency evidence is `docs/metrics/dependency-risk-posture-2026-05-25T05-52-21-853Z.json`, and latest imported-alert audit evidence is `docs/metrics/dependabot-security-audit-2026-05-25T05-52-26-760Z.json`. The stale imported GitHub export still lists 12 `next` runtime alerts until Dependabot rescans, so the next active package patch targets remain `postcss` and `prismjs` if they are still open after rescan. Proof: `npx next --version`, `npm run dependabot:audit:check`, `npm run dependency:risk:check`, `npm run validate:infra-hardening`, `npx tsc --noEmit`, `npm run verify`, and `npm run build` passed.
- OFFLINE-LOCAL-AI-REPORT-CLI is now local: `specs/features/offline-local-ai-report-cli.md` defines the no-network local-AI report slice, `scripts/offline-local-ai-report.mjs` adds `npm run offline:local:report`, and `scripts/validate-offline-local-ai-report-cli.mjs` adds `offline:local:report:check` wired into `offline:local:check`. The command reads the latest sanitized `docs/metrics/readiness-rollup-*.json` and `docs/metrics/phone-local-acceptance-*.json` artifacts by default, supports explicit `--rollup=`, `--phone=`, and `--dir=` review paths, and prints desktop local/free posture, phone-side local AI receipt posture, blockers, and next action without opening JSON by hand. It does not call the network, launch a runtime, call providers, call receipt APIs, reach GitHub, call cloud services, install dependencies, write files, change branches, read secrets/env files, read raw receipt storage, or touch ARPG. Proof: validator failed red before implementation, then `node scripts/validate-offline-local-ai-report-cli.mjs`, `npm run offline:local:report:check`, `npm run offline:local:report`, `npm run offline:local:check`, `npm run ops:preflight`, `npx tsc --noEmit`, `npm run verify`, and `npm run build` passed.
- PHONE-ACCEPTANCE-REPORT-CLI is now local: `specs/features/phone-acceptance-report-cli.md` defines the no-network terminal report slice, `scripts/phone-acceptance-report.mjs` adds `npm run phone:acceptance:report`, and `scripts/validate-phone-acceptance-report-cli.mjs` adds `phone:acceptance:report:check` wired into `phone:acceptance:receipts:check`. The command reads the latest sanitized `docs/metrics/phone-local-acceptance-*.json` capture artifact by default, supports explicit `--file=` and `--dir=` review paths, and prints captured time, sanitized base URL, acceptance state, route status, local/free readiness highlights, phone proof counts, live receipt labels, blockers, and next action without opening JSON by hand. It does not call the network, launch a runtime, call receipt APIs, reach GitHub, call cloud services, install dependencies, write files, change branches, read secrets/env files, read raw receipt storage, or touch ARPG. Proof: validator failed red before implementation, then `node scripts/validate-phone-acceptance-report-cli.mjs`, `npm run phone:acceptance:report:check`, `npm run phone:acceptance:report`, `npm run phone:acceptance:receipts:check`, `npm run phone:lan:check`, `npm run ops:preflight`, `npx tsc --noEmit`, `npm run verify`, and `npm run build` passed.
- REPO-SYNC-STATUS-SUMMARY is now local: `specs/features/repo-sync-status-summary.md` defines the no-network sync-reporting slice, `scripts/repo-sync-status.mjs` adds `npm run repo:sync:status`, and `scripts/validate-repo-sync-status.mjs` adds a structural gate wired into `npm run verify` as `repo:sync:status:check`. The command reads only local Git metadata for branch, upstream, ahead/behind, dirty/clean state, and last commit, then prints a next safe action such as commit local changes, run `npm run handoff:pull` when network is available, or publish from normal PowerShell with `npm run git:safe -- push`. It does not fetch, pull, push, call GitHub, probe network, read auth-token config, read private env files, write artifacts, change branches, run destructive Git commands, or touch ARPG. Proof: validator failed red before implementation, then `node scripts/validate-repo-sync-status.mjs`, `npm run repo:sync:status:check`, `npm run repo:sync:status`, `npx tsc --noEmit`, `npm run ops:preflight`, `npm run verify`, and `npm run build` passed.
- OPERATOR-PREFLIGHT-RUNNER is now local: `specs/features/operator-preflight-runner.md` defines the one-command readiness slice, `scripts/operator-preflight.mjs` adds `npm run ops:preflight`, and `scripts/validate-operator-preflight.mjs` adds a focused structural gate wired into `npm run verify` as `ops:preflight:check`. The preflight runner executes existing local npm checks for handoff freshness, offline/local readiness, phone access, phone LAN/QR/receipt posture, publication safety, and static security, continues through all checks, then exits once with a concise pass/fail summary and recovery hints. It does not call cloud services, reach GitHub, check Docker, simulate a physical phone, install dependencies, start background services, read auth-token config, read private env files, collect raw LAN proof, widen public routes, add proxy/VPN/IP-hiding behavior, or touch ARPG. Proof: validator failed red before implementation, then `node scripts/validate-operator-preflight.mjs`, `npm run ops:preflight:check`, `npx tsc --noEmit`, `npm run verify`, and `npm run build` passed.
- PHONE-ACCEPTANCE-QR-HANDOFF is now local: `specs/features/phone-acceptance-qr-handoff.md` defines the operational phone/iPad handoff slice, `lib/phoneHandoffQr.ts` generates a dependency-free QR matrix for the already-visible direct HQ LAN URL, and `components/ui/FreeLocalReadinessPanel.tsx` now renders an inline `Scan direct HQ` QR card inside the existing phone handoff area while keeping the copyable phone and HQ URLs as fallbacks. The QR helper does not call cloud QR services, add dependencies, fetch remote images, embed tokens, store raw LAN proof, open public routes, or claim proxy/VPN/IP-hiding behavior; overlong or invalid URLs fall back to the existing copy flow. `phone:lan:check` now runs `phone:acceptance:qr:check`, and `scripts/validate-phone-lan-readiness.mjs` asserts the QR handoff remains wired. Proof: validator failed red before implementation, then `node scripts/validate-phone-acceptance-qr-handoff.mjs`, `npm run phone:lan:check`, `npx tsc --noEmit`, `npm run verify`, and `npm run build` passed.
- PHONE-ACCEPTANCE-CAPTURE-STATUS is now local: `specs/features/phone-acceptance-capture-status.md` and `scripts/validate-phone-acceptance-capture-status.mjs` add the capture-side proof contract for the existing phone acceptance lane. `scripts/phone-acceptance-capture.mjs` now consumes the protected receipt API `status` payload and writes `receiptLiveStatus` plus label-only `missingReceiptProofItems` into sanitized `phone-local-acceptance-*.json` artifacts while preserving `receiptPhoneProof`, `combinedPhoneProof`, blockers, and manual flags. `scripts/readiness-rollup.mjs` now carries those same fields into the wide readiness rollup. The capture still writes an honest blocked artifact when the local runtime or protected receipt API is unavailable, and it does not store token values, cookies, auth headers, raw LAN IPs, full user-agent strings, screenshots, prompt text, response text, transcripts, file contents, account/payment proof, proxy/VPN/IP-hiding behavior, or ARPG state. `phone:acceptance:receipts:check` now runs `phone:acceptance:capture:check`. Proof: validator failed red before implementation, dry capture to `tmp-codex-runtime` wrote `receiptLiveStatus` and `missingReceiptProofItems` while remaining blocked because the local runtime was down, then `node scripts/validate-phone-acceptance-capture-status.mjs`, `npm run phone:acceptance:receipts:check`, `npm run phone:lan:check`, `npx tsc --noEmit`, and `npm run verify` passed.
- PHONE-ACCEPTANCE-LIVE-STATUS is now local: `specs/features/phone-acceptance-live-status.md` and `lib/phoneAcceptanceStatus.ts` add a client-safe live proof model derived only from the existing sanitized phone acceptance receipt summary. Protected `GET` and `POST /api/phone-acceptance/receipt` now return a `status` payload, and the shared `components/ui/FreeLocalReadinessPanel.tsx` shows live receipt proof for phone opened, phone login, browser storage, ping receipt, local AI receipt, PWA capability, and PWA installed without changing the broader visual design. The panel renders booleans, counts, and sanitized timestamps only; it does not store or render token values, cookies, auth headers, raw LAN IPs, full user-agent strings, screenshots, prompt text, response text, transcripts, account/payment proof, proxy/VPN/IP-hiding behavior, or ARPG state. `phone:acceptance:receipts:check` now runs `phone:acceptance:status:check`. Verification passed with the expected red validator first, then `node scripts/validate-phone-acceptance-live-status.mjs`, `npm run phone:acceptance:receipts:check`, `npm run phone:lan:check`, `npx tsc --noEmit`, and `npm run verify`.
- PHONE-ASSISTANT-RECEIPT-BRIDGE is now local: `specs/features/phone-assistant-receipt-bridge.md`, `lib/phoneAssistantReceiptBridge.ts`, and the shared `components/assistant/AssistantTurnReceipt.tsx` now let Home chat, CommandBar, and HQ chronicle silently mark the existing phone acceptance receipt lane after assistant turns. The bridge only sends sanitized booleans to protected `/api/phone-acceptance/receipt`: `localFastPathReceipt` for local fast-path turns such as `ping`, and `localAiReceipt` when the runtime receipt shows a local/free Ollama-style turn with no paid APIs, no file changes, and no recovery code. It does not store prompt text, response text, transcripts, token values, cookies, auth headers, raw LAN IPs, full user-agent strings, screenshots, account/payment proof, proxy/VPN/IP-hiding behavior, or any ARPG state. `phone:acceptance:receipts:check` now runs `phone:assistant:receipts:check`. Verification passed with the expected red validator first, then `node scripts/validate-phone-assistant-receipt-bridge.mjs`, `npm run phone:lan:check`, `npx tsc --noEmit`, and `npm run verify`.
- PHONE-ACCEPTANCE-RECEIPTS is now local: `specs/features/phone-acceptance-receipts.md`, `lib/phoneAcceptanceReceipts.ts`, and protected `/api/phone-acceptance/receipt` add a local-only sanitized receipt lane for the existing Free Local Readiness panel. The panel now silently records route path, device class, browser-storage readiness, PWA posture, session/token booleans, and network mode without changing visuals and without storing token values, cookies, auth headers, raw LAN IPs, full user-agent strings, screenshots, account/payment proof, or public exposure. `scripts/phone-acceptance-capture.mjs` now fetches receipt summaries, writes `receiptPhoneProof` plus `combinedPhoneProof`, and still allows manual flags for ping, local-AI, and PWA proof. `data/phone-acceptance-receipts*.json` is ignored local storage, `/api/phone-acceptance/receipt` is route-policy covered as protected `local_only`, and `phone:lan:check` now runs `phone:acceptance:receipts:check`. Verification passed with the expected red validator first, then `node scripts/validate-phone-acceptance-receipts.mjs`, `npm run phone:lan:check`, `npx tsc --noEmit`, `npm run verify`, `git diff --check`, and `npm run build`; a dry capture to ignored `tmp-codex-runtime` produced the new receipt fields but remained `acceptanceReady: false` because no `3100` runtime or physical phone/iPad receipt was available. Live `3100` proof is not accepted from this shell: `runtime:launch:3100` printed healthy, then the tool timeout killed the child runtime, and direct background launch was blocked by shell policy.
- ESCAPE-IP-PRIVACY-GUARD is now local inside the `/resources?view=escape` stream connect shelf: `specs/features/escape-ip-privacy-guard.md` and `docs/superpowers/plans/2026-05-24-escape-ip-privacy-guard.md` define the no-false-anonymity boundary, `lib/secureLink.ts` now classifies links as same-app, private, public, blocked, or unknown, and public HTTPS links now carry `requiresIpPrivacy`. `components/resources/SecureLinkOpenPanel.tsx` shows IP guard posture and locks public Connect actions until the operator confirms a VPN, Tailscale exit node, or other privacy route is active for the current session. Nexus does not look up, display, store, publish, proxy, or magically hide the user's IP; private/local/Tailscale links remain distinct from public links, and all connect actions keep `target="_blank"`, `rel="noopener noreferrer"`, and `referrerPolicy="no-referrer"`. Verification passed with `npx tsc --noEmit`, `npm run lint`, `npm run verify`, `git diff --check`, and live dev-route proof: `/api/health` returned 200, `/resources?view=escape` returned 200, unauthenticated `/api/subscription-escape` returned 401, and unauthenticated `/api/subscription-escape/assets/not-real.png` returned 401.
- ESCAPE-STREAM-CONNECT-SHELF is now local inside `/resources?view=escape`: `specs/features/escape-stream-connect-shelf.md` and `docs/superpowers/plans/2026-05-24-escape-stream-connect-shelf.md` define the streaming-style private connection shelf, `lib/subscriptionEscape.ts` now persists `secureStreamLinks`, and `lib/subscriptionEscapeStore.ts` normalizes saved links through `inspectSecureLink` so unsafe backup/import records are filtered before they reach the UI. `components/resources/SecureLinkOpenPanel.tsx` now behaves like a private streaming launcher: paste a local/Tailscale/HTTPS link, validate it, save it as a visual tile, search/filter/sort favorites, then Connect with `target="_blank"`, `rel="noopener noreferrer"`, and `referrerPolicy="no-referrer"`. This opens approved links only; it does not proxy streams, scrape paid libraries, bypass DRM/paywalls, store credential-bearing URLs, fetch remote previews, or allow unsafe public HTTP. Verification passed with `npx tsc --noEmit`, `npm run lint`, `npm run verify`, `git diff --check`, and live dev-route proof: `/api/health` returned 200, `/resources?view=escape` returned 200, unauthenticated `/api/subscription-escape` returned 401, and unauthenticated `/api/subscription-escape/assets/not-real.png` returned 401.
- ESCAPE-SECURE-LINK-OPENER is now local inside `/resources?view=escape`: `specs/features/escape-secure-link-opener.md` and `docs/superpowers/plans/2026-05-24-escape-secure-link-opener.md` define the protected link-opening slice, `lib/secureLink.ts` validates links without network access, and `components/resources/SecureLinkOpenPanel.tsx` lets an operator paste, classify, copy, and explicitly open allowed links. The opener allows HTTPS, same-app relative paths, and private/local HTTP for localhost, LAN, Tailscale CGNAT, `.local`, and `.ts.net` hosts; it blocks unsafe protocols, public plain HTTP, embedded credentials, empty/oversized input, and raw whitespace. Open actions use `target="_blank"`, `rel="noopener noreferrer"`, and `referrerPolicy="no-referrer"` and do not fetch, unfurl, store, or expose pasted links. Verification passed with `npx tsc --noEmit`, `npm run lint`, `npm run verify`, `git diff --check`, and live dev-route proof: `/api/health` returned 200, `/resources?view=escape` returned 200, unauthenticated `/api/subscription-escape` returned 401, and unauthenticated `/api/subscription-escape/assets/not-real.png` returned 401.
- MEDIA-ESCAPE-INTAKE-QUEUE is now local inside `/resources?view=escape`: `specs/features/media-escape-intake-queue.md` and `docs/superpowers/plans/2026-05-23-media-escape-intake-queue.md` define the local review-gated intake slice, `lib/subscriptionEscape.ts` now includes media intake record types, status labels, filename/path parsing, and duplicate detection helpers, and `lib/subscriptionEscapeStore.ts` normalizes `mediaIntake` for old and new local state files. `components/resources/MediaIntakeReviewPanel.tsx` adds paste-to-review quick add, duplicate badges, inline cleanup, import/ignore actions, and a missing-info board; `components/resources/MediaEscapeLibrary.tsx` mounts the panel and confirms likely manual duplicates before save. This pass does not scan folders, read media files from disk, expose media publicly, add paid metadata APIs, or build piracy/DRM/paywall/ad-circumvention scope. Verification passed with `npx tsc --noEmit`, `npm run lint`, `npm run verify`, `git diff --check`, and live dev-route proof: `/api/health` returned 200, `/resources?view=escape` returned 200, unauthenticated `/api/subscription-escape` returned 401, and unauthenticated `/api/subscription-escape/assets/not-real.png` returned 401.
- ESCAPE-PRIVATE-FOUNDATION is now local inside `/resources?view=escape`: `specs/features/escape-private-foundation.md` and `docs/superpowers/plans/2026-05-22-escape-private-foundation.md` define the Tailscale-first hardening slice, `lib/subscriptionEscape.ts` now tracks access posture and authorized people/devices, and `lib/subscriptionEscapeStore.ts` normalizes those records for old and new local state files. `components/resources/EscapeAccessBackupPanel.tsx` adds JSON backup export/import, cloud-as-optional-backup posture, authorized access tracking, and revoke steps that keep actual Tailscale/Nexus removal manual and reviewable. Private media covers/posters now upload through protected `/api/subscription-escape/assets`, store under ignored `data/subscription-escape-assets/`, and read back only through protected asset URLs; `components/resources/MediaEscapeLibrary.tsx` can attach those protected covers to media records. Verification passed with `npx tsc --noEmit`, `npm run lint`, `npm run verify`, `git diff --check`, and live dev-route proof: `/api/health` returned 200, `/resources?view=escape` returned 200, unauthenticated `/api/subscription-escape` returned 401, and unauthenticated `/api/subscription-escape/assets/not-real.png` returned 401.
- MEDIA-ESCAPE-VISUAL-LIBRARY is now local inside the Subscription Escape lane: `specs/features/media-escape-visual-library.md` defines the movie/music catalog scope, `lib/subscriptionEscape.ts` now includes media item types, labels, counts, filter, and sort helpers, and `lib/subscriptionEscapeStore.ts` normalizes `mediaLibrary` records in the same protected local state file used by the Escape console. `components/resources/MediaEscapeLibrary.tsx` adds a Netflix-style shelf UI under `/resources?view=escape` with big Add movie/Add music controls, a single simple add/edit form, cover/poster cards, a selected-item detail panel, favorites, search, type filtering, and sort options for movies and music together. This pass is catalog-only: local file paths are metadata, and it does not add streaming/playback, public media endpoints, piracy, DRM bypass, paywall bypass, ad-circumvention, or paid-library scraping. Verification passed with `npx tsc --noEmit`, `npm run lint`, `npm run verify`, `git diff --check`, and live dev-route proof: `/api/health` returned 200, `/resources?view=escape` returned 200, and unauthenticated `/api/subscription-escape` returned 401 as expected.
- SUBSCRIPTION-ESCAPE-LOCAL-HUB is now local: `specs/features/subscription-escape-local-hub.md` defines the MacBook-hosted subscription replacement lane, `lib/subscriptionEscape.ts` defines the replacement catalog, source shelf, Tailscale/private-access posture, guardrails, safety checklist, and savings helpers, and `lib/subscriptionEscapeStore.ts` stores personal state in an untracked local file at `data/subscription-escape.json` by default or `NEXUS_SUBSCRIPTION_ESCAPE_FILE` when configured. A protected `/api/subscription-escape` route is now route-policy covered as `local_only`, and `components/resources/SubscriptionEscapeConsole.tsx` is mounted under `RESOURCES -> Utilities -> Escape` at `/resources?view=escape`. The first slice treats the MacBook as the always-on Nexus host and desktop/iPad as Tailscale/LAN clients; it does not build a VPN/proxy/anonymizer, does not use public unauthenticated exposure, and blocks piracy, DRM bypass, paywall bypass, ad-circumvention claims, account-ban evasion, auto-canceling accounts, Nexus-side billing, and cloud sync database scope. Verification passed with `npx tsc --noEmit`, `npm run lint`, `npm run verify`, `git diff --check`, and live dev-route proof: `/api/health` returned 200, `/resources?view=escape` returned 200, and unauthenticated `/api/subscription-escape` returned 401 as expected.
- SMOOTHNESS-OPERATIONAL-LIGHTS is now local: `lib/operationalLights.ts`, `hooks/useOperationalLights.ts`, and `components/ui/OperationalLightGrid.tsx` define a sanitized read-only light model for runtime, auth/session, local AI/Ollama, provider posture, phone LAN, security gates, dependency posture, Git sync, agent health, release proof, and external tool posture. The toprail now shows a compact operational light cluster, the shared Free Local Readiness panel used by HQ and COMMAND shows grouped readiness lights, and the 3D office room has a lightweight server-rack Ops Link lamp strip tied to the same state language. Polling remains visibility-aware and aborts stale fetches; old floating health/status widgets stay unmounted. `npm run smoothness:check` is now wired into `npm run verify` and validates the light contract, visibility-aware polling, legacy status-surface containment, and no `networkidle` proof waits. Verification passed with `npm run smoothness:check`, `npm run publication:safety:check`, `npm run security-scan`, `npm run type-check`, `npm run lint`, `git diff --check`, merge-marker sweep, and `npm run verify`. Runtime HTTP proof returned 200 for `/api/health`, `/hq?focus=hq-chronicle`, and `/command`, while raw `/api/free-local-readiness` returned `401 AUTH-PROTECTED`; hydrated browser proof is still blocked by the local Windows Playwright `spawn EPERM` launcher issue.
- SELF-WORKING-AI-AUTONOMY is now local: `scripts/autonomy-self-work.mjs` plus `npm run autonomy:self-work` add a policy-aware self-work runner that reads `tasks/todo.md` and `docs/SYSTEM_STATE.md`, selects the first active open-ready task, classifies approval risk, blocks dirty-tree/self-work starts, reports Docker/worktree isolation posture, and writes sanitized `docs/metrics/autonomy-self-work-*.json` artifacts. Execute mode can prepare an isolated git worktree only from a clean checkout, but it still refuses manual/external or approval-required work before mutation; approval mode prepares merge instructions only for an existing verified `readyForMerge` run and never auto-merges or pushes. Latest artifact `docs/metrics/autonomy-self-work-asw-2026-05-10T07-55-04-041Z.json` correctly blocks autonomous execution on the current manual phone/PWA task and missing Docker strong-isolation proof. `docs/agents/self-working-ai-autonomy.md` records the operator contract and required gates.
- NEXT-UPDATES-COMPLETION-PASS is now captured as the current release-candidate checkpoint: Codex attempted to publish the local commit stack, refreshed hardening/dependency/Dependabot evidence, captured blocked phone and release artifacts honestly, and regenerated the wide readiness rollup without package upgrades or simulated proof. Push remains blocked from this Codex shell by GitHub port 443, so use `npm run repo:sync:status` for the current ahead/dirty count and `npm run git:safe -- push` from normal PowerShell when GitHub connectivity is available.
- SECURITY-INFRASTRUCTURE-HARDENING is now local: `scripts/dependency-risk-posture.mjs`, `scripts/validate-security-boundaries.mjs`, `scripts/infra-hardening-audit.mjs`, and `scripts/validate-infra-hardening.mjs` add repeatable dependency posture, route/security boundary validation, sanitized infra hardening evidence, and structural hardening validation without package upgrades or public route changes. `npm run verify` now includes `security:boundaries`, `dependency:risk:check`, and `validate:infra-hardening`; `.husky/pre-push` runs `validate:infra-hardening` after publication safety and security scan. Latest proof: `docs/metrics/infra-hardening-2026-05-09T22-28-42-970Z.json` has no critical failures, with Docker CLI and staged-host proof still externally blocked.
- DEPENDENCY-RISK-POSTURE is now local and does not require GitHub access: `npm run dependency:risk:posture` writes sanitized `docs/metrics/dependency-risk-posture-*.json` artifacts and `npm run dependency:risk:check` performs the same package graph checks without writing files for `npm run verify`. Latest artifact: `docs/metrics/dependency-risk-posture-2026-05-25T05-52-21-853Z.json` records 40 runtime direct dependencies, 23 dev direct dependencies, 1070 transitive lockfile packages, no duplicate direct dependency declarations, no package/lockfile range mismatches, Next runtime package `15.5.18`, and one lifecycle-script review warning group covering 242 installed packages with lifecycle install scripts.
- WIDE-STABILIZATION-READINESS-ROLLUP is now local: `scripts/readiness-rollup.mjs` plus `npm run readiness:rollup` write sanitized `docs/metrics/readiness-rollup-*.json` artifacts that link the latest phone acceptance evidence, release diagnostics, dependency posture, infra hardening, Dependabot audit starter, publication safety posture, and runtime metric artifacts without adding a public route. Current rollup: `docs/metrics/readiness-rollup-2026-05-09T22-29-00-772Z.json` is intentionally blocked, not release-candidate ready, because phone/PWA proof is still manual, release diagnostics still need staged-host/Docker proof, and Dependabot alert package metadata is pending.
- DEPENDABOT-SECURITY-AUDIT remains a separate audit lane, not a package-upgrade tranche: `scripts/dependabot-security-audit.mjs` plus `npm run dependabot:audit:classify -- --alerts=...` wrote latest sanitized paginated imported-alert artifact `docs/metrics/dependabot-security-audit-2026-05-25T05-52-26-760Z.json`. The imported GitHub export is stale until rescan and still lists 14 runtime alerts in `package-lock.json`, but the local root app now has Next patched to `15.5.18`; remaining active package patch targets are `postcss` and `prismjs` if they remain open after GitHub refresh. `archive/` still accounts for 90 retired-manifest alerts in the old export, but live manifest names are quarantined so rescan can drop that retired noise.
- PUBLICATION-SAFETY-LOCKDOWN is now the publish gate before more acceptance evidence or dependency work: real LAN IPs and Windows home paths are replaced with placeholders, `scripts/validate-publication-safety.mjs` scans git-tracked text for private LAN literals, local home paths, env-local files, raw asset intake, key material, secret assignments, and private proof metadata, and `npm run publication:safety:check` is wired into both `npm run verify` and `.husky/pre-push` alongside the existing security scan.
- PHONE-ACCEPTANCE-EVIDENCE-LANE is now local: `scripts/phone-acceptance-capture.mjs` plus `npm run phone:acceptance:capture` write sanitized `docs/metrics/phone-local-acceptance-*.json` artifacts with route status, Free Local Readiness posture, manual phone proof flags, blockers, and `acceptanceReady`. The capture can read `NEXUS_TOKEN` only to call protected local APIs through the internal auth header and stores only boolean/token-presence posture, never token values, cookies, auth headers, real LAN IPs, local home paths, screenshots, or account/payment proof. Latest blocked artifact: `docs/metrics/phone-local-acceptance-2026-05-09T22-28-55-871Z.json` records `acceptanceReady: false` because the local runtime was not listening during capture and no manual phone flags were supplied.
- PHONE-ACCEPTANCE-FLUIDITY-UPGRADE is now local on top of the publication-safety gate: `lib/freeLocalOperations.ts` builds a phone acceptance brief with the runtime phone URL, direct HQ URL, `ping`, the local AI proof prompt, expected receipt posture, PWA install step, and a placeholder-only evidence reminder; HQ and COMMAND expose it through **Copy acceptance steps** in the existing Free Local Readiness phone handoff card. `docs/deployment/phone-access-free-local.md`, `npm run offline:local:check`, and `npm run phone:lan:check` cover the new brief. `npm run phone:lan:start` reached desktop health, and the sanitized route harness returned 200 for `/api/health`, `/hq?focus=hq-chronicle`, and `/command`; raw `/api/free-local-readiness` returned `401 AUTH-PROTECTED` without a browser session as expected.
- FREE-LOCAL-PHONE-ACCEPTANCE and LOCAL-AI-OFFLINE-OPERATIONS now have desktop runtime proof, but the physical-phone/PWA acceptance step is still manual. `npm run phone:lan:start` printed publish-safe LAN URL placeholders `http://<LAN-IP>:3100` and `http://<LAN-IP>:3100/hq?focus=hq-chronicle`. An authenticated local session returned `/api/free-local-readiness` as `ready` with `isolated` network mode, paid APIs `blocked`, Ollama `reachable`, resolved model `gemma4:latest`, agent health `100%`, authenticated session, review-gated tools, and phone LAN `enabled`. `/api/ai` answered through `provider=ollama` with `model=gemma4:latest`; a non-destructive bad-local-endpoint probe returned structured `ollama_unavailable` recovery copy without stopping the operator's actual Ollama process. Remaining acceptance is physical phone login, phone chat receipts, PWA install, and rerunning `npm run phone:acceptance:capture -- --phone-opened --phone-login --ping-receipt --local-ai-receipt --pwa-installed`.
- GIT-PERMISSION-RECOVERY is now closed for the working Windows flow: `scripts/git-with-acl-repair.ps1` and `npm run git:safe -- <git args>` remove the known explicit `.git` DENY ACL inside the same process before running Git, and `npm run handoff:pull` now routes through that wrapper. Normal PowerShell proved GitHub connectivity, `npm run handoff:pull` returned `Already up to date`, `npm run git:safe -- fetch --all --prune` completed, and push to `origin/codex/homefront-vision-expansion` succeeded. The Codex sandbox can still reapply the DENY ACL between tool calls, so `npm run repo:sync:health` may remain useful as a diagnostic, but day-to-day Git operations should use `npm run git:safe -- ...` when the sandbox shows ACL friction.
- BLOCKER-FUTURE-PROOFING is now local: the recurring `.git` ACL/lock blocker has a repo-native recovery lane instead of living only in chat. `lib/repoSyncRecovery.ts` defines the repo sync blocker model, manual recovery steps, and proof commands; `scripts/repo-sync-health-check.ps1` plus `npm run repo:sync:health` inspect `.git` DENY ACL entries, lock files, and active Git processes without mutating the repository; and `docs/repo-hygiene/git-permission-recovery.md` documents the safe Windows ACL repair flow. Free Local Readiness now renders the diagnostic command, recovery runbook path, manual steps, and proof commands in the repo-sync report. Current proof: `npm run repo:sync:health` reports blocked because `.git` still has explicit DENY ACL entries, while no lock files and no active Git processes are detected; `npm run offline:local:check` and `npx tsc --noEmit` passed.
- FREE-LOCAL-HOMEFRONT-OPERATIONS-ROADMAP is now local: the major updates roadmap now appears as an operator-facing local/free flow instead of staying only as a plan. `lib/freeLocalOperations.ts` defines `PhoneAcceptanceChecklist`, `LocalAiProofSnapshot`, `AssistantTurnProof`, `RepoSyncHealthReport`, and the free-local major update queue. The shared `FreeLocalReadinessPanel` now renders a phone acceptance checklist, local AI proof summary, major update queue, and `.git` repo-sync blocker/proof commands, so both HQ and COMMAND inherit the same guidance without a new route. Verification passed with `npm run type-check`, `npx tsc --noEmit`, `npm run lint`, `npm run offline:local:check`, `npm run phone:lan:check`, `npm run eval:agent-runtime:ci`, and `npm run verify`. `npm run runtime:proof:3100` initially failed because 3100 was down, so runtime proof used the known controlled PowerShell dev-runtime fallback: `/api/health`, `/`, `/hq?focus=hq-chronicle`, and `/command` returned 200; raw `/api/free-local-readiness` returned 401 without a browser session as expected.
- LOCAL-AI-OFFLINE-OPERATIONS-UX is now local: the working LAN/phone path now has visible assistant proof instead of silent chat state. `AssistantRuntimeReceipt` and `loadAssistantRuntimeReceipt` enrich Home chat, HQ chronicle, and CommandBar assistant turns with provider, resolved model, network mode, paid-API posture, recovery code, and file-change proof. Local fast replies such as `ping` now show `Local fast path` and `No file changes`; real AI turns pull `/api/free-local-readiness` so the receipt can show Ollama/free posture; structured failures keep their recovery code and action visible. HQ has a `Check local AI` chip that sends the local/free acceptance prompt, `AssistantTurnReceipt` now uses responsive CSS classes, and the compact HQ Free Local Readiness details collapse behind an expandable block for phone screens. Verification passed with `npm run type-check`, `npx tsc --noEmit`, `npm run lint`, `npm run offline:local:check`, `npm run phone:lan:check`, `npm run eval:agent-runtime:ci`, and `npm run verify`. Runtime proof used a controlled PowerShell dev-runtime job: `/api/health`, `/hq?focus=hq-chronicle`, and `/command` returned 200; raw `/api/free-local-readiness` returned 401 without a browser session as expected.
- FREE-LOCAL-PHONE-UX-OPTIMIZATION is now local: the working desktop-to-phone LAN path is easier to operate from inside Homefront. `PhoneLanReadinessSnapshot` now carries desktop URLs, LAN URLs, direct `/hq?focus=hq-chronicle` phone URLs, and preferred phone/HQ links. The shared `FreeLocalReadinessPanel` in HQ and COMMAND now shows a phone handoff card with copy buttons for the phone home URL and direct HQ URL, plus clearer local-model status. `npm run phone:lan:start` now prints both phone home URLs and direct HQ URLs so the operator does not have to assemble route paths manually. `docs/deployment/phone-access-free-local.md` documents the same flow. Verification passed with `npm run phone:lan:check`, `npm run offline:local:check`, `npm run type-check`, `npx tsc --noEmit`, `npm run lint`, `npm run verify`, and `npm run runtime:proof:3100 -- --routes=/,/hq?focus=hq-chronicle,/command,/api/free-local-readiness`; the protected readiness API returned `401 AUTH-PROTECTED` from raw shell as expected.
- FREE-LOCAL-AI-PHONE-READINESS is now local: Homefront has a free/local readiness lane for website runtime, Ollama-first AI, paid-API posture, token/session state, tool gates, agent-health proof, browser storage, and phone LAN access. `FreeLocalReadinessSnapshot` and `PhoneLanReadinessSnapshot` define the contract, `/api/free-local-readiness` returns the protected snapshot, and the shared `FreeLocalReadinessPanel` now appears in HQ chronicle and COMMAND. `/api/ai` now returns structured local recovery codes for network-locked, paid-provider-blocked, Ollama-required, and Ollama-unavailable failures, and `readNetworkMode()` now honors `NEXUS_NETWORK_MODE=isolated` explicitly. The default phone path is now `docs/deployment/phone-access-free-local.md`: desktop stays on, Ollama stays local, phone connects over LAN/Tailscale optional, no in-app charges, no required paid VPS, and LAN binding only starts through `npm run phone:lan:start`. Coolify/VPS phone access is reframed as optional hosted mode. Verification passed with `npm run offline:local:check`, `npm run phone:lan:check`, `npm run phone:access:check`, `npm run type-check`, `npx tsc --noEmit`, `npm run eval:agent-runtime:ci`, `git diff --check`, merge-marker sweep, and `npm run verify`. `npm run runtime:launch:3100` timed out in the known Windows wrapper path after reporting healthy, so runtime proof used a controlled PowerShell dev-runtime job: `/`, `/hq?focus=hq-chronicle`, `/command`, and `/command?focus=provider-health` returned 200; `/api/free-local-readiness`, `/api/ollama/catalog`, and `/api/agent-health` returned `401 AUTH-PROTECTED` from raw shell as expected.
- NEXT-UPDATE-STABILIZATION-AUDIT is now local: the current dirty batch is reviewable before more expansion. `docs/repo-hygiene/local-change-set-map-2026-05-08.md` classifies local changes into Homefront polish, assistant/operator, vehicle readiness, private ARPG, source intelligence, deployment/runbooks, repo hygiene, local agent tooling, generated temp, and unknown groups. `tmp-codex-runtime/` is now ignored so runtime pids/logs/screenshots stay out of product staging. The unfinished-ideas ledger now marks the shipped vehicle/drone readiness rows and shared assistant workflow rows complete instead of open-ready. ARPG release readiness now has a `fallbackProofMatrix` for static gates, deterministic gameplay validators, browser fallback route proof, and private-lane isolation; `npm run arpg:release:check` validates it, and `/hq` Production shows `data-testid="arpg-fallback-proof-matrix"`. Security camera/drone, IoT, and Voice Lab placeholder states were replaced with explicit passive/simulated states, and `npm run surface:polish:check` is now part of `npm run verify`. Verification passed with `git diff --check`, merge-marker sweep, `npm run surface:polish:check`, `npm run arpg:release:check`, `npm run type-check`, `npx tsc --noEmit`, `npm run eval:agent-runtime:ci`, `npm run verify`, and runtime proof for `/`, `/hq?focus=hq-chronicle`, `/command`, `/resources`, `/internal/vehicle`, `/labs/security`, and `/internal/iot`. Git permission recovery is now handled by the `git:safe` wrapper and no longer blocks normal PowerShell pull/fetch/push.
- HOMEFRONT-AUTH-SHELL-POLISH is now local: the authenticated shell now carries the landing-page premium language deeper into logged-in surfaces without changing capability scope. `HomefrontVisualSurfaceSpec` has an `interiorPolish` contract with lead intent, stale-info policy, media moment, active-state label, and support density for HQ, COMMAND, INTEL, ALPHA, CYBER, RECON, VAULT, RESOURCES, and secondary protected surfaces. `ShellPage`, `ShellStage`, `HomefrontVisualParityBand`, `HomefrontWorkplaneSummary`, `HomefrontRouteTabs`, `HomefrontActionDock`, and `HomefrontMediaPanel` now render that contract through liquid-glass workplanes, support-density metadata, media moments, and clearer active route/focus affordances. CSS now strengthens non-HQ glass modules, action buttons, active segmented tabs, and workplane summaries while keeping reduced-motion-safe behavior and keeping polish primitives out of the private ARPG playfield. Coverage now asserts interior polish metadata on non-HQ routes, HQ chrome polish outside ARPG, and no polish injection inside `data-testid="arpg-room"`. Verification passed with `npm run type-check`, `npx tsc --noEmit`, `npm run eval:agent-runtime:ci`, `npm run verify`, and route proof for `/command`, `/intel`, `/alpha`, `/cyber`, `/recon`, `/vault`, `/resources`, `/labs/security`, `/internal/skills`, `/internal/vehicle`, and `/hq?focus=hq-chronicle`. The managed launcher still hit the known Windows wrapper timeout after reporting health, so live route proof used a temporary PowerShell dev-runtime job that compiled the current source.
- WORKPLANE-OPERATOR-SOURCE-HYGIENE-ROADMAP is now local: the core authenticated routes now answer purpose, next action, and proof through one shared compact workplane strip. `HomefrontVisualSurfaceSpec` carries `workplaneSummary`, and `ShellPage` renders `data-testid="homefront-workplane-summary"` for COMMAND, INTEL, ALPHA, CYBER, RECON, VAULT, and RESOURCES while keeping HQ and the private ARPG playfield excluded. Assistant usefulness now has a shared `AssistantTurnReceipt` across Home chat, HQ chronicle, and CommandBar, with receipt fields for answer mode, workspace, tool posture, recovery, and file-change posture without widening write permissions. `/resources?view=sources` now renders `data-testid="resources-source-ledger"` from `HOMEFRONT_SOURCE_LEDGER`, classifying the supplied GitHub/X resources as mapped, candidate, blocked, rejected, or private-lane with decision reasons, next actions, source links, and VAULT filing links. Branch cleanup remains review-gated through `docs/repo-hygiene/branch-cleanup-decision-ledger-2026-05-08.md`; no branches, remotes, or worktrees were deleted. Verification passed with `npm run type-check`, `npx tsc --noEmit`, `npm run eval:agent-runtime:ci`, `npm run verify`, and route proof for `/command`, `/intel`, `/alpha`, `/cyber`, `/recon`, `/vault`, `/resources`, and `/hq?focus=hq-chronicle`. The standard runtime launcher still times out in the known Windows wrapper path after reporting health, so live route proof used a temporary PowerShell runtime job.
- RELEASE-RUNBOOK-PREP is now local: FD2/CP2 still remain externally blocked on a real Coolify/VPS hostname and Docker proof, but the diagnostics and rollback lane is executable. `docs/deployment/fd2-release-runbook.md` defines prerequisites, local proof, staged proof, route proof, rollback, and acceptance criteria. `scripts/release-diagnostics-capture.mjs` plus `npm run release:diagnostics:capture` writes a sanitized timestamped artifact under `docs/metrics/` with route status, base URL source, token presence, Docker availability, and blocked prerequisites. Latest capture `docs/metrics/release-diagnostics-2026-05-09T22-28-56-079Z.json` is blocked because the local runtime was not listening during capture, `NEXUS_RELEASE_BASE_URL` is still absent from operator-local config, and Docker CLI is unavailable.
- PHONE-ACCESS-COOLIFY-RUNBOOK is now local: the phone-while-desktop-off path is documented as a VPS/Coolify + HTTPS PWA deployment rather than a desktop `localhost` connection. `docs/deployment/phone-access-coolify.md` defines the v1 model: host on Coolify, expose port 3000, sign in with `NEXUS_TOKEN`, use a free/BYOK cloud AI provider while desktop Ollama is unavailable, keep browser state phone-local, and keep high-risk tools disabled. The Coolify/web deployment docs, README, and `.env.example` now point to the safe phone profile (`NEXUS_DEPLOYMENT_PROFILE=web-self-hosted`, `NEXUS_NETWORK_MODE=internal`, high-risk off, paid APIs off). `scripts/validate-phone-access-readiness.mjs` and `npm run phone:access:check` validate the runbook, env defaults, Docker/PWA posture, and route safety; `npm run verify` includes that check.
- AI-OPERATOR-WORKFLOW-GAPS is now local: the assistant now exposes a review-first operator workflow instead of hiding planning and edit posture inside chat replies. `lib/assistantOperatorWorkflow.ts` derives phase, review requirement, task plan, proposed-edit summary, change log, and skill/tool visibility from the shared dispatch state. `AssistantDispatchPlan` now carries `operatorWorkflow`, `localReply`, the assistant context includes an `[OPERATOR WORKFLOW]` block that forbids claiming code changes without tool proof, and `AssistantChatActionModel` adds local-only workflow focus actions. Home chat, HQ chronicle, and CommandBar render the shared `AssistantOperatorWorkflowPanel`; edit-like prompts show that real diffs still belong to the existing `ProposedEditPanel` approval flow, while greetings, pings/tests, and acknowledgements now answer locally without entering `LIVE EXECUTION`. HQ also shows a slow local-runtime watchdog with a Provider Health recovery lane when Ollama/model execution stays active past the wait threshold, and the private RPG playfield is untouched. Verification passed with `npm run eval:agent-runtime:ci`, `npm run type-check`, `npm run verify`, `npx tsc --noEmit`, and `npm run runtime:proof:3100 -- --routes=/hq?focus=hq-chronicle,/command,/`.
- VEHICLE-BENCH-BRIDGE-READINESS-V1 is now local: the F450 props-off checklist and passive bridge prep are closed as review-first readiness only. `buildVehicleBenchBridgeReadiness` now combines bench checklist proof, first hardware-day proof, connector posture, and Vault package readiness into a visible gate at `data-testid="vehicle-bench-bridge-readiness"`. `/api/vehicle/telemetry` now exists as a protected local-only passive ingest route for normalized bridge frames, `scripts/vehicle-bridge-stub.mjs` generates a dry-run read-only payload with optional local post, and `docs/deployment/vehicle-passive-bridge-stub.md` documents the arrival-day contract. The guardrail remains explicit: Nexus does not arm, steer, or mode-switch the aircraft, and Mission Planner/QGroundControl proves heartbeat first. `npm run vehicle:readiness:check`, `npm run type-check`, `npm run lint`, `npm run verify`, and `npm run runtime:proof:3100 -- --routes=/vehicle,/internal/vehicle,/api/vehicle/telemetry` passed; the raw-shell telemetry API check returns `401 AUTH-PROTECTED` as expected.
- VEHICLE-DRONE-SIM-REPLAY-V0 is now local: the Homefront vehicle/drone readiness lane has a simulation-only replay contract and visible operator proof path without wiring real flight control. `VehicleReplayScenario` extends the passive telemetry schema, `makeSimulatedTelemetryFrame` produces deterministic replay frames from the existing telemetry shape, and `lib/vehicle/flightReplayScenarios.ts` defines four review scenarios: perimeter patrol, link degradation, battery return, and operator incident package. The vehicle telemetry panel now exposes `data-testid="vehicle-replay-scenarios"` controls that cycle scenario frames while stating Nexus does not arm, steer, or mode-switch an aircraft. The session bundle card now exposes `data-testid="vehicle-replay-vault-package"` so the simulated replay has Vault package title, tags, and incident type visible before hardware exists. `scripts/validate-vehicle-readiness.mjs` and `npm run vehicle:readiness:check` validate the lane. `npm run vehicle:readiness:check`, `npm run type-check`, `npm run verify`, and `npm run runtime:proof:3100 -- --routes=/vehicle,/internal/vehicle` passed; `/vehicle` redirected with 307 and `/internal/vehicle` returned 200.
- RUNTIME-PROOF-RESILIENCE is now local: the repo has a Node-only `npm run runtime:proof:3100` fallback lane for the known Windows `3100` launcher timeout and Playwright `spawn EPERM` proof gap. `scripts/runtime-proof-check.mjs` always checks `/api/health`, prints per-route status lines, accepts healthy page routes and redirects, and labels protected diagnostics as `AUTH-PROTECTED` instead of treating raw shell 401/403 as product regressions. `npm run runtime:launch:3100` found the managed runtime already healthy on `127.0.0.1:3100`; `npm run runtime:proof:3100 -- --routes=/,/hq?focus=hq-chronicle,/command,/resources,/vehicle,/internal/vehicle` returned 0 with `/api/health`, `/`, `/hq?focus=hq-chronicle`, `/command`, `/resources`, and `/internal/vehicle` healthy plus `/vehicle` redirecting with 307. Protected `/api/agent-health` and `/api/ollama/catalog` returned 401 `AUTH-PROTECTED` from raw shell as expected. `npm run type-check` and `npm run verify` passed.
- UNFINISHED-IDEAS-TRIAGE-QUEUE is now local: unfinished, half-implemented, stale, blocked, and rejected ideas are reconciled into `docs/plans/unfinished-ideas-triage-2026-05-07.md`. `tasks/todo.md` now separates the active open-ready queue from blocked/manual watchlist items, keeps Homefront vehicle/drone work as simulation/readiness only, keeps Aether Reliquary private inside `/hq`, and keeps FD2/CP2 blocked until real staged host/Docker/env proof exists. MW6FR1 is closed from its stale proof-held checkbox because its accepted fallback proof passed and later MW6FR2-FR9 town slices already depend on that foundation. Source hygiene found no merge markers, and verification passed with `npm run type-check`, `npx tsc --noEmit`, `npm run eval:agent-runtime:ci`, `npm run verify`, and `npm run build`. `npm run runtime:launch:3100` timed out in the known Windows wrapper path, but live HTTP checks returned 200 for `/api/health`, `/`, `/hq?focus=hq-chronicle`, `/command`, `/command?focus=agent-health`, `/resources`, `/vehicle`, and `/internal/vehicle`.
- HOMEFRONT-LANDING-SCENE-BOX-REMOVAL is now local: the public Homefront hero no longer shows the oversized floating scenario explanation box. `HomefrontCommandBackdrop` now keeps the photoreal home, separate patrol drone, left-click drone signal, headline, CTA, and no-automatic-emergency-call boundary, while removing `landing-scenario-reel`, the scenario cards, and the scenario control strip from the first viewport. The animal/rodent false-alarm and perimeter-review idea stays in the hero copy and should be depicted through the home/drone background rather than a card UI. Focused landing coverage now asserts those scenario panel elements are absent. Verification passed with `npx tsc --noEmit`, `npm run lint`, `npm run verify`, and `npm run build`; the first build attempt hit the known Windows `.next` lock until `npm run runtime:stop:3100`, then the clean retry passed. `npm run runtime:launch:3100` still timed out in its known wrapper path, but live `/api/health` returned 200 on `127.0.0.1:3100`.
- HOMEFRONT-LANDING-DRONE-CLICK-SIGNAL is now local: the public Homefront hero has a small left-click interaction that makes the approved patrol drone respond without replacing the home or drone assets. Clicking the hero backdrop now stores the click position, briefly marks the drone as `data-signal-state="acknowledged"`, and renders one restrained fading pulse at the click point; button/link clicks are ignored so CTA behavior stays normal. The photoreal `homefront-guardian-hero.webp` home, `homefront-drone-patrol.webp` drone, drone patrol route, headline, CTA, and private RPG boundary remain intact, and the previously removed heavy hero effects stay absent. Verification passed with `npx tsc --noEmit`, `npm run lint`, `npm run verify`, and `npm run build`; the first build attempt hit the known Windows `.next` lock until `npm run runtime:stop:3100`, then the clean retry passed. `npm run runtime:launch:3100` still timed out in its known wrapper path, but live `/api/health` returned 200 on `127.0.0.1:3100`.
- HOMEFRONT-LANDING-HERO-EFFECT-CLEANUP is now local: the public Homefront hero keeps the approved photoreal home scene and separate patrol drone, but the extra first-viewport effects have been stripped. The removed layers are limited to the hero-only capability video overlay, command-motion/glass sweeps, scan bands, pulsing rings, marker chips, warning beam, and tiny camera-ready indicator. The headline, CTA, patrol drone route, no-automatic-emergency-call boundary, and private RPG separation remain intact. Focused landing coverage now asserts the effect layers stay absent while `homefront-guardian-hero.webp` and `homefront-drone-patrol.webp` still mount. Verification passed with `npx tsc --noEmit`, `npm run lint`, `npm run verify`, and `npm run build`; the first build attempt hit the known Windows `.next` lock until `npm run runtime:stop:3100`, then the clean retry passed. `npm run runtime:launch:3100` still timed out in its known wrapper path, but live checks returned 200 for `/api/health`, `/`, `/images/homefront-guardian-hero.webp`, and `/images/homefront-drone-patrol.webp` on `127.0.0.1:3100`.
- HOMEFRONT-LANDING-PHOTOREAL-HOME-PERIMETER is now local: the public Homefront landing no longer depends on the outline/SVG house fallback as the active home backdrop. A generated photoreal modern residential night-perimeter scene was copied into `public/images/homefront-guardian-home-photoreal-source.png`, with a preview derivative at `public/images/homefront-guardian-home-photoreal.webp`. `scripts/generate-homefront-guardian-hero.mjs` now prefers that home source when producing `homefront-guardian-hero.webp` and `homefront-guardian-hero-fallback.webp`, while the drone remains a separate patrol object sourced through `homefront-drone-patrol.webp`. `scripts/generate-homefront-capability-media.mjs` regenerated the active capability reel against the new home source, so the drone can hover/patrol around a real-looking home instead of an abstract outline. Verification passed with `npm run type-check`, `npm run lint`, `npm run verify`, `npm run build`, and live `3100` 200 checks for `/api/health`, `/`, `/images/homefront-guardian-hero.webp`, `/images/homefront-guardian-home-photoreal.webp`, `/images/homefront-drone-patrol.webp`, and `/videos/homefront-capability-reel.webm`. `npx playwright screenshot` remains blocked by the known Windows `spawn EPERM` browser-worker issue, so this pass used direct asset inspection plus route/asset HTTP proof.
- HOMEFRONT-LANDING-PREMIUM-DRONE-OBJECT-V2 is now local: the active Homefront drone object has moved beyond the illustrated/SVG replacement into a photoreal drone option. A generated graphite patrol drone with carbon arms, real rotors, landing skids, and a camera/gimbal stack was chroma-key cleaned into `public/images/homefront-drone-patrol-photoreal-source.png`, with a preview derivative at `public/images/homefront-drone-patrol-photoreal.webp`. `scripts/generate-homefront-guardian-hero.mjs` now prefers that photoreal source when producing `homefront-drone-patrol-fallback.webp`, and `scripts/generate-homefront-capability-media.mjs` regenerated the active `public/images/homefront-drone-patrol.webp` plus `public/videos/homefront-capability-reel.webm` from it. Verification passed with `npm run type-check`, `npm run lint`, `npm run verify`, `npm run build`, and live `3100` 200 checks for `/api/health`, `/`, `/images/homefront-drone-patrol.webp`, and `/videos/homefront-capability-reel.webm`. The in-app browser can now refresh `http://localhost:3100/` against the updated local runtime.
- HOMEFRONT-LANDING-PREMIUM-DRONE-OBJECT is now local: the public Homefront landing no longer uses the abstract top-down drone glyph as its active patrol object. `scripts/generate-homefront-guardian-hero.mjs` now emits a camera-forward premium patrol drone source with rotor, body, gimbal, skid, light, and motion-streak detail; `scripts/generate-homefront-capability-media.mjs` regenerated `public/images/homefront-drone-patrol.webp` and the capability reel from that source. The landing hero, capability spine, and shared shell backdrop now give the landscape drone a wider frame so it reads as a real capability object instead of a small sticker. Verification passed with `npm run type-check`, `npm run lint`, `npm run verify`, `npm run build`, live `3100` 200 checks for `/api/health`, `/`, `/images/homefront-drone-patrol.webp`, and `/videos/homefront-capability-reel.webm`, plus desktop/mobile browser proof showing the drone loads at 790x445 natural size, renders without overflow, the video is ready, runtime overlay is absent, and console/page/local request errors are empty.
- HOMEFRONT-LANDING-CLEAN-HERO-MEDIA is now local: the public Homefront hero no longer has baked background words or the lingering dog/animal silhouette from the earlier generated media pass. `scripts/generate-homefront-guardian-hero.mjs` now produces the active `homefront-guardian-hero.webp` plus the fallback image without dog/person silhouettes, and `scripts/generate-homefront-capability-media.mjs` now generates `homefront-capability-reel.webm` with motion-only HUD geometry instead of readable labels like pet-safe, entry camera, observe/classify, or approve-before-response. The first-viewport decorative response rail and scenario rail were removed, the camera-ready label became a non-text indicator, the playable scenario surface moved to the right visual field, and the hero video opacity was lowered so background media no longer competes with foreground copy. Verification passed with `npm run type-check`, `npm run lint`, `npm run verify`, `npm run build`, live `3100` 200 checks for `/api/health`, `/`, `/hq?focus=hq-chronicle`, and `/videos/homefront-capability-reel.webm`, plus browser proof showing both scenario controls still work, the removed text rails are absent, known baked-text strings are absent from the DOM, the scenario panel no longer overlaps the headline or CTA, public RPG text is absent, runtime overlay is absent, horizontal overflow is zero, and console/page/local request errors are empty.
- HOMEFRONT-LANDING-PLAYABLE-SCENARIO-BACKGROUND is now local: the public Homefront hero no longer treats the animal/dog idea as a passive or vague background cue. The hero scenario plane is now playable: Scene 01 runs Mario's animal/rodent false-alarm flow with perimeter trigger, drone inspection, false-alarm classification, and record-only outcome; Scene 02 runs the unwanted-person breach flow with perimeter breach, drone follow, operator-approved warning script, person leaving, and proof recording. The breach scene may say the operator can contact 911, but the landing keeps the boundary explicit that Homefront does not place emergency calls automatically. Decorative hero overlays and rails were made pointer-transparent so the scene controls can be clicked and replayed from the background without disturbing the main CTA. Focused landing coverage now asserts the playable controls and both authored scenario flows. Verification passed with `npm run type-check`, `npm run lint`, `npm run verify`, `npm run build`, live `3100` 200 checks for `/api/health`, `/`, `/hq?focus=hq-chronicle`, and `/videos/homefront-capability-reel.webm`, plus browser proof showing both scenario controls click, both scenes swap/replay, no-call/911 boundary copy is present during breach, public RPG text is absent, runtime overlay is absent, horizontal overflow is zero, and console/page/local request errors are empty. The focused Playwright runner remains locally blocked before test execution by Windows worker `spawn EPERM`.
- HOMEFRONT-LANDING-SCENARIO-REEL is now local: the public Homefront hero no longer treats the pet/animal cue as passive scenery. It now runs a two-scene surveillance/drone scenario reel over the cinematic home perimeter: Scene 01 shows a harmless animal-trigger crossing the perimeter, drone review, non-threat classification, and record-only outcome; Scene 02 shows an unwanted perimeter breach, drone follow, operator-approved warning script, subject leaving, and proof recording. The warning script may say the operator can contact 911, but the landing explicitly states that Homefront does not place emergency calls automatically, and the breach card says no emergency call is placed by Homefront. The implementation keeps the scenario as deterrence/review proof only: no autonomous harm, face recognition, citizen scoring, police/military framing, automatic emergency call, remote media dependency, or public RPG positioning. Focused landing coverage now asserts the scenario reel, animal card, breach card, warning-script text, and no-call boundary. Verification passed with `npm run type-check`, `npm run lint`, `npm run verify`, `npm run build`, live `3100` 200 checks for `/api/health`, `/`, `/hq?focus=hq-chronicle`, and `/videos/homefront-capability-reel.webm`, plus direct browser proof showing the scenario markers/cards/beam visible, the no-call boundary present, public RPG text absent, runtime overlay absent, horizontal overflow at zero, and console/page/local request errors empty. The focused Playwright runner remains locally blocked before test execution by Windows worker `spawn EPERM`.
- HOMEFRONT-LANDING-CAPABILITY-SPINE is now local: the public Homefront landing now explains the product's core functions through an interactive Capability Spine directly after Guardian Protocol. Perimeter, Command, Intel, Cyber, Recon, and Vault each show what Homefront watches, how it reasons, what it recommends, and what gets recorded before the operator acts. The right-side media plane reuses the local `homefront-capability-reel.webm` plus the drone asset, and the "How Homefront thinks" chain makes Observe, Filter, Classify, Context, Recommend, Approve, and Record visible without implying autonomous escalation, face recognition, citizen scoring, police/military use, remote media dependency, or public RPG positioning. Focused landing coverage now asserts the spine, switcher, and thought chain. Verification passed with `npm run type-check`, `npm run lint`, `npm run verify`, `npm run build`, live `3100` 200 checks for `/api/health`, `/`, `/hq?focus=hq-chronicle`, and `/videos/homefront-capability-reel.webm`, plus direct browser proof showing the switcher changes Cyber/Vault panels, public RPG text is absent, the runtime overlay is absent, horizontal overflow is zero, and console/page/local request errors are empty. The focused Playwright runner remains locally blocked before test execution by Windows worker `spawn EPERM`.
- HOMEFRONT-LANDING-CAPABILITY-MEDIA-REEL is now local: the public Homefront hero no longer depends on a still-only backdrop for the surveillance/drone story. The landing now uses a photoreal local night-home scene, a real-looking drone overlay, and a generated local `public/videos/homefront-capability-reel.webm` loop that demonstrates camera intake, patrol motion, pet-safe classification, operator approval, and incident memory without remote media, autonomous-harm, face-recognition, citizen-scoring, police/military, or public RPG positioning. The same capability reel now appears in the Guardian Protocol media panel and as a quiet backdrop layer in the default/HQ shared shell while the Aether Reliquary/RPG playfield stays untouched. `scripts/generate-homefront-capability-media.mjs` regenerates the image/video assets locally from source images without FFmpeg, Playwright, or external downloads. Verification passed with `npm run type-check`, `npm run verify`, `npm run build`, live `3100` 200 checks for `/api/health`, `/`, `/hq`, `/videos/homefront-capability-reel.webm`, `/images/homefront-guardian-hero.webp`, and `/images/homefront-drone-patrol.webp`, plus static public HTML checks confirming the capability media sources are present, old agency copy is absent, and public RPG copy is absent. Focused Playwright remains locally blocked before test execution by Windows worker `spawn EPERM`.
- HOMEFRONT-LANDING-REAL-DRONE-HOME-VISUAL is now local: the public landing no longer uses the abstract CSS drone glyph. It now loads a dedicated `homefront-drone-patrol.webp` raster drone image over the regenerated `homefront-guardian-hero.webp` home/perimeter scene, with the drone animating as the live patrol object instead of duplicating into the static background. Root Open Graph/Twitter metadata now points at the Homefront perimeter image, and the default/HQ shell backdrop now inherits the same local home scene plus a reduced-motion-safe animated drone layer without touching the private Aether Reliquary/RPG playfield. Verification passed with `npm run type-check`, `npm run verify`, `npm run build`, live `3100` 200 checks for `/api/health`, `/`, and `/hq`, and Node REPL browser proof showing the landing drone is an IMG loading `/images/homefront-drone-patrol.webp`, the hero image loads at 1920px, the drone transform changes over time, old agency copy is absent, and there are zero console errors, page errors, failed local requests, runtime overlays, or horizontal overflow. Terminal Playwright remains polluted by stale `.worktrees` duplicate Playwright installs, and authenticated HQ browser proof could not be cookie-seeded from this shell because the runtime has auth configured but the token is not exported to Codex.
- HOMEFRONT-LANDING-GUARDIAN-PROTOCOL is now local: the public page now carries the new cinematic perimeter idea below the hero instead of leaving it as a one-screen visual. A new Guardian Protocol section explains the operating sequence with live perimeter feeds, pet-safe classification, unknown-approach review, operator-owned response posture, and a decision-trail timeline, all over a restrained local image/motion layer. The copy keeps the surveillance/drone story bounded to local inference, operator approval, and proof memory with no autonomous-harm, face-recognition, citizen-scoring, fake-deployment, remote-media, or public RPG positioning. Focused landing coverage now asserts the protocol section and operator-owned language. Verification passed with `npm run type-check`, `npm run verify`, `npm run build`, live `3100` `/api/health` and `/` HTTP checks, and Node REPL browser proof showing the section visible, the hero image still animating, old agency copy absent, and zero console errors, page errors, failed local requests, runtime overlay, or horizontal overflow. `npm run runtime:launch:3100` still times out in the known wait path while leaving a healthy runtime.
- HOMEFRONT-LANDING-CINEMATIC-GUARDIAN-REWORK is now local: the public hero no longer depends on the amateur-feeling line-art map/debug-camera composition. The first viewport now uses a regenerated local `homefront-guardian-hero.webp` property-perimeter scene, sparse moving patrol-beam overlays, a subtle drone, RTSP/ONVIF readiness, and stacked response/event HUD rails that preserve the pet-safe, operator-approved escalation story without adding remote media or exposing the private `/hq` RPG lane. The old hidden SVG map and camera label nodes were removed from the component so the hero is image-led instead of diagram-led. Verification passed with `npm run type-check`, `npm run verify`, `npm run build` after the known first-attempt Windows `spawn EPERM` retry, live `3100` `/api/health` and `/` HTTP checks, and Node REPL browser proof showing the 1920x1080 local hero image mounted, image/drone transforms moved over time, response and event rails stayed inside a 1440x900 viewport, old agency copy stayed gone, and there were zero console errors, page errors, failed local requests, runtime overlay, or horizontal overflow. `npm run runtime:launch:3100` still times out in the known wait path while leaving a healthy port owner.
- HOMEFRONT-LANDING-GUARDIAN-SCENARIO is now local: the public Homefront hero no longer reads as an abstract command room. It now opens with a concrete guardian scenario: "Homefront Guards The Perimeter", existing cameras, future patrol drones, local AI classification, pet-safe/non-threat language, RTSP/ONVIF readiness, camera coverage cones, moving drone patrol, event posture, and a progressive response ladder that stays operator-approved before action. The private `/hq` RPG lane remains out of public positioning, and the hero adds no new remote media dependency. Focused landing coverage now has explicit guardian/headline/camera/response hooks, and the feature spec records the no-autonomous-harm, no-face-recognition, no-citizen-scoring guardrails. Verification passed with `npm run type-check`, `npm run verify`, `npm run build`, live `3100` `/api/health` and `/` HTTP checks, and Node REPL browser proof showing the guardian scene mounted, the drone transform/opacity changed over time, the old agency hero copy stayed gone, and there were zero console errors, page errors, failed local requests, or runtime overlay. One build attempt briefly hit Windows `spawn EPERM` after compilation, then a clean retry passed; `npm run runtime:launch:3100` still times out in the known wait path while leaving a healthy runtime.
- HOMEFRONT-LANDING-CINEMATIC-MOTION is now local: the public Homefront hero keeps the new local command-room backdrop but no longer reads as static line art. It now has layered cinematic motion through drifting command/map plates, glass sweeps, radar-style rotation, scan wash, animated route traces, pulsing route nodes, and active proof readouts while preserving the no-remote-media and private `/hq` RPG boundaries. Focused landing coverage now asserts the animated command-motion layer, and reduced-motion coverage continues to verify the global reduced profile plus common viewport constraints. Verification passed with `npm run type-check`, `npm run verify`, `npm run build`, live `3100` health and `/` HTTP checks, local hero asset checks, and Node REPL browser proof showing the motion layer mounted, sampled transforms/opacity changing over time, no console errors, no page errors, no failed local requests, no runtime overlay, and no old remote hero video. The managed Windows runtime wrapper still times out in its known wait path, so the final proof used the actual fresh standalone port owner after clearing the stale child process.
- HOMEFRONT-LANDING-COMMAND-BACKGROUND is now local: the public landing hero no longer uses the unrelated remote botanical/cinematic video as its first impression. It now opens with a Homefront-native command-room backdrop built from local tactical/map assets, route nodes, live-proof readouts, scan motion, and reduced-motion-safe static layers while preserving the private `/hq` RPG boundary. Focused landing coverage now asserts the command backdrop and the lower hero video count. Verification passed with `npm run type-check`, `npm run verify`, `npm run build`, a managed `3100` runtime returning 200 for `/api/health`, `/`, and `/resources?view=playbooks&playbook=runtime-finalize-loop`, direct 200 checks for the local hero SVG assets, and a Node REPL browser probe showing the command backdrop mounted with zero console errors, page errors, failed local requests, or unhandled rejections. A stale dev server with a broken `.next` graph was stopped during proof; terminal Playwright still hits the known Windows `spawn EPERM`, so Node REPL browser proof was used for acceptance.
- HOMEFRONT-RUNTIME-FINALIZE-PLAYBOOK is now local: Resources now has a native `Runtime Finalize Loop` engineering playbook and support-rail entry so visual/shell updates have an in-app path for type-check, verify, clean build, direct `3100` runtime restart, HTTP route checks, browser console/pageerror/requestfailed/unhandled-rejection probes, and handoff freshness before push or USB transfer. COMMAND and Resources action guidance now point at the same proof loop, with aliases for finalize/bug-finalize/quality-finalize resolving to the exact playbook. Verification passed with `npm run type-check`, `npm run verify`, `npm run build`, a fresh direct `3100` runtime health check, HTTP 200 checks for `/api/health`, `/resources?view=playbooks&playbook=runtime-finalize-loop`, `/resources?view=playbooks`, `/command?focus=agent-health`, and `/`, plus quiet headless browser probes on the public/protected-redirect path. Authenticated route content was not forced from this shell because the local token was not exported.
- HOMEFRONT-RUNTIME-FINALIZE-LOOP is now local: the Next dev `[object Event]` overlay from the premium landing/media pass was traced to blocked external font/video resources and the expected pre-hydration `<html>` boot attributes. CSP now allows Google Fonts, CloudFront hero video, Mux HLS, and Mux redirect hosts, while `app/layout.tsx` suppresses the intentional root hydration mismatch. Verification passed with `npm run type-check`, `npm run verify`, a clean `npm run build` after stopping the live dev runtime, a fresh direct `3100` runtime restart, live 200 checks for `/api/health`, `/`, `/hq?focus=hq-chronicle`, `/command`, `/intel?view=news`, and `/resources?view=sources`, plus headless browser probes showing zero console errors, page errors, failed requests, or unhandled rejections on the public/redirect route path. Cookie-authenticated browser proof was not forced from this shell because the local auth token was not exported.
- HOMEFRONT-INFORMATION-ACTION-CONTROL is now local: authenticated non-HQ Homefront shell routes now include a shared purpose/action control rail backed by `lib/surfaceCapabilities.ts`, so each protected surface explains what belongs there, what to use next, what signal posture to watch, and the strongest route/subsection actions without adding another dashboard. `/hq` and the Aether Reliquary/RPG playfield remain excluded from this pass. Verification passed with `npm run type-check`, `npm run verify`, `npm run build`, and live `3100` checks returning 200 for `/api/health`, `/hq?focus=hq-chronicle`, `/command`, `/intel?view=news`, and `/resources?view=sources` after restarting the direct dev runtime through the persistent Node REPL; cookie-authenticated HTML probing was not available in this shell because `NEXUS_TOKEN` was not exported.
- HOMEFRONT-PREMIUM-SHELL-COHESION is now local: the premium live landing visual system has been carried into authenticated shared shell chrome through liquid-glass top navigation, editorial route heads, softened non-HQ work panels, shared source-intake posture, and a quieter bootstrap guard that does not show recovery when the hydrated root is healthy. `/hq` and the Aether Reliquary/RPG playfield remain excluded from this shell-restyle pass. `/resources?view=sources` now remains canonical instead of auto-normalizing back to Finder. Verification passed with `npm run type-check` and `npm run verify`; foreground `npm run dev:direct:3100` proof returned 200 for `/api/health`, `/resources?view=sources`, and `/intel?view=news`. Playwright/browser-worker proof remains blocked on this Windows machine by `spawn EPERM`, so `npm run dev:direct:3100` is the foreground local fallback when the normal Next CLI fork cannot start.
- MW6FR9 Pilgrim Rows Rest + Road Runtime is now implemented for `Aether Reliquary`: Pilgrim Rows now has three validated rest/checkpoint options and three Sister Vael road rumors in `lib/arpgTownServicesContent.json/.ts`, with recovery rewards, route-prep tags, reputation deltas, and durable local-first story flags. `/hq` Map now exposes the Pilgrim Rows runtime card after visiting the district, `/hq` Kit mirrors the rest options, and `/hq` Journal tracks road rumors beside the Bellroot mystery row. `docs/game/aether-reliquary/pilgrim-rows-rest-road-runtime.md`, `docs/game/aether-reliquary/mw6-full-game-completion.md`, `lib/arpgCompletionContent.json`, `lib/massiveWinPlan.ts`, validation, and focused E2E assertions are updated. Verification passed with `npm run arpg:content:check`, `npm run arpg:production:check`, `npm run arpg:release:check`, `npm run type-check`, `npm run build`, `npm run verify`, live `3100` checks returning 200 for `/api/health`, `/hq`, `/hq?focus=hq-chronicle`, and `/resources?view=impact&impactMode=graph`, plus `NEXUS_RELEASE_BASE_URL=http://127.0.0.1:3100 npm run route:integrity`; `npm run runtime:launch:3100` timed out in the known Windows wait path but left a healthy runtime.
- PREMIUM-LIVE-RPG-VISUAL-EXPANSION is now live: authenticated non-HQ Homefront shell thresholds now include a compact premium live-vision strip with image-plane, route-focus, and private-RPG boundary proof while `/hq` stays excluded from Homefront product positioning. `Aether Reliquary` now has a validated visual replacement lane in `lib/arpgVisualReplacementContent.json/.ts` that retires `prologue-location-cards`, `prologue-companion-portraits`, and `prologue-story-prop-icons` from production display, maps ten Bellroot replacement targets to the queued `prologue-hifi-story-pack`, and allows only approved fallback sheets while replacement art is pending. `/hq` Production and Assets/Credits now surface the replacement queue; `docs/game/aether-reliquary/visual-replacement-lane.md`, `docs/assets/arpg-asset-ledger.md`, `specs/features/premium-live-rpg-visual-expansion.md`, package validation, and focused E2E assertions are updated. Verification passed with `npm run arpg:visual-replacements:check`, `npm run type-check`, `npm run verify`, `npm run build`, `npm run tabs:e2e`, and `NEXUS_RELEASE_BASE_URL=http://127.0.0.1:3100 npm run route:integrity`; `npm run hq:e2e` timed out locally and the targeted authenticated HQ browser check hit Windows Playwright `spawn EPERM`, while `http://127.0.0.1:3100/api/health` and route-integrity checks returned 200.
- MW6V First-Town Presentation Cues is now live for `Aether Reliquary`: Bellroot/Veyrhold now has a typed presentation-cue contract in `lib/arpgFirstTownPresentationContent.json/.ts`, with ambient copy, approved-runtime-art links, VFX intent, audio staging intent, low-flash/reduced-motion alternatives, and explicit rejected-prologue-art guardrails. `/hq` Adventure surfaces the active Bellroot cue, Map surfaces Veyrhold district presentation cues, and Production surfaces cue readiness so the first town feels more authored without pretending final bespoke art/audio packs exist. `docs/game/aether-reliquary/first-town-presentation-cues.md`, `docs/game/aether-reliquary/mw6-full-game-completion.md`, `lib/arpgCompletionContent.json`, validation, and focused E2E assertions are updated. Verification passed with `npm run arpg:presentation:check`, `npm run arpg:content:check`, `npm run type-check`, `npm run verify`, `npm run build`, and live `3100` checks returning 200 for `/api/health`, `/hq`, `/hq?focus=hq-chronicle`, and `/resources?view=impact&impactMode=graph`; `npm run hq:e2e` is still locally blocked by Windows Playwright worker `spawn EPERM`, so browser-suite proof remains a fresh-shell follow-up rather than a product regression signal.
- MW6FR8 HQ Runtime Overlay Fix + blocker cleanup is now closed: `/hq` no longer has the game-size hydration mismatch or unsafe Phaser resize path that made the live game surface fail in the in-app browser. The shell height readouts are mount-gated, Phaser `syncViewport` guards `cameras.main`, `npm run orbit:next` now reads the canonical `## Next Up` queue before older backlog items, and `npm run audit:full` delegates to `npm run verify` before printing audit status so sandboxed child-process spawning no longer blocks the full audit lane. Verification passed with `npm run orbit:next`, `npm run audit:full`, `npm run build`, and live `3100` checks returning 200 for `/api/health`, `/hq`, `/hq?focus=hq-chronicle`, and `/resources?view=impact&impactMode=graph`; `npm run hq:e2e` still hits the known Windows Playwright worker `spawn EPERM`, so browser-suite proof remains a fresh-shell follow-up rather than a product regression signal.
- MW6FR7 Bellroot Commons Alchemy + Mystery Runtime is now implemented for `Aether Reliquary`: Bellroot Commons now has three validated safe brews and three Ilo-led oath-lamp readings in `lib/arpgTownServicesContent.json/.ts`, with item rewards, condition tags, mystery hooks, reputation deltas, and durable local-first story flags. `/hq` Map now exposes the Bellroot Commons Lamp Still and readings after visiting the district, `/hq` Kit mirrors the brew list for recovery prep, and `/hq` Journal tracks the lamp-reading clues. `docs/game/aether-reliquary/bellroot-commons-alchemy-mystery-runtime.md`, `docs/game/aether-reliquary/mw6-full-game-completion.md`, `lib/arpgCompletionContent.json`, `lib/massiveWinPlan.ts`, validation, and focused E2E assertions are updated. Verification passed with `npm run arpg:content:check`, `npm run arpg:production:check`, `npm run arpg:release:check`, `npm run type-check`, `npm run build`, and `npm run verify`; `npm run hq:e2e` timed out locally waiting for the Playwright web server, while `http://127.0.0.1:3100/api/health`, `/hq`, `/hq?focus=hq-chronicle`, and `/resources?view=impact&impactMode=graph` returned 200 after the known `runtime:launch:3100` wait-path timeout.
- MW6FR6 Warden's Steps Forge + Armor Runtime is now implemented for `Aether Reliquary`: Warden's Steps now has four validated armor fitting orders for helm, armor, gloves, and boots plus three civic oath contracts in `lib/arpgTownServicesContent.json/.ts`, with quality-path copy, starter item links, rewards, reputation deltas, and durable local-first story flags. `/hq` Map now exposes the Warden's Steps forge and oath-board flow after visiting the district, while `/hq` Armory mirrors the armor fitting path beside the existing gear plan. `docs/game/aether-reliquary/wardens-steps-forge-armor-runtime.md`, `docs/game/aether-reliquary/mw6-full-game-completion.md`, `lib/arpgCompletionContent.json`, `lib/massiveWinPlan.ts`, validation, and focused E2E assertions are updated. Verification passed with `npm run arpg:content:check`, `npm run arpg:production:check`, `npm run arpg:release:check`, `npm run type-check`, `npm run build`, and `npm run verify`; `npm run hq:e2e` timed out locally waiting for the Playwright web server, while `http://127.0.0.1:3100/api/health`, `/hq`, `/hq?focus=hq-chronicle`, and `/resources?view=impact&impactMode=graph` returned 200 after the known `runtime:launch:3100` wait-path timeout.
- MW6FR5 Oathmarket Vendor + Job Runtime is now implemented for `Aether Reliquary`: Oathmarket now has four validated starter wares and three ledger-choice outcomes in `lib/arpgTownServicesContent.json/.ts`, with city-scrip pricing, accessory comparison copy, reward items, reputation deltas, and durable local-first story flags. `/hq` Map now exposes the Oathmarket Exchange and ledger-choice flow after visiting the district, while `/hq` Kit/Inventory exposes the starter accessory wares before a full vendor engine exists. `docs/game/aether-reliquary/oathmarket-vendor-job-runtime.md`, `docs/game/aether-reliquary/mw6-full-game-completion.md`, `lib/arpgCompletionContent.json`, `lib/massiveWinPlan.ts`, validation, and focused E2E assertions are updated. Verification passed with `npm run arpg:content:check`, `npm run arpg:production:check`, `npm run arpg:release:check`, `npm run type-check`, `npm run build`, and `npm run verify`; `npm run hq:e2e` timed out locally with a Playwright reporter `EPIPE`, while `http://127.0.0.1:3100/api/health`, `/hq`, `/hq?focus=hq-chronicle`, and `/resources?view=impact&impactMode=graph` returned 200 after the known `runtime:launch:3100` wait-path timeout.
- MW6FR4 Veyrhold District Hub Runtime is now implemented for `Aether Reliquary`: Veyrhold now has four validated district hub nodes in `lib/arpgTownServicesContent.json/.ts`, linking Oathmarket, Warden's Steps, Bellroot Commons, and Pilgrim Rows to their NPCs, services, mini-quests, rewards, and durable visit flags. `/hq` Map now exposes a compact Town hub board so the first town behaves like a visitable location before the player opens service/job details, while People and Journal keep the NPC/outcome depth from MW6FR3. `docs/game/aether-reliquary/veyrhold-district-hub-runtime.md`, `docs/game/aether-reliquary/mw6-full-game-completion.md`, `lib/arpgCompletionContent.json`, `lib/massiveWinPlan.ts`, validation, and focused E2E assertions are updated. Verification passed with `npm run arpg:content:check`, `npm run arpg:production:check`, `npm run arpg:release:check`, `npm run type-check`, `npm run build`, and `npm run verify`; `npm run hq:e2e` timed out locally, while `http://127.0.0.1:3100/api/health`, `/hq`, `/hq?focus=hq-chronicle`, and `/resources?view=impact&impactMode=graph` returned 200 after the known `runtime:launch:3100` wait-path timeout.
- MW6FR3 Veyrhold NPCs + Mini-Quests is now implemented for `Aether Reliquary`: the first release town now has five named locals, four district mini-quests, and five service outcome cards in `lib/arpgTownServicesContent.json/.ts`, all validated against Veyrhold sub-cities, services, rewards, and story flags. `/hq` Map now exposes town jobs, `/hq` People exposes Dame Ivara, Mara Cindersmith, Ilo, Tovin, and Sister Vael, and `/hq` Journal exposes service outcomes that can record local-first story/reward progress without adding `/game`, cloud saves, multiplayer, or unapproved art. `docs/game/aether-reliquary/veyrhold-npcs-miniquests.md`, `docs/game/aether-reliquary/mw6-full-game-completion.md`, `lib/arpgCompletionContent.json`, `lib/massiveWinPlan.ts`, validation, and focused E2E assertions are updated. Verification passed with `npm run arpg:content:check`, `npm run arpg:production:check`, `npm run arpg:release:check`, `npm run type-check`, `npm run build`, and `npm run verify`; `npm run hq:e2e` timed out locally, while `http://127.0.0.1:3100/api/health`, `/hq`, `/hq?focus=hq-chronicle`, and `/resources?view=impact&impactMode=graph` returned 200 after the known `runtime:launch:3100` wait-path timeout.
- MW6FR2 Veyrhold Town Services is now implemented for `Aether Reliquary`: Veyrhold now has a typed first-town hub contract in `lib/arpgTownServicesContent.json/.ts`, covering Bellroot Anvil, Oath-Lamp Still, Oathmarket Stalls, Pilgrim Rest, Warden Oath Board, four district hooks, and starter gear progression for helm, armor, gloves, boots, rings, and amulet. The core equipment model now includes gloves, `ring-left`, `ring-right`, and `amulet`, with starter items for each slot; `/hq` Map exposes Veyrhold services and gear paths after town unlock, while `/hq` Armory shows the Veyrhold gear plan beside the existing Hero Kit/arsenal surfaces. `docs/game/aether-reliquary/veyrhold-town-services.md`, `docs/game/aether-reliquary/mw6-full-game-completion.md`, `lib/arpgCompletionContent.json`, `lib/massiveWinPlan.ts`, validation, and focused E2E assertions are updated. Verification passed with `npm run arpg:content:check`, `npm run arpg:production:check`, `npm run arpg:release:check`, `npm run type-check`, `npm run build`, and `npm run verify`; `npm run hq:e2e` still times out locally, while `http://127.0.0.1:3100/api/health`, `/hq`, `/hq?focus=hq-chronicle`, and `/resources?view=impact&impactMode=graph` returned 200.
- MW6FR1 First Town Release Slice is now implemented for `Aether Reliquary`: `/hq` Map now frames first release progression as Bellroot -> north gate -> Veyrhold, with a dedicated first-release town card, locked/route-ready/road-active/town-open states, Veyrhold as the only first open town, four Veyrhold district cards, and locked previews for later cities so the 12-city atlas no longer feels accidentally playable before future slices. `docs/game/aether-reliquary/first-town-release-slice.md`, `docs/game/aether-reliquary/mw6-full-game-completion.md`, `lib/arpgCompletionContent.json`, `lib/massiveWinPlan.ts`, `/hq` Map UI, and focused E2E assertions are updated. Verification passed with `npm run arpg:content:check`, `npm run arpg:production:check`, `npm run arpg:release:check`, `npm run type-check`, `npm run build`, and `npm run verify`; `npm run hq:e2e` timed out locally and the focused map retry hit Windows Playwright `spawn EPERM`, while `http://127.0.0.1:3100/api/health`, `/hq`, `/hq?focus=hq-chronicle`, and `/resources?view=impact&impactMode=graph` returned 200.
- MW6V Enemy/Boss High-Fidelity Art + Bellroot Intro Runtime is now implemented for `Aether Reliquary`: `enemy-boss-hifi-cards` is promoted from brief to an approved runtime batch, with generated source at `assets/arpg/illustrated/generated-source/enemy-boss-hifi-cards.png`, normalized runtime output at `public/arpg/illustrated/enemy-boss-hifi-cards.png`, provenance in `docs/game/aether-reliquary/generation-records/2026-04-29-enemy-boss-hifi-cards.md`, and manifest/bench tracking in `lib/arpgAssetManifestData.json` plus `lib/arpgIllustratedAssetBenchContent.json`. `/hq` now prefers the high-fidelity enemy/boss sheet for Adventure and Journal/Codex enemy cards, while the Bellroot opening advances through ledger, lamps, Ilo, gate, Loom-Shard, forge, and Warden's Antechamber before combat actions unlock. The arsenal lane now carries the next armor/accessory escalation requirement for helmet, armor, gloves, boots, necklaces, and rings across common through ultimate/mythic-style tiers. Verification passed with `npm run arpg:illustrated:generate`, `npm run arpg:visual-briefs:check`, `npm run arpg:visual:check`, `npm run arpg:assets:check`, `npm run arpg:content:check`, `npm run arpg:production:check`, `npm run arpg:release:check`, `npm run type-check`, `npm run build`, and `npm run verify`; `npm run hq:e2e` is still locally blocked by Windows `spawn EPERM`, while `http://127.0.0.1:3100/api/health`, `/hq`, `/hq?focus=hq-chronicle`, and `/resources?view=impact&impactMode=graph` returned 200.
- MW6V High-Fidelity Asset Briefs is now live for `Aether Reliquary`: `lib/arpgVisualAssetBriefs.json/.ts` defines 44 Hero Kit-quality production prompts across four batches: 8 enemy/boss cards, 10 Bellroot prologue replacement assets, 14 weapon quality variants/frames, and 12 city cards. `scripts/validate-arpg-visual-briefs.mjs` validates prompt wording, safe output paths, sheet specs, style-reference links, visual-batch links, unique ids, and blocked franchise/flat-glyph drift; `npm run arpg:visual-briefs:check` is now part of `npm run verify`. `/hq` Assets now surfaces the prompt brief count and the next ready batch beside the approved Hero Kit style target. The next recommended real art pass is `enemy-boss-hifi-cards`, because it will make the live game feel less like a UI prototype fastest. Verification passed with `npm run arpg:visual-briefs:check`, `npm run arpg:assets:check`, `npm run type-check`, `npm run build`, and `npm run verify`; `verify` still reports only existing DESIGN.md unused-token warnings and zero design errors.
- MW6V Hero Kit Style Lock is now live for `Aether Reliquary`: Mario approved the current Hero Kit character, outfit, weapon, and armor quality as the visual baseline, so `lib/arpgVisualDirectionContent.json/.ts` now defines the high-fidelity illustrated 2D target, rejected style signals, approved Hero Kit asset ids, and the next four visual batches: enemy/boss cards, Bellroot prologue replacement art, weapon quality variants, and 12 city cards. `npm run arpg:visual:check` validates that approved style-target assets exist, are Hero Kit tagged, are operator-approved, and are not rejected/reference-only; it is now part of `npm run verify`. `/hq` Assets shows an approved style-target card using the current Hero Kit sheets, while the rejected prologue glyph batch remains provenance-only. Verification passed with `npm run arpg:visual:check`, `npm run arpg:assets:check`, `npm run type-check`, `npm run build`, and `npm run verify`; `verify` still reports only existing DESIGN.md unused-token warnings and zero design errors.
- MW6V Art Direction Guardrail is now live for `Aether Reliquary`: Mario rejected the Bellroot prologue flat vector/glyph sheets as not appropriate production game art, so those batches are now marked rejected/reference-only in `lib/arpgIllustratedAssetBenchContent.json`, tagged `style-rejected` in `lib/arpgAssetManifestData.json`, removed from `/hq` production preview usage in `components/home/arpg/ArpgHud.tsx`, and documented as provenance-only in the generation record, asset ledger, illustrated bench, generator pipeline, and MW6 completion docs. The corrected direction is high-fidelity illustrated 2D browser RPG art: painted/rendered cards, portraits, gear, and environment assets, not retro pixel art, not flat SVG glyphs, and not 3D-first. Verification passed with `npm run arpg:illustrated:generate`, `npm run arpg:assets:check`, `npm run arpg:content:check`, `npm run arpg:production:check`, `npm run arpg:release:check`, `npm run type-check`, `npm run build`, and `npm run verify`; `npm run hq:e2e` still timed out locally with no assertion output, while `http://127.0.0.1:3100/api/health` returned 200 on the running runtime.
- MW6E Prologue Story Foundation is now live for `Aether Reliquary`: the opening is canonical as a non-combat Bellroot descent with a player-created protagonist identity, no forced name or gender, the Bellroot Vestibule first location, Descent Ledger, oath-lamps, Ilo's oracle wake-up, Gate Monolith, Loom-Shard Charm, first quest flags, and reusable visual prompt hooks. `lib/arpgPrologueContent.json/.ts`, `docs/game/aether-reliquary/prologue-story-foundation.md`, `docs/game/aether-reliquary/campaign-outline.md`, `docs/game/aether-reliquary/production-bible.md`, `lib/arpgGameContent.ts`, `lib/arpgGame.ts`, `components/home/arpg/ArpgHud.tsx`, `scripts/validate-arpg-content.mjs`, `lib/arpgCompletionContent.json`, and `lib/massiveWinPlan.ts` are updated. Verification passed with `npm run arpg:content:check`, `npm run type-check`, and `npm run verify`; `verify` still reports only the existing DESIGN.md unused-token warnings and zero design errors.
- MW6V Illustrated World + Codex Runtime is now live for `Aether Reliquary`: the approved illustrated 2D sheets now lead more of the actual `/hq` RPG surface, with skill/VFX icons on the hotbar and Skills drawer, location cards in the Map drawer, enemy cards in Journal/Codex rows, and character portrait seed art in People/companion rows. `docs/game/aether-reliquary/illustrated-world-codex-runtime.md`, `docs/game/aether-reliquary/mw6-full-game-completion.md`, `lib/arpgCompletionContent.json`, `lib/massiveWinPlan.ts`, `components/home/arpg/ArpgHud.tsx`, and `tests/e2e/hq-shell.spec.ts` are updated. Verification passed with `npm run arpg:content:check`, `npm run arpg:asset-candidates:check`, `npm run arpg:assets:check`, `npm run arpg:production:check`, `npm run arpg:save:check`, `npm run arpg:balance:check`, `npm run arpg:release:check`, `npm run type-check`, `npm run build`, `npm run hq:e2e`, `npm run route:e2e`, `npm run tabs:e2e`, and `npm run verify`; the first `hq:e2e` attempt reused a stale managed `3100` runtime and passed after `npm run runtime:stop:3100` forced a fresh server. `MW6V-REAL-ASSET-INTAKE` remains blocked until approved real pack files exist locally.
- MW6V Hero Kit Runtime Presentation is now live for `Aether Reliquary`: approved illustrated Hero Kit art moved beyond drawer previews into the actual `/hq` RPG loop. Persistent loadout slots now show matching Hero Kit item/equipment icons, the compact click-RPG panel shows the active class portrait/outfit plus reward icon, Adventure cards now show validated location/enemy/loot art, and Wardbreaker/Relicweaver/Ashrunner class choices are image-led while unapproved classes keep procedural fallback. `docs/game/aether-reliquary/hero-kit-runtime-presentation.md`, `docs/game/aether-reliquary/hero-kit-image-assets.md`, `docs/game/aether-reliquary/mw6-full-game-completion.md`, `lib/arpgCompletionContent.json`, and `lib/massiveWinPlan.ts` are updated. Verification passed with `npm run arpg:content:check`, `npm run arpg:assets:check`, `npm run arpg:release:check`, `npm run type-check`, `npm run hq:e2e`, `npm run verify`, and `npm run build`; the first build attempt was incorrectly run in parallel with `verify` and hit a transient Next page-collection race, then a clean standalone `npm run build` passed.
- MW6U/V Hero Kit Image Assets is now live for `Aether Reliquary`: operator-approved image generation produced 3 hero/class portraits, 3 class outfit cards, 12 weapon/item icons, and 8 armor/equipment icons, with generated source sheets stored under `assets/arpg/illustrated/generated-source/` and normalized runtime sheets under `public/arpg/illustrated/`. `lib/arpgIllustratedAssetBenchContent.json`, `lib/arpgAssetManifestData.json`, `lib/arpgAssetManifest.ts`, and `scripts/validate-arpg-assets.mjs` now track `outfit-card` support, per-batch prompt records, generation metadata, rights posture, cost posture, source/runtime paths, frame dimensions, and `generator-assisted` tags. `/hq` now prefers the approved Hero Kit art in the Assets drawer plus compact Hero, Kit, and Armory previews while Phaser gameplay, command input, local-first saves, reduced-motion posture, and route stability remain intact. Canon docs are updated in `docs/game/aether-reliquary/hero-kit-image-assets.md`, `docs/game/aether-reliquary/generation-records/2026-04-27-hero-kit-image-assets.md`, `docs/game/aether-reliquary/illustrated-2d-asset-bench.md`, `docs/game/aether-reliquary/generator-assisted-art-pipeline.md`, `docs/assets/arpg-asset-ledger.md`, and `docs/game/aether-reliquary/mw6-full-game-completion.md`. Verification passed with `npm run arpg:illustrated:generate`, `npm run arpg:content:check`, `npm run arpg:asset-candidates:check`, `npm run arpg:assets:check`, `npm run arpg:production:check`, `npm run arpg:release:check`, `npm run type-check`, `npm run hq:e2e`, `npm run build`, `npm run verify`, `npm run route:e2e`, and `npm run tabs:e2e`; the first `route:e2e` attempt reused a stale dev server after `.next` was rebuilt, then passed after `npm run runtime:stop:3100` let Playwright start a fresh server. `npm run runtime:launch:3100` timed out in the known Windows wait path, but `/api/health` returned 200; in-app browser visual acceptance is auth-gated and requires operator token entry, which was not typed or inspected by Codex.
- MW6U/V Illustrated 2D Asset Bench is now live for `Aether Reliquary`: high-quality illustrated 2D is the primary visual production lane while real 3D intake remains optional/reference until approved local packs exist. `lib/arpgIllustratedAssetBenchContent.json/.ts` defines five reviewed bench batches covering 3 character portraits, 4 enemy cards, 3 location cards, 8 gear icons, and 6 skill/VFX icons; `assets/arpg/illustrated/source/*.svg` is rasterized by `npm run arpg:illustrated:generate` into `public/arpg/illustrated/*.png`; `lib/arpgAssetManifestData.json`, `lib/arpgAssetManifest.ts`, and `scripts/validate-arpg-assets.mjs` now validate illustrated asset kinds, prompt records, operator approval, rights posture, cost posture, source/runtime paths, tags, frame dimensions, and no forced paid dependency. `/hq` Assets now shows the illustrated bench previews before the blocked real-model intake state, and `docs/game/aether-reliquary/illustrated-2d-asset-bench.md`, `docs/game/aether-reliquary/generator-assisted-art-pipeline.md`, `docs/assets/arpg-asset-ledger.md`, and `docs/game/aether-reliquary/mw6-full-game-completion.md` record that GPT Image 2 is optional/operator-approved rather than a required paid dependency. Verification passed with `npm run arpg:illustrated:generate`, `npm run arpg:content:check`, `npm run arpg:asset-candidates:check`, `npm run arpg:assets:check`, `npm run arpg:production:check`, `npm run arpg:tools:check`, `npm run arpg:save:check`, `npm run arpg:balance:check`, `npm run arpg:release:check`, `npm run type-check`, `npm run hq:e2e`, `npm run build`, `npm run route:e2e`, `npm run tabs:e2e`, and `npm run verify`; the first `hq:e2e` run reused a stale `3100` runtime, then passed after `npm run runtime:stop:3100` forced a fresh server. Post-acceptance `npm run runtime:launch:3100` timed out during the known Windows wait path, then `/api/health`, `/hq`, `/hq?focus=hq-chronicle`, and `/resources?view=impact&impactMode=graph` returned 200 after the fallback runtime finished compiling.
- MW6U/V Sprite Tool Candidate Intake is now recorded for `Aether Reliquary`: Mario-provided `agent-sprite-forge` and `spritefusion-pixel-snapper` links are tracked in `lib/arpgAssetToolCandidateSources.json` as guarded MIT tooling references, validated by `npm run arpg:asset-candidates:check`, and surfaced in the `/hq` Assets drawer without treating either repo as a runtime asset pack. `scripts/validate-arpg-asset-candidates.mjs` now validates both real asset source candidates and optional sprite tool candidates, while `docs/game/aether-reliquary/generator-assisted-art-pipeline.md`, `docs/game/aether-reliquary/real-asset-acquisition.md`, and `docs/assets/arpg-asset-ledger.md` clarify that these tools can help original sprite/map production only after operator approval, prompt/provenance capture, rights review, normalization, and manifest validation. `MW6V-REAL-ASSET-INTAKE` remains blocked until approved local asset packs exist; no external repo was vendored and no unverified asset was committed. Verification passed with `npm run arpg:asset-candidates:check`, `npm run arpg:content:check`, `npm run arpg:production:check`, `npm run arpg:assets:check`, `npm run type-check`, `npm run hq:e2e`, `npm run build`, and `npm run verify`.
- MW6Z Balance + Playtest Foundation is now live for `Aether Reliquary`: `lib/arpgBalancePlaytestContent.json` and `lib/arpgBalancePlaytestContent.ts` define the MW6Z balance/playtest contract for session pacing, level bands, XP anchors, loot cadence, boss timing, potion pressure, class and lineage viability, +1 through +5 upgrade economy, browser performance budgets, input-feel checks, and a 20-row playtest checklist covering the prologue, Acts I-V, The Hollow Regent finale, postgame, and all 12 major city hubs. `npm run arpg:balance:check` now validates that registry and reports the playtest row count; `/hq` Production shows the balance/playtest summary with stable proof chips; `arpg:release:check` requires the new E2E signals; and `docs/game/aether-reliquary/balance-playtest.md` records the durable contract. `MW6Z-ARPG-BALANCE-PLAYTEST` is removed from `tasks/todo.md` and marked done in `lib/arpgCompletionContent.json`; `MW6U`, `MW6V`, `MW6AA`, and the MW6 parent remain open, with real asset intake still blocked until approved local packs exist. Verification passed with `npm run arpg:content:check`, `npm run arpg:asset-candidates:check`, `npm run arpg:assets:check`, `npm run arpg:production:check`, `npm run arpg:tools:check`, `npm run arpg:save:check`, `npm run arpg:balance:check`, `npm run arpg:release:check`, `npm run type-check`, `npm run hq:e2e`, `npm run build`, `npm run route:e2e`, `npm run tabs:e2e`, and `npm run verify`. The first post-build `route:e2e` run hit a stale managed `3100` runtime returning `/api/health` 500; `npm run runtime:stop:3100` plus `npm run runtime:launch:3100` restored `/api/health` 200, and one follow-up `spawn EPERM` retry then passed from a fresh shell. Final live checks returned 200 for `/api/health`, `/hq`, `/hq?focus=hq-chronicle`, and `/resources?view=impact&impactMode=graph`.
- MW6Y Content Tools Foundation is now live for `Aether Reliquary`: `lib/arpgContentToolsContent.json/.ts` defines the MW6Y tooling contract for 12 expansion registries, 8 authoring helpers, 6 fixture-save references, 8 progression checks, and 10 authoring rules covering cities, sub-cities, quests, enemies, bosses, items, vendors, recipes, companions, dialogue, maps, flags, map coordinates, fixture saves, balance snapshots, and dev-only debug posture. `npm run arpg:tools:check` validates the registry and is now part of `npm run verify`; `/hq` Production shows the content-tooling summary and proof chips so future city/quest/map/enemy/item/vendor/companion/dialogue/flag work starts from validated contracts, not renderer callbacks or hand-edited save state. `MW6Y-ARPG-CONTENT-TOOLS` is removed from `tasks/todo.md` and marked done in `lib/arpgCompletionContent.json`; `MW6U`, `MW6V`, `MW6Z`, `MW6AA`, and the MW6 parent remain open, with real asset intake still blocked until approved local packs exist. Verification passed with `npm run arpg:content:check`, `npm run arpg:tools:check`, `npm run arpg:production:check`, `npm run arpg:release:check`, `npm run type-check`, `npm run hq:e2e`, `npm run build`, `npm run route:e2e`, `npm run tabs:e2e`, and `npm run verify`; the first post-build `route:e2e` run reused a stale managed `3100` runtime returning `/api/health` 500, then passed after `npm run runtime:stop:3100` let Playwright start a fresh server. Post-acceptance `npm run runtime:launch:3100` timed out in the known wait path, but `/api/health`, `/hq`, `/hq?focus=hq-chronicle`, and `/resources?view=impact&impactMode=graph` returned 200 on the managed runtime.
- MW6W Production Menus + Codex Runtime is now live for `Aether Reliquary`: `lib/arpgProductionReadinessContent.json` now treats the 14-panel RPG menu matrix as enforceable production UI metadata with drawer targets, stable test IDs, empty states, keyboard-safe posture, and reduced-motion rules. `/hq` now renders that matrix through a focused `ArpgProductionMenuIndex` component, launches every production menu into its real compact drawer, shows an active menu context strip, and adds a Room-drawer tutorial/control panel for movement, attacks, skills, dodge, interact, and playfield resizing while keeping the Phaser surface dominant and the HQ command input usable. `MW6W-ARPG-HUD-MENUS-CODEX` is removed from `tasks/todo.md` and marked done in `lib/arpgCompletionContent.json`; this slice left `MW6U`, `MW6V`, `MW6Y`, `MW6Z`, `MW6AA`, and the MW6 parent open, with MW6Y closed by the newer content-tools slice above. Verification passed with `npm run arpg:content:check`, `npm run arpg:asset-candidates:check`, `npm run arpg:assets:check`, `npm run arpg:production:check`, `npm run arpg:save:check`, `npm run arpg:balance:check`, `npm run arpg:release:check`, `npm run type-check`, `npm run build`, `npm run hq:e2e`, `npm run route:e2e`, `npm run tabs:e2e`, and `npm run verify`; one post-build `hq:e2e` attempt timed out because the stale managed `3100` runtime returned `/api/health` 500, then passed after `npm run runtime:stop:3100` let Playwright start a fresh server. Post-acceptance `npm run runtime:launch:3100` timed out in the known wait path, but `/api/health`, `/hq`, `/hq?focus=hq-chronicle`, and `/resources?view=impact&impactMode=graph` returned 200 on the managed runtime.
- MW6X Save Actions + MW6W Menu Launcher is now live for `Aether Reliquary`: `lib/arpgSaveEnvelope.ts` now normalizes real autosave/manual/checkpoint slots, imports/export envelopes with slot metadata, and keeps raw-save imports compatible while the core `ArpgSaveState` stays version 3. `store/useStore.ts` now persists `arpgSaveSlots` plus `arpgActiveSaveSlotId` and exposes manual save, checkpoint save, slot load, and confirmed reset actions; older persisted state that only has `arpgSave` now normalizes into a full slot set. `/hq` Production turns the 14-panel MW6 menu index into launcher buttons mapped to existing drawers, and the Room drawer now supports Continue, Save manual, Save checkpoint, Load slot, Export envelope, Import/repair, corrupted JSON blocking, and a two-click reset guard without adding `/game`, cloud saves, accounts, multiplayer, or unverified assets. `MW6X-ARPG-SAVE-MIGRATION` is removed from `tasks/todo.md` and marked done in `lib/arpgCompletionContent.json`; this slice left `MW6W`, `MW6U`, `MW6V`, `MW6Y`, `MW6Z`, `MW6AA`, and the MW6 parent open, with MW6W closed by the newer production-menu slice above. Verification passed with `npm run arpg:content:check`, `npm run arpg:asset-candidates:check`, `npm run arpg:assets:check`, `npm run arpg:production:check`, `npm run arpg:save:check`, `npm run arpg:balance:check`, `npm run arpg:release:check`, `npm run type-check`, `npm run build`, `npm run hq:e2e`, `npm run route:e2e`, `npm run tabs:e2e`, and `npm run verify`; the first `type-check` attempt raced with parallel `build` cleanup, then passed when rerun after build, and the first `route:e2e` attempt found a stale managed runtime returning 500 before `runtime:stop:3100` plus `runtime:launch:3100` restored `/api/health` to 200.
- MW6-AA Large-Chunk Readiness Foundation is now live for `Aether Reliquary`: `lib/arpgProductionReadinessContent.json/.ts` now tracks the 14-panel full-game menu matrix, autosave/manual/checkpoint slot policies, fixture paths for MW5 v2/raw MW6 v3/envelope v1/corrupted saves, seven validation scripts, eight balance fixture suites, eight class viability targets, static release gates, and 19 release-flow targets. New dedicated gates `npm run arpg:save:check`, `npm run arpg:balance:check`, and `npm run arpg:release:check` are wired into `npm run verify`; `/hq` Production drawer now exposes menu/save/balance/release posture and the new script gates; `docs/game/aether-reliquary/save-fixtures/` carries the save fixture set; `docs/game/aether-reliquary/mw6-aa-large-chunk-readiness.md` records the durable contract; and `scripts/start-runtime.mjs` now safely falls back from missing standalone output to local Next runtime launch so `http://127.0.0.1:3100` can still be reviewed. Verification passed with `npm run arpg:content:check`, `npm run arpg:asset-candidates:check`, `npm run arpg:assets:check`, `npm run arpg:production:check`, `npm run arpg:save:check`, `npm run arpg:balance:check`, `npm run arpg:release:check`, `npm run type-check`, `npm run build`, `npm run hq:e2e`, `npm run route:e2e`, `npm run tabs:e2e`, and `npm run verify`; `npm run runtime:launch:3100` timed out in the known launcher wait path, but `/api/health`, `/hq`, `/hq?focus=hq-chronicle`, and `/resources?view=impact&impactMode=graph` returned 200 afterward on the fallback runtime. `MW6V-REAL-ASSET-INTAKE` remains blocked until approved real pack files exist locally; the MW6 parent remains open until MW6U-AA runtime/presentation/save/tooling/balance/release tracks are fully closed.
- MW6 Production Readiness Foundation is now live for `Aether Reliquary`: `lib/arpgProductionReadinessContent.json/.ts` defines enforceable MW6U-AA readiness across accepted licenses, commercial-license proof rules, optional GPT Image 2 / Seedance 2.0 policy, save envelope rules, content-tooling fixtures, balance targets, 12-city playtest coverage, browser budgets, and release gates. `npm run arpg:production:check` validates that contract and is now part of `npm run verify`; `lib/arpgAssetManifest.ts` and asset validation now support redacted `commercial-license` records without allowing marketplace previews, personal-use-only assets, forced-paid AI, or unverified licenses. `/hq` now exports `aether-reliquary-save-envelope-v1`, imports either envelopes or legacy raw saves, shows production readiness in the `Prod` drawer, and shows real-model preview as honestly blocked in the `Assets` drawer until official assets exist. `MW6U/V-GENERATOR-ASSISTED-GAME-ART` is closed as an optional policy/tooling lane, while `MW6U`, `MW6V`, `MW6W`, `MW6X`, `MW6Y`, `MW6Z`, and `MW6AA` remain open for runtime asset import, presentation, menus, save slots, tooling, balance, and release coverage. Verification passed with `npm run arpg:content:check`, `npm run arpg:asset-candidates:check`, `npm run arpg:assets:check`, `npm run arpg:production:check`, `npm run type-check`, `npm run build`, `npm run hq:e2e`, `npm run route:e2e`, `npm run tabs:e2e`, and `npm run verify`; `npm run runtime:launch:3100` still timed out in the known Windows launcher path, but `http://127.0.0.1:3100/api/health` returned 200 afterward.
- MW6W Playfield Resize + Compact HUD Density is now live for `Aether Reliquary`: `/hq` now exposes explicit `S / M / L / XL` game-window controls beside the ARPG/Command-room toggle, persists the selected size through settings, applies the size through the existing HQ split-height path, and also resizes the actual playable frame inside the constrained shell so Codex's limited browser viewport can shrink or expand the game surface without hidden gestures. `components/home/arpg/ArpgHud.tsx` now defaults to compact HUD density with smaller objective, loadout, combat, prompt, AI, drawer-nav, and drawer chrome so oversized text blocks no longer dominate the playfield. The HQ E2E playable-loop coverage now asserts compact density, visible size controls, real playfield resizing, movement, combat, loot, skills, command input usability, and command-room fallback. Verification passed with `npm run type-check`, `npm run build`, `npm run hq:e2e`, and `npm run verify`; `npm run runtime:launch:3100` still timed out in the known Windows launcher path, but `http://127.0.0.1:3100/api/health` returned 200 afterward.
- MW6 Completion Control Plane is now live for `Aether Reliquary`: `lib/arpgCompletionContent.json/.ts` defines the MW6 parent closure contract across shipped foundations and remaining MW6U-AA production tracks, and `npm run arpg:content:check` now validates the completion registry, required track IDs, doc links, statuses, categories, gates, and the honest blocked state for real asset intake. `/hq` now includes a compact `Prod` drawer showing MW6 completion percent, current/blocked production tracks, next gates, and parent-close readiness; the Room drawer now exposes save export plus import/normalization controls for recovery testing without breaking local-first persistence. Resources now tracks the active massive-win lane as `MW6 full-game production completion`, and `docs/game/aether-reliquary/mw6-full-game-completion.md` records the closure contract. Verification passed with `npm run arpg:content:check`, `npm run type-check`, `npm run build`, `npm run hq:e2e`, `npm run tabs:e2e`, `npm run route:e2e`, and `npm run verify`; `npm run verify` still reports only existing DESIGN.md unused-token warnings and zero design errors. The MW6 parent and MW6U-AA tracks remain open because production assets, full menus, save hardening, content tools, balance fixtures, and release gates still need their own completion slices.
- MW6W Image-Driven Browser RPG Shell is now live for `Aether Reliquary`: `/hq` now exposes a compact `Click RPG` action strip inside the combat panel plus a new `Adventure` drawer with illustrated world-travel, encounter, loot, and story cards. The cards reuse the validated MW6 world-loop, combat, inventory, quest, and save systems so players can click routes, fight, claim loot, and accept story beats like a browser RPG while Phaser continues to prove movement/combat underneath. Resources now tracks the active massive-win lane as `MW6 image-driven browser RPG shell`, and `docs/game/aether-reliquary/image-driven-browser-rpg.md` captures the durable presentation contract. Verification passed with `npm run type-check`, `npm run build`, `npm run hq:e2e`, `npm run route:e2e`, `npm run tabs:e2e`, and `npm run verify`; `npm run verify` still reports only existing DESIGN.md unused-token warnings and zero design errors. `npm run runtime:launch:3100` timed out in the known Windows launcher path, but `http://127.0.0.1:3100/api/health` returned 200 afterward.
- MW6V Real Asset Intake Bridge is now live as the honest next step for `Aether Reliquary` art quality: `scripts/import-arpg-real-assets.mjs` scans ignored `assets/arpg/intake/work/<candidate-id>/` folders, rejects raw archives, unknown candidate folders, malformed GLB/glTF, and oversized browser files, then copies accepted runtime assets into `public/arpg/imported/<candidate-id>/` for manifest review. `lib/arpgAssetIntake.ts` derives the in-game intake posture from the validated candidate queue and manifest, `lib/arpgAssetManifest.ts`/`scripts/validate-arpg-assets.mjs` now support real imported GLB/glTF and preview kinds with stricter provenance requirements, and the `/hq` Assets drawer now shows the real-asset readiness state, approved Quaternius/Kenney source queue, and blocked/no-model status without pretending placeholder art is production asset work. The broader `MW6V-REAL-ASSET-INTAKE` task remains open because the sandbox cannot download Quaternius directly and no real pack files have been placed in `assets/arpg/intake/raw/` or imported yet. Verification passed with `npm run arpg:assets:import`, `npm run arpg:asset-candidates:check`, `npm run arpg:assets:check`, `npm run type-check`, `npm run hq:e2e`, `npm run route:e2e`, `npm run tabs:e2e`, `npm run build`, and `npm run verify`; `npm run verify` still reports only existing DESIGN.md unused-token warnings and zero design errors.
- MW6V Character + Armor Illustrated Art Batch is now live for `Aether Reliquary`: `assets/arpg/original/player-character-sprites.svg` and `assets/arpg/original/armor-cosmetic-icons.svg` are hand-authored project-original source sheets rasterized through `npm run arpg:art:generate` into `public/arpg/player-character-sprites.png` and `public/arpg/armor-cosmetic-icons.png`; Phaser now uses the 96x128 playable class sprite atlas instead of the tiny colored player marker, Hero/Armory drawers show the new illustrated player/armor art, and the art lane explicitly pivots away from pixel art. `lib/arpgAssetManifestData.json`, `docs/assets/arpg-asset-ledger.md`, and `docs/game/aether-reliquary/art-batches/character-armor-sprites.md` capture provenance, frame metadata, and the no-paid-AI/no-marketplace/no-franchise-derived boundary. Verification passed with `npm run arpg:art:generate`, `npm run arpg:assets:check`, `npm run arpg:content:check`, `npm run type-check`, `npm run build`, and `npm run hq:e2e` after rerunning build and E2E separately so `.next` was not cleaned under the browser server.
- MW6T Aether Reliquary Dungeons + Endgame Foundation is now live as the next long-term-play bridge: `lib/arpgEndgameContent.json/.ts` defines and derives 5 difficulty tiers, 6 dungeon archetypes, 8 elite affix rotations, 12 city challenge dungeons, 12 relic trials, 48 timed treasure rooms, 48 treasure maps, 23 boss rematches across city/act/world/final memories, 8 class arena challenges, 7 collection goals, 8 cosmetics, and reward tracks against the existing MW6 canon. `lib/arpgGame.ts` keeps save version 3 and adds only additive `endgame` state plus local-first actions for difficulty selection, dungeon/trial/rematch start and completion, treasure-map resolution, arena clears, collection goals, and cosmetic claims; `store/useStore.ts` exposes those actions; and `components/home/arpg/ArpgHud.tsx` adds a compact `Trials` drawer that proves the loop inside `/hq` without adding `/game`, cloud saves, accounts, multiplayer, or unverified assets. Resources now tracks the active massive-win lane as `MW6 dungeons + endgame foundation`, and `docs/game/aether-reliquary/mw6-t-dungeons-endgame.md` captures the durable contract. Verification passed with `npm run arpg:content:check`, `npm run arpg:assets:check`, `npm run type-check`, `npm run hq:e2e`, `npm run route:e2e`, `npm run tabs:e2e`, `npm run build`, and `npm run verify`; `npm run verify` still reports only existing DESIGN.md unused-token warnings and zero design errors.
- MW6I-S Aether Reliquary Systems + World Loop Foundation is now live as the next full-game production bridge: `lib/arpgEnemyTaxonomyContent.json/.ts`, `lib/arpgArmoryEconomyContent.json/.ts`, and `lib/arpgWorldLoopContent.json/.ts` now validate and derive enemy taxonomy, traits/buffs/debuffs, 48 regional archetypes, 48 sub-city champions, 12 city bosses, Hollow Regent final forms, postgame world bosses, 21 low-tech weapon families, expanded gear/quality/economy rules, recipes, salvage, city armor sets, 7 campaign phases, 12 city storylines, 48 sub-city arcs, route events, NPC roles, dialogue flags, and 8 companion arcs against the MW6 production bible. `lib/arpgGame.ts` keeps save version 3 and adds only additive journey/travel, quest, companion, crafting, salvage, and reputation actions while preserving older save normalization; `store/useStore.ts`, `components/home/arpg/ArpgHud.tsx`, and `components/home/arpg/phaser/ArpgPhaserGame.tsx` now expose compact Map, Armory, Journal, People, and north-gate travel proof inside `/hq` without adding `/game`, changing auth, or burying the Phaser playfield. Resources now tracks the active massive-win lane as `MW6 systems + world loop foundation`, and `docs/game/aether-reliquary/mw6-i-s-systems-world-foundation.md` captures the durable contract. Verification passed with `npm run arpg:content:check`, `npm run arpg:assets:check`, `npm run type-check`, `npm run hq:e2e`, `npm run route:e2e`, `npm run tabs:e2e`, `npm run build`, and `npm run verify`; `npm run verify` still reports only existing DESIGN.md unused-token warnings and zero design errors.
- MW6I-L/V Aether Reliquary Combat Art Foundation is now live as the next full-game production slice: `lib/arpgCombatContent.ts` defines first-zone damage types, statuses, enemy traits/families, combat profiles, elite modifiers, and sprite/icon frame keys; `lib/arpgGame.ts` now carries additive combat state for player/enemy statuses, cooldowns, latest combat events, target enemy, codex discovery, and dodge timing while preserving v3 save compatibility; `store/useStore.ts`, `components/home/arpg/phaser/ArpgPhaserGame.tsx`, and `components/home/arpg/ArpgHud.tsx` now expose target selection, Space/basic, 1/2 hotbar skills, Shift/dodge, damage numbers, telegraphs, status icons, target/status chips, item icons, and a compact combat codex without blocking the HQ command input. The first project-original art pack is committed as hand-authored SVG source under `assets/arpg/original/`, generated locally into `public/arpg/enemies-first-reliquary.png`, `public/arpg/items-first-reliquary.png`, and `public/arpg/status-effects.png` through `npm run arpg:art:generate`, with manifest/ledger coverage in `lib/arpgAssetManifestData.json` and `docs/assets/arpg-asset-ledger.md`; no paid AI, paid marketplace, external download, or franchise-derived art was used. Resources now tracks the active massive-win lane as `MW6 combat art foundation`, and `docs/game/aether-reliquary/combat-art-foundation.md` captures the durable combat-art contract. Verification passed with `npm run arpg:art:generate`, `npm run arpg:content:check`, `npm run arpg:assets:check`, `npm run type-check`, `npm run hq:e2e`, `npm run route:e2e`, `npm run tabs:e2e`, `npm run build`, and `npm run verify`; `npm run runtime:launch:3100` still timed out in the known Windows launcher path, but `http://127.0.0.1:3100/api/health` returned 200 afterward.
- MW6F-H Aether Reliquary Character Foundation is now live as the next full-game production slice: `lib/arpgCharacterContent.json` plus `lib/arpgCharacterContent.ts` define the focused playable protagonist registry for eight lineages, eight class trees, two subclasses per class, starter active/passive skills, palettes, portraits, dialogue tags, city reputation hooks, and default `Veyr Human / Wardbreaker / Shieldcleaver` identity; `lib/arpgGame.ts` now normalizes MW5/v2 saves into MW6/v3 saves while preserving movement, inventory, gear, quests, enemies, world flags, and story flags; `store/useStore.ts` exposes local-first character creation, respec, cosmetic, and skill actions; `/hq` now includes compact Hero and Skills drawers plus a persistent identity chip; the Phaser avatar reflects palette/class identity through procedural tinting; Resources now tracks the active massive-win lane as `MW6 character foundation`; and `docs/game/aether-reliquary/character-foundation.md` captures the durable character contract. Verification passed with `npm run arpg:content:check`, `npm run arpg:assets:check`, `npm run type-check`, `npm run build`, `npm run verify`, `npm run hq:e2e`, `npm run route:e2e`, and `npm run tabs:e2e`; `npm run runtime:launch:3100` timed out in the known Windows launcher path, but `/api/health` returned 200 on `http://127.0.0.1:3100`, and the browser suites ran green against the healthy managed runtime.
- MW6 Aether Reliquary Bible Foundation is now live as the first full-game production slice: `docs/game/aether-reliquary/` now owns the production bible, world atlas, campaign outline, systems bible, and content-validation rules; `lib/arpgProductionContent.json` plus `lib/arpgProductionContent.ts` define the typed MW6 registry for exactly 12 major cities, 48 sub-cities, route unlocks, campaign acts, eight races, eight classes, enemy/status/weapon/gear/economy lists, and companion anchors; `lib/arpgGame.ts` now exposes the v3 production save contract shape for future character creation, reputations, companions, quest log, map flags, codex, and crafting migration work without flipping the active MW5 runtime save yet; `npm run arpg:content:check` validates the registry and is now part of `npm run verify`; `npm run arpg:assets:check` now also enforces license proof plus frame metadata for sprite/tile/icon/FX assets; and Resources now tracks the active massive-win lane as `MW6 full-game production`. Verification passed with `npm run arpg:content:check`, `npm run arpg:assets:check`, `npm run type-check`, `npm run verify`, `npm run build`, `npm run hq:e2e`, `npm run route:e2e`, and `npm run tabs:e2e`; the first build attempt hit the known Windows `.next` EPERM lock, then passed after `npm run runtime:stop:3100`. Post-build `npm run runtime:launch:3100` did not relaunch because `.next/standalone/server.js` was missing from the build output, and a dev-server fallback launcher was blocked by local command policy, so the in-app browser runtime may need a manual `npm run dev` or a later standalone-output repair before visual smoke.
- P1 Whole-app Cinematic IA Completion is now live across the protected GA shell: `lib/cinematicIA.ts` defines the `cinematic-ia-v1` contract for HQ, COMMAND, INTEL, ALPHA, CYBER, RECON, VAULT, and RESOURCES; root chrome now stamps the active cinematic surface/posture/hierarchy onto `main`; `ShellPage`/`ShellStage` expose the same contract plus lead/support/inspector/continuity zones; and legacy empty/loading surfaces now render through the shared surface primitives instead of one-off inline styles. HQ visual integrity stays covered by the existing ARPG/command-shell E2E path, and the GA tab sweep now asserts the cinematic contract for each supported surface. Verification passed with `npm run type-check`, `npm run build`, `npm run hq:e2e`, `npm run verify`, and `NEXUS_RELEASE_BASE_URL=http://127.0.0.1:3100 npm run route:integrity`; a later `route:e2e`/`tabs:e2e` rerun plus ad-hoc Playwright browser launch were blocked by Windows `spawn EPERM` after the managed `3100` runtime returned `/api/health` 200.
- MW5 Aether Reliquary Phaser RPG Replacement is now live inside the existing HQ command shell: the `/hq` ARPG stage no longer relies on the React Three Fiber reliquary room as the primary game surface, and instead mounts a Phaser 2D browser RPG for `The First Reliquary` while keeping the DOM HUD, command input, AI-working indicators, Pixi/Canvas VFX overlay, and command-room fallback intact. The local-first save contract now normalizes old ARPG saves into a v2 RPG shape with HP/mana/XP/level/gold, class path, active quest, eight equipment slots, stackable inventory entries, gear qualities, +0 to +5 upgrade ranks, affixes, unlocked/equipped skills, world/chest/enemy flags, and story progression. The authored slice includes the Gate Monolith, Oracle Companion, Relic-Forge Echo, loot caches, consumables, upgrade materials, Wardbreaker/Relicweaver/Ashrunner starter paths, Hollow Sentry, Ashling Scout, Rune Husk, and the mini-boss `The Brass Warden`; Resources now tracks the active massive-win lane as `Phaser RPG replacement`. Verification passed with `npm run arpg:assets:check`, `npm run type-check`, `npm run build`, `npm run hq:e2e`, `npm run route:e2e`, `npm run tabs:e2e`, and `npm run verify`; live `3100` health checks returned 200 for `/hq`, `/hq?focus=hq-chronicle`, and `/resources?view=impact&impactMode=graph`. A fresh dev-server E2E attempt on `3101` still hit the known Windows `spawn EPERM` path, so browser proof used the rebuilt managed `3100` runtime.
- MW4 PixiJS + GSAP ARPG Game UI/VFX Lab is now live inside the existing HQ command shell: `Aether Reliquary` keeps the React Three Fiber reliquary as the primary `/hq` playfield while a new Pixi-ready, non-intercepting VFX stage sits above the canvas and below the DOM HUD for ambient, objective, oracle, loot, equip, and hit cues. The current sandbox cannot fetch `pixi.js`, `@pixi/react`, `gsap`, or `@gsap/react` because npm registry requests fail with `EACCES`, so the shipped slice uses a Canvas VFX fallback plus Web Animations HUD/drawer fallback until those packages can be installed and bundled from a network-enabled shell. The HUD remains accessible DOM, reduced-motion browser settings calm the overlay, command input and command-room fallback stay usable, and Resources now tracks the active massive-win lane as `PixiJS + GSAP game-feel lab`. Verification passed with `npm run arpg:assets:check`, `npm run type-check`, `npm run hq:e2e`, `npm run tabs:e2e`, `npm run route:e2e`, `npm run build`, and `npm run verify`; live `3100` browser smoke confirmed `/hq`, `/hq?focus=hq-chronicle`, and `/resources?view=impact&impactMode=graph`.
- MW3 Aether Reliquary Playfield-First Assets is now live inside the existing HQ command shell: the ARPG room keeps `/hq` as the only game surface but shifts from large dashboard panels to compact objective/loadout chips, a contextual interaction prompt, tiny AI-oracle indicators, and drawer-based inventory/journal/settings/assets controls so the 3D playfield dominates the first view. The game state now exposes proximity-driven lore/loot/enemy prompts, the scene has richer stylized low-poly procedural arch/column/brazier/crate/floor fallback props, and Resources now tracks the active lane as `Aether Reliquary playfield-first ARPG lane`. Asset intake is governed by `lib/arpgAssetManifest.ts`, `lib/arpgAssetManifestData.json`, `docs/assets/arpg-asset-ledger.md`, and `npm run arpg:assets:check`, with CC0-first sources and CC-BY guarded by visible credits. Verification passed with `npm run arpg:assets:check`, `npm run type-check`, `npm run hq:e2e`, `npm run route:e2e`, `npm run tabs:e2e`, `npm run build`, and `npm run verify`; `/api/health` returns 200 on `http://127.0.0.1:3100`. Direct ad-hoc browser launch was blocked by sandbox `spawn EPERM`, so authenticated Playwright browser suites covered the acceptance pass.
- MW2 Ancient Techno-Fantasy HQ ARPG Room Foundation is now live inside the existing HQ command shell: the 3D room defaults to the playable `Aether Reliquary` scene with local-first movement, story objective, lore interaction, loot pickup, gear equip, a Hollow Sentry combat proof, and ambient AI-oracle indicators mapped from active-agent/dispatch posture. The old command-room renderer remains reachable through an explicit fallback toggle, no `/game` route or new dependency was added, and Resources now marks the functional ARPG downtime lane active with `/hq` as the playtest target. Verification passed with `npm run type-check`, `npm run build`, `npm run hq:e2e`, `npm run tabs:e2e`, `npm run route:e2e`, and `npm run verify`; in-app browser acceptance confirmed `/hq?focus=hq-chronicle` keeps the ARPG room, loadout, command input, and fallback controls available, while `/resources?view=impact&impactMode=graph` remains authenticated and reflects the new massive-win lane.
- MW1 Massive Win Center is now live inside RESOURCES: `/resources?view=wins` opens a native `Massive wins` chamber with a Homefront-styled planning console for large fixes, improvements, upgrades, design adaptation, and verification-heavy tranches. The lane is backed by typed plan data in `lib/massiveWinPlan.ts`, a dedicated `MassiveWinConsole`, canonical Resources view normalization, persisted workbench state, and surface redesign metadata, so broad "massive win" requests now become phase-gated route targets with design posture, next safe action, and proof gates instead of loose chat intent. Verification passed with intentional red/green `npm run tabs:e2e`, `npm run type-check`, `npm run build`, `npm run verify`, and in-app browser smoke on `http://127.0.0.1:3100/resources?view=wins` with no auth gate or console errors.
- UXA3 authenticated acceptance is now complete on merged `main`: the protected `3100` runtime was rebuilt and reviewed through the existing authenticated Playwright path without reading or printing `NEXUS_TOKEN`; `/hq?focus=hq-chronicle`, `/command`, `/security`, `/vault`, and `/resources` all keep their primary workplane in the first viewport at `1440x900` and `1366x768`, with measurements recorded in `docs/metrics/uxa3-first-viewport-review.md`. The measured polish adds short-viewport Homefront shell compaction, prevents broad `/vault` from carrying exact-memory rail expansion state back into the archive workplane, and updates the landing E2E copy to the current `Launch Homefront` CTA. Verification passed with `npm run auth:e2e`, `npm run hq:e2e`, `npm run route:e2e`, `npm run tabs:e2e`, `npm run type-check`, `npm run build`, and `npm run verify`; static search confirmed no new `/api/mcp` or `/api/external-tools` route was introduced, and `/api/health` returns 200 on the relaunched runtime.
- UXA3 VAULT first-viewport compaction is now live on merged `main`: `/vault` no longer stacks a separate route telemetry strip plus standalone chamber-briefing disclosure ahead of the workplane, instead using one compact archive mission strip with chamber status, archive/graph/compiled telemetry, and inline briefing disclosure; memory, relations, and publish support rails now expose compact preview chips before deeper panels open. Route paths, `view` values, focus tokens, graph/export contracts, compiled-page flow, and memory lanes are unchanged. Verification passed with `npm run type-check`, `npm run build`, and `npm run verify`; the in-app browser smoke reached the protected `/vault` auth gate on healthy `3100`, so authenticated visual acceptance remains part of the broader UXA3 route review.
- UXA3 Resources first-viewport compaction is now live on merged `main`: `/resources` now opens with a compact chamber mission strip instead of a tall orientation module, keeps `How This Helps` as a lightweight disclosure, and gives external-stack plus operator-readiness support rails compact preview chips before their deeper panels so the primary workplane stays dominant without changing route paths, `view` tokens, exact-session links, or the existing field-manual/reference contracts. Verification passed with `npm run type-check`, `npm run build`, and `npm run verify`; the in-app browser smoke reached the protected `/resources` auth gate on healthy `3100`, so authenticated visual acceptance remains part of the broader UXA3 review.
- P2A External MCP Tool Bridge Contract is now live on merged `main`: external tool integrations now have typed descriptors, OAuth-aware sanitized auth metadata, result-envelope boundaries, status diagnostics, and trust-surface posture without adding `/api/mcp`, `/api/external-tools`, or generic arbitrary MCP execution; `local_mcp_gateway` remains descriptor-only while `n8n_run_workflow` stays the only live external exec adapter behind the existing high-risk, step-up, connector, and isolation policy chain.
- P1D Bounded Background Missions are now live on merged `main`: mission-type scheduled jobs now carry a typed local-first review contract with explicit scope, target agent, output target, approval posture, expiry window, and re-entry summary; the scheduler governance drawer now counts missing, pending, and expired overnight review posture instead of treating recurring work as anonymous cron text; HQ workflow-ops continuity now surfaces expired or pending review posture through the existing mission queue signals; and SKILLS Workflow Forge now echoes the same bounded mission-review state without adding a new route family, silent autonomous expansion, or hidden background follow-on loops.
- P1C Repo Assimilation Workflow Upgrade is now live on merged `main`: RECON repo intel, repo compare, and repo assimilation now reuse approved correction-memory constraints for repeat ecosystem fits; saved repo-assimilation briefs now resolve into explicit `adopt / adapt / reject` implementation posture with local fit, extension points, smallest-slice guidance, and boundaries instead of broad fit-only summaries; ORBIT handoffs now inherit those same local constraints and real Nexus file seams; and VAULT compiled pages now surface the decision-first brief metadata without widening Nexus into private-repo access, GitHub write actions, or background repo scanning.
- P1B Passive AI Exposure Review Packs are now live on merged `main`: RECON OSINT and CYBER triage now share a deterministic `ai-exposure-review` advisory pack lane for exposed LLM endpoints, leaked-key posture, vector-store or MCP exposure, and unsafe agent deployment posture; the reviews file durable `ai-exposure-review` compiled pages into VAULT through the existing memory-pages contract; artifact classification now treats those pages as first-class AI exposure review artifacts; and the VAULT compiled-page archive now recognizes, filters, and exactly reopens the new advisory thread without widening Nexus into active scanning, exploit automation, or a new route family.
- P1A Artifact Classification for VAULT and Project Inspection is now live on merged `main`: compiled memory pages now derive shared `artifactClassification` metadata with artifact type, parser hint, confidence, and sensitivity posture; the local project architecture lanes (`graph`, `ownership`, `hotspots`, `security`) now classify repo files through the same contract; and the existing VAULT compiled-page surfaces plus the Resources impact console now expose the new metadata without adding routes, upload execution, or a separate artifact-management dashboard.
- P0B Eval Governance + Isolated Tool Execution is now live on merged `main`: runtime experiments now have typed definitions/runs/verdicts, a repo-native artifact ledger under `docs/metrics`, internal metrics endpoints for history and run execution, and Blacksite plus runtime-eval visibility for baseline-vs-variant evidence without auto-promoting live behavior; `/api/tools` now applies an exec-only isolation contract with adapter readiness, fail-closed blocking for non-ready exec paths, structured isolation headers/JSON metadata, and trust/status surfaces that expose isolation posture beside the newer privacy-shield and protected-action lanes.
- The standalone `3100` runtime now resolves the new P0B experiment and isolation paths back to the true repo root instead of `.next/standalone`: runtime-experiment artifacts, eval runner state, and the exec isolation runner all use the shared runtime-root helper, so the managed production review lane can execute runtime variants, report isolation readiness, and return trustworthy status diagnostics instead of breaking on missing repo-root scripts.
- Root `main` now has a repo-owned detached review-runtime lane for the IAB: `npm run runtime:launch:3100` launches the standalone app from `<repo-root>` onto `http://localhost:3100` with deterministic pid/log artifacts, `npm run runtime:stop:3100` tears that managed runtime down cleanly, `.nexus-runtime.pid` plus the paired stdout/stderr files are ignored locally, and `scripts/start-runtime.mjs` is back to being the canonical foreground standalone entrypoint instead of carrying Windows-specific detached-launch behavior itself.
- The original checkout at `<repo-root>` is back on `main` as the canonical working/runtime folder: the dirty pre-restore root state is preserved on `codex/root-preserve-before-main-restore-2026-04-21`, the former dedicated `main` worktree now lives on `codex/mainline-runtime-archive-2026-04-21`, root-main verification is green with `npm run type-check`, `npm run build`, `npm run route:e2e`, `npm run hq:e2e`, `npm run tabs:e2e`, `npm run auth:e2e`, and `npm run runtime:fresh-proof`, and the local proof scripts now match the shipped auth boundary by using the trusted internal auth header plus the public landing contract instead of older bearer-root assumptions.
- Merged-main continuity-bundle compaction is now live on the active `3100` runtime: `COMMAND` no longer renders four equal-weight continuity cards, instead folding offline fallback plus route probes into one connectivity posture surface while keeping runtime-efficiency as the second primary telemetry surface with deeper detail behind disclosure, and `SECURITY` physical now keeps the incident queue summary-first with counts, filters, and top alerts visible before the full ACK-capable feed opens. This pass preserves the existing `focus=runtime-efficiency` and `focus=agent-health` contracts while pushing the deepest continuity desks behind disclosure instead of letting them dominate the first viewport.
- Merged-main module-stack compaction is now live on the active `3100` runtime: `COMMAND` keeps the top-level `Dispatch / Programs` switch but no longer stacks a second segmented-control row inside each branch, `Action brief` and `Business builder` now stay primary while `Efficiency ops` and `Risk scan` open through compact disclosures only when needed, and `SECURITY` physical ops now opens as one dominant live-monitoring field with perimeter sweep tucked behind an in-field disclosure instead of two full-height stacked panes. The live measurement pass shows the biggest depth win on `SECURITY` physical, tightening from roughly `2414px` to `2144px` total page height, while `COMMAND` keeps the previous shorter chrome posture and trades the former nested control stack for a much shorter support rail without losing runtime-efficiency access.
- Merged-main route-strip telemetry and compact control-lane compaction is now live: shared route strips now flatten into a thinner telemetry row instead of reading like a second content band, route/subview segmented controls can stay in a single horizontal control lane with scroll instead of wrapping into extra vertical height, and the pass now shows real first-view gains on the live `3100` runtime with VAULT archive tightening from roughly `2164px` to `2065px` total page height, RESOURCES finder from `2067px` to `1987px`, COMMAND from `3189px` to `3111px`, and route-strip height dropping to about `40px` on the routes that still use it, all without changing routes, `view` tokens, or exact-session continuity contracts.
- Shared non-HQ shell compaction on merged `main` is now live: the compact route hero trims its identity copy, plate height, and telemetry sizing more aggressively, the non-HQ stage/page top padding is slimmer, the spatial command strip now has a compact mode with a flatter dock and hidden secondary note/disclosure, and the non-HQ mission strip now renders as a thinner telemetry row so COMMAND, SECURITY, VAULT, and RESOURCES reach their primary workplanes sooner without changing routes, focus/session links, or route-level capability contracts.
- Route follow-through compaction on merged `main` now extends beyond COMMAND: `SECURITY`, `VAULT`, and `RESOURCES` now skip empty mission-handoff chrome, route support rails can use the shared sticky/capped viewport budget on wide screens, and heavyweight secondary lanes such as readiness, stewardship, continuity, external-stack, and memory-brief content now sit behind disclosure-first shells so the primary workplane stays dominant without removing the underlying capability.
- UXA3 Mainline Consolidation, Lockdown, and Spatial Layout Overhaul is now merged into `main` together with the replayed S1 runtime/security tranche: the protected shell now carries a registry-backed spatial command strip with tighter mission-handoff disclosure, shared shell/layout spacing now uses more adaptive first-view height budgets so protected routes keep a spacious editorial look without obvious dead zones, HQ's 3D room now favors heavier mission-table hardware and smaller projected telemetry with guided camera focus instead of blocking HUD boards, and the local-first lockdown posture now exposes summary-first trust/local-data metadata while blocking sensitive project paths and keeping isolated mode honest for local-only AI/runtime flows without changing routes, `focus`/`view` tokens, or auth/session contracts.
- Merged-main follow-through compaction is now live on the active `3100` runtime: the protected top rail now measures itself and writes the real `--top-rail-height` at runtime instead of relying on a stale fixed offset, medium-width desktops keep the command header in a two-row cockpit layout instead of collapsing into a tall one-column stack, non-HQ route heroes now default to a compact density profile so the first workplane starts sooner, and COMMAND now removes empty handoff chrome when no mission payload is present while reflowing its brief/support lanes into adaptive grids with actual `provider-health`, `agent-health`, and `runtime-efficiency` focus targets.
- `main` is now upgraded to Next.js `15.5.15` plus React `19`, active runtime build metadata now prefers `NEXUS_BUILD_COMMIT_SHA` / `NEXUS_BUILD_ID` / `GITHUB_SHA` instead of `VERCEL_*`, `next/image` stays enabled through the unoptimized self-host profile, the landing/auth browser specs now match token-enabled versus token-disabled local posture, and the merged mainline verifies with `npm run type-check`, `npm run verify`, `npm run build`, `npm run route:e2e`, `npm run auth:e2e`, `npm run hq:e2e`, `npm run tabs:e2e`, and `npm audit --json` showing `0` high / `0` critical vulnerabilities on the shipped runtime line.
- M4 Trust/Auth/Protected-Action Substrate Replay is now active on `codex/m4-trust-substrate-replay` as the main-based replay candidate for the protected shell and backend trust chain: the accepted M3 browser-parity surfaces now carry the replayed trust strip, route trust rails, cookie-only auth/session helpers, protected-action telemetry, trusted-origin internal fetch path, and aligned tools/settings/verify posture from the preserved branch without touching `main`, and the clean worktree now verifies with `npm run type-check` plus `npm run auth:e2e`.
- M3 Visual-First HQ and Route Replay is now active on `codex/m3-hq-route-replay` as the accepted browser-parity slice for the clean mainline migration: the public landing/root-route contract, HQ command-table flagship, and first-view COMMAND / SECURITY / SKILLS / VEHICLE / RESOURCES / VAULT interiors now match the accepted preserved-branch surfaces on a fresh `main`-based worktree, with `npm run type-check`, `npm run route:e2e`, `npm run hq:e2e`, and `npm run tabs:e2e` all green there.
- M2 Shell + Taste Replay is now active on `codex/m2-shell-taste-replay` as the first clean mainline substrate slice: the Nexus taste contract, colder Satellite Ops shell grammar, route plates, and public landing/root-route contract were replayed onto a fresh `main`-based worktree without pulling trust/auth substrate forward.
- UXS7 Live Surface Completion and Replay Hold is now live on `codex/preserve-main-2026-04-11` as the visible-first cleanup tranche that removes the last browser-facing sanctum/doctrine/dashboard residue before any more `main` replay work: HQ keeps the 3D flagship structure and tactical rail from UXS6 but now gives the chronicle/workplane clearer first-viewport dominance, shared shell/toprail/hero framing no longer draws the live `sanctum-*` ornaments that made the app read like a recolored older theme, SECURITY and shared trust-facing copy now use tighter controls/operations language while preserving backward-compatible continuity tokens such as `view=doctrine`, the auth gate and command dock no longer present Citadel-era wording or art in the browser, and replay to `main` remains paused until this preserved-branch surface is manually accepted in the IAB.
- UXS6 Full HQ Migration and Trust Closure is now live on `codex/preserve-main-2026-04-11` as the tranche that finally makes HQ match the claimed Satellite Ops redesign in the browser: the live HQ side lane was rebuilt into an embedded tactical rail with a route-native trust strip, the console shell now reads as a command table instead of an older office/sanctum hybrid, the 3D room shifted further toward mission hardware by replacing the most office-flavored lounge/support props with continuation consoles and uplink hardware, `/api/auth-diagnostics` now acts as the single trust-state source for shell and route trust surfaces, `/api/verify` now uses the trusted internal fetch helper instead of host-derived smoke targets, and protected-action descriptors are now aligned across diagnostics, tools, settings, and verification without changing public routes, exact-session behavior, or workflow IDs.
- UXS5 Route Interior Completion and Mainline Migration Prep is now live as the preserved-branch route-finish tranche: SECURITY physical ops now reads as one doctrine-led monitoring chamber with embedded threat and trust posture instead of boxed surveillance panels, SKILLS now replaces the repeated guided-learning catalog wall with tighter tutor/workflow brief lists plus embedded memory and playbook launch rails, VEHICLE now carries its lower telemetry, launch, checklist, and session-bundle lanes inside one coherent systems-table composition instead of separate embed grids, the Resources study lane now uses the same lighter brief-list language, and the tranche explicitly preserves the follow-on migration contract that `main` should receive a clean replayed slice rather than a direct merge of `codex/preserve-main-2026-04-11`.
- UXS4 Taste-Translated Satellite Ops Redesign is now live as the full-app taste-calibration tranche: `taste-skill` was translated into a Nexus-native design contract through `docs/NEXUS_TASTE_CONTRACT.md` and `lib/nexusTasteContract.ts`, the shared shell/header/mission-strip hierarchy now follows that contract with tighter typography-led command fascia and route directives instead of lingering dashboard-era framing, SECURITY plus SKILLS and VEHICLE now inherit the same Satellite Ops workplane/rail/continuity grammar as the GA web lanes instead of visibly lagging behind on older interior composition patterns, and the trust strip language now reads as native command instrumentation inside the same cold blue / steel system without changing public routes, API paths, workflow IDs, or exact-session behavior.
- UXS2 Route Interior Completion and Trust Operations is now live as the route-interior completion tranche after UXS1: COMMAND, INTEL, ALPHA, CYBER, RECON, VAULT, and RESOURCES now use the Satellite Ops workplane/rail/strip grammar for their first-view interiors instead of leaning on visible compatibility-era module shells, HQ now carries the same ops-field support language with a deeper embedded trust-operations rail alongside its 3D flagship scene, the shared trust layer now exposes clearer session/step-up/protected-action posture with inline revalidation, internal auth-bearing server-side fetches now run through one trusted-origin helper instead of repeated host logic, and the test harness now explicitly supports Next's `server-only` marker so auth, tools-route, and exact-session verification stay green without changing public routes, workflow IDs, or release-gate posture.
- UXS1 Trusted Satellite Ops Completion is now live as the balanced visual-migration plus trust-hardening tranche: the shared shell now exposes a compact trust posture strip inside the command header, route interiors and support lanes are flattened further into the Satellite Ops rail grammar instead of lingering boxed dashboard modules, HQ keeps the 3D flagship surface while aligning its support lanes to the same colder work-first command-table language, browser auth now rides short-lived stateless signed session cookies with separate short-lived step-up cookies instead of treating the configured `NEXUS_TOKEN` as the browser session, middleware no longer honors normal browser bearer auth, internal auth-bearing fetches stay pinned to trusted local/configured origins only, risky `/api/settings`, `/api/verify`, and dangerous `/api/tools` capabilities now require step-up posture, and RECON operator-visible markup now prefers structured safe rendering instead of loose HTML display.
- AS1 Attack Surface Reduction is now live as a targeted hardening tranche: browser-side API auth now rides the httpOnly session cookie instead of persisting the master `NEXUS_TOKEN` in `sessionStorage`, the auth gate now scrubs lingering legacy browser token state on mount, `/api/settings` is rate-limited and no longer allows browser-driven mutation of runtime policy or deployment-control keys, `/api/verify` now runs through a tighter fixed-command spawn path with rate limiting instead of shell-backed command execution, `/api/events/sweeps` now prefers the configured release base URL when building auth-bearing internal fetches, and recon HTML rendering now strips a wider class of active-content/source attributes before operator-visible rendering.
- UX7 Full Satellite Ops Migration and Interactive Experience is now live as the true completion tranche for the redesign: shared route interiors no longer depend on the old visible boxed module grammar, support lanes now render as embedded rails and strips instead of dashboard cards, the HQ operations deck now uses shorter operational language and an authored board layout instead of sanctum/doctrine framing, the HQ 3D flagship scene now runs on a colder Satellite Ops palette with pointer-reactive camera drift and tactical wall/table treatments, and the same cold blue command-fascia grammar now carries across route interiors and continuity strips without changing routes, exact-session behavior, workflow IDs, or API contracts.
- UX6 Bespoke Satellite Ops Redesign is now live as a full visual-system replacement instead of an incremental UX5 extension: the shared shell now resolves into route-specific Satellite Ops plates with pointer-reactive tactical depth, a flatter command fascia, rail-based support structure, and a full-canvas operations world instead of reused hero frames and boxed support cards; HQ now opens as a custom operating table with the live console elevated into the primary work plane, a mission-image support lane, and strip-based tactical continuity instead of the old prelude/support split; and the same bespoke plate, rail, and continuity grammar now carries across the core tabs without changing routes, exact-session behavior, workflow IDs, or assistant logic.
- UX4 Monochrome Tactical War-Room Redesign is now live as a true art-direction pivot instead of another sanctum polish pass: the shared shell now resolves into a colder monochrome tactical room with image-led route plates, quieter ambient structure, flatter mission-board materials, and a flatter command-header top rail; HQ now opens as one operating picture with a dominant mission poster, a compact inspector lane, and sharper action rails instead of a hero card beside equal-weight support cards; route-level surface copy now reads as functional tactical desks instead of codename-heavy themed chambers; and the same visual grammar now carries across the core tabs without changing routes, exact-session behavior, workflow IDs, or assistant logic.
- UX3 Shell Stability and Operator-Cinematic Redesign is now live as a shell-and-composition tranche instead of another sanctum polish pass: shell surface, motion profile, and ingress posture now resolve through an explicit pre-hydration boot contract so the first paint no longer restyles itself after mount; route transitions and ambient layers no longer tear down shell-wide visual attributes during normal navigation; HQ's first viewport now reads as a dominant proclamation chamber with one command-map anchor and one compact status/inspector lane instead of equal-weight stacked cards; the top rail now behaves like a real command header with active-surface ownership and a routed sector rail instead of decorated nav chips; and the shared shell hero/stage system now carries the calmer operator-cinematic composition across core tabs without changing routes, exact-session behavior, workflow IDs, or assistant logic.
- VX1 Local Voice, Privacy Shield, and Repo Security Intelligence is now live as a ship-ready completion tranche instead of an active local program: Resources now keeps Voice Lab trustworthy across runtime-backed and browser-only paths with exact `voice-lab` reopen continuity, local project/profile persistence, and clean runtime-down fallbacks; the local Impact lane now acts as one architecture workbench with tighter path normalization, git-activity fallback, coupling review, and stronger security heuristics for secrets, risky HTML injection, and unsafe shell composition; cloud-bound sensitive turns now anonymize through `/api/ai` with stable request/session surrogates, safe protected-value summaries, explicit privacy-shield posture, and de-anonymized operator output; and CYBER vuln-review now keeps privacy posture and the strongest repair lane visible while durable `vuln-review` briefs continue to reopen exactly through VAULT compiled pages.
- UX2 Visual Redesign Across the Core App is now live as a visual-system tranche rather than a product rewrite: the shared shell, top rail, page heroes, panel chrome, segmented tabs, and supporting shell primitives now use a denser, more graphics-heavy premium operator language; HQ now reads more clearly as the flagship command chamber with the chronicle/composer kept dominant over the support decks; and the rest of the core tabs inherit the same tighter visual hierarchy and stronger surface identity without changing routes, exact-session behavior, workflow IDs, or assistant logic.
- VG2 VAULT Archive Compounding, Capture, and Weekly Synthesis is now live as the completion tranche for the older archive-compounding lane: saved VAULT clips now gain local semantic archive links and backlink-aware graph continuity, HQ can now show explicit dismissible `Save to VAULT` capture prompts for qualifying non-workflow answers without introducing autosave, `/weekly` now runs one explicit seven-day archive synthesis that files durable `vault-weekly` briefs into compiled memory with exact reopen, and VAULT/export surfaces now expose backlink repair cues, weekly continuity, frontmatter-rich Obsidian markdown, and confirmed wikilinks without widening Nexus into a scheduler, vector DB, or external graph service.
- RG1 Reasoning RAG + VAULT Article Index Completion is now live as the trustworthy finish to the older PageIndex-style reasoning lane: HQ keeps the cheap keyword router as its fast path, low-confidence prompts now trigger one bounded async reasoning fallback instead of noisy multi-domain drift, saved VAULT clips now gain background claim/entity/domain indexing through the existing AI spine without blocking the save action, and those indexed cues now feed HQ retrieval hints, saved-clip search, graph text, and memory-spine summaries without widening Nexus into a vector DB, a new public route, or an always-on indexing daemon.
- DU1 Dynamic UI Contract Completion is now live as the trustworthy finish to the older context-aware UI lane: dynamic rule evaluation now mounts once at the shell level instead of only inside HQ/home, the same signal feeds now refresh globally so rule posture stays coherent across routes, dismissible alerts now suppress only the current activation instead of muting every future recurrence forever, the missing top-rail header indicator contract now renders live agent-state cues such as Parliament Mode, and the lane now carries dedicated dynamic-UI coverage instead of remaining a partially-rendered experimental shell effect.
- SO1 Switch Operator + Provider Resilience Completion is now live as the trustworthy finish to the older switch-operator lane: COMMAND now exposes an exact `provider-health` focus that merges the server-scored provider chain with true local-runtime reachability, HQ now shows bounded failover and free-key onboarding posture instead of raw provider confusion, `/operator` plus explicit natural-language operator triggers now run one-shot Switch Operator orchestration through the existing runtime without inventing a second provider chain, and operator status now records the chosen lane, selected agent, readiness posture, and actual provider used after completion without widening Nexus into scheduler automation, background backlog loops, or durable operator artifacts.
- PC1 Persona Council Completion is now live as the trustworthy completion tranche for the older PersonaPlex-style HQ idea: single-agent persona mode still rides the existing prompt-suffix path, Council mode now dispatches through the shared AI/runtime spine instead of a broken side helper, council membership now adapts to the detected lane instead of always using one static trio, HQ now renders explicit use-or-merge actions for saved council answers, and the council/runtime/helper contracts now carry dedicated coverage so the feature no longer sits in the repo as presentation-only scaffolding.
- VG1 VAULT Graph Reliability and Librarian Completion is now live as the completion tranche for the older knowledge-graph idea: VAULT now supports exact `nodeId` graph-focus reopen inside the existing relations chamber, the librarian lane now exposes a one-click `Audit VAULT` workflow plus explicit exact repair actions instead of separate transient analysis buttons, durable `vault-librarian` briefs now file through the existing compiled-memory path with exact reopen and graph/archive continuity, restricted compiled pages keep protected graph topology without leaking linkage text, and the graph/lint/synthesis helpers now carry dedicated coverage without widening Nexus into an external graph service, raw-AI linking engine, or post-dispatch auto-save system.
- RI3 Public Repo Compare Matrix is now live as the broader public-safe completion tranche for the older GitHub repo idea: RECON repo intel now supports explicit 2–3 repo compare inside the existing OSINT lane, HQ now exposes `/compare-repos` and routes compare-versus-adoption prompts into the existing RECON lane instead of inventing a new route family, the agent runtime now exposes a real `compare_repos` tool through `/api/tools`, explicit compare runs file durable `repo-compare` compiled pages into VAULT with exact reopen and source-aware metadata, and ORBIT handoff now prefers the saved comparison brief over single-repo assimilation or lighter RI1 metadata without adding private-repo access, GitHub token UX, GitHub write actions, or background repo scanning.
- RI2 GitHub Repo Assimilation Completion is now live as the full public-safe completion tranche for the older GitReverse-style idea: RECON repo intel now supports an explicit second-step repo assimilation flow instead of stopping at metadata-only assessment, HQ now exposes `/assimilate-repo` and routes explicit adopt / adapt / fit prompts into the existing RECON lane, the agent runtime now exposes a real `assimilate_repo` tool through `/api/tools`, explicit assimilation runs file durable `repo-assimilation` compiled pages into VAULT with exact reopen and source-aware metadata, and ORBIT handoff now prefers the saved assimilation brief over the lighter RI1 metadata profile without adding private-repo access, GitHub token UX, background scanning, or GitHub write actions.
- DR1 NOVA Deep Research Engine is now live as the native completion tranche for the existing deep-research lane: the active agent runtime now exposes a real bounded `deep_research` tool through `/api/tools`, explicit deep-research prompts stay in the existing INTEL/NOVA workspace instead of widening into a new surface, prompt and workflow guidance now point at the real engine instead of a stale manual tool recipe, explicit `/deepresearch` runs keep filing durable compiled pages through the existing `research-workflow` artifact path with citation-aware metadata, and SKILLS/RESOURCES now expose a matching deep-research playbook without adding a new dashboard or public route.
- RI1 RECON Repo Intel is now live as a local-only read-only repo-assessment tranche: Nexus now exposes a protected `/api/repo-intel` route for normalized public GitHub metadata, RECON OSINT now carries a compact `recon-repo-intel` module and exact-session focus for dependency / competitor / reference-library assessment, ORBIT can call the shared `analyze_repo` tool on the same contract, and HQ now stages GitHub/repo-assessment prompts into that RECON lane without widening the shell into a new tab, adding token UX, or ingesting remote source code.
- MR3 Market Postmortem Coach is now live as a durable local-only market-review completion tranche: ALPHA market reviews now capture repeat / avoid / next-rule lessons alongside thesis, result, and emotional posture; the coach fields seed deterministically from the current draft plus the strongest prior review; VAULT compiled pages now surface those lesson cues prominently for exact reopen; and HQ plus SKILLS/RESOURCES frame the lane as lesson capture without widening into forecast execution, broker automation, or a new dashboard surface.
- XR2 Project-Local Assimilation Polish is now complete as an exact-continuity tranche instead of a partial metadata follow-on: VAULT compiled pages now accept exact `pageId` deep links inside the existing `vault-compiled-pages` session, market-review / OSINT casefile / vehicle continuity actions now reopen specific durable artifacts instead of stopping at workflow-level archive filters, reused market and OSINT source trails now point at exact compiled pages, and HQ now treats OSINT casefile archive prompts as deliberate VAULT continuity work while keeping cyber-routed live investigation in CYBER instead of forcing RECON.
- XR1 Comprehensive Assimilation is now live as a native multi-surface tranche instead of a repo-idea queue item: `market-review` ships as a governed workflow pack and exact ALPHA focus target; ALPHA now carries a compact market-review support rail that writes deterministic compiled pages into VAULT; VAULT compiled pages now honor `workflowId` exact-session filtering, surface a compact recent-durable-thread hot cache, and render dedicated market-review / OSINT casefile cues; RECON and CYBER now share a passive-first OSINT casefile loop that files deterministic durable casefiles through the existing memory-pages contract; VEHICLE session bundles remain on `nexus-vehicle-session-v1` but now accept an optional advisory-only `radar` block for later passive sensor continuity; and SKILLS plus RESOURCES now expose compact XR1 playbooks and guidance entries without widening the shell into new tabs, tool catalogs, or flight/trading automation.
- A6 Governance Control Plane and Cyber Pack Baseline is now live as a full native control-plane tranche: shared governance metadata now covers skills, workflow packs, assistant capabilities, and key exact-session continuations through `lib/governanceCatalog.ts`; `cyber-triage` now ships as the first governed cyber workflow pack baseline; assistant capability detection and HQ continuation staging now carry approval-aware governance posture instead of relying on route-only risk hints; SKILLS/System Brain now acts as the operator-facing governance inventory; HQ, COMMAND, and RESOURCES inherit the same compact governance posture through readiness lanes; and CYBER triage now carries a quiet governed support rail that stages RECON and VAULT follow-through without widening the shell into a dashboard wall.
- A5 Efficiency Ops Completion is now live as a balanced backend-plus-shell tranche: the scheduler now writes a sanitized recurring-mission efficiency source snapshot under `.nexus/metrics`, runtime eval carries nested scheduler-efficiency posture with latest/history artifacts under `docs/metrics/scheduler-efficiency-*`, COMMAND now hosts an `Efficiency Ops` chamber with lane usage, cache/batch proof, repair candidates, and bounded execution ledger context, HQ continuity now summarizes the same ops spine compactly, SKILLS and RESOURCES now inherit quiet efficiency/readiness posture, and the runtime trend surface can run and display scheduler-efficiency proof alongside forecast and runtime eval without widening Nexus into another dashboard.
- A7 Forecast Evaluation and Readiness is now live as an eval-first forecasting tranche: Nexus now has a native forecast contract plus TimesFM-compatible provider metadata, a deterministic `native_baseline` rolling backtest recorder that persists latest/history artifacts under `docs/metrics/forecast-eval-*`, protected forecast read/run endpoints under `/api/metrics/runtime-eval/forecast*`, nested forecast posture inside the runtime-eval lane, a quiet Forecast Lab support rail on ALPHA, and compact forecast-readiness signals inside HQ/COMMAND support posture without adding forecast overlays or decision automation.
- Advisory hardening and simplification follow-on is now live: the TradingView embed no longer uses `innerHTML` for third-party widget config, the agent verification runner now resolves tracked task artifacts from an explicit repo root instead of inline `../` path climbs, `SettingsDrawer` now hydrates `/api/settings` through one shared loader instead of duplicating state-sync fetch logic, `OfficeRoom3D` now accepts a compact control contract instead of a long callback prop chain, and the HQ prompt builder now carries persona suffixes through one function instead of a redundant wrapper.
- Full secret lockdown is now live as a repo-wide hardening layer instead of a source-only lint: `security-scan` now walks tracked files across app/components/lib/docs/public/root assets, Husky runs staged plus pre-push secret checks, CI now enforces tracked-file and history audit passes, temp/demo artifacts were sanitized to canonical local-only placeholders, and secret posture is now exposed to the UI only as booleans/counts through `/api/settings` and the protected status surfaces.
- Native fortification posture is now visible inside the shell instead of living only in scripts: HQ continuity, COMMAND doctrine rail, and RESOURCES support rails now share an operator-readiness lane that summarizes secret posture, provider readiness, guarded browser-ops posture, workflow queue strength, memory lifecycle state, and capability-audit signals without widening those routes back into module walls.
- Claude-Mem-style lifecycle posture is now deeper and deterministic inside VAULT and COMMAND: memory artifacts now carry citation IDs, lifecycle state, next-action guidance, and sensitivity tags; `/api/memory/stats` now exposes lifecycle summary plus compact previews; and memory surfaces now show promote/compact/reopen posture without reviving ambient memory injection.
- Native assimilation is now live as a release-safe shell extension instead of a launcher sprawl: COMMAND now exposes governed workflow queue, owner, handoff, and review-gate posture; HQ continuity notes now surface the same ops spine; VAULT now summarizes citation-ready recall, compaction backlog, and reopen lifecycle; guarded browser-ops readiness is available through `/api/recon/status` for COMMAND and RECON; and SKILLS plus RESOURCES now expose a native capability audit so Multica, xyOps, Lightpanda, Claude-Mem, and autoskills patterns land inside existing routes instead of as separate products.
- GA route condensation is now live as a shared chamber grammar: each route resolves a lead chamber, support rail, and continuity strip through the surface-condensation registry; COMMAND, VAULT, RESOURCES, ALPHA, and CYBER now merge adjacent internal views into fewer switchable chambers instead of rendering card walls; and deeper HQ support surfaces now sit behind progressive disclosure so the chronicle and room stay primary.
- Curated upstream ecosystem integrations are now staged inside RESOURCES instead of being dumped into the shell: Multica, Lightpanda Browser, Claude-Mem, xyOps, and autoskills are surfaced as chamber-aware external-stack references so Nexus can absorb browser-ops, memory, scheduler, and skill-system ideas without widening the UI. `udemy-downloader-gui` was intentionally not integrated because it is off-mission and carries avoidable policy/TOS risk.
- The Aegis cinematic systems tranche is now live across the GA shell: routes resolve to explicit atmosphere, sequence, hero-media, and signal-motion specs; shell depth planes and chamber spotlighting are now shared CSS/SVG layers; HQ chronicle order/dispatch/handoff states now animate as a ritual loop; and the HQ room now uses scene-cue camera drift plus mood lighting so the 3D sanctum reacts to live command state instead of sitting beside it.
- The sanctum redesign no longer stops at recolor: HQ now opens with a proclamation chamber, doctrine tower, sequenced mission rail, and utility rail that read as a single room; the toprail tabs now behave like numbered sector studs; and shared module/composer controls now use the same harder-edged command-plate geometry instead of falling back to rounded dashboard chips.
- `npm run type-check` is stable again because baseline verification now runs through `tsconfig.typecheck.json`, which intentionally omits the transient `.next-e2e` type tree even when Playwright builds rewrite `tsconfig.json` for the isolated auth/browser lane.
- Local `main` is now realigned to `origin/main`, while the previously diverged history and dirty worktree were preserved on `codex/preserve-main-2026-04-11` so session startup no longer depends on reconciling `main` first.
- `npm run handoff:pull` is now branch-safe during session startup: when `git pull --ff-only` hits a diverged local branch, it falls back to `git fetch origin main`, refreshes remote refs, and leaves the dirty worktree/history untouched instead of hard-blocking the session.
- Sanctum completion is now live across the GA shell: the app now uses an ultramarine/brass/iron plate system with original sanctum SVG framing, the toprail reads like a command dais with engraved sector studs, HQ opens as a proclamation chamber plus doctrine tower and continuity strip, and shared routes now inherit the same command-plate language instead of falling back to premium-dashboard cards.
- The Aegis cinematic continuation tranche is now live: HQ chronicle/composer now reads as a cinematic command feed, route heroes use explicit per-sector media composition rules, and the HQ room now shifts lighting/emissive posture from shared mission/runtime scene cues under the global motion profile.
- The Aegis motion and graphics upgrade is now live: the global starfield was replaced by a route-aware ambient renderer, page transitions and toprail active states now use sector-aware motion presets, HQ VFX now obey a global motion profile, and command pings/live ribbons now read as part of the shell instead of bolted-on widgets.
- The Aegis flagship visual upgrade is now live: the shell and toprail have stronger sector identity, HQ now puts the live console ahead of the strategium so the chronicle stays primary, and shared module cards/sections carry a calmer premium hierarchy across COMMAND and RESOURCES.
- HQ now uses deterministic selective context loading instead of linear prompt accretion: the 4-file spine is sliceable through `/api/project?section=...&slice=...`, context lanes choose a bounded manifest, and SKILLS/System Brain now exposes the last context-policy report for diagnostics.
- Context Spine Consolidation is now the active canonical context model: `AGENTS.md`, `docs/STANDARDS.md`, `docs/SYSTEM_STATE.md`, and `docs/PROJECT_BIBLE.md` are the live context spine, while legacy handoff/task files are compatibility mirrors only.
- The research/study stack is already in place: DeepTutor + MemPalace assimilation and Claude Scholar assimilation landed, so HQ, VAULT, RESOURCES, and SKILLS now support guided learning, memory mining, workflow-pack research, and repo-bound project memory.
- The authenticated-shell browser baseline is green again: `auth:e2e`, `hq:e2e`, `route:e2e`, and `tabs:e2e` were recovered under the isolated Playwright auth/shell contract.

## Active Blockers

- This Codex shell currently cannot reach GitHub over port 443, so `npm run git:safe -- push` failed before this tranche could publish. Use `npm run repo:sync:status` to inspect ahead/dirty state without network, then run `npm run git:safe -- push` from normal PowerShell when GitHub connectivity is available; do not claim the branch is synced until that succeeds.
- Phone/PWA acceptance still requires a physical phone or iPad on the same trusted network even though the receipt engine, assistant proof bridge, live status panel, capture-status artifact, direct-HQ QR handoff, one-command local preflight, no-network repo sync status, and latest-capture report are now implemented: run `npm run repo:sync:status`, run `npm run ops:preflight`, scan the `Scan direct HQ` QR or open the printed LAN HQ URL, log in with `NEXUS_TOKEN`, let the Free Local Readiness panel create the mobile receipt, send `ping`, ask one local Ollama prompt, watch live receipt proof turn ready, install the PWA, rerun capture, then run `npm run phone:acceptance:report` until `acceptanceReady: true`.
- The wide readiness rollup is not release-candidate ready yet because `docs/metrics/readiness-rollup-2026-05-09T22-29-00-772Z.json` links a blocked phone acceptance artifact, release diagnostics used local fallback with routes down, Docker is unavailable, and Dependabot alert package metadata is not reachable from this shell.
- Local Docker proof is still blocked because Docker is not currently available on this machine.
- The target-runtime gate now reads repo-root `.env.local`, but the current project-local config still does not define `NEXUS_RELEASE_BASE_URL`, so the combined `FD2`/`FD3` proof remains blocked until the real Coolify host is added there.
- `MW6V-REAL-ASSET-INTAKE` remains intentionally blocked until approved local CC0/CC-BY/commercial-license asset packs exist and are routed through the manifest/credit intake lane.

## Release Posture

- Current stage: late internal beta / release-candidate hardening for the web lane.
- Current active priority: keep the newly shipped smoothness/operational-light layer stable while closing the remaining proof lanes: physical phone/PWA acceptance, Dependabot classification, Docker/staged-host release proof, and GitHub publish from a network-enabled shell.
- Current wide rollup status: blocked, not release-candidate ready, with sanitized evidence in `docs/metrics/readiness-rollup-2026-05-09T22-29-00-772Z.json`.
- Highest-value milestone: first web deployment proof, not another major feature tranche.
- First release remains web-only and GA-scope only: `/hq`, `/command`, `/intel`, `/alpha`, `/cyber`, `/recon`, `/vault`, `/resources`.
- Desktop remains the second release wave after the web lane is proven in staged hosting.

## Known Environment Issues

- `handoff:pull` now uses `scripts/git-with-acl-repair.ps1` so the known `.git` DENY ACL is removed before Git runs. Normal PowerShell previously proved GitHub connectivity and push, but this Codex shell currently cannot reach GitHub port 443; inside the Codex sandbox, `npm run repo:sync:health` may still show the sandbox-applied DENY between tool calls, so use `npm run git:safe -- <git args>` for Git operations and retry publish from normal PowerShell when network connectivity is available.
- The original checkout at `<repo-root>` is back on `main` and is now the canonical local coding/runtime folder again. The previous dirty root state is preserved on `codex/root-preserve-before-main-restore-2026-04-21`, and the former dedicated `main` worktree now lives on `codex/mainline-runtime-archive-2026-04-21` for rollback/reference.
- The merged `main` runtime launcher prefers `.next/standalone` and mirrors `public/` plus `.next/static/` into it before boot; if a local Next build does not emit standalone output or a `BUILD_ID`, `scripts/start-runtime.mjs` now falls back to a repo-root Next runtime so local review on `http://localhost:3100` can still become healthy.
- In this shell harness, `npm run runtime:launch:3100` can time out even when the managed runtime is already healthy; check `http://127.0.0.1:3100/api/health` plus `.nexus-runtime.pid` before assuming the launch failed.
- Direct background dev-server launch may be blocked by shell policy in Codex; if `runtime:launch:3100` is killed by a tool timeout, rerun `npm run dev` or `npm run runtime:launch:3100` from normal PowerShell for live phone/iPad acceptance.
- Local browser automation can still hit Windows `spawn EPERM` at either the Playwright worker or Edge launch layer after a successful prior run; this reproduced on 2026-05-02 during `npm run hq:e2e` after build/audit/live routes passed and again during the operational-lights hydrated-browser check. When it happens, record the blocker, keep the managed `3100` runtime/source proof, and rerun the browser suites from a fresh shell before using the result as a product regression signal.
- The canonical root-main review flow is now `npm run build` followed by `npm run runtime:launch:3100` from `<repo-root>`; any merged-main visual change still needs that rebuild/relaunch cycle before the IAB reflects it.
- Docker CLI is unavailable on this machine, so `FD2` cannot be proven locally here yet.
- Target-runtime proof now auto-loads repo-root `.env.local` for `NEXUS_RELEASE_BASE_URL` and `NEXUS_TOKEN`, so `npm run launch:gate:target` no longer requires inline env export when the local operator file is populated.
- The repo-local deployment docs intentionally use placeholder hostnames such as `your-host.example`, `target-host.example`, and `<staging-host>`; no real staging hostname is committed under `docs/`, `scripts/`, `.github/`, or env examples, and the current `.env.local` still needs the operator's real Coolify domain.
- The preserved-branch candidate now has a fresh `.nexus-release-boundary.json` capture on `codex/preserve-main-2026-04-11`, and `npm run release:boundary` plus `npm run launch:gate` both pass again on that captured state.
- Local release proof surfaced and closed two deployment-gate regressions during this session: the forecast and scheduler-efficiency runtime-eval endpoints now have explicit route-policy coverage, and their protected `run` handlers now emit direct no-store protected responses.
- Some local shells still do not expose a working `vitest` binary even though repo-level verification and browser harnesses are green.
- Authenticated Playwright suites should still run sequentially on Windows because they share the `.next-e2e` dist dir, but `type-check` no longer depends on that transient folder and `clean-next` now retries removal before failing.
- In the current Codex sandbox, npm package fetches for the MW4 PixiJS/GSAP dependency set fail with registry `EACCES`; install `pixi.js`, `@pixi/react`, `gsap`, and `@gsap/react` from a network-enabled shell before converting the shipped Canvas/Web Animations fallback into a bundled Pixi/GSAP runtime.

## VX1 Verification Checklist

- Targeted proof: `npm exec -- vitest run __tests__/voiceLab.test.ts __tests__/projectArchitecture.test.ts __tests__/aiAnonymization.test.ts __tests__/vulnerabilityReview.test.ts __tests__/assistantCanonicalRegistry.test.ts __tests__/exactSessionLinks.test.ts`
- Baseline proof: `npm run type-check`
- Surface proof: `npm run hq:e2e`, `npm run route:e2e`, and `npm run tabs:e2e` run sequentially on Windows
- Voice Lab truth: `/resources?view=voice-lab&voiceProject=...` must reopen the exact project, browser speech must remain usable when the local runtime is down, and runtime-backed render/export must never leave stale success state behind after degradation
- Architecture truth: `/api/project?section=graph|ownership|hotspots|security` must stay local-only, normalize repo-relative paths correctly, and keep coupling plus sink/secret heuristics readable inside the compact Resources workbench
- Privacy-shield truth: `/api/ai` anonymization must default to cloud-bound sensitive turns only, keep local providers raw unless explicitly forced, and always de-anonymize operator-visible output before it reaches HQ/CYBER/RECON surfaces
- Vuln-review truth: `workflowId=vuln-review` must keep filing durable `/cyber` artifacts into VAULT compiled pages with exact reopen and a visible repair-lane handoff
- AI-exposure-review truth: `workflowId=ai-exposure-review` must stay passive-first and advisory-only, file durable `/recon` or `/cyber` artifacts into VAULT compiled pages with exact reopen, and never widen into active scanning, endpoint probing, or exploit guidance

## Next Up

- Active queue source: `tasks/todo.md`, `docs/plans/unfinished-ideas-triage-2026-05-07.md`, and `docs/repo-hygiene/local-change-set-map-2026-05-08.md` now define the current completion order; current priority is project security, vulnerability protection, dependency auditability, infrastructure validation, and optimization rather than RPG or drone expansion.
- Security infrastructure hardening is now shipped locally with latest proof in `docs/metrics/infra-hardening-2026-05-09T22-28-42-970Z.json`: keep using `npm run infra:hardening:audit` for sanitized evidence and `npm run verify` for the full hardening gate. The next security move is GitHub Dependabot metadata classification, then one minimal dependency upgrade batch at a time with `npm run verify` after each batch.
- Free local phone acceptance is next: desktop proof already showed the LAN HQ URL and local routes working, and the protected receipt API, assistant bridge, live status panel, capture-status artifact, direct-HQ QR handoff, `npm run ops:preflight`, `npm run repo:sync:status`, and `npm run phone:acceptance:report` commands are now wired. From the phone/iPad, run `npm run repo:sync:status` plus the preflight command first, use the Free Local Readiness checklist in HQ or COMMAND, scan the `Scan direct HQ` QR or copy the direct HQ URL, log in with `NEXUS_TOKEN`, let `/api/phone-acceptance/receipt` record the mobile/browser receipt, send `ping`, ask one local Ollama prompt so the shared assistant turn receipt marks the proof booleans, confirm the live receipt proof reads complete, install the PWA from the phone browser, rerun `npm run phone:acceptance:capture`, and use `npm run phone:acceptance:report` until `receiptLiveStatus.acceptanceReady` and `acceptanceReady` are both true.
- Local AI offline operations now have authenticated desktop proof, visible receipt support, and `npm run offline:local:report` for a no-network summary of the latest sanitized rollup plus phone acceptance artifacts; the remaining acceptance step is manual phone/browser validation: send `ping`, then use `Check local AI` or ask one Ollama prompt, confirm the receipt shows local/free posture plus `No file changes`, rerun capture, then rerun the local AI report.
- Git permission recovery is closed for the working flow: use `npm run git:safe -- <git args>` when the Codex sandbox shows `.git` ACL friction, and keep branch cleanup review-gated.
- Dependency-security audit now has real paginated imported metadata, archive manifest quarantine, and the first root runtime patch: use `docs/metrics/dependabot-security-audit-2026-05-25T05-52-26-760Z.json` and `docs/metrics/dependency-risk-posture-2026-05-25T05-52-21-853Z.json` as the current starters. Wait for GitHub to rescan the renamed `archive/` manifests and the `next@15.5.18` patch before treating stale imported Next/archive alerts as active work. The next patch move is the smallest remaining active root-app runtime batch (`postcss` first, then `prismjs` if still open), followed by `npm run dependency:risk:posture` and `npm run verify`. Do not mix package upgrades into phone/local AI acceptance unless a dependency blocks verification.
- Private RPG closure is deferred for now while local AI/phone operations are the active priority; when resumed, keep Aether Reliquary private and route-stable.
- Homefront vehicle/drone readiness is complete for the current no-hardware phase; future work should wait for real hardware or new simulated scenario requirements, and must remain passive/readiness-only with no flight-critical control path.
- AI operator workflow gaps are closed locally; future assistant work should build on `lib/assistantOperatorWorkflow.ts`, the shared chat action model, and the existing `ProposedEditPanel` approval surface instead of adding another mutation path.
- Release engineering stays blocked externally, but the exact FD2/CP2 diagnostics and rollback runbook now exists; the next release move requires the real staged Coolify/VPS hostname, token, and Docker proof before any FD2 completion claim.
- Cleanup follow-through: retire the older replay worktree queue (`m2`/`m3`/`m4` and the temporary UXA3 review worktree) only after there is no remaining need for side-by-side rollback or visual-reference inspection.
- Mainline replay status: `main` now contains the M2/M3/M4 stack plus UXA3 and the S1 runtime/security replay, so no more clean-replay slices are required before normal product work resumes.
- FD2 remains the next external release gate; staged Coolify/VPS proof is still blocked on the real staged hostname being present in repo-root `.env.local`.
- Historical replay reference remains in `docs/plans/m2-fresh-mainline-replay-stack-2026-04-18.md`; the older `m2`/`m3`/`m4` and `uxs45` replay worktrees now serve as rollback/reference material only until they are explicitly retired.
- The successful replay order remains the historical record for this migration: shared shell and taste primitives first, HQ plus route-interior completion second, then trust/auth/protected-action substrate third, followed by UXA3 plus the S1 runtime/security replay.
- UXS4 is now a shipped visual contract, so follow-on UI work should preserve `docs/NEXUS_TASTE_CONTRACT.md` plus `lib/nexusTasteContract.ts` as the taste source of truth unless a future tranche explicitly replaces them.
- Project-local rule remains in force until staging is real: upstream repos are idea sources only, and implementation still lands inside Nexus routes, contracts, state, and docs under `<repo-root>`.
- Repo-boundary rule now remains a shipped contract for the GitHub-repo lane: public GitHub repos only, explicit operator action only, metadata-first only, durable output to VAULT only, and no private repo support, token UX, GitHub write flows, or background operator loops.
- VX1 ship guard: keep Voice Lab, project architecture, privacy shield, and vuln-review inside their current Nexus seams and contracts unless a new tranche explicitly widens them; no new top-level tabs, no provider bypasses, and no remote code-upload service.
- FD2 through FD5 and A7b remain blocked exactly as they are; the shipped UX tranches do not change staged-host, deployment, or forecast-execution posture.
- [x] XR1 — Comprehensive assimilation program: keep Nexus native to its existing shell, routes, governance posture, and trust boundaries while mapping the current external source set into three sequenced subprograms instead of vendoring third-party products
- [x] MR1 — Market Research Workbench: adapt the DefiLeo trader-journaling cue plus reference inputs such as `public-apis`, `ai-engineering-from-scratch`, and the existing `timesfm_companion` contract into governed thesis review, loss review, and emotion-aware market continuity across `/hq`, `/alpha`, and `/vault` without adding autonomous trading or a new top-level route
- [x] MO1 — Memory + OSINT Casefile Loop: adapt `claude-obsidian`, `D4rk_Intel-OSINT-Investigative-Toolkit`, and `public-apis` as native patterns for VAULT memory stewardship plus passive-first RECON/CYBER case progression without bundling third-party binaries, widening the shell into a tool catalog, or relaxing the current advisory boundary
- [x] VR1 — Vehicle + Radar Readiness: adapt `PLFM_RADAR` and reference-only learning patterns from `ai-engineering-from-scratch` into `/vehicle` readiness, onboarding, artifact packaging, and future radar session continuity without adding RF control, flight-critical behavior, or new public routes
- [x] XR2 — Keep the next tranche project-local while polishing reuse, metadata stewardship, exact durable-artifact reopen, and continuity handoffs for the shipped XR1 surfaces without adding new tabs, routes, or external repo dependencies
- [x] MR2 — Market review continuity polish: populate source metadata, reuse strongest prior reviews directly in ALPHA, and reopen exact prior reviews in VAULT without widening into forecast execution
- [x] MR3 — Market Postmortem Coach: extend the existing market-review lane with durable repeat / avoid / next-rule lesson capture across `/hq`, `/alpha`, and `/vault` so exact reopen preserves operator discipline without widening into forecast execution or a new dashboard
- [x] MO2 — Memory + OSINT stewardship polish: reuse strongest prior casefiles, file explicit source/evidence metadata, deepen VAULT cues, and reopen exact casefiles while keeping RECON and CYBER passive-first
- [x] VR2 — Vehicle + radar continuity polish: keep `nexus-vehicle-session-v1` backward compatible while improving radar-readiness guidance, imported bundle review, and exact VAULT handoffs from the existing vehicle artifact lane
- [x] RI1 — RECON Repo Intel: ship a protected public-GitHub metadata brief under `/api/repo-intel`, expose it as a compact RECON OSINT module and `recon-repo-intel` exact-session focus, wire an `analyze_repo` tool into the active agent runtime, and stage GitHub/dependency/competitor repo assessment through HQ without widening the shell into a new tab or code-ingestion surface
- [x] RI2 — GitHub Repo Assimilation Completion: finish the public-safe repo loop by adding explicit repo assimilation through RECON and HQ, exposing an `assimilate_repo` tool, filing durable repo-assimilation briefs into VAULT compiled pages, and upgrading ORBIT handoff to use the saved assimilation brief instead of only the lighter RI1 metadata profile
- [x] RI3 — Public Repo Compare Matrix: add explicit 2–3 repo comparison inside the existing RECON repo-intel lane, expose `compare_repos`, support `/compare-repos`, file durable `repo-compare` briefs into VAULT, and hand ORBIT the saved comparison brief instead of only single-repo intel
- [x] DR1 — NOVA Deep Research Engine: expose a real bounded `deep_research` tool through `/api/tools`, align `/deepresearch` guidance and INTEL/NOVA staging to that runtime, and keep explicit deep-research briefs filing durable `research-workflow` pages into VAULT without widening into a new research product surface
- [x] PC1 — Persona Council Completion: finish the older multi-persona HQ lane by routing Council mode through the existing AI/runtime spine, preserving single-persona prompts for normal dispatch, rendering trustworthy side-by-side council answers with explicit pick-or-merge actions, and adding the missing contract coverage so the feature stops being presentation-only scaffolding
- [x] SO1 — Switch Operator + Provider Resilience Completion: add explicit `/operator` one-shot orchestration, provider-health exact-session focus, shared COMMAND/HQ resilience posture, and actual-provider runtime reporting without inventing scheduler automation or a second provider-routing system
- [x] DU1 — Dynamic UI Contract Completion: finish the existing rules-and-alerts lane by making signal evaluation shell-wide, making dismissals event-aware instead of permanent, rendering the missing top-rail header indicator contract, and proving the behavior with dedicated dynamic-UI coverage before promoting the lane beyond experimental status
- [x] RG1 — Reasoning RAG + VAULT Article Index Completion: keep keyword routing as the cheap fast path, add bounded async reasoning fallback for low-confidence HQ retrieval, enrich saved VAULT clips with claim/entity indexes through the existing AI spine, and reuse those cues across HQ, VAULT, and the memory spine without adding a vector DB, a new public route, or an always-on background process
- [x] VG2 — VAULT Archive Compounding, Capture, and Weekly Synthesis: add local semantic cross-linking and backlink health, explicit dismissible `Save to VAULT` capture prompts for qualifying HQ answers, explicit `/weekly` archive synthesis that files durable `vault-weekly` briefs, and richer Obsidian-style export with frontmatter and wikilinks without widening Nexus into autosave, schedulers, or external graph services
- [x] UX2 — Visual Redesign Across the Core App: redesign the shared shell, HQ flagship composition, and all core tabs into a denser, graphics-heavy, premium operator system without changing routes, exact-session behavior, or assistant workflows
- [x] UX4 — Monochrome Tactical War-Room Redesign: pivot the shell and HQ from the older brass sanctum language into a unified monochrome tactical room with image-led route plates, flatter command-header framing, sharper functional surface naming, and one coherent war-room grammar across the core tabs without changing routes, workflow IDs, or continuity contracts
- [x] UX5 — Creative Work-First Tactical Redesign: absorbed into the shipped UX6 Satellite Ops redesign so the older box-removal and work-first HQ goals now live inside one bespoke whole-app visual system instead of a separate intermediate pass
- [x] UX6 — Bespoke Satellite Ops Redesign: replace the visible UX5 shell objects entirely with a bespoke Satellite Ops command environment built from route-specific operations plates, a new command header, reactive tactical motion, a custom HQ operating table, and a whole-app rail-based visual system that no longer reads like reused shell primitives
- [x] UX7 — Full Satellite Ops Migration and Interactive Experience: finish the redesign as a true full migration by replacing the remaining legacy route module/card wrappers with bespoke workplane/rail/continuity primitives, rebuilding HQ’s 3D scene into a real Satellite Ops command room, and adding richer stateful interaction so no obvious sanctum-era or boxed-dashboard objects remain in first-view route compositions
- [x] VX1 — Local Voice, Privacy Shield, and Repo Security Intelligence: keep Resources Voice Lab trustworthy across runtime-backed and browser-only paths, harden the local Impact graph/ownership/hotspot/security workbench, prove cloud-bound privacy shield behavior inside `/api/ai`, and ship the defensive `vuln-review` workflow with durable VAULT continuity
- [ ] FD2 — Blocked on the real staged Coolify hostname in repo-root `.env.local`; once present, stand up the first Coolify/VPS staging deployment from the repo-root `Dockerfile` with conservative web-self-hosted defaults and the preserved candidate branch `codex/preserve-main-2026-04-11`
- [ ] FD3 — Blocked on FD2 host config; run `npm run launch:gate:target` against that exact host after repo-root `.env.local` carries the real staged `NEXUS_RELEASE_BASE_URL` and local `NEXUS_TOKEN`
- [ ] FD4 — Blocked on staged-host proof; capture remote `/api/diagnostics`, GA route/manual smoke, and the focused A6 CYBER/SKILLS/HQ/COMMAND staging pass on the staged host
- [ ] FD5 — Blocked on staged-host proof; document the first production rollback target and promotion checklist before going live
- [x] A6 — Add shared governance metadata (risk tier, approval requirement, domain tags, operator-only/automation posture) and cyber pack baseline across skills, workflow packs, assistant capabilities, readiness lanes, and CYBER support rails
- [ ] A7b — Blocked forecast execution lane: compare a TimesFM companion provider against the native baseline only after decision-lift measurement and staging proof are stable

## Compatibility Tranche

- `CLAUDE.md`, `tasks/todo.md`, `tasks/lessons.md`, and `docs/AGENT_HANDOFF.md` remain in the repo for compatibility only.
- Historical execution detail remains in `docs/plans/`, `docs/ideas/`, and git history.
