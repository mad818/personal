# UditAkhourii/adhd — Analysis complete

**What it is:** A TypeScript/Node divergent-ideation skill, CLI, and library that branches across isolated frames and applies a critic pass.

**Stack:** TypeScript/Node plus Markdown skill instructions.

**Nexus decision:** Reuse Council with exactly three frames and one optional pinned JANSKY merge. Do not import the larger upstream runtime.

**Benefit:** More diverse options without the upstream method's roughly ten-call default cost.

**Proof:** `lib/councilDivergence.ts`, `lib/personaEngine.ts`, `components/home/office/CouncilResultsPanel.tsx`, and `npm run focus-motion-divergent-council:check`.
