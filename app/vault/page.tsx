// ── vault/page ──────────────────────────────────────────────
// VAULT tab: bookmarked articles, search, folders, export functionality.

import { redirect } from 'next/navigation'

export default function VaultPage() {
  redirect('/home')
}
