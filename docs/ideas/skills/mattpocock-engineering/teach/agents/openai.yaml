interface:
  display_name: "Teach"
  short_description: "Run a bounded stateful learning sequence in the workspace."
  default_prompt: "Use $teach to build a bounded multi-session learning path for this concept."
policy:
  allow_implicit_invocation: false
