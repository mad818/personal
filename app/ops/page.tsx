// ── ops/page ────────────────────────────────────────────────
// OPS tab: conflict tracker, Shadowbroker live intel, FX, commodities, OSINT.

import { redirect } from "next/navigation";

export default function OpsPage() {
  redirect("/intel");
}
