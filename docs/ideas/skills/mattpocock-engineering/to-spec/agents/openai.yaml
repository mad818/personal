interface:
  display_name: "To Spec"
  short_description: "Synthesize approved context into a Nexus feature contract."
  default_prompt: "Use $to-spec to turn this conversation into a bounded Nexus specification."
policy:
  allow_implicit_invocation: false
