# Homefront System Cohesion Pass

## Goal

Make the public landing and authenticated non-HQ shell feel like one professional Homefront command system before any Git push or Mac transfer.

## Scope

- Keep work local-first and avoid Git publishing during this phase.
- Add a shared operating-contract layer to protected non-HQ surfaces.
- Mirror the same contract language on the public landing.
- Preserve `/hq` and RPG World as a private/personal lane, not public product positioning.

## Acceptance

- Public `/` includes the command contract and still avoids stale agency or public RPG language.
- Authenticated non-HQ surfaces expose `homefront-operating-contract` near the existing threshold.
- Existing threshold, live-vision, route-proof, and doctrine strip behavior remains intact.
- `npm run type-check`, `npm run verify`, `npm run build`, and live `3100` route proof pass or blockers are recorded.
