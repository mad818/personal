# Nexus Prime — Task List

## Status: Active Development

## Completion Program (2026-04-02)

See [docs/plans/nexus-completion-program-2026.md](../docs/plans/nexus-completion-program-2026.md) and [docs/regression-memory-checklist.md](../docs/regression-memory-checklist.md).

### Future vehicle / drone lane

- [x] V0.1 — Simulated telemetry generator and replay mode for `/internal/vehicle`
- [x] V0.2 — Standardize future flight telemetry schema (heartbeat, mode, GPS, battery, link quality, failsafes, mission status)
- [x] V0.3 — Archive flight-log / incident artifacts into VAULT conventions
- [x] V1.1 — Bench bring-up checklist for the F450 (props-off validation, RC, GPS, orientation, failsafes)
- [x] V1.2 — Passive telemetry bridge from flight stack into Nexus without making Nexus flight-critical

### P2 — Release engineering closure

- [ ] CP2.1 — Web release rehearsal: Docker + target deployment + diagnostics snapshot + rollback proof
- [ ] CP2.2 — Desktop isolation validation: secure runtime, no-outbound proof in isolated mode, capability lockdown evidence
- [ ] CP2.3 — Desktop trust chain: checksum verification, signing status record, SBOM status record. The status-record command and canonical SBOM now exist; remaining release work is real packaged artifacts, checksums for those artifacts, and configured signing strategy.
- [ ] CP2.4 — Final launch gate: `type-check`, `lint`, `verify`, `route:integrity`, `eval:agent-runtime:ci`, `release:smoke`, auth E2E

## Security ideas (next)

Concrete hardening items for future sprints — surgical, scoped, ordered by impact:

1. ~~**SSRF via DNS rebinding**~~ — **shipped 2026-06-22:** `lib/security/privateNetwork.ts` + post-DNS check in `api/headers`.
2. **Rate-limit in-memory is per-process** — `lib/security/rateLimit.ts` resets on every dev-server restart. For LAN-exposed deployments, back the rate-limit store with a lightweight persistent file or SQLite ledger so abuse windows survive restarts.
3. **CSP `unsafe-inline` on scripts** — Remove `'unsafe-inline'` from `script-src` by migrating to nonce-based CSP. Next.js 14 supports `nonce` via middleware; inject it into `<Script>` tags and the inline hydration block.
4. **Subresource Integrity (SRI) on TradingView embeds** — `s3.tradingview.com` scripts are whitelisted in CSP without SRI hashes. Add `integrity` + `crossorigin` attributes so supply-chain tampering is caught by the browser.
5. ~~**Cookie `SameSite=Strict` audit**~~ — **shipped 2026-06-22:** `nexus_session_token` now uses `SameSite=Strict` (logout clear cookie aligned).
6. **Phone-tier route audit** — Audit every `/api/*` route for phone-token tier bypass. `requireMasterSessionForAction()` added in `lib/security/masterSession.ts`; wire to any route that only checks step-up today.
7. ~~**`X-Forwarded-For` spoofing in rate limiter**~~ — **shipped 2026-06-22:** `getRequestIdentity` ignores `X-Forwarded-For` unless `NEXUS_TRUST_PROXY=true`.
8. ~~**`writeEnvFile` path traversal hardening**~~ — **shipped 2026-06-22:** `assertAnchoredRuntimeEnvFilePath()` guards settings env writes.

## Audit backlog (2026-06-22)

**Must (operational)**
- Restart LAN server with `npm run phone:lan:start` when Next.js memory-restarts; compact HQ reduces load.
- Desktop local AI proof: one Ollama prompt on PC HQ → `npm run offline:local:report`.
- CI green: `npm run verify` + push Node runtime alignment (`CI-GREEN-NODE-RUNTIME`).

**Should (security / quality)**
- Nonce-based CSP (remove `unsafe-inline` on scripts).
- Persistent rate-limit store for LAN-exposed installs.
- Wire `requireMasterSessionForAction` to settings mutations and any high-risk `/api/*` routes missing phone-tier checks.
- Dependabot: merge postcss/prismjs fixes to `main` and wait for GitHub rescan.

**Could (later, non-RPG)**
- SRI on TradingView embeds.
- Host-header / DNS rebinding guard on other server-side fetch proxies (if any beyond `api/headers`).
- Default `hqCompactOperatorLayout` onboarding hint for users who want the RPG playfield back (Settings toggle exists).
- Playwright `hq:e2e` on Windows (spawn EPERM) — fix worker permissions or document WSL lane.

## Expanded backlog (2026-06-22)

**Must** (manual verify)
- INTEL `/intel?view=sweeps` — run a theater sweep on desktop; confirm geo snapshot + source cards.
- VAULT / COMMAND — run one Memory Ask query; confirm citations when local memory exists.

**Should** (remaining)
- TradingView SRI hashes when TradingView publishes stable embed hashes (lazy load + `crossOrigin` shipped).
- Windows Playwright `spawn EPERM` — document WSL lane or fix worker spawn for `hq:e2e`.
- Desktop Tauri: one-click restart runtime tray action.

**Shipped this pass**
- [x] INTEL `region` query param wired — auto-opens world view, filters ConflictFeed, banner with Show all theaters.
- [x] VAULT `VaultLibrarianPanel` surfaces librarian audit-page load failures.
- [x] Skills forge `SkillSpectrumValidatorStrip` — SkillSpector safe/review/blocked summary on Workflow Forge.
- [x] CYBER `CVEFeed` reads `cveFetchError` from store when NVD fetch fails.
- [x] MQTT `MQTTStatus` + `SensorDashboard` stream error surfacing and cleanup.
- [x] `api/sweeps` GET/POST rate limit + desktop session gate for connector sweeps.
- [x] `pruneBotHistory` on Zustand hydrate (cap 120).
- [x] RECON `RepoIntelPanel` memory-pages load failures surfaced.
- [x] `LiveEventFusionStrip` links to `/intel?view=world` with `region` param + separate Source link.
- [x] ALPHA TradingView lazy IntersectionObserver load + `crossOrigin` on embed scripts.

## Next Up

Active open-ready queue:

Use `npm run ops:first-three` for the current combined status of the first three non-RPG operational lanes before opening individual JSON artifacts: `postcss` runtime patch, phone/iPad acceptance, and local AI offline proof. Use `npm run phone:acceptance:guide` for the plain phone/iPad checklist and LAN HQ URL candidates. The first-three command also points GitHub/X/YouTube idea pressure back to the existing source-intake docs rather than adding surprise scope.

- [x] WHOLE-APP-CLIPBOARD-FEEDBACK — Replace all thirteen source-proven silent or unhandled clipboard writes outside the private RPG lane with truthful shared success/failure feedback and reject future regressions. Spec: `specs/features/whole-app-clipboard-feedback.md`.
  - Visual thesis: preserve every current workplane and copy control; reuse the compact shared toast signal with no new card, banner, or modal language. Content plan: name the artifact, confirm only a completed write, and provide one retry instruction after browser rejection. Interaction thesis: one click starts one write, success stays polite, and rejected promises are always contained.
  - Progress: added one client-only `copyTextWithFeedback()` helper that checks clipboard availability, awaits the browser write, announces named success only after fulfillment, emits a retryable medium-severity failure through the existing toast surface, returns a boolean result, and never includes copied text in feedback. Migrated eleven empty rejection handlers across vehicle and Settings plus the two unhandled VAULT graph exports without changing controls, payloads, layout, downloads, persistence, or routes. Extended the secondary-surface TypeScript-AST gate to reject unhandled writes, empty promise catches, empty `try/catch` blocks, and property/bracket equivalents while accepting substantive handlers, delegated catches, and raw promise forwarding. Added the correction to the lessons log. Benefits: operators now know whether commands, profiles, runbooks, bundles, checklists, briefs, and graph summaries actually reached the clipboard; permission failures are recoverable; and future definite regressions fail normal verification. Independent audit: 314 active TSX files, 13 existing direct writes with handling/forwarding, 13 shared-feedback calls, and zero silent or unhandled writes outside the private RPG lane. Proof: focused validator/accessibility, type-check, targeted ESLint, new/normalized-file formatting plus changed-section review, `git diff --check`, the complete `npm run verify` lane, and `npm run build` passed.
  - [x] Add one shared clipboard-feedback helper and migrate the eleven swallowed rejections plus two unhandled VAULT awaits.
  - [x] Reject silent/unhandled clipboard writes with TypeScript-AST equivalence fixtures across active non-RPG TSX sources.
  - [x] Run independent, focused, full, build, handoff, and git proof.

- [x] WHOLE-APP-MUTATION-RESPONSE-TRUTH — Close the remaining definite unchecked client mutation in the ESPectre WiFi viewer and extend the existing AST gate so failed HTTP responses cannot be presented as prepared control envelopes. Corrective extension to `specs/features/whole-app-client-fetch-resilience.md`.
  - Visual thesis: no visible redesign; retain the existing WiFi sensing workplane, local busy state, and consent/readiness copy. Content plan: accept the server note only after a successful response and otherwise keep the explicit inability message. Interaction thesis: failed control requests never become prepared-command feedback, while the current in-flight lock always releases.
  - Progress: the ESPectre configure/calibrate POST now requires `response.ok` before parsing or presenting its response, so protected-route failures fall into the existing failure message and cannot masquerade as queued operator-managed commands. Extended the secondary-surface TypeScript-AST gate to classify literal POST/PUT/PATCH/DELETE methods, reject assigned responses without an `.ok` check, reject ignored status results, accept checked or deliberately forwarded responses, and leave dynamic methods to explicit review. Added positive and negative fixtures plus the correction to the lessons log. Benefits: WiFi sensing controls now report server truth, future definite client mutation gaps fail the normal verification lane, and no provider, route, state, dependency, or visual surface was added. Independent audit: 314 active TSX files, zero unchecked assigned literal mutations outside the private RPG lane. Proof: focused validator/accessibility, type-check, targeted ESLint, validator/spec/lesson formatting, changed-section review, `git diff --check`, the complete `npm run verify` lane, and `npm run build` passed.
  - [x] Require `response.ok` before accepting the ESPectre control response and preserve explicit failure feedback.
  - [x] Reject unchecked POST/PUT/PATCH/DELETE fetch responses through focused AST fixtures and the active non-RPG source audit.
  - [x] Run independent, focused, full, build, handoff, and git proof.

- [x] WHOLE-APP-CLIENT-FETCH-RESILIENCE — Remove the three definite ignored client load chains, distinguish loading/error/empty state, make Workflow Forge mutations response-truthful, and reject future unhandled effect chains. Spec: `specs/features/whole-app-client-fetch-resilience.md`.
  - Visual thesis: reuse the shared skeleton and compact callout materials with no new card language. Content plan: name the dataset or action, state failure plainly, and expose one recovery move. Interaction thesis: polite loading, assertive failure, local retry, cleanup-safe effects, and action-specific busy feedback.
  - Progress: added polite live/busy semantics to the shared loading state; Geo Delta, Registry, and Workflow Forge now use effect-local guarded loaders, require successful responses, prevent stale cleanup commits, distinguish loading/error/empty state, and expose local retry actions. Workflow Forge now has save/clone/run/copy-specific busy labels, disables concurrent actions, validates every mutation response, preserves local state on failure, keeps selection valid after reload, and reports success/failure through the shared toast surface. Extended the existing surface-polish gate with TypeScript-AST fixtures for direct ignored fetch/Promise chains, explicit `.catch()` and rejection handlers, guarded loaders, and `void`-launched local loaders whose internal source is unguarded. Benefits: failed routes no longer masquerade as zero data, effect rejections are contained, retries stay in context, and mutations cannot claim success after HTTP failure. Independent audit: 314 active TSX files, zero ignored or locally unguarded fetch/Promise launches outside the private RPG lane; the accessibility gate now proves 624 named controls. Proof: focused surface/accessibility, type-check, ESLint, validator/spec formatting, `git diff --check`, the complete `npm run verify` lane, and `npm run build` passed.
  - [x] Make shared loading semantics explicit and give Geo Delta, Registry, and Workflow Forge honest loading/error/retry state.
  - [x] Require successful Workflow Forge responses and add action-specific busy plus toast feedback.
  - [x] Add AST regression proof and run independent, focused, full, build, handoff, and git verification.

- [x] WHOLE-APP-NATIVE-DIALOG-CLOSURE — Correct the two bare `alert()` calls that bypassed the completed action-dialog contract, make draft finalization reject failed HTTP responses, and replace the string-only regression scan with AST-backed coverage. Corrective extension to `specs/features/whole-app-action-dialogs.md`.
  - Visual thesis: preserve the compact draft queue and use the shared signal strip instead of introducing another modal. Content plan: state the missing-key or failed-write condition, identify the next operator move, and confirm successful finalization. Interaction thesis: feedback stays nonblocking, focus never leaves the draft action, and failed requests leave the draft pending for retry.
  - Progress: replaced both HomeChat draft-finalization alerts with shared missing-key, success, and failure toast feedback; draft writes now require `response.ok`, failed requests remain pending, and the active row exposes a disabled `Finalizing…` state that prevents dismissal or duplicate submission. Replaced the `window.*` substring scan with TypeScript-AST detection for bare calls, `window`/`globalThis`/`self` property calls, and bracket access while explicitly accepting unrelated component methods; fixture proof covers all forms. Added the correction to the lessons log. Benefits: draft actions no longer block or steal focus, server errors cannot create false finalized state, retry remains available, and equivalent native-dialog syntax now fails the normal gate. Independent audit: 314 active TSX files, zero blocking browser-dialog calls outside the private RPG lane. Proof: focused accessibility, type-check, ESLint, changed-section formatting review, `git diff --check`, the complete `npm run verify` lane, and `npm run build` passed.
  - [x] Replace both blocking HomeChat alerts with shared toast feedback and finalize only after a successful tools response.
  - [x] Detect bare and explicitly global alert/confirm/prompt calls through TypeScript AST fixtures across active TSX sources.
  - [x] Record the validator lesson and run independent, focused, full, build, handoff, and git proof.

- [x] WHOLE-APP-HONEST-ACTION-AFFORDANCES — Eliminate every source-proven dead action outside the private RPG lane by implementing honest local behavior where supported, removing impossible controls, and rejecting future actionless buttons. Spec: `specs/features/whole-app-honest-action-affordances.md`.
  - Visual thesis: preserve the dense operator panels and modal material; add only terse scope copy and reuse the shared confirmation/toast surfaces. Content plan: session inventory, no hardware provisioning, staged local simulation, and no transmitted vehicle command. Interaction thesis: one focus-contained IoT editor, confirmed removal, impossible screenshot removal, and immediate vehicle staging feedback.
  - Progress: replaced the UI-only IoT form with a focus-contained controlled editor and session-local add/configure/remove behavior; removal now uses the shared confirmation dialog, every mutation reports through the shared toast surface, and concise copy explicitly rules out discovery, provisioning, connectivity checks, persistence, and live telemetry. Removed the passive-feed screenshot button because no capture path exists. `Stage Base Return` now changes only local vehicle simulation state and explicitly reports that no vehicle command was transmitted. Expanded the TypeScript-AST gate with actionless and suppression-only button fixtures; the permanent gate proves 621 named controls with no definite pointer-only containers or dead button actions, while an independent audit of 314 active TSX files reports zero actionless and zero suppression-only buttons outside the private RPG lane. Benefits: operators no longer lose IoT input, destructive session changes are confirmed, physical-world panels stop overclaiming capabilities, and future dead buttons fail normal verification. Proof: focused accessibility, type-check, ESLint, formatting, `git diff --check`, the complete `npm run verify` lane, and `npm run build` passed.
  - [x] Replace the UI-only IoT registry form with functional session add/configure/remove behavior and honest boundaries.
  - [x] Remove the impossible passive-feed screenshot control and wire staged local base-return behavior.
  - [x] Extend the AST gate for actionless/suppression-only buttons and run focused, full, build, handoff, and git proof.

- [x] WHOLE-APP-POINTER-CONTROL-SEMANTICS — Replace every simple active pointer-only container outside the private RPG lane with a native keyboard-operable control, remove the dead VAULT folder affordance, and extend the shell accessibility proof to shared button wrappers. Spec: `specs/features/whole-app-pointer-control-semantics.md`.
  - Visual thesis: preserve the exact tile, switch, row, widget, and camera compositions through native-control reset styles. Content plan: reuse visible labels and add state-aware names only where the action would otherwise be ambiguous. Interaction thesis: preserve pointer behavior while gaining native Enter/Space activation, focus semantics, and exposed switch/expanded state.
  - Progress: converted all six source-proven pointer-only control sites to native buttons or switches across IoT automation rules, IoT device rows, Knowledge Base entries, the floating agent-status widget, VAULT folders, and vehicle camera tiles. Existing layouts are preserved through explicit native-control resets; Enter/Space now work without custom handlers, and switch, expanded, or pressed state is exposed through ARIA. Removed the inert duplicate VAULT `Bookmarks` tile and its unreachable branch. Expanded the TypeScript-AST gate to include `motion.button`, `motion.a`, and `ShellButton`, complete keyboard fallbacks, nested-control exceptions, and a negative pointer-only fixture; the shell gate now proves 622 named controls and zero definite pointer-only containers outside the private RPG lane. Benefits: keyboard and switch-device users gain native operation, screen readers receive state, VAULT no longer advertises a dead action, and shared wrapper controls now carry the same regression guarantee as native controls. Proof: independent zero-result audit, validator self-tests, `npm run shell:accessibility:check`, `npx tsc --noEmit`, targeted ESLint, targeted formatting, the complete `npm run verify` lane, `npm run build`, and `git diff --check` passed.

- [x] WHOLE-APP-INTERACTIVE-CONTROL-NAMES — Give every active symbol-only or title-only button/link outside the private RPG lane a reliable programmatic name and enforce definite regressions through the existing shell accessibility command. Spec: `specs/features/whole-app-interactive-control-names.md`.
  - Visual thesis: zero visible redesign; preserve every icon, glyph, layout, token, focus ring, and animation. Content plan: concise action-oriented names, dynamic only where existing state distinguishes the current action. Interaction thesis: preserve handlers, focus order, disabled states, navigation, submission, and persistence.
  - Progress: remediated all 33 confirmed gaps across 27 active components, including panel dismissals, search clearing, send/refresh actions, SEC filing navigation, camera actions, network-target removal, and stateful Vault/recording toggles. Added a recursive TypeScript-AST validator with negative fixture self-tests, exact file/line diagnostics, Unicode letter/number recognition, dynamic-content handling, `aria-hidden` and image-alt awareness, and a hard private-RPG boundary. `npm run shell:accessibility:check` now proves 536 interactive controls named alongside 248 named form controls. Benefits: screen readers and voice-control tools can identify glyph actions reliably, tooltip-only naming no longer depends on hover, and future definite symbol/title-only regressions fail the normal repository gate without rejecting legitimate data-driven labels. Proof: independent validator and self-test, `npm run shell:accessibility:check`, `npx tsc --noEmit`, targeted ESLint, the complete `npm run verify` lane, `npm run build`, and `git diff --check` passed.

- [x] WHOLE-APP-FORM-CONTROL-NAMES — Give every active, non-hidden form control outside the private RPG lane a reliable programmatic name and enforce the contract through the existing shell accessibility command. Spec: `specs/features/whole-app-form-control-names.md`.
  - Visual thesis: zero visible redesign; preserve existing workplanes, tokens, focus rings, and placeholders. Content plan: reuse the concise operator term already visible beside each field, with task-specific ARIA text only where native association would restructure the surface. Interaction thesis: focus order, pointer/keyboard behavior, validation, submission, and persistence remain identical while assistive technology gains a stable name on every control.
  - Progress: remediated all 112 audited gaps across 37 active components spanning COMMAND, INTEL, ALPHA, CYBER, RECON, VAULT, SKILLS, RESOURCES, IOT, VEHICLE, HQ, and shared scheduler/memory UI. Names now cover text fields, textareas, selects, ranges, file inputs, read-only outputs, mapped fields, and policy controls without changing visible copy, layout, state, handlers, submission, persistence, or provider behavior. Added a recursive TypeScript-AST validator with a negative self-test, native wrapped-label and `htmlFor` support, non-empty ARIA checks, exact file/line diagnostics, hidden-input exclusion, and a hard private-RPG boundary; `npm run shell:accessibility:check` now proves 248 named controls with five hidden inputs excluded. Benefits: screen readers and voice-control tools can identify every active form control, placeholders remain hints rather than labels, and future unnamed controls fail the normal repository gate. Proof: independent zero-violation audit, `npm run shell:accessibility:check`, `npx tsc --noEmit`, targeted ESLint, the complete `npm run verify` lane, `npm run build`, and `git diff --check` passed.

- [x] ROUTE-ENTRY-CONTROL-ACCESSIBILITY — Give the global command bar and the canonical operator entry control on every active workspace route a stable programmatic name, then protect the contract in the existing shell accessibility gate. Spec: `specs/features/route-entry-control-accessibility.md`.
  - Visual thesis: no visible redesign; preserve the current token, focus-ring, and workplane composition. Content plan: one concise task-specific accessible name per canonical control while placeholders remain format hints only. Interaction thesis: pointer, keyboard, submission, and route behavior stay unchanged while assistive technology gains a reliable name on focus.
  - Progress: added stable, task-specific accessible names to the global command bar and canonical entry controls across HQ, COMMAND, INTEL, ALPHA, CYBER, RECON, VAULT, SKILLS, RESOURCES, IOT, and VEHICLE. The contract covers dynamic IoT field names, both CYBER review inputs, RECON and VAULT selectors, and the vehicle range control while preserving every placeholder, layout, state path, and interaction. The existing shell accessibility gate now fails if any required route-level name disappears. Benefits: screen readers announce each major work surface immediately on focus, voice-control tooling can target stable names, and future regressions are caught in the normal repository gate. Proof: `npm run shell:accessibility:check`, `npx tsc --noEmit`, targeted ESLint, the complete `npm run verify` lane, `npm run build`, and `git diff --check` passed.

- [x] WHOLE-APP-ACTION-DIALOGS — Replace all eight remaining blocking browser-native alert, confirm, and token-prompt calls with one product-native, focus-contained action-dialog system across trust, phone onboarding, and Resources workflows. Spec: `specs/features/whole-app-action-dialogs.md`.
  - Visual thesis: one quiet modal workplane with a dimmed canvas, hairline structure, monochrome instrument copy, and a single state edge. Content plan: terse context label, explicit action heading, one short consequence or instruction, then only the controls needed to continue or cancel. Interaction thesis: focus enters the safest control for confirmations and the protected input for step-up; Tab remains contained; Escape or backdrop cancels; focus returns to the opener; async validation exposes a busy state and inline result without blocking the browser.
  - Progress: added a session-only promise controller and body-portaled shared action dialog using the existing focus-trap, Escape, scroll-lock, and focus-return contract; added a masked protected-action dialog that clears token state, preserves existing elevated `validateToken()` behavior, blocks dismissal while busy, and reports rejection/rate-limit/runtime failures inline. Replaced all eight native browser dialogs across trust revalidation, phone PWA guidance, secure-link removal, media duplicate/import/removal flows, and Escape backup restore without changing their underlying mutations. Token-only responsive styling adds safe initial focus, visible focus, destructive state signaling, action stacking, and dual reduced-motion guards. The shell gate now recursively rejects native dialog calls anywhere under active `app/` and `components/` TSX. Proof: `npm run shell:accessibility:check`, `npx tsc --noEmit`, targeted ESLint, `npm run security-scan`, the complete `npm run verify` lane, `npm run build`, and `git diff --check` passed. Rendered interaction remains unclaimed because the in-app browser policy rejects local targets and no DOM test dependency is installed.

- [x] WHOLE-APP-TOAST-FEEDBACK — Make shared transient notifications readable, keyboard-safe, motion-aware, and limited to genuinely new events instead of replaying persisted notification history on launch. Spec: `specs/features/whole-app-toast-feedback.md`.
  - Visual thesis: a compact monochrome signal strip with one severity edge, hairline structure, and no floating-card ornament. Content plan: title, optional message, terse severity label, one dismiss control, and a quiet remaining-time indicator. Interaction thesis: motion is a restrained lateral entrance only when allowed; countdown pauses while hovered, keyboard-focused, or backgrounded; critical and high signals announce assertively while lower severities remain polite.
  - Progress: replaced the legacy inline brown/rose toast card with one responsive token-only signal strip; added severity-specific alert/status announcements, atomic live semantics, focus-visible dismissal, a touch-safe close target, reduced-motion-aware Framer transitions, and countdown pausing for hover, focus-within, and hidden documents. The notification bridge now waits for Zustand hydration and seeds persisted IDs before emitting later unseen notifications, preventing launch-time history replay. The focused shell gate proves the feedback and hydration contracts. Proof: `npm run shell:accessibility:check`, `npx tsc --noEmit`, targeted ESLint, the full `npm run verify` sequence completed in bounded runs after the 120-second wrapper timeout, `npm run build`, and `git diff --check` passed.

- [x] WHOLE-APP-ROUTE-RESILIENCE — Replace blank or generic framework loading, error, root-failure, and missing-route states with one product-native recovery plane across public and authenticated Nexus surfaces. Spec: `specs/features/whole-app-route-resilience.md`.
  - Visual thesis: one quiet, cardless recovery plane with hairline structure, monochrome instrument text, and a single state signal. Content plan: terse state label, actionable heading, one-sentence scope/recovery guidance, then only the controls needed to continue. Interaction thesis: loading uses one restrained signal pulse; retry remounts the failed segment without a reload; return/reload actions use the shell focus treatment and reduced-motion behavior.
  - Progress: added one cardless `RouteStatePanel` with public-main/authenticated-section semantics, atomic live status, visible focus-compatible recovery actions, token-only styling, responsive stacking, and dual reduced-motion guards; wired App Router loading, segment error, global error, and not-found files; kept local event-bus diagnostics while hiding raw error/stack detail from production; and migrated the existing root React boundary without losing retry, reload, safe-entry, custom-fallback, or remount behavior. The focused shell gate now proves the complete contract and rejects the legacy fault/stack UI. Proof: `npm run shell:accessibility:check`, `npm run type-check`, `npm run lint`, `npm run build`, `npm run verify`, and `git diff --check` passed. The managed production runtime reached healthy on `127.0.0.1:3100`; the wrapper timed out after reporting health, and process policy rejected the isolated rendered 404 probe before launch, so rendered state interaction remains unclaimed.

- [x] WHOLE-APP-MODAL-POLISH — Make every real modal surface keyboard-contained, clearly named, Escape-closeable, scroll-safe, and able to return focus to its opener without changing the established visual composition.
  - Visual thesis: calm containment with the current side-panel and centered-workplane materials left intact. Content plan: one explicit dialog heading, one discoverable close action, and the existing operational body with no new decorative copy. Interaction thesis: focus enters the close action, Tab and Shift+Tab cycle inside the active dialog, Escape or backdrop closes it, and focus returns to the originating control while existing entrance/exit motion remains unchanged.
  - Progress: added one shared `useModalDialog` contract with initial focus, Tab/Shift+Tab containment, Escape closure, reference-counted body-scroll locking, and opener focus restoration; adopted it in Settings, Notifications, and the ALPHA trade-thesis modal; moved modal names to visible heading relationships; and converted unread notification rows into real keyboard-operable buttons while keeping dismiss separate. The focused shell gate now proves every modal adoption. Proof: `npm run shell:accessibility:check`, `npm run type-check`, `npm run lint`, `npm run verify`, `npm run build`, and `git diff --check` passed. Rendered localhost interaction remains unavailable under the in-app browser policy, and the repo has no DOM-test dependency; no test-only package was added.

- [x] WHOLE-APP-ROUTE-ORIENTATION — Give every public, authenticated, supported, and internal route a meaningful document title, labeled main landmark, and polite client-side route announcement through the shared shell.
  - Visual thesis: orientation without ornament. Content plan: surface name in the browser title, workspace name on the main landmark, and one short hidden route-loaded status. Interaction thesis: route changes update title and screen-reader context without stealing focus or adding visible motion.
  - Progress: shared route branding now updates the browser title to `Surface · Homefront` on authenticated routes while preserving the public Homefront title; every authenticated main landmark carries its workspace label; and a polite atomic status announces public landing, access redirect, and route workspace changes without moving focus. The existing shell accessibility gate now proves these contracts. Proof: `npm run shell:accessibility:check`, `npm run type-check`, `npm run lint`, `npm run verify`, `npm run build`, and `git diff --check` passed.

- [x] WHOLE-APP-KEYBOARD-POLISH — Add one shared skip-to-workspace path across the public landing, access redirect, and authenticated Nexus shell; correct the route-link navigation semantics; and prove the contract through a focused gate under full verification.
  - Visual thesis: invisible until needed, then crisp cyan command instrumentation. Content plan: one skip action, one canonical main target, and one correctly labeled navigation landmark. Interaction thesis: keyboard focus reveals the link, activation transfers focus to the workspace, and pointer-first composition stays unchanged.
  - Progress: the public landing, unauthenticated redirect, and authenticated shell now expose one focus-only skip link backed by a stable focusable main target; activation focuses and scrolls the workspace; the primary route links now remain ordinary links inside a correctly labeled navigation landmark instead of an invalid tablist. Added reduced-motion-safe cyan focus styling and `npm run shell:accessibility:check` under the existing surface-polish/full-verify lane. Proof: `npm run surface:polish:check`, `npm run type-check`, `npm run lint`, `npm run verify`, `npm run build`, and `git diff --check` passed. Rendered localhost inspection remains unavailable because the in-app browser policy explicitly rejects `127.0.0.1:3100`.

- [x] ELDER-PLINIUS-SOURCE-INTAKE — Review the public `elder-plinius` repository portfolio, add only the feasible patterns to the existing Nexus Company Map, document benefits and licensing/security boundaries, and verify the project without vendoring third-party runtimes or widening provider authority.
  - Progress: reviewed the 45-repository public profile and analyzed the five strongest product-safe fits; added AutoTemp evaluation, LeakHub evidence verification, P4RS3LT0NGV3 defensive transform awareness, AutoStoryGen staged drafting, and ourobopus measured-review guidance to the narrowest matching Company Map departments. Added scoped repo context, a mixed-license source-parity matrix, ecosystem mapping, and focused runtime/static assertions. Offensive/jailbreak tooling, leaked-prompt rewards, hidden-message creation, direct-provider clients, autonomous self-modification, and copied AGPL code remain excluded. Proof: `npm run company-map:check`, `npm run source:parity:check`, `npx tsc --noEmit`, `npm run lint`, the full `npm run verify` sequence (completed in two bounded runs after the shell timeout), and `git diff --check` passed.

- [x] NEXUS-COMPANY-MAP — Rework the supplied “AI company” skill chart into a Nexus-native department map under the existing Skills library, with MAX as the operator-facing lead, the existing five agents as owners, honest Codex/ChatGPT/MCP compatibility labels, HQ mission handoff, and a copyable ChatGPT brief. Spec: `specs/features/nexus-company-map.md`.
  - Progress: added a typed seven-department/eighteen-source company registry, corrected Graphify from “org chart” to optional local-first knowledge-graph tooling, mapped every department to MAX plus the existing specialist bench, exposed native/Codex/MCP/reference/translation-required posture in the Agency Role Library, wired one-click HQ prompt staging and a copy-only ChatGPT brief, and added focused static/runtime gates to `npm run verify`. Last30Days supports recent-signal research, Emil Kowalski remains a single Design source, Frontend Slides supports review-first presentation work, Matt Pocock's collection supports selective engineering workflows with PR #505 tracked separately as an in-progress deep-module reference, and David Ondrej's collection supports individually reviewed operations/engineering/research skills. No new agent, route, provider, API key, paid service, auto-install, automatic publishing, or professional legal/finance claim was added. Proof: `npm run company-map:check`, `npm run source:parity:check`, `npx tsc --noEmit`, `npm run lint`, `npm run verify`, and `git diff --check` passed. Rendered localhost QA remains unavailable because the in-app browser security policy explicitly blocks `localhost:3000`.
- [x] NEXUS-COMPANY-MAP-SOURCE-EXPANSION — Add `mvanhorn/last30days-skill` and `emilkowalski/skills` to the existing Company Map as reviewed Codex-compatible sources, without installing, vendoring, or duplicating Nexus research and design systems. Spec: `specs/features/nexus-company-map.md`.
  - Progress: added both sources to the typed registry and the narrowest matching departments; recorded the required scoped `REPO_CONTEXT.md`, `AGENTS.md`, `.cursorrules`, and `response.md` analysis artifacts; added exhaustive `in_progress` parity matrices; documented Codex/ChatGPT compatibility and credential/design-authority boundaries; and strengthened focused static/runtime checks. Last30Days requires a reviewed pinned install plus preflight before any external source access, while Emil's skills remain subordinate to the Nexus taste contract. No installer, browser-session access, credential, MCP server, provider path, or third-party runtime was added. Proof: `npm run company-map:check`, `npm run source:parity:check`, `npx tsc --noEmit`, `npm run verify`, and `git diff --check` passed.
- [x] NEXUS-COMPANY-MAP-SOURCE-EXPANSION-2 — Add `zarazhangrui/frontend-slides`, `mattpocock/skills`, merged PR `mattpocock/skills#505`, and `davidondrej/skills` as reviewed Company Map sources while preserving the single existing `emilkowalski/skills` entry. Spec: `specs/features/nexus-company-map.md`.
  - Progress: added four registry entries across the narrowest matching Operations, Engineering, Design, Research, and Marketing departments; kept Emil's URL unique; recorded three complete scoped analysis packs; added Frontend Slides and David Ondrej parity matrices; corrected the stale Matt Pocock matrix to the current engineering-workflow repository; and strengthened static/runtime checks for unique IDs, unique URLs, placements, review posture, and PR-reference status. No skill, dependency, hook, credential, provider, deployment, publishing flow, self-scheduler, or server mutation was installed or activated. Proof: `npm run company-map:check`, `npm run source:parity:check`, `npx tsc --noEmit`, `npm run verify`, and `git diff --check` passed.
- [x] NEXUS-COMPANY-MAP-POLISH — Refine the shipped Company Map into a calmer operational sequence with stronger department selection, source scanning, responsive behavior, and explicit handoff feedback while preserving every data and routing contract. Spec: `specs/features/nexus-company-map.md`.
  - Progress: replaced the uniform chip/card presentation with a seven-department operational rail, dominant mission workplane, collapsible source evidence rail, compact inventory strip, explicit session/copy feedback, and restrained reduced-motion-aware transitions. Department tabs now support arrow/Home/End navigation and visible focus; the Skills workplane contains both library fields on phones while the department rail remains intentionally scrollable. No data, source, routing, persistence, provider, dependency, or installation behavior changed. Proof: `npm run company-map:check`, `npm run source:parity:check`, `npx tsc --noEmit`, `npm run verify`, and `git diff --check` passed. Authenticated Playwright QA proved all seven desktop departments fit, keyboard selection reached Research & Knowledge, clipboard feedback succeeded, and the 390px viewport had zero document/workspace overflow; restricted Google Fonts/Mux/live-data requests remained environment-only failures outside this component.
- [x] LYRA-PROMPT-FORGE — Add a named, session-only prompt-optimization capability under Skills with 4-D methodology, AUTO/BASIC/DETAIL modes, platform tailoring, DETAIL clarification questions, structured copyable output, JANSKY exact-session routing, restored prompt-recipe injection, and verify-gated no-memory boundaries. Spec: `specs/features/lyra-prompt-forge.md`.
  - Progress: added the pure LYRA optimizer engine, transient Prompt Forge workbench, Nexus/ChatGPT/Claude/Gemini/Other targeting, deterministic mode detection and override, two-stage DETAIL clarification, structured response validation, copy-only results, governed JANSKY exact-session routing, canonical Skills focus, and restored per-agent prompt recipes. No sixth agent, durable prompt content, automatic execution, new route handler, or direct provider call was added. Proof: `npm run lyra:check`, `npm run prompt-recipes:check`, `npm run agent:taxonomy:check`, `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm run verify`, and `git diff --check` passed. Rendered browser interaction remains unverified because the in-app browser policy blocks localhost.
- [x] WINDOWS-OPTIMIZATION-ADVISOR — Adapt optimizerDuck's useful safety model into a protected, read-only Windows posture advisor with sanitized inventory, risk-rated recommendations, rollback prerequisites, and no elevated or mutating execution.
  - Progress: added the protected local-only `/api/windows-optimization-advisor` route, sanitized aggregate inventory and risk-rated review recommendations, fixed-command read-only PowerShell collection, a human/JSON CLI, incomplete-provider warnings, rollback/measurement prerequisites, exhaustive optimizerDuck source-parity accounting, and operator documentation. No registry, service, scheduled-task, app, power-plan, file, or system settings are changed, and no elevation is requested. Proof: `npm run windows:optimization:check`, `npm run source:parity:check`, `npm run type-check`, `npm run security:boundaries`, `npm run security-scan`, live advisor CLI, and `git diff --check` passed.
- [x] OLLAMA-FIRST-STANDALONE — Ship Ollama-first standalone operations: `lib/localInferencePosture.ts` blocks silent cloud escalation, `/api/ai` validates loopback Ollama endpoints and uses local-first provider chains, intel-only degraded gate on HQ/COMMAND when Ollama is down, Settings Ollama catalog picker, route policy for geocode/papers/mcp gateway/ideas intake, tools POST schema validation, `npm run local-inference:check`, `npm run agentshield:check`, and `docs/deployment/ollama-huggingface-local.md`.
  - Proof: `npm run local-inference:check`, `npm run agentshield:check`, `npm run security-scan`, `npx tsc --noEmit`, `OLLAMA_ONLY=1 npm run eval:agent-runtime:ci`. Desktop isolated acceptance: `npm run secure:start:check` + `npm run offline:local:check` + optional `npm run ollama:stack:check` when Ollama is installed.
  - 2026-06-22 extensions: (1) `localInferenceDegraded` wired from `useIntelOnlyPosture` into `resolveAssistantDispatch` via `AssistantDispatchOptions`, threaded to `buildAssistantOperatorWorkflowState` — workflow recovery phase now shows in `HQTerminalSection` readiness panel and in `HomeChat` send path when Ollama is down. (2) Settings `allowAdvanced` safeguard: operator must click "I understand — show advanced lanes" before the paid-API unlock checkbox is exposed; unchecking re-hides the gate. (3) fainir `FAINIR_READER_CONTRACT_BLOCK` injected into `buildSystemPrompt` via `lib/agentYagniGuardrails.ts`; parity matrix row `reader-contract-and-build-order` updated to `adapted`. (4) Osiris `unified-map-and-event-layers`: `LiveEventFusionStrip` added to `components/intel/` and mounted on INTEL news+world segments; parity matrix row updated to `adapted`. (5) dmore adversarial: `detectCssHiddenPromptPatterns()` added to `lib/skillSpectrumPolicy.ts`; parity matrix note updated. (6) `docs/ideas/external-links-mapping.md` entries added for ponytail, fainir, osiris, pear-desktop, and dmore CSS repo. All: `npx tsc --noEmit` clean, `npm run verify` pass.
- [ ] CI-GREEN-NODE-RUNTIME — Align GitHub Actions with the supported Node runtime, rerun the full local verification lane, push the committed fixes, and confirm every PR #47 workflow is green.
  - Progress: all four GitHub Actions workflows use Node 24, the local verification lane passed on Node `24.14.0`, and the local branch contains the committed runtime fixes. Remaining closure is remote-only: publish the ahead commits and confirm every PR #47 workflow after GitHub connectivity returns.
- [x] NO-MISTAKES-RELEASE-GATE — Adapt `kunchenguid/no-mistakes` into a Nexus-owned, read-only operator gate that requires intent, composes the existing handoff/preflight/full-verify checks, classifies Git publish readiness, and leaves push/PR/CI actions under human control. Spec: `specs/features/no-mistakes-release-gate.md`.
  - Progress: reviewed upstream release `v1.34.0`, added scoped repo context and an exhaustive `in_progress` parity matrix, implemented `npm run release:gate -- --intent "..."` with full/quick and human/JSON modes, added focused static/runtime validators, and wired `release:gate:check` into the full verify chain and pre-push hook. The gate performs no edits, service launches, remote writes, PR creation, CI mutation, rebase, or merge. Proof: `npm run release:gate:check`, `npm run source:parity:check`, `npx tsc --noEmit`, `npm run handoff:check`, `npm run verify`, and `git diff --check` passed.
- [x] CENTRAL-AI-ORCHESTRATOR — Make MAX the one operator-facing control plane for cross-domain tasks, with a single-agent fast path, at most three advisory specialist workers, typed handoffs, and MAX-only synthesis. Spec: `specs/features/central-ai-orchestrator.md`.
  - Progress: adapted `mco-org/squad` `v0.7.6` manager/worker/inspector and structured completion patterns into the existing Nexus runtime. Cross-domain dispatch now selects MAX, exposes the bounded `delegate_specialist` tool, normalizes worker output into typed evidence/code/notes/risk/verification handoffs, preserves the existing proposed-edit gate, and shows the MAX-led plan in active HQ. The separate OpenClaw `ask_max` path is no longer triggered by generic delegation. No Rust CLI, SQLite state, terminal launcher, background worker, sixth durable agent, worker memory, or automatic file mutation was added. Proof: `npm run orchestrator:check`, `npm run source:parity:check`, `npx tsc --noEmit`, `npm run lint`, `npm run verify`, and `git diff --check` passed.
- [x] PREMORTEM-SKILL-BUNDLE — Convert the saved English pre-mortem reference into a validated, install-ready Codex skill bundle with the failure-analysis, parallel deep-dive, synthesis, HTML report, and Markdown transcript workflow.
  - Progress: the full translation remains at `docs/ideas/PREMORTEM-SKILL-EN.md`; the condensed skill is at `docs/ideas/skills/premortem` with valid `SKILL.md` and `agents/openai.yaml`. `quick_validate.py` passes. Activation remains a one-copy step because this Codex workspace mounts `.agents/skills` read-only and rejected the normal installer.
- [x] OPENHUMAN-PERSONAL-AI-PROFILE — Correct the stale OpenHuman source inventory and adapt its useful persistent-context pattern into an explicit, operator-controlled Personal AI Profile used by the existing Nexus AI runtime. Spec: `specs/features/openhuman-personal-ai-profile.md`.
  - Progress: corrected the source from a four-row persona sketch and incorrect MIT label to an exhaustive current GPL-3.0 capability inventory; added scoped repo analysis; compiled the existing goals, skills, learning, and context settings into a normalized, bounded, injection-labeled profile; exposed its active categories in Settings; and reused the same profile contract in direct chat and the agent system prompt. No sixth agent, emotional modeling, inferred traits, background ingestion, autonomous goal mutation, new database, or GPL source was added. Proof: skill validation, `npm run personal-profile:check`, `npm run source:parity:check`, `npx tsc --noEmit`, `npm run lint`, `npm run verify`, `npm run build`, and `git diff --check` passed. Broader useful OpenHuman capabilities remain honestly `in_progress` in the parity matrix.
- [x] FILE-FIRST-SECOND-BRAIN-HUMAN-EDITOR — Make one human-owned Markdown index authoritative over AI-generated memory, load it through the protected AI route, expose its file posture in VAULT, and add a strict session-only Human Editor workbench under Skills. Spec: `specs/features/file-first-second-brain-human-editor.md`.
  - Progress: added the human-owned `SECOND_BRAIN.md` authority contract; bounded, allowlisted server loading through the protected AI route; metadata-only VAULT file status; and a session-only Human Editor workbench with six cleaned rewrite modes and Mega as the default. `AGENTS.md` now applies the same protocol to project-aware ChatGPT/Codex sessions, prose-rewrite dispatch routes to Skills, code/embed tasks cannot trigger the writing protocol, and AI cannot silently write second-brain files. Proof: skill validation, `npm run second-brain:check`, `npm run verify`, `npm run build`, and `git diff --check` passed.
- [x] SECOND-BRAIN-NIGHT-SHIFT — Adapt the attached Kimi/Obsidian second-brain course into a private plain-Markdown refinery with immutable intake, source-traced atoms, staged AI proposals, human-gated promotion, friction reporting, scheduled review drafts, and audit-only integrity sweeps. Spec: `specs/features/second-brain-night-shift.md`.
  - Progress: added a validated project-aware ChatGPT/Codex skill plus tracked scout/refinery/editor/audit playbooks; an ignored `data/second-brain/` Obsidian vault; immutable capture and source fingerprinting; bounded source preparation; strict JSON proposals; stale-source and collision blocking; source-linked atom, immutable thread-revision, friction, orphan, briefing, rejection-archive, and report-only audit output; a protected night-shift API; a VAULT capture/review/approve/reject/audit workbench; and one-click 03:00 refinery plus Sunday 22:00 audit schedules. Automatic runs stop at desk proposals or audit reports, use the existing protected AI route, and never silently promote permanent knowledge. Proof: skill validation, `npm run second-brain:check`, `npm run second-brain-night-shift:check`, `npm run security:boundaries`, `npm run publication:safety:check`, `npx tsc --noEmit -p tsconfig.typecheck.json`, `npm run lint`, `npm run verify`, `npm run build`, and `git diff --check` passed.
- [x] SOURCE-PARITY-CONTRACT — Replace partial-pattern assimilation with exhaustive primary-source parity accounting for GitHub/X ideas.
  - Current pass: define a machine-checkable source capability matrix, require source version/license/evidence, distinguish implemented/adapted/excluded/pending capabilities, prevent completion while useful capabilities remain pending, and reopen Feynman against the actual `companion-inc/feynman` source.
  - Guardrail: adapt capabilities into Nexus architecture instead of blindly vendoring upstream code; exclude only for documented security, legal, licensing, free/local, or product-purpose conflicts; do not claim parity from command labels, prompts, plans, or placeholder UI.
  - Progress: shipped `docs/ideas/source-assimilation-contract.md`, the machine-readable `docs/ideas/source-parity/feynman.json` inventory, `scripts/validate-source-assimilation-parity.mjs`, and `npm run source:parity:check` under the full verify gate. Feynman is now honestly `in_progress` with 15 implemented/adapted rows, 2 explicit exclusions, and 7 useful pending rows after the research-continuity, progressive-research, and Hugging Face inspection tranches. Also corrected secure-runtime fixture string construction so the existing publication-safety gate remains strict without flagging generated test-token assignments. Proof: `npm run source:parity:check`, `npm run feynman:check`, `npm run publication:safety:check`, `npm run secure:start:check`, `npm run verify`, and `git diff --check` passed.
- [ ] FREE-LOCAL-PHONE-ACCEPTANCE — Desktop LAN runtime proof, the phone checklist, the protected receipt lane, assistant receipt bridge, live receipt status, capture-status artifact, direct-HQ QR handoff, `npm run ops:preflight`, `npm run repo:sync:status`, `npm run phone:acceptance:guide`, and `npm run phone:acceptance:report` burden-reduction commands are in place. The existing Free Local Readiness panel silently posts sanitized local receipts to protected `/api/phone-acceptance/receipt`, shared assistant turn receipts now mark `ping`/local Ollama proof without storing chat content, the panel shows live booleans/counts/timestamps for phone proof, ignored local storage stays under `data/phone-acceptance-receipts.json`, `npm run phone:acceptance:capture` writes `receiptPhoneProof`, `combinedPhoneProof`, `receiptLiveStatus`, and label-only `missingReceiptProofItems` before falling back to manual flags, `npm run phone:acceptance:guide` prints LAN HQ URL candidates plus a plain phone/iPad checklist, and `npm run phone:acceptance:report` prints the latest sanitized capture status without opening JSON. Remaining physical acceptance from a real phone/iPad: run `npm run repo:sync:status`, run `npm run ops:preflight`, run `npm run phone:acceptance:guide`, scan the direct-HQ QR or open one LAN URL, log in with `NEXUS_TOKEN`, create the mobile receipt from HQ/COMMAND, send `ping`, ask one local Ollama prompt, install the PWA, confirm the live receipt status completes, rerun capture, then run `npm run phone:acceptance:report` until `acceptanceReady: true`.
  - Latest proof: `docs/metrics/phone-local-acceptance-2026-06-13T04-25-10-345Z.json` captured a real isolated LAN runtime with `/api/health`, `/command`, `/api/free-local-readiness`, and `/api/phone-acceptance/receipt` healthy; `/hq` exceeded the cold-compilation capture timeout. Physical phone/iPad login, browser storage, ping, local-AI receipt, PWA-capable, and PWA-installed proof remain manual.
- [ ] LOCAL-AI-OFFLINE-OPERATIONS — Authenticated desktop proof remains captured from the local session: Free Local Readiness returned `ready`, `isolated`, paid APIs `blocked`, Ollama `reachable`, resolved model `gemma4:latest`, agent health `100%`, authenticated session, review-gated tools, and phone LAN `enabled`; `/api/ai` answered through `provider=ollama` with `model=gemma4:latest`, and a non-destructive bad-local-endpoint probe returned structured `ollama_unavailable` recovery copy. `npm run offline:local:report` now summarizes the latest sanitized readiness rollup plus phone acceptance artifacts without network calls or JSON inspection. The latest rollup `docs/metrics/readiness-rollup-2026-05-09T22-29-00-772Z.json` keeps local-AI offline readiness open until the physical phone/browser receipt is confirmed. Remaining manual acceptance: confirm the same proof in the browser panel/phone receipt and optionally perform a real Ollama stop/restore from a separate shell.
  - Latest proof: the June 13 LAN capture reconfirmed isolated mode, paid APIs blocked, Ollama reachable, `gemma4:latest` resolved, high-risk tools off, and phone LAN enabled. The capture correctly leaves browser-session authentication and phone-side local-AI receipt unproven until the physical device completes them.
- [ ] DEPENDABOT-SECURITY-AUDIT — Triage the GitHub Dependabot alert set without broad package churn. Latest sanitized paginated imported-alert artifact `docs/metrics/dependabot-security-audit-2026-05-25T05-52-26-760Z.json` classified 108 exported alerts from the stale GitHub export into 14 active runtime alerts, 1 dev-only alert, 2 active transitive alerts, 90 retired-manifest alerts from `archive/`, and 3 blocked Cargo/non-npm alerts; it also records `retiredManifestQuarantine.quarantined: true`. Local package posture is available at `docs/metrics/dependency-risk-posture-2026-05-25T05-52-21-853Z.json`, with root `next` now patched to `15.5.18`; next safe patch target is the smallest remaining active runtime batch, starting with `postcss`, then `prismjs` if still open after GitHub rescans.
  - Current pass: keep patched floors enforced across every active npm lockfile path, consume the verified Tauri security update, keep incompatible glib migration work explicit, and wait for merged-main GitHub rescanning before closing the audit queue.
  - Progress: merged verified Dependabot PR 53 into the feature branch, updating Tauri from `2.10.3` to `2.11.2` and removing vulnerable `rand 0.7.3` / `0.8.5` chains. Patched and override-normalized every active PostCSS path to `8.5.10`, every PrismJS path to `1.30.0`, and the vulnerable brace-expansion `5.x` chain to `5.0.6` without forcing incompatible old-major consumers. Added `npm run dependency:security:check` under the infra/full verify gates, regenerated the desktop SBOM, and proved `npm run dependency:security:check`, `npm ls postcss prismjs brace-expansion --all`, `npm run validate:infra-hardening`, `npm run verify`, and `npm run build`. `glib 0.18.5` remains a documented GTK3 compatibility migration instead of an invalid forced lockfile edit; GitHub alert closure still requires merging this branch into `main` and allowing Dependabot to rescan.

Deferred product queues while website/local AI/phone access are the priority:

- [ ] MW6-ARPG-FULL-GAME-PRODUCTION — Expand `Aether Reliquary` from the MW5 Phaser vertical slice into a complete browser RPG inside `/hq`, not a single chapter: target one playable protagonist with race/class/subclass customization, 12 major cities with 4 sub-cities each, a massive multi-act storyline, low-tech hand-weapon combat, deep gear/armory progression, companion/NPC relationships, enemy families with traits/buffs/debuffs, final boss arcs, and repeatable endgame while preserving the HQ shell, command input, route stability, local-first saves, legal asset policy, reduced-motion support, and Resources massive-win tracking.
- [ ] MW6U-ARPG-ASSET-PIPELINE — Build the production asset lane: source CC0-first sprite/tile/icon/audio packs, allow CC-BY only with complete attribution and visible credits, allow cheap operator-approved commercial packs only with redacted proof records, normalize frame sizes and bottom-center anchors, create sprite atlas conventions, optimize tilesets, extend `lib/arpgAssetManifest.ts` for imported assets, maintain `docs/assets/arpg-asset-ledger.md`, and keep `npm run arpg:assets:check` plus `npm run arpg:production:check` as gates.
- [ ] MW6V-ARPG-ART-AUDIO-VFX — Replace placeholder feel with production presentation: city tilesets, sub-city district props, character/enemy sprites, attack animations, hit sparks, loot beams, UI icons, ambient particles, screen shake tuning, city music, dungeon music, combat stingers, UI sounds, accessibility volume toggles, and calm reduced-motion/low-flash alternatives.
  - Current pass: add a typed Bellroot/Veyrhold presentation cue layer using only approved existing runtime art and metadata for ambient copy, VFX/audio intent, and reduced-motion alternatives; surface it in `/hq` Adventure, Map, and Production without promoting rejected prologue art or requiring external asset packs.
  - Progress: shipped the first-town presentation cue layer with six validated Bellroot/Veyrhold cues, approved runtime asset references, VFX/audio intent, reduced-motion alternatives, `/hq` Adventure/Map/Production surfacing, and rejected-prologue-art validation. `npm run arpg:presentation:check`, `npm run arpg:content:check`, `npm run type-check`, `npm run verify`, `npm run build`, and live `3100` route checks passed; `npm run hq:e2e` remains locally blocked by Windows Playwright worker `spawn EPERM`.
- [ ] MW6AA-ARPG-TESTING-RELEASE-GATES — Add release-grade coverage for the full game: `npm run arpg:assets:check`, static content validation, save migration tests, deterministic combat/item/enemy formula tests, city/sub-city registry checks, quest dependency checks, `npm run hq:e2e` flows for movement/combat/loot/equip/upgrade/skills/story/map/quest/codex/companions/cities, route checks for `/hq` and `/hq?focus=hq-chronicle`, build/verify, and browser acceptance that proves the first viewport reads as a game while HQ command input still works.

Blocked or manual watchlist, not the active queue:

- [ ] MW6V-REAL-ASSET-INTAKE — Keep real 3D/pack intake available as an optional reference and preview lane behind the illustrated 2D primary path; blocked until official CC0/operator-approved pack files are placed in `assets/arpg/intake/raw/` and can be manifest-tracked.
- [ ] UXA3 — After acceptance on merged `main`, retire the older replay worktree queue only once we no longer need side-by-side rollback or visual-reference inspection.
- [ ] Retire the older M2/M3/M4 review queue only after UXA3 is accepted on merged `main` and there is no remaining need for replay-era rollback/reference worktrees.
- [ ] FD2 remains blocked on the real staged Coolify hostname in repo-root `.env.local`, so release-proof work still stays out of this replay stack.

## In Progress

- [x] FEYNMAN-HUGGING-FACE-INSPECTION — Add bounded public model/dataset repository inspection and use it as direct Feynman evidence only when relevant.
  - Current pass: public metadata, access posture, bounded top-level files, dataset split/schema information, safe small text-file reads, protected `huggingface_inspect` tool wiring, and relevant-reference Feynman integration.
  - Guardrail: no token/private access, inference, training, repository clone, binary/model-weight download, code execution, external write, new dependency, visual change, public route, or ARPG change.
  - Design: `specs/features/feynman-hugging-face-inspection.md`; plan: `docs/superpowers/plans/2026-06-14-feynman-hugging-face-inspection.md`.
  - Progress: complete. The protected `huggingface_inspect` network tool now inspects public model/dataset metadata, reports access posture without attempting gated/private access, returns bounded top-level files and dataset split/schema structure, and permits only explicit allowlisted small text-file reads under a streaming 64 KiB cap. Relevant Hugging Face repository URLs now add one direct evidence receipt to Feynman's first parallel collection wave inside the existing eight-read budget. The `hugging-face-inspection` parity row is adapted, leaving 7 useful Feynman rows pending. Proof: red focused gate, `npm run feynman:huggingface:check`, `npm run feynman:check`, `npm run source:parity:check`, `npm run type-check`, `npm run lint`, `npm run security:boundaries`, `npm run verify`, and `git diff --check` passed.
- [x] FEYNMAN-PROGRESSIVE-RESEARCH — Add bounded parallel wide-to-narrow collection, recency/domain refinement, and explicit evidence-quality coverage to every Feynman workflow.
  - Current pass: four initial read-only search lanes, at most one three-query refinement wave, settled parallel direct reads, minimum evidence thresholds, and honest report/notebook coverage receipts.
  - Guardrail: no recursive search loop, unbounded concurrency, new provider, paid dependency, public route, visual redesign, execution, installation, or false full-parity claim.
  - Design: `specs/features/feynman-progressive-research.md`; plan: `docs/superpowers/plans/2026-06-14-feynman-progressive-research.md`.
  - Progress: complete. Every Feynman workflow now runs the paper lane and four varied web lanes concurrently, prioritizes authoritative/paper/repository URLs inside an eight-read budget, assesses five-discovered/three-direct/two-high-confidence coverage, and runs at most one distinct three-query refinement wave using discovered terminology plus recency/domain qualifiers. Settled failures preserve successful lanes; final reports, synthesis prompts, provenance sidecars, and continuity notebooks carry coverage receipts; unmet thresholds degrade the Researcher stage honestly. The `parallel-progressive-research` parity row is adapted, leaving 8 useful Feynman rows pending. Proof: red focused gate, priority red/green gate, `npm run feynman:progressive:check`, `npm run feynman:check`, `npm run source:parity:check`, `npm run type-check`, `npm run lint`, `npm run security:boundaries`, `npm run verify`, and `git diff --check` passed.

- [x] FEYNMAN-RESEARCH-CONTINUITY — Complete Feynman session search/resume, chronological lab notebooks, collision-safe intermediate artifacts, separate provenance sidecars, and protected local preview/PDF export.
  - Current pass: extend the existing `feynman_research` and `feynman_outputs` path with one bounded local continuity store under `agent-workspace/feynman/sessions/`; preserve VAULT compiled pages as the final human-facing artifact.
  - Guardrail: no arbitrary-path reads/writes, cloud or paid service, dependency addition, direct provider call, silent execution, visual redesign, new top-level route, or false full-parity claim.
  - Design: `specs/features/feynman-research-continuity.md`; plan: `docs/superpowers/plans/2026-06-13-feynman-research-continuity.md`.
  - Progress: complete. Every Feynman research run now starts a collision-safe bounded local session, appends chronological workflow/stage notebook events, and finalizes plan, report, evidence, claim, review, separate provenance, HTML preview, PDF, and manifest artifacts under `agent-workspace/feynman/sessions/`. `feynman_outputs` now lists, searches, resumes, and exports real sessions; protected local-only `/api/feynman/artifacts` serves only fixed validated artifacts. The five continuity/export parity rows are implemented/adapted, while the parent Feynman source-parity task remains open. Proof: red focused gate, `npm run feynman:continuity:check`, `npm run feynman:check`, `npm run source:parity:check`, `npm run type-check`, `npm run lint`, `npm run security:boundaries`, `npm run verify`, and `git diff --check` passed.

- [x] CODEWHALE-OBLITERATUS-NATIVE-ASSIMILATION — Add CodeWhale-aligned authority, lifecycle, harness posture, and evidence-first completion receipts to the active Nexus agent runtime; adapt OBLITERATUS only into passive local Model Lab safety evaluation.
  - Current pass: focused pure contracts, active runtime integration, validated Model Lab persistence, exhaustive source parity matrices, and focused verification gates.
  - Guardrail: no second agent runtime, upstream vendoring, auto-installs, hosted relay, model-weight modification, steering-vector application, safeguard removal, remote telemetry, model upload, remote code execution, paid service, new top-level tab, or RPG work.
  - Design: `specs/features/codewhale-obliteratus-native-assimilation.md`; plan: `docs/superpowers/plans/2026-06-13-codewhale-obliteratus-native-assimilation.md`.
  - Progress: complete under the Nexus-aligned source contract. The active agent prompt now consumes a machine-checkable authority hierarchy and protected invariants; every recorded agent-run artifact receives an evidence-first continuity receipt with harness posture, changes, evidence, risks, blockers, and verification truth; stale running lifecycle state reconciles to interrupted. Model Lab now validates requests and persists derived local passive-safety manifests and metrics while explicitly disabling model mutation, steering vectors, safeguard removal, telemetry, uploads, and remote code. Exhaustive CodeWhale and OBLITERATUS parity matrices are complete. The full verification pass also repaired the earlier MasterDnsVPN runtime fixture to use a reserved documentation IP so publication safety remains clean. Proof: red focused gate, `npm run source:codewhale-obliteratus:check`, `npm run source:parity:check`, `npm run network:source-integrations:runtime:check`, `npm run publication:safety:check`, `npm run type-check`, `npm run lint`, `npm run verify`, and `git diff --check` passed.

- [x] ESPECTRE-MASTERDNSVPN-NATIVE-ASSIMILATION — Extend the existing IoT/WiFi-sensing lane with authenticated, consent-aware ESPectre telemetry and bounded control envelopes; add loopback-only MasterDnsVPN readiness inside the existing legal IP-privacy guard without launching a tunnel or claiming anonymity.
  - Current pass: pure contracts, protected local APIs, existing-route panels, exhaustive source parity matrices, operator setup docs, and a focused verification gate.
  - Guardrail: no upstream vendoring, raw CSI exposure, public probes, DNS tunnel creation, traffic disguise, weak XOR approval, tunnel launch, public-link unlock from MasterDnsVPN, paid service, new top-level tab, or RPG work.
  - Design: `specs/features/espectre-masterdnsvpn-native-assimilation.md`; plan: `docs/superpowers/plans/2026-06-13-espectre-masterdnsvpn-native-assimilation.md`.
  - Progress: complete under the Nexus-aligned assimilation contract. `/iot` now has a consent-aware ESPectre WiFi-sensing viewer backed by protected normalized telemetry ingest, multi-sensor readiness, a simulated fixture, and review-required command queues that external bridges can poll and acknowledge. The existing Escape legal privacy guard now includes sanitized MasterDnsVPN readiness that validates authorization, delegated-domain/resolver posture, AES/ChaCha20, and a loopback-only client listener without starting or contacting a tunnel. Both exhaustive source parity matrices are complete. Proof: red static gate, red runtime behavior gate, `npm run network:source-integrations:check`, `npm run source:parity:check`, `npm run type-check`, `npm run lint`, `npm run verify`, and `git diff --check` passed. The managed `3100` runtime reported healthy, but the in-app browser security policy refused the local target, so browser rendering proof remains unavailable in this environment.

- [x] CYBER-INTEL-BUNDLE-ISOLATION — Continue the non-RPG performance program by deferring hidden Cyber and Intel render trees and locking the remaining operational routes against bundle regressions.
  - Current pass: keep Cyber Triage and Intel News immediate; extract Cyber Matrix/Review/Evidence/Drone and Intel World/Markets/Sweeps into route-local dynamic boundaries; preserve route state, URLs, loaders, and actions; budget Cyber, Intel, Alpha, and Recon.
  - Guardrail: no removed capabilities, visual changes, loading UI, state-contract changes, provider changes, dependency changes, security weakening, public-route changes, HQ work, or RPG work.
  - Design: `specs/features/cyber-intel-bundle-isolation.md`; plan: `docs/superpowers/plans/2026-06-13-cyber-intel-bundle-isolation.md`.
  - Progress: complete. Cyber's hidden Matrix, Review, Evidence, and Drone chambers and Intel's hidden World, Markets, and Sweeps segments now load through route-local dynamic boundaries while default surfaces, route state, URLs, loaders, actions, and existing UI remain unchanged. Cyber's route-owned production chunk fell from `27.5 KB` to `25.5 KB` (`-2.0 KB`, about `7%`); Intel fell from `17.3 KB` to `11.5 KB` (`-5.8 KB`, about `34%`) and first-load JS fell from `321 KB` to `319 KB`. Conservative route-owned budgets now protect Cyber `40 KB`, Intel `25 KB`, Alpha `25 KB`, and Recon `20 KB`. Production artifact inspection confirmed representative deferred bodies are outside the initial route manifests. Focused deep-link coverage was added, but the in-app browser security policy refused the local `127.0.0.1:3100` target, so no alternate browser route was used. Proof: red source gate, `npm run performance:check`, `npm run verify:fast`, fresh `npm run build`, `npm run build:verified`, `npm run handoff:check`, and `git diff --check` passed.

- [x] VAULT-COMMAND-BUNDLE-ISOLATION — Continue the non-RPG performance program by deferring Vault's hidden Relations and Publish chambers and locking the already-isolated Command route against bundle regressions.
  - Current pass: keep Vault Archive immediate; extract Relations and Publish render trees into dynamic route-local components; preserve graph/state/URL behavior; add Command dynamic-boundary regression checks; verify deferred surfaces; lock production route budgets.
  - Guardrail: no removed capabilities, visual changes, loading UI, graph/state contract changes, provider changes, dependency changes, public-route changes, HQ work, or RPG work.
  - Design: `specs/features/vault-command-bundle-isolation.md`; plan: `docs/superpowers/plans/2026-06-13-vault-command-bundle-isolation.md`.
  - Progress: complete. Vault's hidden Relations and Publish render trees now load through route-local dynamic boundaries while graph construction, filtering, selection, URL synchronization, Archive behavior, and store synchronization remain in the route. Vault's route-owned production chunk fell from `40.9 KB` to `31.1 KB` (`-9.8 KB`, about `24%`) and first-load JS is now `324 KB`; Command remains protected at `29.5 KB` route-owned and `332 KB` first-load. Both routes have `45 KB` production budgets, and Command's existing deferred modules now have source regression guards. Production artifact inspection confirmed the Relations and Publish chamber bodies are outside Vault's initial route manifest. Proof: red source gate, `npm run performance:check`, `npm run type-check`, `npm run verify:fast`, fresh `npm run build`, `npm run build:verified`, and `git diff --check` passed. The focused browser test is committed, but the in-app browser security policy refused the local `127.0.0.1:3100` target, so no alternate browser route was used.

- [x] VEHICLE-IOT-BUNDLE-ISOLATION — Reduce Vehicle and IoT startup work without removing operational panels or changing the approved UI.
  - Current pass: keep each route's first posture panel immediate while dynamically isolating chart-heavy and lower-page sensor, device, automation, flight-ops, and archive panels.
  - Plan: add a failing source-boundary gate; convert selected panel imports to dynamic modules; prove lazy panels remain reachable; compare fresh production route sizes; lock achieved route budgets; run full verification and production build.
  - Guardrail: no removed capabilities, visual redesign, new loading UI, telemetry/state changes, provider changes, dependency changes, public-route changes, or HQ/RPG work.
  - Design: `specs/features/vehicle-iot-bundle-isolation.md`.
  - Progress: complete. Vehicle first-load JS fell from about `458 KB` to `305 KB` (`-153 KB`, about `33%`), and IoT fell from `419 KB` to `293 KB` (`-126 KB`, about `30%`). Recharts and selected lower-page modules no longer enter either initial route bundle; the first posture panels remain immediate. Production route-owned app chunks are locked at `55 KB` and `25 KB` against achieved sizes of `36.6 KB` and `9.8 KB`. Proof: source gate failed red then passed green; focused production browser acceptance rendered Vehicle telemetry/sensor/hardware panels and IoT weather/registry/automation panels with zero page errors; `npm run verify:fast`, `npm run build`, `npm run performance:check`, `npm run build:verified`, and `git diff --check` passed. The Playwright dev-server wrapper still timed out before reporting its focused result, so final browser proof used a bounded production probe.

- [x] ROUTE-BUNDLE-ISOLATION — Reduce feature-specific first-load JavaScript without removing routes, chambers, controls, or approved visuals.
  - Current pass: isolate hidden Skills, Security, and Resources chamber panels behind dynamic boundaries while preserving the currently active chamber, deep links, persisted view state, and exact-session behavior.
  - Plan: add a failing source-boundary gate; convert hidden chamber imports to dynamic modules; compare fresh production route sizes; tighten route budgets to the achieved baseline; run focused E2E, full verification, and a production build.
  - Guardrail: no removed features, copy/layout changes, new loading UI, dependency changes, state-contract changes, public-route changes, provider changes, or RPG work.
  - Design: `specs/features/route-bundle-isolation.md`.
  - Progress: complete. Skills first-load JS fell from about `504 KB` to `362 KB` (`-142 KB`, about `28%`); Security fell from `432 KB` to `302 KB` (`-130 KB`, about `30%`); Resources fell from `434 KB` to `368 KB` (`-66 KB`, about `15%`). Hidden chambers now load through route-local dynamic boundaries, deep links and persisted views remain intact, and production route-owned chunk budgets are locked at `50 KB`, `50 KB`, and `45 KB` against achieved sizes of `32.0 KB`, `31.0 KB`, and `24.2 KB`. Development startup now preserves real dev cache but clears incompatible production `.next` output before `next dev`, preventing first-interaction reloads from losing overlay state. Proof: source gate red then green; focused overlay and deep-link E2E both passed; `npm run verify:fast`, `npm run build`, `npm run performance:check`, `npm run build:verified`, and `git diff --check` passed. Playwright reported both focused tests passing but its dev-server wrapper still hung during shutdown.

- [x] SHELL-PERFORMANCE-CONTROL-PLANE — Speed up authenticated routes and local development without removing capabilities or changing the approved UI.
  - Current pass: defer non-route-owned shell services until browser idle/first interaction, keep route-owned data immediate, deduplicate repeated visibility-aware polling, preserve background intelligence/automation after warm-up, retain full verification, and stop clearing the Next development cache unless explicitly requested.
  - Plan: add focused runtime acceptance first; implement the route-aware activation plan and shared poll coordinator; move heavy root services behind dynamic idle boundaries; add fast/fresh development and verified-build commands; add enforceable bundle/static performance budgets; then run focused checks, type-check, full verify, and production build.
  - Guardrail: no visual changes, removed capabilities, disabled alerts, weakened auth/security/privacy, skipped full verification, provider changes, dependency additions, public-route changes, or RPG work.
  - Design: `specs/features/shell-performance-control-plane.md`.
  - Progress: complete for the approved first performance pass. Route-owned data stays immediate; non-route-owned intelligence, memory durability, and cron automation activate on browser idle/first interaction within a bounded timeout; repeated loaders share one visibility-aware polling coordinator; closed settings/notification overlays load on demand; normal development preserves `.next` while `dev:fresh` remains explicit; `verify:fast`, `build:verified`, focused runtime/E2E proof, and production-aware bundle/static budgets are wired. Proof: red/green runtime and static gates, focused overlay E2E passed, `npm run verify:fast`, full `npm run verify`, `npm run build`, `npm run build:verified`, production budget check, and `git diff --check` passed. HQ remains the largest feature-specific first-load bundle; Skills, Security, Resources, Vehicle, and IoT now have route-isolation budgets.

- [x] LOCAL-ACCELERATION-PLANE — Fully assimilate `RyanCodrai/turbovec` and `0xSero/turboquant` where they fit Nexus.
  - Current pass: align completion with Nexus rather than reproducing either upstream project. Make complete useful Nexus adaptation, fallback operation, security boundaries, and source accounting the required completion gate; keep real TurboVec/TurboQuant installation proof as transparent optional acceleration readiness behind a separate strict upstream-runtime gate.
  - Plan: update the acceptance model and focused checks first; separate Nexus integration completion from optional upstream readiness; update package commands, specs, deployment guidance, system state, and the sanitized acceptance artifact; then run the aligned completion gate, strict optional-runtime gate, full verification, and production build.
  - Guardrail: no partial command-label assimilation, public routes, automatic install/download/start, paid API, secret/raw embedding/raw document output, unsafe remote endpoint, GPL vendoring, false claim that an unavailable external runtime is live, or requirement that Nexus reproduce optional upstream runtimes to be complete.
  - Design: `docs/superpowers/specs/2026-06-07-local-acceleration-plane-design.md`; plan: `docs/superpowers/plans/2026-06-07-local-acceleration-plane.md`.
  - Progress: complete under the Nexus-aligned assimilation contract. Nexus-owned implementation, offline operational lifecycle, aligned integration acceptance, and source capability parity are each evidence-backed at `100%`. The protected client/API, exact TurboVec `IdMapIndex` adapter, VAULT indexing/retrieval, explicit TurboQuant provider routing, double-gated controls, dependency-free standard-library bridge, automatic Python discovery, private deterministic hash embeddings, and exact-search fallback are proven. TurboQuant exposes only the actual `proof.py` and `benchmark.py` files from reviewed commit `7ac9b8d165a3f7d5e6df33b0450bc1f88ec0d4d5`; its audit remains exact-commit, clean-worktree, and GPL-boundary aware. `npm run local:acceleration:acceptance:require-complete` passes on complete useful Nexus adaptation, while `npm run local:acceleration:acceptance:require-upstream-runtime` separately fails closed until optional real TurboVec and TurboQuant runtimes are installed and proven. Latest artifact: status `complete`, integration acceptance `100%`, optional upstream readiness `0%`/`unavailable`, no required blockers, and three transparent optional environment gaps. Proof: focused red/green acceptance checks, the expected strict optional-runtime failure, aligned completion gate, `npm run verify`, and `npm run build`.

- [ ] FEYNMAN-SOURCE-PARITY — Complete the useful Feynman capability model against primary source `companion-inc/feynman`, not only its workflow names.
  - Current pass: retain the shipped Researcher/Writer/Verifier/Reviewer core while adding an exhaustive source-parity matrix and reopening missing useful capabilities such as real replication execution choices/results, bounded autoresearch loops, enabled recurring watches, session search/log continuity, separate provenance artifacts, preview/export, stronger paper/code inspection, and Hugging Face dataset/repository inspection.
  - Guardrail: no silent execution, installation, training, paid compute, external writes, auth bypass, direct provider calls, or unsafe vendoring. Modal/RunPod paid compute may be explicitly excluded from Nexus parity because the product is free/local; safe local execution still requires operator approval and isolation.
  - Progress: the native foundation, research-continuity, progressive-research, and Hugging Face inspection tranches are shipped. Feynman now runs bounded parallel wide-to-narrow source collection with recency/domain refinement, minimum evidence-quality receipts, and relevant public Hugging Face model/dataset inspection, while preserving collision-safe local sessions, chronological lab notebooks, externalized artifacts, separate provenance sidecars, ranked search/resume context, protected local preview/PDF exports, and the Researcher/Writer/Verifier/Reviewer engine. Source audit against Feynman `v0.2.58` still leaves 7 useful pending rows: complete paper inspection, workflow-specific contracts, paper-code audit, approved local replication execution, Docker-isolated experiments, measured autoresearch, and enabled recurring watches.

- [x] SECURE-RUNTIME-GATE — Make secure, efficient production startup the default operational path.
  - Current pass: add local and explicitly confirmed tailnet profiles, strong-token/build requirements, a fast fail-closed safety gate, check-only/full-verify modes, and guarded delegation to the existing production runtime.
  - Guardrail: no weak/missing token, no secret output, no development fallback, no inherited paid/high-risk enablement, no broad bind without confirmation, no false Tailscale-only claim, no dependency/network/UI/public-route/provider/ARPG change.
  - Progress: shipped `npm run secure:init`, `npm run secure:start`, and `npm run secure:start:check`; local and confirmed-tailnet profiles; strong-token initialization/preservation; production-build enforcement; fast publication/security/boundary gates; optional full verification; and shutdown forwarding. Proof: weak-token and unconfirmed-tailnet checks blocked as expected; token initialization/preservation, local/tailnet check-only runs, live health/shutdown proof, `npm run secure:start:check`, `npm run verify`, and final production build passed.

- [x] LOCAL-RECOVERY-BUNDLE — Protect Nexus-owned private local state with an offline checksummed snapshot and guarded restore command.
  - Current pass: add create/list/verify/restore commands for an explicit allowlist of Escape state, private cover assets, and phone acceptance receipts under ignored `.nexus/backups/`.
  - Guardrail: no network/cloud/subscription dependency, no env/token/key/git reads, no arbitrary workspace backup, no file-content output, no automatic restore/overwrite/delete/retention behavior, no UI/public-route/provider/dependency/ARPG changes.
  - Progress: shipped `npm run local:recovery -- create|list|verify|restore`, SHA-256 manifests, tamper/extra/missing/symlink/path rejection, dry-run restore, exact apply confirmation, overwrite protection, operator documentation, and `npm run verify` coverage. The real list command reports no snapshots yet because this workspace currently has no allowlisted local-state files; isolated fixtures prove create, list, ID/path verification, tamper rejection, dry-run, confirmation blocking, conflict blocking, and reviewed overwrite restore. Proof: red validator failed before the runtime existed; `npm run local:recovery:check`, real list/JSON list, security scan, full verify, production build, and diff check passed.

- [x] DESKTOP-SBOM-GENERATOR — Generate a deterministic offline CycloneDX SBOM for the Next/npm and Tauri/Cargo desktop release lane.
  - Current pass: add `npm run desktop:sbom`, a freshness validator, canonical `docs/metrics/desktop-sbom.cdx.json`, desktop trust-chain integration, and `npm run verify` coverage.
  - Guardrail: no network calls, dependency installs/upgrades, Cargo downloads, registry lookups, certificate/key/token/env reads, Tauri packaging, signing, notarization, release-ready claim, UI/product changes, public routes, or ARPG work.
  - Progress: shipped a deterministic offline CycloneDX 1.5 artifact with 1,316 components (841 npm and 475 Cargo), a combined lockfile digest/freshness check, desktop trust-chain integration, and `npm run verify` coverage. Proof: the validator failed before the generator existed, then failed before the canonical artifact existed; `npm run desktop:sbom`, `npm run desktop:sbom:check`, `npm run desktop:trust-chain:check`, publication safety, security scan, type-check, full verify, production build, and diff check now pass.

- [x] ESCAPE-LEGAL-PRIVACY-ROUTE — Add legal VPN/proxy/Tailscale exit-node route readiness on top of the existing Escape IP guard.
  - Current pass: add a route selector for none/VPN/Tailscale exit node/legal proxy, explicit session confirmation, public-link lock/unlock posture, no-false-anonymity copy, and validator coverage under `npm run verify`.
  - Guardrail: no built-in VPN, open proxy, relay, tunnel, IP lookup service, public route, dependency install, public-IP storage/display, anonymity guarantee, DRM/paywall/geo-restriction bypass, or ARPG work.
  - Progress: shipped `lib/legalPrivacyRoute.ts`, the session-only route selector and confirmation panel in `components/resources/SecureLinkOpenPanel.tsx`, `scripts/validate-escape-legal-privacy-route.mjs`, and `npm run escape:privacy-route:check` wired into `npm run verify`. Public HTTPS tiles remain locked until VPN, Tailscale exit node, or Legal proxy is selected and confirmed for the session; private/local/Tailscale links remain open through the existing no-referrer launch path. Proof: red validator failed on missing helper first, then `npm run escape:privacy-route:check`, `npm run type-check`, `npm run lint`, `npm run publication:safety:check`, `npm run privacy:shield:check`, `npm run verify`, `npm run build`, and compiled artifact checks passed.

- [x] PRIVACY-SHIELD-FULL-RUNTIME — Fully implement Privacy Shield 2.0 beyond the CLI preview by covering every cloud-bound AI request fragment and adding an in-app protected preview panel.
  - Current pass: sanitize `messages`, `system`, `tools`, and `tool_choice`; add structured policy/field metadata; add protected `POST /api/privacy-shield/preview`; mount a COMMAND provider-health preview panel; and keep the full gate under `npm run privacy:shield:check` / `npm run verify`.
  - Guardrail: no AI/provider calls from preview, no CLI network calls, no env/token/cookie/auth-header/raw-receipt reads, no local file reads except CLI stdin, no file writes/artifacts, no dependency installs, no public routes, no provider-routing changes, no proxy/VPN/IP-hiding claims, no background workers, and no ARPG work.
  - Progress: shipped runtime protection for `messages`, `system`, `tools`, and `tool_choice`, policy/field headers, protected preview API, COMMAND preview panel, client-store posture parsing, route-policy coverage, and validator coverage. Proof: red validator failed on the missing preview API route first, then `node scripts/validate-privacy-shield-preview-cli.mjs`, `npm run privacy:shield:preview -- --check`, `npm run privacy:shield:check`, `npm run type-check`, `npm run publication:safety:check`, `npm run security-scan`, `npm run verify`, `npm run build`, and compiled artifact checks passed.

- [x] PRIVACY-SHIELD-PREVIEW-CLI — Tackle the next secure/local idea by making Privacy Shield 2.0 inspectable before any cloud-bound model dispatch.
  - Current pass: add a no-network `npm run privacy:shield:preview` command, a red/green validator, a scoped feature spec, and `npm run verify` coverage so Mario can see class counts, blocked reason, and sanitized preview text without sending anything to a provider.
  - Guardrail: no AI/provider calls, network calls, file reads beyond stdin, file writes, env/secret/raw receipt reads, proxy/VPN/IP-hiding claims, provider-routing changes, public routes, background workers, dependency installs, or ARPG work.
  - Progress: shipped `specs/features/privacy-shield-preview-cli.md`, `scripts/privacy-shield-preview.mjs`, `scripts/validate-privacy-shield-preview-cli.mjs`, `npm run privacy:shield:preview`, and `npm run privacy:shield:check` wired into `npm run verify`. The preview reports credential, internal-host, protected-path, and sensitive-evidence counts, blocks cloud dispatch when operator-only evidence markers are present, and prints only sanitized preview text. Proof: red validator first failed on the missing runner, then `node scripts/validate-privacy-shield-preview-cli.mjs`, `npm run privacy:shield:preview -- --sample`, `npm run privacy:shield:preview -- --sample --json`, `npm run privacy:shield:preview -- --check`, `npm run privacy:shield:check`, `npm run security-scan`, `npm run verify`, `git diff --check`, and `npm run build` passed.

- [x] AGENCY-AGENTS-VISIBLE-ROLE-LIBRARY — Finish the `agency-agents` application by making the curated Nexus role packs visible and testable inside the existing Skills library surface.
  - Current pass: add a prompt-match helper to `lib/agentRoleTaxonomy.ts`, build `components/skills/AgencyRoleLibrary.tsx`, mount it under `/skills?view=library`, extend `npm run agent:taxonomy:check`, and prove the UI compiles without adding a new route.
  - Guardrail: no upstream install/convert scripts, no copied upstream prompt bodies, no generated `.codex` agents, no separate marketplace/agent-builder route, no direct provider calls, no unreviewed background workers, no public positioning change, and no ARPG work.
  - Progress: shipped `matchAgencyRolePrompt()` and `getAgencyRoleInventorySummary()`, added the `/skills?view=library` Agency role library panel with source guardrails, pack counts, role cards, and routing preview, and extended the taxonomy validator to require the visible panel and test IDs. Proof: red `npm run agent:taxonomy:check` failed before implementation, then `npm run agent:taxonomy:check`, `npm run type-check`, `npm run verify`, `npm run build`, `git diff --check`, and a `.next` artifact string check passed. The Windows runtime launcher reported `http://127.0.0.1:3100` healthy but held the shell until timeout, so final live-route proof was not used.

- [x] AGENCY-AGENTS-NATIVE-ROLE-TAXONOMY — Apply `msitarzewski/agency-agents` fully where it fits by turning the reference into a Nexus-native specialist taxonomy, routing hint set, and prompt/context guardrail.
  - Current pass: add a curated local role registry for JANSKY/ORBIT/NOVA/CIPHER/FLUX, inject compact role-pack guidance through `lib/liveContext.ts`, align `detectAgent()` scoring with the new specialist hints, and add a validator wired into `npm run verify`.
  - Guardrail: no upstream install/convert scripts, no copied prompt bodies, no generated `.codex` agents, no new UI route, no public positioning change, no direct provider calls, no unreviewed background agents, no ARPG work, and no raw external source vendoring.
  - Progress: shipped `lib/agentRoleTaxonomy.ts` with curated role packs for all five Nexus agents, injected the per-agent `[AGENCY ROLE PACK]` block through `buildCapabilitiesBlock()`, added taxonomy keyword scoring to `detectAgent()`, documented the source as implemented locally, and wired `npm run agent:taxonomy:check` into `npm run verify`. Proof: red validator first failed on missing taxonomy, then `npm run agent:taxonomy:check`, `npm run type-check`, `npm run verify`, and `npm run build` passed.

- [x] SOURCE-INTAKE-AGENCY-AGENTS — Triage `msitarzewski/agency-agents` into the external-source map without vendoring prompt packs or creating runtime agents.
  - Current pass: inspect the GitHub README/integration notes, document a Nexus-safe fit for ORBIT/NOVA/CIPHER/JANSKY prompt taxonomy and eval/checklist use, and promote only the reviewed pattern to the assimilated ecosystem docs.
  - Guardrail: no install/convert scripts, no copied prompt bodies, no generated Codex agents under `.codex`, no runtime routing changes, no app UI change, no public positioning change, and no ARPG work.
  - Progress: mapped `msitarzewski/agency-agents` into `docs/ideas/external-links-mapping.md`, promoted the distilled fit to `docs/ideas/assimilated-ecosystem.md`, and added it to the P1.3 repo-assimilation backlog as a prompt-taxonomy reference only. Proof: `git diff --check` and `npm run verify` passed.

- [x] PHONE-ACCEPTANCE-GUIDE-CLI — Add one read-only command that turns the current phone/iPad acceptance artifact into a simple desktop-and-phone checklist with exact next commands.
  - Current pass: summarize the latest sanitized phone acceptance artifact, show local LAN/HQ URL candidates from local network interfaces, list the phone-side proof items as done/missing, and print the safest next command sequence for `phone:lan:start`, capture, report, local-AI report, and first-three closure.
  - Guardrail: no network calls, no runtime launch, no receipt API calls, no provider calls, no file writes, no `.env.local` reads, no raw receipt storage reads, no token value reads/prints, no proof simulation, no manual flag claim unless the operator actually performed the steps, no UI change, no public route change, and no ARPG work.
  - Progress: added `specs/features/phone-acceptance-guide-cli.md`, `scripts/phone-acceptance-guide.mjs`, `scripts/validate-phone-acceptance-guide-cli.mjs`, `npm run phone:acceptance:guide`, `npm run phone:acceptance:guide:check`, and validation wiring through `phone:acceptance:receipts:check`. The guide reads the latest sanitized phone acceptance artifact, prints desktop and phone/iPad HQ URL candidates, labels proof items as done/missing/unknown, prints the post-phone capture/report/local-AI/first-three commands, and shows the manual fallback command only as a real-action fallback. Proof: `npm run phone:acceptance:guide`, `npm run phone:acceptance:guide:check`, `npm run phone:acceptance:receipts:check`, `npm run ops:first-three`, `npx tsc --noEmit`, `npm run verify`, and `npm run build` passed.

- [x] FIRST-THREE-OPERATIONAL-CLOSURE — Add one local no-network command that summarizes the first three non-RPG operational blockers: the active `postcss` runtime patch, free local phone acceptance, and local AI offline operations.
  - Current pass: read existing sanitized Dependabot, phone acceptance, and readiness artifacts; print a least-surprise closure report with status, proof source, blocker class, and exact next action for each lane; point external GitHub/X idea pressure back to the existing source-intake docs without turning it into surprise implementation scope.
  - Guardrail: no package install, no `npm audit fix`, no GitHub/network calls, no runtime launch, no phone/iPad simulation, no secret/env reads, no raw receipt reads, no artifact writes by default, no UI/product redesign, no public route change, no ARPG work, and no claim that manual/external proof is complete.
  - Progress: added `specs/features/first-three-operational-closure.md`, `scripts/first-three-operational-closure.mjs`, `scripts/validate-first-three-operational-closure.mjs`, `npm run ops:first-three`, `npm run ops:first-three:check`, and verify wiring. Current report honestly returns `blocked_or_manual`: `postcss` remains `blocked_external` at `8.4.39 -> 8.5.10`, phone acceptance remains blocked by runtime/physical proof, local AI remains blocked by missing phone-side Ollama proof, and GitHub/X/YouTube sources remain mapped intake rather than active implementation. Proof: `npm run ops:first-three`, `npm run ops:first-three:check`, `node scripts/first-three-operational-closure.mjs --check`, `npm run offline:local:report`, `npm run phone:acceptance:report`, `npx tsc --noEmit`, `npm run verify`, and `npm run build` passed.

- [x] DESKTOP-TRUST-CHAIN-STATUS — Add a no-network desktop trust-chain status record for CP2.3.
  - Current pass: record checksum verification posture, signing configuration posture, and SBOM/dependency-inventory posture for the desktop release lane without claiming unsigned artifacts are ready.
  - Guardrail: no actual signing, no certificate/private-key reads, no secret/env reads, no network calls, no dependency installs, no Tauri packaging run, no UI changes, no RPG work, and no release-ready claim without artifacts.
  - Progress: added `specs/features/desktop-trust-chain-status.md`, `scripts/desktop-trust-chain-status.mjs`, `scripts/validate-desktop-trust-chain-status.mjs`, `npm run desktop:trust-chain`, `npm run desktop:trust-chain:check`, and `verify` wiring. The canonical SBOM is now recorded; the current honest blockers are the missing desktop artifact directory, checksum manifest, and signing strategy. Proof: `npm run desktop:trust-chain`, `npm run desktop:trust-chain:check`, `npx tsc --noEmit`, `npm run verify`, and `npm run build` passed.

- [x] DEPENDABOT-NEXT-RUNTIME-PATCH-HELPER — Add a no-network command that identifies the next direct Dependabot runtime patch and prints the exact normal-PowerShell install/proof steps.
  - Current pass: read the latest sanitized Dependabot audit artifact, skip already-patched direct npm alerts, identify `postcss` alert 85 as the next direct runtime patch, and print the narrow install command plus proof commands.
  - Guardrail: no broad dependency sweep, no `npm audit fix --force`, no archive package updates, no Cargo updates, no UI/product changes, no auth/provider changes, no public route changes, and no raw Dependabot export commit.
  - Progress: added `specs/features/dependabot-next-runtime-patch-helper.md`, `scripts/dependabot-next-runtime-patch.mjs`, `npm run dependabot:next-runtime-patch`, `npm run dependabot:next-runtime-patch:check`, and validator coverage through `npm run dependabot:audit:check`. The actual `postcss` package patch is still pending because this Codex shell cannot fetch `https://registry.npmjs.org/postcss`; run the printed command from normal PowerShell to complete the package update. Proof: `npm run dependabot:next-runtime-patch`, `npm run dependabot:next-runtime-patch:check`, `npm run dependabot:audit:check`, `npx tsc --noEmit`, `npm run validate:infra-hardening`, `npm run verify`, and `npm run build` passed.

- [ ] DEPENDABOT-POSTCSS-RUNTIME-PATCH — Patch the active root-app `postcss` alert with the smallest safe dependency batch.
  - Current pass: update only root `postcss` from `^8.4.39` to the recorded patched floor `8.5.10` for alert 85, refresh lockfile evidence, and leave `prismjs`, dev-only `brace-expansion`, Cargo alerts, and retired `archive/` alerts for separate batches.
  - Guardrail: no broad dependency sweep, no `npm audit fix --force`, no archive package updates, no Cargo updates, no UI/product changes, no auth/provider changes, no public route changes, and no raw Dependabot export commit.
  - Progress: blocked in the Codex shell by npm registry `EACCES`; `npm run dependabot:next-runtime-patch` now prints the normal-PowerShell command and proof sequence needed to finish it, and `npm run ops:first-three` shows the same active `postcss@8.5.10` blocker beside phone/local AI proof.

- [x] MEDIA-ESCAPE-BOOK-LIBRARY — Add books to the existing Subscription Escape media library without widening into scraping, piracy, or public media hosting.
  - Current pass: extend the protected local media catalog from movies/music to movies/music/books, add book intake parsing, surface book counts/filter/shelves, and include `https://fmhy.net/` as an operator-supplied reference link only.
  - Guardrail: no scraping, downloading, torrenting, DRM bypass, paywall bypass, piracy workflow, public media endpoints, paid APIs, cloud database, or new tab.
  - Progress: shipped book support through the protected local media catalog, add/edit form, counts, filters, shelves, intake fallback review, secure link categories, source shelf, and Calibre-Web replacement catalog entry. Proof: `npx tsc --noEmit`, `npm run verify`, and `npm run build` passed.

- [x] DEPENDABOT-NEXT-RUNTIME-PATCH — Patch the active root-app Next.js alerts with the smallest runtime-safe dependency batch.
  - Current pass: update only the root app `next` dependency from `15.5.15` to the patched `15.5.18` floor identified by the sanitized Dependabot artifact, refresh dependency/audit evidence, and leave `postcss`, `prismjs`, Cargo, and retired `archive/` manifest work for separate batches.
  - Guardrail: no broad dependency sweep, no archive package updates, no Cargo updates, no UI/product changes, no auth/provider changes, no public route changes, no raw Dependabot export commit, and no ARPG work.
  - Progress: patched `package.json` and `package-lock.json` to `next@15.5.18`, confirmed local `node_modules` reports `next`, `@next/env`, and `@next/swc-win32-x64-msvc` at `15.5.18`, and confirmed `npx next --version` prints `Next.js v15.5.18`. Refreshed evidence in `docs/metrics/dependency-risk-posture-2026-05-25T05-52-21-853Z.json` and `docs/metrics/dependabot-security-audit-2026-05-25T05-52-26-760Z.json`; the imported GitHub alert export still shows old Next alerts until Dependabot rescans. Proof: `npm run dependabot:audit:check`, `npm run dependency:risk:check`, `npm run validate:infra-hardening`, `npx tsc --noEmit`, `npm run verify`, and `npm run build` passed.

- [x] DEPENDABOT-ARCHIVE-MANIFEST-QUARANTINE — Preserve retired `archive/` dependency files without letting GitHub treat them as live manifests.
  - Current pass: rename `archive/package.json` and `archive/pnpm-lock.yaml` to non-manifest archival names, add an archive README explaining that the folder is historical-only, add a validator so package manifests do not return to `archive/`, and refresh Dependabot audit state.
  - Guardrail: preserve file contents, do not delete archived assets, do not update archive dependencies, do not touch root runtime dependencies, do not dismiss GitHub alerts automatically, do not commit raw Dependabot export JSON, and do not touch ARPG.
  - Progress: preserved the old archive dependency files as `archive/package.archived.json` and `archive/pnpm-lock.archived.yaml`, added `archive/README.md`, added `scripts/validate-archive-manifest-quarantine.mjs`, wired `npm run archive:manifest:check` through `validate:infra-hardening`, and regenerated `docs/metrics/dependabot-security-audit-2026-05-25T05-19-22-006Z.json` with `retiredManifestQuarantine.quarantined: true`. Proof: `npm run archive:manifest:check`, `npm run dependabot:audit:check`, and `npm run validate:infra-hardening` passed.

- [x] DEPENDABOT-ALERT-IMPORT-TRIAGE — Turn the Dependabot security audit from metadata-only into a real local triage path that can consume exported GitHub Dependabot alert JSON and classify alerts without upgrading packages.
  - Current pass: add a scoped feature spec, a red validator, an exported-alert fixture, import/classification support in `scripts/dependabot-security-audit.mjs`, package script wiring, and refreshed sanitized audit artifacts.
  - Guardrail: no dependency upgrades, package-lock churn, GitHub writes, alert dismissal, auth/token reads, raw GitHub CLI config reads, private proof values, public route changes, provider bypasses, or ARPG work.
  - Progress: added `specs/features/dependabot-alert-import-triage.md`, `scripts/validate-dependabot-security-audit.mjs`, `scripts/fixtures/dependabot-alerts-sample.json`, `--alerts=` and `--dry-run` support in `scripts/dependabot-security-audit.mjs`, `npm run dependabot:audit:check`, and validator wiring through `validate:infra-hardening`. The classifier now reads exported Dependabot alert JSON, sanitizes package/severity/scope/patched-range fields, and writes runtime-impact, dev-only, transitive, and blocked/deferred queues without writing raw GitHub alert pages. Proof: validator failed red before implementation, then `npm run dependabot:audit:check`, `npm run dependabot:audit:classify -- --dry-run --alerts=scripts\fixtures\dependabot-alerts-sample.json`, `npm run dependabot:audit:classify`, `npm run validate:infra-hardening`, and `npm run dependency:risk:check` passed; `npm audit --json` was attempted but the npm registry audit endpoint was unreachable from this shell.

- [x] DEPENDABOT-REAL-ALERT-QUEUE — Refine the imported real-alert artifact after normal PowerShell exported Dependabot metadata.
  - Current pass: make the export command paginated, mark `archive/` manifest alerts as retired-manifest review instead of active runtime fire, regenerate the sanitized audit artifact from `docs\metrics\dependabot-alerts-source.json`, and refresh canon/handoff.
  - Guardrail: no dependency upgrades, package-lock churn, GitHub writes, alert dismissal, auth/token reads, raw GitHub CLI config reads, private proof values, public route changes, provider bypasses, or ARPG work.
  - Progress: normal PowerShell imported 108 paginated alerts from GitHub, then `scripts/dependabot-security-audit.mjs` was tightened to emit a paginated `gh api --paginate --slurp` command, preserve the old push-warning counts as history only, split `archive/` manifests into `classification.retiredManifest`, and keep active runtime queues focused on the current root app. Latest sanitized artifact: `docs/metrics/dependabot-security-audit-2026-05-24T22-56-24-044Z.json` with 14 active runtime alerts, 1 dev-only alert, 2 active transitive alerts, 90 retired-manifest alerts, and 3 blocked Cargo/non-npm alerts. Proof: `npm run dependabot:audit:check`, fixture dry-run classification, real imported-alert dry-run classification, real imported-alert classification, `npm run validate:infra-hardening`, `npm run dependency:risk:check`, `npx tsc --noEmit`, `npm run verify`, and `npm run build` passed.

- [x] OFFLINE-LOCAL-AI-REPORT-CLI — Add one local no-network command that summarizes desktop and phone-side local AI proof from sanitized artifacts without opening JSON files.
  - Current pass: add a scoped feature spec, red validator, dependency-free report runner, package script wiring, and verify coverage.
  - Guardrail: no network calls, runtime launch, provider calls, receipt API calls, GitHub calls, cloud calls, dependency install, file writes, branch changes, secrets/env reads, raw receipt storage reads, or ARPG work.
  - Progress: added `specs/features/offline-local-ai-report-cli.md`, `scripts/offline-local-ai-report.mjs`, `scripts/validate-offline-local-ai-report-cli.mjs`, `npm run offline:local:report`, and `npm run offline:local:report:check` wired through `offline:local:check`. Proof: validator failed red before implementation, then `node scripts/validate-offline-local-ai-report-cli.mjs`, `npm run offline:local:report:check`, `npm run offline:local:report`, `npm run offline:local:check`, `npm run ops:preflight`, `npx tsc --noEmit`, `npm run verify`, and `npm run build` passed.

- [x] PHONE-ACCEPTANCE-REPORT-CLI — Add one local no-network command that summarizes the latest sanitized phone/iPad acceptance capture artifact without opening JSON files.
  - Current pass: add a scoped feature spec, red validator, dependency-free report runner, package script wiring, and verify coverage.
  - Guardrail: no network calls, runtime launch, receipt API calls, GitHub calls, cloud calls, dependency install, file writes, branch changes, secrets/env reads, raw receipt storage reads, or ARPG work.
  - Progress: added `specs/features/phone-acceptance-report-cli.md`, `scripts/phone-acceptance-report.mjs`, `scripts/validate-phone-acceptance-report-cli.mjs`, `npm run phone:acceptance:report`, and `npm run phone:acceptance:report:check` wired through `phone:acceptance:receipts:check`. Proof: validator failed red before implementation, then `node scripts/validate-phone-acceptance-report-cli.mjs`, `npm run phone:acceptance:report:check`, `npm run phone:acceptance:report`, `npm run phone:acceptance:receipts:check`, `npm run phone:lan:check`, `npm run ops:preflight`, `npx tsc --noEmit`, `npm run verify`, and `npm run build` passed.

- [x] REPO-SYNC-STATUS-SUMMARY — Add one local no-network command that reports branch, ahead/behind, dirty state, last commit, and the next safe sync action.
  - Current pass: add a scoped feature spec, red validator, dependency-free Git status reporter, package script wiring, and verify coverage.
  - Guardrail: no fetch, pull, push, remote API calls, GitHub calls, network probes, auth-token reads, `.env.local` reads, file writes, branch changes, destructive Git commands, or ARPG work.
  - Progress: added `specs/features/repo-sync-status-summary.md`, `scripts/repo-sync-status.mjs`, `scripts/validate-repo-sync-status.mjs`, `npm run repo:sync:status`, and `npm run repo:sync:status:check` wired through `npm run verify`. Proof: validator failed red before implementation, then `node scripts/validate-repo-sync-status.mjs`, `npm run repo:sync:status:check`, `npm run repo:sync:status`, `npx tsc --noEmit`, `npm run ops:preflight`, `npm run verify`, and `npm run build` passed.

- [x] OPERATOR-PREFLIGHT-RUNNER — Add one local command that runs the key no-network operational readiness checks and prints a concise pass/fail summary before MacBook/iPad use.
  - Current pass: add a scoped feature spec, red validator, dependency-free preflight runner, package script wiring, and verify coverage.
  - Guardrail: no cloud calls, GitHub calls, Docker checks, physical-device simulation, dependency install, background service launch, auth-token reads, `.env.local` reads, raw LAN IP collection, public routes, proxy/VPN/IP-hiding behavior, or ARPG changes.
  - Progress: added `specs/features/operator-preflight-runner.md`, `scripts/operator-preflight.mjs`, `scripts/validate-operator-preflight.mjs`, `npm run ops:preflight`, and `npm run ops:preflight:check` wired through `npm run verify`. Proof: validator failed red before implementation, then `node scripts/validate-operator-preflight.mjs`, `npm run ops:preflight:check`, `npx tsc --noEmit`, `npm run verify`, and `npm run build` passed.

- [x] PHONE-ACCEPTANCE-QR-HANDOFF — Make the operational phone/iPad acceptance flow easier by showing a local, dependency-free QR for the direct HQ LAN URL inside the existing Free Local Readiness phone handoff card.
  - Current pass: add a scoped feature spec, local QR matrix helper, inline SVG rendering in `FreeLocalReadinessPanel`, validator coverage, and phone LAN check wiring.
  - Guardrail: no visual redesign, public route, cloud QR service, dependency install, token embedding, raw LAN IP literals in source/docs, screenshot capture, proxy/VPN/anonymity behavior, or ARPG changes.
  - Progress: added `specs/features/phone-acceptance-qr-handoff.md`, `lib/phoneHandoffQr.ts`, an inline SVG QR card under `data-testid="free-local-phone-handoff-qr"`, `scripts/validate-phone-acceptance-qr-handoff.mjs`, and `phone:acceptance:qr:check` through `phone:lan:check`. Proof: validator failed red before implementation, then `node scripts/validate-phone-acceptance-qr-handoff.mjs`, `npm run phone:lan:check`, `npx tsc --noEmit`, `npm run verify`, and `npm run build` passed.

- [x] PHONE-ACCEPTANCE-CAPTURE-STATUS — Add the same sanitized live receipt proof status to `npm run phone:acceptance:capture` artifacts so terminal capture and phone panel proof agree.
  - Current pass: add a scoped feature spec, consume the receipt API `status` payload during capture, write `receiptLiveStatus` and label-only `missingReceiptProofItems`, add validator coverage, and wire it into the phone acceptance receipt check chain.
  - Guardrail: no visual redesign, public route, raw LAN IPs, user-agent strings, token values, cookies, auth headers, screenshots, prompt/response text, account/payment proof, proxy/VPN/anonymity behavior, dependency install, or ARPG changes.
  - Progress: added `specs/features/phone-acceptance-capture-status.md`, taught `scripts/phone-acceptance-capture.mjs` to consume the protected receipt API `status` payload, added `receiptLiveStatus` plus label-only `missingReceiptProofItems` to capture artifacts, preserved those fields in `scripts/readiness-rollup.mjs`, and wired `scripts/validate-phone-acceptance-capture-status.mjs` through `phone:acceptance:receipts:check`. Proof: validator failed red before implementation, dry capture to `tmp-codex-runtime` wrote the new fields while honestly blocked with the local runtime down, then `node scripts/validate-phone-acceptance-capture-status.mjs`, `npm run phone:acceptance:receipts:check`, `npx tsc --noEmit`, and `npm run verify` passed.

- [x] PHONE-ACCEPTANCE-LIVE-STATUS — Show live sanitized phone/iPad receipt proof in the existing Free Local Readiness panel so physical acceptance can be completed without inspecting local JSON artifacts.
  - Current pass: add a scoped feature spec, shared live-status model, protected receipt GET status payload, panel fetch/render wiring, validator coverage, and package-script wiring.
  - Guardrail: no visual redesign, public route, raw LAN IPs, user-agent strings, token values, cookies, auth headers, screenshots, prompt/response text, account/payment proof, proxy/VPN/anonymity behavior, or ARPG changes.
  - Progress: added `specs/features/phone-acceptance-live-status.md`, `lib/phoneAcceptanceStatus.ts`, protected receipt API `status` payloads, live receipt proof rendering in `components/ui/FreeLocalReadinessPanel.tsx`, and `scripts/validate-phone-acceptance-live-status.mjs` wired through `phone:acceptance:receipts:check`. Proof: validator failed red before implementation, then `node scripts/validate-phone-acceptance-live-status.mjs`, `npm run phone:acceptance:receipts:check`, `npm run phone:lan:check`, `npx tsc --noEmit`, and `npm run verify` passed.

- [x] PHONE-ASSISTANT-RECEIPT-BRIDGE — Let existing assistant turn receipts automatically mark phone acceptance proof for `ping` and local Ollama turns, so the physical phone/iPad flow needs fewer manual flags.
  - Current pass: add a scoped feature spec, a sanitized client receipt bridge, shared `AssistantTurnReceipt` wiring, validator coverage, and package-script wiring.
  - Guardrail: no visual redesign, no prompt/response text, no transcripts, no token values, cookies, auth headers, raw LAN IPs, full user-agent strings, screenshots, proxy/VPN/anonymity behavior, account/payment proof, or ARPG changes.
  - Progress: added `specs/features/phone-assistant-receipt-bridge.md`, `lib/phoneAssistantReceiptBridge.ts`, shared silent marker wiring in `components/assistant/AssistantTurnReceipt.tsx`, and `scripts/validate-phone-assistant-receipt-bridge.mjs` wired through `phone:acceptance:receipts:check`. Proof: validator failed red before implementation, then `node scripts/validate-phone-assistant-receipt-bridge.mjs`, `npm run phone:lan:check`, `npx tsc --noEmit`, and `npm run verify` passed.

- [x] PHONE-ACCEPTANCE-RECEIPTS — Add a protected, local-only receipt lane that records sanitized phone/browser proof when the existing Free Local Readiness panel opens, then let `npm run phone:acceptance:capture` consume receipts before falling back to manual flags.
  - Current pass: add a scoped feature spec, receipt helper, protected API route, silent panel ping, capture merge, ignored local receipt storage, and validator wiring.
  - Guardrail: no visual redesign, no public route, no token values, cookies, auth headers, raw LAN IPs, full user-agent strings, screenshots, proxy/VPN/anonymity claims, account/payment proof, or ARPG changes.
  - Progress: added `specs/features/phone-acceptance-receipts.md`, `lib/phoneAcceptanceReceipts.ts`, protected `/api/phone-acceptance/receipt`, silent receipt pinging in `components/ui/FreeLocalReadinessPanel.tsx`, capture merge fields in `scripts/phone-acceptance-capture.mjs`, ignored local receipt storage, and `scripts/validate-phone-acceptance-receipts.mjs` wired through `phone:lan:check`. Proof: validator failed red before implementation, then `node scripts/validate-phone-acceptance-receipts.mjs`, `npm run phone:lan:check`, `npx tsc --noEmit`, `npm run verify`, `git diff --check`, and `npm run build` passed; dry capture to `tmp-codex-runtime` confirmed `receiptPhoneProof` and `combinedPhoneProof` while honestly remaining blocked because no runtime/mobile receipt was available. Live `3100` proof is not accepted from this shell: `runtime:launch:3100` printed healthy, then the tool timeout killed the child runtime, and direct background launch was blocked by shell policy.

- [x] ESCAPE-IP-PRIVACY-GUARD — Prevent accidental public-IP exposure from the stream connect shelf by classifying links as private/local or public and locking public links until a VPN/Tailscale exit-node/privacy route is confirmed.
  - Current pass: add a scoped feature spec, network-scope metadata in `inspectSecureLink`, public-link lock states in the stream shelf, and copy that makes clear Nexus cannot hide a browser IP by itself.
  - Guardrail: no false anonymity claims, no built-in proxy, no IP lookup service, no public route, no logging/publishing of client IPs, and no unsafe link-opening bypass.
  - Progress: added `specs/features/escape-ip-privacy-guard.md`, `docs/superpowers/plans/2026-05-24-escape-ip-privacy-guard.md`, network-scope and `requiresIpPrivacy` metadata in `lib/secureLink.ts`, and public-link locking plus session-only privacy route confirmation in `components/resources/SecureLinkOpenPanel.tsx`. `npx tsc --noEmit`, `npm run lint`, `npm run verify`, `git diff --check`, and live dev-route proof passed: `/api/health` returned 200, `/resources?view=escape` returned 200, unauthenticated `/api/subscription-escape` returned 401, and unauthenticated `/api/subscription-escape/assets/not-real.png` returned 401.

- [x] ESCAPE-STREAM-CONNECT-SHELF — Make secure link opening feel like a private streaming site: paste a link, validate it, save it as a sorted tile, and connect securely from the Escape shelf.
  - Current pass: add a scoped feature spec, persisted secure stream-link records, safe state normalization, a streaming-style connect shelf, search/filter/sort/favorite controls, and secure open/copy/remove actions.
  - Guardrail: Nexus opens approved links only; it does not proxy streams, scrape paid libraries, bypass DRM/paywalls, store credential-bearing URLs, fetch remote previews, or allow unsafe public HTTP.
  - Progress: added `specs/features/escape-stream-connect-shelf.md`, `docs/superpowers/plans/2026-05-24-escape-stream-connect-shelf.md`, persisted `secureStreamLinks` in `lib/subscriptionEscape.ts`, safe normalization through `inspectSecureLink` in `lib/subscriptionEscapeStore.ts`, a streaming-style tile shelf in `components/resources/SecureLinkOpenPanel.tsx`, and Escape persistence wiring in `components/resources/SubscriptionEscapeConsole.tsx`. `npx tsc --noEmit`, `npm run lint`, `npm run verify`, `git diff --check`, and live dev-route proof passed: `/api/health` returned 200, `/resources?view=escape` returned 200, unauthenticated `/api/subscription-escape` returned 401, and unauthenticated `/api/subscription-escape/assets/not-real.png` returned 401.

- [x] ESCAPE-SECURE-LINK-OPENER — Add a safe local option for opening links from the Escape lane with URL validation, private-network awareness, and explicit operator click-through.
  - Current pass: add a scoped feature spec, secure URL inspection helper, protected Escape UI panel, and verification without fetching/unfurling pasted links or storing link history.
  - Guardrail: no automatic opening, no unsafe protocols, no public plain-HTTP links, no credential-bearing URLs, no remote preview fetches, and no public unauthenticated route.
  - Progress: added `specs/features/escape-secure-link-opener.md`, `docs/superpowers/plans/2026-05-24-escape-secure-link-opener.md`, deterministic local URL inspection in `lib/secureLink.ts`, the protected `components/resources/SecureLinkOpenPanel.tsx`, and mounted it inside `components/resources/SubscriptionEscapeConsole.tsx`. `npx tsc --noEmit`, `npm run lint`, `npm run verify`, `git diff --check`, and live dev-route proof passed: `/api/health` returned 200, `/resources?view=escape` returned 200, unauthenticated `/api/subscription-escape` returned 401, and unauthenticated `/api/subscription-escape/assets/not-real.png` returned 401.

- [x] MEDIA-ESCAPE-INTAKE-QUEUE — Add a local review queue so movies and music can be pasted in from filenames/paths, checked for likely duplicates, cleaned up, and imported into the library only after review.
  - Current pass: add a scoped feature spec, intake-state normalization, filename parsing helpers, duplicate detection, a review queue UI, import/ignore actions, and a missing-info board without scanning arbitrary host folders or exposing media publicly.
  - Guardrail: pasted paths are catalog metadata only; no automatic filesystem crawling, piracy, DRM bypass, paid metadata lookup, public media endpoint, or cloud source of truth.
  - Progress: added `specs/features/media-escape-intake-queue.md`, `docs/superpowers/plans/2026-05-23-media-escape-intake-queue.md`, media intake types/helpers in `lib/subscriptionEscape.ts`, `mediaIntake` normalization in `lib/subscriptionEscapeStore.ts`, `components/resources/MediaIntakeReviewPanel.tsx`, duplicate confirmation in `components/resources/MediaEscapeLibrary.tsx`, and combined media/intake persistence in `components/resources/SubscriptionEscapeConsole.tsx`. Also updated the private asset dynamic route to the current Next route-handler params signature required by `tsc`. `npx tsc --noEmit`, `npm run lint`, `npm run verify`, `git diff --check`, and live dev-route proof passed: `/api/health` returned 200, `/resources?view=escape` returned 200, unauthenticated `/api/subscription-escape` returned 401, and unauthenticated `/api/subscription-escape/assets/not-real.png` returned 401.

- [x] ESCAPE-PRIVATE-FOUNDATION — Solidify the Escape media/subscription base with Tailscale-first access posture, revocable access tracking, backup export/import, and protected local cover/poster storage.
  - Current pass: add a scoped feature spec, access-state normalization, a private asset store under ignored local data, protected asset upload/read routes, backup/restore controls, authorized person/device tracking, and media cover upload without adding paid APIs or public media exposure.
  - Guardrail: cloud is optional backup only; no paid metadata services, cloud source-of-truth database, public unauthenticated media endpoint, Tailscale Funnel default, piracy, DRM bypass, paywall bypass, or ad-circumvention scope.
  - Progress: added `specs/features/escape-private-foundation.md`, `docs/superpowers/plans/2026-05-22-escape-private-foundation.md`, access posture/record types in `lib/subscriptionEscape.ts`, normalization in `lib/subscriptionEscapeStore.ts`, protected asset helpers/routes, ignored `data/subscription-escape-assets/`, `components/resources/EscapeAccessBackupPanel.tsx`, and private cover upload in `components/resources/MediaEscapeLibrary.tsx`. `npx tsc --noEmit`, `npm run lint`, `npm run verify`, `git diff --check`, and live dev-route proof passed: `/api/health` returned 200, `/resources?view=escape` returned 200, unauthenticated `/api/subscription-escape` returned 401, and unauthenticated `/api/subscription-escape/assets/not-real.png` returned 401.

- [x] MEDIA-ESCAPE-VISUAL-LIBRARY — Add a Netflix-style local media catalog inside `/resources?view=escape` for movies and music together, with cover/poster cards, simple search/sort/filter, one obvious add/edit form, favorites, and protected MacBook-hosted state.
  - Current pass: extend the existing Subscription Escape local state with media records, add a visual media library component, and keep all data in the same protected server-side state file.
  - Guardrail: no piracy, DRM bypass, paywall bypass, ad-circumvention, paid-library scraping, public media endpoint, or streaming/playback server in this pass; local file paths are catalog metadata only.
  - Progress: added `specs/features/media-escape-visual-library.md`, media record types/helpers in `lib/subscriptionEscape.ts`, local-state normalization in `lib/subscriptionEscapeStore.ts`, and `components/resources/MediaEscapeLibrary.tsx`; mounted it inside the Escape console with shared persistence through `/api/subscription-escape`. `npx tsc --noEmit`, `npm run lint`, `npm run verify`, `git diff --check`, and live dev-route proof passed: `/api/health` returned 200, `/resources?view=escape` returned 200, and unauthenticated `/api/subscription-escape` returned 401 as expected.

- [x] SUBSCRIPTION-ESCAPE-LOCAL-HUB — Add a private Resources lane for replacing monthly subscriptions with safe local/free/open alternatives, using the always-on MacBook as the Nexus host and desktop/iPad as Tailscale clients.
  - Current pass: add a scoped feature spec, protected server-side local storage, a Resources Escape console, Tailscale/private-access posture, cancellation safety checks, and source-backed replacement catalog without building a VPN/proxy or adding public exposure.
  - Guardrail: no piracy, DRM bypass, paywall bypass, ad-circumvention claims, account-ban evasion, auto-canceling accounts, Nexus-side billing, cloud sync database, new top-level tab, or public unauthenticated route.
  - Progress: added `specs/features/subscription-escape-local-hub.md`, `lib/subscriptionEscape.ts`, `lib/subscriptionEscapeStore.ts`, protected `/api/subscription-escape`, and `components/resources/SubscriptionEscapeConsole.tsx`; mounted it at `/resources?view=escape`, excluded local `data/subscription-escape*.json` from git, and added route policy coverage. `npx tsc --noEmit`, `npm run lint`, `npm run verify`, `git diff --check`, and live dev-route proof passed: `/api/health` returned 200, `/resources?view=escape` returned 200, and unauthenticated `/api/subscription-escape` returned 401 as expected.

- [x] SMOOTHNESS-OPERATIONAL-LIGHTS — Make Nexus feel calmer and always connected by adding a shared publish-safe operational light grid plus smoothness guardrails across the authenticated shell, HQ, COMMAND, and 3D office.
  - Current pass: added `lib/operationalLights.ts`, `hooks/useOperationalLights.ts`, and `components/ui/OperationalLightGrid.tsx`; mounted compact fixed-size lights in the toprail, shared Free Local Readiness panel used by HQ/COMMAND, and the 3D office server-rack ambience.
  - Guardrail: lights are read-only indicators, not controls; no token values, cookies, auth headers, real LAN IPs, local home paths, account/payment proof, raw proof screenshots, paid-provider defaults, public route widening, package upgrades, or private RPG work.
  - Progress: `npm run smoothness:check` validates the shared light contract, visibility-aware polling, legacy floating status-widget non-mounting, and no `networkidle` proof waits; `npm run verify` now includes the smoothness gate. `npm run smoothness:check`, `npm run publication:safety:check`, `npm run security-scan`, `npm run type-check`, `npm run lint`, `git diff --check`, merge-marker sweep, and `npm run verify` passed. Runtime HTTP proof returned 200 for `/api/health`, `/hq?focus=hq-chronicle`, and `/command`, while raw `/api/free-local-readiness` returned `401 AUTH-PROTECTED`; hydrated browser proof remains locally blocked by Windows Playwright `spawn EPERM`.

- [x] SELF-WORKING-AI-AUTONOMY — Add a safe self-development loop so Nexus Prime can select bounded tasks, prepare isolated work, record sanitized run artifacts, and keep risky mutation review-gated instead of silently changing the live repo.
  - Current pass: add `npm run autonomy:self-work`, a policy-aware self-work runner, sanitized `docs/metrics/autonomy-self-work-*.json` artifacts, dirty-tree blocking, approval-required classification, worktree preparation for execute mode, and verification gate reporting.
  - Guardrail: no `.env.local` reads, token values, cookies, auth headers, private LAN IPs, local home paths, account/payment data, raw private assets, direct provider calls, unreviewed auth/security/dependency/deploy mutations, or automatic merge/push.
  - Progress: added `scripts/autonomy-self-work.mjs`, `npm run autonomy:self-work`, and `docs/agents/self-working-ai-autonomy.md`. Dry-run selects the first active open-ready task and writes a sanitized readiness artifact; execute mode starts only from a clean tree and blocks manual/external or approval-required work before mutation; approval mode prepares merge instructions only for a verified `readyForMerge` run. Latest artifact `docs/metrics/autonomy-self-work-asw-2026-05-10T07-55-04-041Z.json` correctly blocks autonomous work on the current manual phone/PWA task, unpushed commits, approval-required auth/token posture, and missing Docker strong-isolation proof; `npm run autonomy:self-work -- --execute --out-dir=tmp-codex-runtime` exited blocked as expected while the implementation tree was dirty.

- [x] NEXT-UPDATES-COMPLETION-PASS — Move hardening from built-local into the next release-candidate checkpoint without publishing vital local information or simulating external proof.
  - Current pass: attempted to publish the local commit stack, refreshed security hardening/dependency/Dependabot evidence, captured blocked phone and release artifacts honestly, and regenerated the wide readiness rollup.
  - Guardrail: no token values, cookies, auth headers, real LAN IPs, local home paths, screenshots, receipts, account/payment details, raw asset intake, package upgrades, auth loosening, public route widening, or private RPG work.
  - Progress: Codex-shell push is blocked by GitHub port 443, so the branch remains ahead by 6 commits until `npm run git:safe -- push` succeeds from normal PowerShell. Latest hardening proof `docs/metrics/infra-hardening-2026-05-09T22-28-42-970Z.json` has no critical failures; latest dependency posture `docs/metrics/dependency-risk-posture-2026-05-09T22-28-44-321Z.json` is ready with one lifecycle-script review warning group; latest rollup `docs/metrics/readiness-rollup-2026-05-09T22-29-00-772Z.json` is blocked only by physical phone/PWA proof, staged-host/Docker proof, and GitHub Dependabot metadata.

- [x] SECURITY-INFRASTRUCTURE-HARDENING — Make project security, vulnerability resistance, and infrastructure validation the active priority before more acceptance or product expansion.
  - Current pass: add local dependency risk posture, enrich Dependabot audit metadata, validate security boundaries, add a sanitized infra hardening rollup, and wire stable non-mutating gates into verification/pre-push.
  - Guardrail: no token values, cookies, auth headers, real LAN IPs, local home paths, receipts, account/payment data, raw asset intake, package upgrades, auth loosening, public route widening, or RPG/drone scope.
  - Progress: added `npm run dependency:risk:posture`, `npm run dependency:risk:check`, `npm run security:boundaries`, `npm run infra:hardening:audit`, and `npm run validate:infra-hardening`; `npm run verify` now includes security boundary, dependency risk check, and infra validator gates, while `.husky/pre-push` includes the infra validator. Latest hardening proof: `docs/metrics/infra-hardening-2026-05-09T22-28-42-970Z.json` has no critical failures; Docker CLI and staged-host proof remain externally blocked. Latest readiness rollup: `docs/metrics/readiness-rollup-2026-05-09T22-29-00-772Z.json`.

- [x] WIDE-STABILIZATION-READINESS-ROLLUP — Move the local safety/phone evidence lane into a broader release-readiness posture without widening public routes or publishing vital information.
  - Current pass: add `npm run readiness:rollup` and `npm run dependabot:audit:classify`, capture fresh sanitized blocked phone/release artifacts, and keep physical phone proof plus dependency metadata honest instead of simulated.
  - Guardrail: no token values, cookies, auth headers, real LAN IPs, local home paths, screenshots, receipts, payment/order/account details, provider bypass, auth loosening, paid provider default, public hosting default, package upgrades, or RPG/drone scope.
  - Progress: `docs/metrics/readiness-rollup-2026-05-09T22-29-00-772Z.json` links the latest phone acceptance artifact, release diagnostics artifact, Dependabot audit starter, dependency posture, infra hardening proof, publication safety posture, and runtime metric artifacts. The rollup is intentionally not release-candidate ready yet: phone/PWA proof is still manual, release diagnostics still need staged-host/Docker proof, and Dependabot package metadata is pending.

- [x] PHONE-ACCEPTANCE-FLUIDITY-UPGRADE — Build on the publication-safety gate by making the phone/PWA acceptance pass easier to execute without publishing vital local information.
  - Current pass: add a copyable phone acceptance brief inside the existing HQ/COMMAND Free Local Readiness panel, keep committed proof placeholder-only, and refresh the desktop LAN route proof without marking physical-phone proof complete.
  - Guardrail: no token values, cookies, auth headers, real LAN IPs, local account paths, screenshots, receipts, payment/order/account details, provider bypass, auth loosening, paid provider default, public hosting default, dependency upgrades, or RPG/drone scope.
  - Progress: added `buildPhoneAcceptanceBrief`, wired **Copy acceptance steps** into `FreeLocalReadinessPanel`, documented the brief in `docs/deployment/phone-access-free-local.md`, and extended `npm run offline:local:check` plus `npm run phone:lan:check` coverage. `npm run phone:lan:start` reached desktop health; the sanitized route harness returned 200 for `/api/health`, `/hq?focus=hq-chronicle`, and `/command`, while raw `/api/free-local-readiness` returned `401 AUTH-PROTECTED` without an authenticated browser session as expected. Physical phone login, phone chat receipts, and PWA install remain manual.

- [x] PHONE-ACCEPTANCE-EVIDENCE-LANE — Add a publish-safe artifact path for phone/local-AI acceptance so proof can be captured without leaking vital local information.
  - Current pass: add `npm run phone:acceptance:capture`, store sanitized `docs/metrics/phone-local-acceptance-*.json` artifacts, and keep acceptance blocked until manual phone flags prove phone open, login, ping receipt, local AI receipt, and PWA install.
  - Guardrail: capture may read `NEXUS_TOKEN` only for a protected local API call through the internal auth header; it must never print or store token values, cookies, auth headers, real LAN IPs, local home paths, screenshots, or account/payment proof.
  - Progress: added `scripts/phone-acceptance-capture.mjs`, wired `phone:acceptance:capture`, extended `phone:lan:check`, and documented the capture command. First artifact: `docs/metrics/phone-local-acceptance-2026-05-09T12-40-50-374Z.json` records `acceptanceReady: false` because no runtime was listening during capture and no manual phone flags were supplied. Physical phone acceptance remains open.

- [x] PUBLICATION-SAFETY-LOCKDOWN — Stop publishing vital local information by scrubbing committed machine-specific proof values and adding a tracked-file publication guard.
  - Current pass: replace real LAN IP/home-path evidence with placeholders, add `scripts/validate-publication-safety.mjs`, wire `npm run publication:safety:check` into `npm run verify` and `.husky/pre-push`, and keep future runtime evidence placeholder-only.
  - Guardrail: do not read or print `.env.local`, raw tokens, cookies, auth headers, receipts, payment/order/account details, raw asset intake, private key material, or real local network addresses.
  - Progress: publication safety is now a first-class gate ahead of phone/PWA acceptance and the separate Dependabot security audit.

- [x] GIT-PERMISSION-RECOVERY — Make git pull/fetch/staging/push reliable again after the confirmed `.git` DENY ACL blocker.
  - Current pass: preserve `scripts/git-with-acl-repair.ps1` and `npm run git:safe` as the Windows-safe Git command path, and route `npm run handoff:pull` through that wrapper so it removes the known explicit `.git` DENY ACL inside the same process before running Git.
  - Guardrail: do not delete branches, remove worktrees, or remove lock files while Git processes are active; keep cleanup review-gated.
  - Progress: normal PowerShell proved GitHub connectivity, `npm run handoff:pull` returned `Already up to date`, `npm run git:safe -- fetch --all --prune` completed, and push to `origin/codex/homefront-vision-expansion` succeeded. The Codex sandbox can still reapply DENY ACLs between tool calls, but the wrapper now removes both observed deny SIDs in-process, accepts multi-argument commands, and completed a real staging write. The recovery runbook and `repo:sync:status:check` guard the updated contract.

- [x] BLOCKER-FUTURE-PROOFING — Make recurring local blockers visible and recoverable instead of tribal knowledge: add a repo sync health check for `.git` ACL/lock failures, document the safe Windows recovery flow, surface the next commands in Free Local Readiness, and keep runtime/Playwright blocked states on the accepted HTTP/source-proof fallback path.
  - Current pass: start with the confirmed `.git` DENY ACL blocker that prevents `handoff:pull`, `git fetch`, staging, and push, then add read-only diagnostics plus a manual recovery runbook before any cleanup or branch deletion.
  - Guardrail: no automatic ACL mutation, no branch deletion, no lock deletion while git processes are active, no push, no provider/auth changes, and no RPG/drone scope.
  - Progress: added `lib/repoSyncRecovery.ts`, `scripts/repo-sync-health-check.ps1`, `npm run repo:sync:health`, and `docs/repo-hygiene/git-permission-recovery.md`. Free Local Readiness now shows the diagnostic command, recovery runbook path, manual recovery steps, and proof commands in the repo-sync report. `npm run repo:sync:health` currently reports blocked because `.git` still has explicit DENY ACL entries, but it detects no lock files and no active Git processes. `npm run offline:local:check` and `npx tsc --noEmit` passed.

- [x] FREE-LOCAL-HOMEFRONT-OPERATIONS-ROADMAP — Turn the major updates roadmap into an operator-facing local/free command flow: phone acceptance checklist, local AI proof summary, repo-sync blocker visibility, and validation without widening into RPG/drone/deployment work.
  - Current pass: add internal roadmap/checklist contracts, render them inside the shared HQ/COMMAND Free Local Readiness panel, keep `.git` recovery review/manual, and extend local validators.
  - Guardrail: no paid provider default, no VPS dependency, no provider bypass, no auth loosening, no real drone/hardware control, no private RPG changes, and no automatic branch deletion or git cleanup.
  - Progress: added `lib/freeLocalOperations.ts` with `PhoneAcceptanceChecklist`, `LocalAiProofSnapshot`, `AssistantTurnProof`, `RepoSyncHealthReport`, and the free-local major update queue. HQ and COMMAND inherit the new operator-facing flow through `FreeLocalReadinessPanel`: phone acceptance checklist, local AI proof summary, major update queue, and `.git` repo-sync blocker/proof commands. `npm run type-check`, `npx tsc --noEmit`, `npm run lint`, `npm run offline:local:check`, `npm run phone:lan:check`, `npm run eval:agent-runtime:ci`, and `npm run verify` passed. `npm run runtime:proof:3100` initially failed because 3100 was down, so runtime proof used the known controlled PowerShell dev-runtime fallback: `/api/health`, `/`, `/hq?focus=hq-chronicle`, and `/command` returned 200; raw `/api/free-local-readiness` returned 401 as expected without an authenticated browser session.

- [x] LOCAL-AI-OFFLINE-OPERATIONS-UX — Make the phone/LAN path operationally useful: prove local Ollama/free posture in assistant turn receipts, normalize recovery actions for local AI failures, add a Check local AI prompt chip, and tighten HQ readiness/chat layout on phone screens.
  - Current pass: extend the shared assistant receipt/action model, wire provider/model/free-path proof through HQ/Home/CommandBar, and keep the Free Local Readiness panel mobile-friendly.
  - Guardrail: no paid provider default, no VPS dependency, no auth loosening, no new public API, no provider bypass, and no RPG/drone edits.
  - Progress: added `AssistantRuntimeReceipt`, `loadAssistantRuntimeReceipt`, and receipt merging so Home chat, HQ chronicle, and CommandBar can show provider/model/network/paid-API/file-change proof on assistant turns. Local fast replies like `ping` now show `Local fast path`, real AI turns pull `/api/free-local-readiness` for Ollama/free posture, and structured failures carry recovery codes into the receipt. HQ now has a `Check local AI` prompt chip that sends the local/free posture acceptance prompt, `AssistantTurnReceipt` uses responsive CSS classes, and compact HQ readiness details collapse behind an expandable block for phone screens. `npm run type-check`, `npx tsc --noEmit`, `npm run lint`, `npm run offline:local:check`, `npm run phone:lan:check`, `npm run eval:agent-runtime:ci`, and `npm run verify` passed. Runtime proof used a controlled PowerShell dev-runtime job: `/api/health`, `/hq?focus=hq-chronicle`, and `/command` returned 200; raw `/api/free-local-readiness` returned 401 as expected without an authenticated browser session.

- [x] FREE-LOCAL-PHONE-UX-OPTIMIZATION — Make the already-working free LAN phone path easier to operate from HQ/COMMAND: show phone-ready HQ URLs, add copy affordances, clarify model/offline status, and tighten mobile readiness layout without adding paid providers, VPS defaults, or RPG/drone scope.
  - Current pass: improve `FreeLocalReadinessPanel`, `phone:lan:start` output, and local validators so the phone path is practical after it connects.
  - Guardrail: no new paid dependency, no public exposure unless LAN mode is explicitly enabled, no auth loosening, no provider bypass, and no private RPG changes.
  - Progress: `PhoneLanReadinessSnapshot` now carries desktop URLs, LAN URLs, direct HQ URLs, and preferred phone/HQ links. HQ/COMMAND Free Local Readiness now shows a phone handoff card with copy buttons for the phone home URL and `/hq?focus=hq-chronicle`, plus clearer local model status. `npm run phone:lan:start` prints direct HQ URLs. `npm run phone:lan:check`, `npm run offline:local:check`, `npm run type-check`, `npx tsc --noEmit`, `npm run lint`, `npm run verify`, and `npm run runtime:proof:3100 -- --routes=/,/hq?focus=hq-chronicle,/command,/api/free-local-readiness` passed.

- [x] FREE-LOCAL-AI-PHONE-READINESS — Make Homefront usable as a fully free, local-first command room: local website runtime, Ollama-first AI, paid APIs blocked by default, and free phone access through an explicitly enabled desktop/LAN path instead of paid hosting as the default.
  - Current pass: add a shared free-local readiness contract/API, expose it in HQ and COMMAND, add local/phone readiness validators, document the LAN/PWA path, and keep Coolify/VPS as optional hosted mode only.
  - Guardrail: no public billing API, no required paid provider, no paid VPS default, no RPG/drone work, no permission expansion, and no LAN exposure unless explicitly enabled with token protection.
  - Progress: added `FreeLocalReadinessSnapshot` and `PhoneLanReadinessSnapshot`, `/api/free-local-readiness`, the shared `FreeLocalReadinessPanel`, COMMAND/HQ surfacing, structured `/api/ai` recovery codes, strict `isolated` network-mode parsing, `npm run offline:local:check`, `npm run phone:lan:check`, `npm run phone:lan:start`, and `docs/deployment/phone-access-free-local.md`. `npm run offline:local:check`, `npm run phone:lan:check`, `npm run phone:access:check`, `npm run type-check`, `npx tsc --noEmit`, `npm run eval:agent-runtime:ci`, `git diff --check`, merge-marker sweep, and `npm run verify` passed. `npm run runtime:launch:3100` timed out in the known Windows wrapper path after reporting healthy, so runtime proof used a controlled PowerShell dev-runtime job and returned 200 for `/`, `/hq?focus=hq-chronicle`, `/command`, and `/command?focus=provider-health`; protected readiness/catalog/agent APIs returned `401 AUTH-PROTECTED` from raw shell as expected.

- [x] NEXT-UPDATE-STABILIZATION-AUDIT — Make the current local batch reviewable before more expansion, reconcile stale task truth, tighten private ARPG release proof, and replace obvious stale placeholder UI states in secondary protected routes.
  - Current pass: add a local change-set map, record the `.git` permission blocker, reconcile the unfinished-ideas ledger after shipped AI/vehicle work, add ARPG fallback proof matrix coverage, and replace visible placeholder language in Security/Drone/IoT/Voice Lab with honest simulated/passive states.
  - Guardrail: no branch deletion, no push, no real drone/hardware control, no public RPG positioning, no paid provider dependency, and no ARPG playfield restyle.
  - Progress: added `docs/repo-hygiene/local-change-set-map-2026-05-08.md`, ignored `tmp-codex-runtime/`, reconciled the unfinished-ideas ledger so shipped vehicle/assistant rows no longer read open-ready, added an ARPG fallback proof matrix validated by `npm run arpg:release:check`, surfaced that matrix in the `/hq` Production drawer, and replaced Security camera, drone, IoT, and Voice Lab placeholder states with explicit passive/simulated copy. Added `npm run surface:polish:check` to `npm run verify`. `git diff --check`, merge-marker sweep, `npm run surface:polish:check`, `npm run arpg:release:check`, `npm run type-check`, `npx tsc --noEmit`, `npm run eval:agent-runtime:ci`, and `npm run verify` passed. `npm run runtime:launch:3100` still timed out after printing healthy, so runtime proof used the parallel direct-dev fallback and returned 200 for `/`, `/hq?focus=hq-chronicle`, `/command`, `/resources`, `/internal/vehicle`, `/labs/security`, and `/internal/iot`. Git permission recovery is now handled by `npm run git:safe -- <git args>`.

- [x] HOMEFRONT-AUTH-SHELL-POLISH — Continue the premium landing language across authenticated non-HQ routes with clearer workplane purpose, stronger glass/media primitives, and visible active state feedback.
  - Current pass: add an `interiorPolish` contract to the shared Homefront visual surface spec and consume it from the authenticated shell primitives, route tabs, action docks, media panels, and workplane summaries.
  - Guardrail: no public API changes, no provider/auth/tool-permission changes, no new top-level routes, and no edits to the private ARPG playfield or ARPG CSS.
  - Progress: added `interiorPolish` to `HomefrontVisualSurfaceSpec` for HQ, COMMAND, INTEL, ALPHA, CYBER, RECON, VAULT, RESOURCES, and secondary protected surfaces. `ShellPage`, `ShellStage`, `HomefrontVisualParityBand`, `HomefrontWorkplaneSummary`, `HomefrontRouteTabs`, `HomefrontActionDock`, and `HomefrontMediaPanel` now expose polish metadata, support density, media moments, and stronger active-state affordances without injecting anything into the ARPG playfield. Added CSS for stronger liquid-glass workplanes, active tabs/buttons, media moment copy, and polished route surfaces; extended E2E/eval assertions for interior polish and HQ ARPG isolation. `npm run type-check`, `npx tsc --noEmit`, `npm run eval:agent-runtime:ci`, `npm run verify`, and runtime proof for `/command,/intel,/alpha,/cyber,/recon,/vault,/resources,/labs/security,/internal/skills,/internal/vehicle,/hq?focus=hq-chronicle` passed; the standard launcher still timed out in the known Windows wrapper path, so route proof used a temporary PowerShell dev-runtime job that compiled the current source.

- [x] WORKPLANE-OPERATOR-SOURCE-HYGIENE-ROADMAP — Make routes easier to operate, make assistant turns more useful, govern external source intake, and keep branch cleanup review-gated.
  - Current pass: implement route workplane summary strips first, then assistant turn receipts, source-intelligence ledger statuses, and repo-hygiene cleanup decision groups without adding routes, loosening permissions, deleting branches, or touching the private ARPG playfield.
  - Guardrail: no public API changes, no provider dependency, no ARPG styling changes, no vendored external code, and no automatic branch/worktree deletion.
  - Progress: added `workplaneSummary` to the shared Homefront visual surface spec and mounted one compact `homefront-workplane-summary` strip for COMMAND, INTEL, ALPHA, CYBER, RECON, VAULT, and RESOURCES while keeping HQ/ARPG excluded. Assistant messages now carry shared turn receipts across Home chat, HQ chronicle, and CommandBar, including answer mode, workspace, tool posture, recovery, and file-change posture. `/resources?view=sources` now renders a governed source ledger with mapped/candidate/blocked/rejected/private-lane decisions for the supplied GitHub/X links and VAULT filing links. Repo cleanup remains review-gated through `docs/repo-hygiene/branch-cleanup-decision-ledger-2026-05-08.md` with exact commands but no deletion. `npm run type-check`, `npx tsc --noEmit`, `npm run eval:agent-runtime:ci`, `npm run verify`, and runtime proof for `/command,/intel,/alpha,/cyber,/recon,/vault,/resources,/hq?focus=hq-chronicle` passed; the standard launcher still times out in the known Windows wrapper path, so proof used a temporary runtime job.

- [x] RELEASE-RUNBOOK-PREP — Prepare the FD2/CP2 diagnostics capture and rollback lane without claiming staged deployment proof.
  - Current pass: add the FD2 release runbook, local/remote diagnostics capture command, release checklist links, and task/system handoff updates so the deployment proof path is executable once the real Coolify host, token, and Docker lane exist.
  - Guardrail: keep FD2/CP2 blocked until a real staged hostname/Docker proof exists; this pass records missing prerequisites as blocked, not failed product behavior.
  - Progress: added `docs/deployment/fd2-release-runbook.md`, `scripts/release-diagnostics-capture.mjs`, and `npm run release:diagnostics:capture`; linked the lane from deployment docs and the release-readiness checklist. Latest capture `docs/metrics/release-diagnostics-2026-05-09T22-28-56-079Z.json` is blocked because the local runtime was not listening during capture, `NEXUS_RELEASE_BASE_URL` is still absent from operator-local config, and Docker CLI is unavailable.

- [x] PHONE-ACCESS-COOLIFY-RUNBOOK — Make the phone-while-desktop-off path concrete without pretending localhost works after shutdown.
  - Current pass: document the VPS/Coolify + PWA lane, lock safe mobile env defaults, keep AI on free/BYOK cloud providers for this profile, keep state phone-local in v1, and add a validator so the lane stays discoverable.
  - Guardrail: no remote deployment claim without a real domain/VPS, no desktop Ollama dependency while the desktop is off, no high-risk route opening, and no cross-device state sync promise in v1.
  - Progress: added `docs/deployment/phone-access-coolify.md`, linked it from Coolify/web deployment docs and README, added phone-profile env guidance to `.env.example`, added `scripts/validate-phone-access-readiness.mjs`, wired `npm run phone:access:check`, and included it in `npm run verify`.

- [x] AI-OPERATOR-WORKFLOW-GAPS — Close the remaining useful assistant workflow gaps without loosening write gates.
  - Current pass: add a shared operator workflow state to the assistant dispatch layer, expose review-gated edit posture, task-plan, change-log, and skill/tool visibility across Home chat, HQ chronicle, and CommandBar, and prove the private RPG playfield stays untouched.
  - Guardrail: no paid-provider dependency, no mutation permission expansion, no bypass around `ProposedEditPanel`, and no edits to the private RPG playfield.
  - Progress: added `lib/assistantOperatorWorkflow.ts`, wired `operatorWorkflow` into `AssistantDispatchPlan` and `AssistantChatActionModel`, injected the `[OPERATOR WORKFLOW]` context block, added the shared `AssistantOperatorWorkflowPanel`, and rendered it in Home chat, HQ chronicle, and CommandBar with local-only focus actions for task plan, proposed edits, change log, and skills/tools. Follow-up chat audit found small liveliness prompts were still starting the full runtime path, so dispatch now provides a local fast reply for greetings, pings/tests, and acknowledgements across all three chat surfaces; HQ also now shows a slow local-runtime watchdog with a Provider Health recovery lane when Ollama/model execution stays active past the wait threshold. `npm run eval:agent-runtime:ci`, `npm run type-check`, `npm run verify`, `npx tsc --noEmit`, and `npm run runtime:proof:3100 -- --routes=/hq?focus=hq-chronicle,/command,/` passed.

- [x] RUNTIME-PROOF-RESILIENCE — Add a Node-only route proof command for the known `3100` launcher timeout and Windows Playwright `spawn EPERM` fallback lane.
  - Current pass: create `scripts/runtime-proof-check.mjs`, add `npm run runtime:proof:3100`, and prove key routes with HTTP status output so route health can be checked without browser workers.
  - Guardrail: no browser automation dependency, no new route behavior, no authentication loosening, and no claim that protected APIs are unhealthy solely because raw shell access returns 401/403.
  - Progress: added `scripts/runtime-proof-check.mjs` and `npm run runtime:proof:3100`. The command always checks `/api/health`, prints per-route status, accepts 2xx/3xx page routes, labels protected diagnostics as `AUTH-PROTECTED`, and exits nonzero on unhealthy routes. `npm run runtime:launch:3100` found the managed runtime already healthy; `npm run runtime:proof:3100 -- --routes=/,/hq?focus=hq-chronicle,/command,/resources,/vehicle,/internal/vehicle` returned 0 with `/api/health`, `/`, `/hq?focus=hq-chronicle`, `/command`, `/resources`, and `/internal/vehicle` healthy plus `/vehicle` redirecting with 307. Protected `/api/agent-health` and `/api/ollama/catalog` returned 401 `AUTH-PROTECTED` from raw shell as expected. `npm run type-check` and `npm run verify` passed.

- [x] VEHICLE-DRONE-SIM-REPLAY-V0 — Start the Homefront vehicle/drone readiness lane with simulated telemetry replay, a standard passive telemetry schema, and Vault flight-log / incident packaging only.
  - Current pass: add `VehicleReplayScenario`, deterministic replay frame generation, four simulation-only replay scenarios, visible replay controls in the vehicle telemetry panel, a Vault package preview, and a source validator.
  - Guardrail: no real drone/hardware control, no arming/steering/mode-switch authority, no flight-critical loop, and no public RPG positioning.
  - Progress: added `lib/vehicle/flightReplayScenarios.ts`, `makeSimulatedTelemetryFrame`, `data-testid="vehicle-replay-scenarios"` controls, `data-testid="vehicle-replay-vault-package"` preview, `scripts/validate-vehicle-readiness.mjs`, and `npm run vehicle:readiness:check`. `npm run vehicle:readiness:check`, `npm run type-check`, `npm run verify`, and `npm run runtime:proof:3100 -- --routes=/vehicle,/internal/vehicle` passed.

- [x] VEHICLE-BENCH-BRIDGE-READINESS-V1 — Close the F450 props-off checklist and passive telemetry bridge prep as review-first readiness only.
  - Current pass: add a shared bench/bridge readiness gate, expose it in the Vehicle Lab bench checklist, implement the protected local `/api/vehicle/telemetry` ingest route, create a dry-run passive bridge stub, document the first hardware-day bridge contract, and extend `vehicle:readiness:check`.
  - Guardrail: no real drone/hardware control, no arming/steering/mode-switch command path, no flight-critical loop, no public RPG positioning, and no claim that Nexus replaces Mission Planner/QGroundControl.
  - Progress: added `buildVehicleBenchBridgeReadiness`, `data-testid="vehicle-bench-bridge-readiness"`, `app/api/vehicle/telemetry/route.ts`, `scripts/vehicle-bridge-stub.mjs`, and `docs/deployment/vehicle-passive-bridge-stub.md`. `npm run vehicle:readiness:check`, `npm run type-check`, `npm run lint`, `npm run verify`, and `npm run runtime:proof:3100 -- --routes=/vehicle,/internal/vehicle,/api/vehicle/telemetry` passed; the raw-shell telemetry API check returns `401 AUTH-PROTECTED` as expected.

- [x] UNFINISHED-IDEAS-TRIAGE-QUEUE — Turn scattered unfinished or half-implemented ideas into one ranked local completion queue.
  - Current pass: create a compact audit ledger under `docs/plans/`, classify the main open/stale/blocked ideas, separate active open-ready work from blocked/manual watchlist items in `## Next Up`, and reconcile proof-held task state without changing product scope.
  - Guardrail: no new top-level routes, no public RPG positioning, no real drone/hardware control, no vendored external repo code, and no release-proof claims while staged host/Docker prerequisites are missing.
  - Progress: added `docs/plans/unfinished-ideas-triage-2026-05-07.md`, ranked the completion queue, classified key ideas as `complete`, `open-ready`, `blocked-external`, `stale/superseded`, or `rejected`, moved `MW6V-REAL-ASSET-INTAKE` out of the active queue into the blocked watchlist, and closed the stale MW6FR1 checkbox by its accepted fallback proof lane.

- [x] HOMEFRONT-VISUAL-PARITY-AUTH-SHELL — Carry the premium Homefront landing visual language across protected non-RPG routes through shared shell primitives and route-aware visual contracts.
  - Current pass: add `HomefrontVisualSurfaceSpec`, mount shared visual primitives in the shell, bring secondary/internal routes onto the same premium system where they still look old, extend route/HQ coverage, and prove the RPG playfield remains isolated.
  - Guardrail: do not edit `components/home/arpg/**`, do not restyle or reposition the private RPG playfield, do not add remote media or paid-provider dependencies, and keep motion reduced-motion safe.
  - Progress: added `lib/homefrontVisualParity.ts` as the shared visual contract registry, mounted shared Homefront visual primitives through the authenticated shell, converted `/internal/iot` onto the shell system, added compact HQ chrome parity outside the ARPG playfield, and extended route/HQ coverage for visual primitives, active route tabs, and ARPG isolation. `npm run build`, `npm run verify`, `npm run eval:agent-runtime:ci`, and `npx tsc --noEmit` passed; the managed `3100` launcher timed out in its known wrapper path but left a healthy runtime, and authenticated HTTP checks returned 200 with visual primitives present for `/command`, `/intel`, `/alpha`, `/cyber`, `/recon`, `/vault`, `/resources`, `/labs/security`, `/internal/skills`, `/internal/vehicle`, `/internal/iot`, and `/hq?focus=hq-chronicle`. Playwright/Edge browser automation remains locally blocked by Windows `spawn EPERM` / access-denied browser launch, so runtime acceptance used the existing HTTP/source proof fallback.

- [x] AI-CHAT-RELIABILITY-UX-UPGRADE — Finish the assistant upgrade by making route choices, workspace actions, recovery actions, live readiness, and runtime proof visible across HQ chronicle, Home chat, and CommandBar.
  - Current pass: add a shared chat action/recovery/readiness model, render route-choice/action buttons instead of text-only prompts, merge HQ readiness with live diagnostics, add focused planner/UX coverage, and prove the `3100` runtime before marking chat healthy.
  - Guardrail: do not touch the private RPG world, do not add paid-provider dependency, do not loosen mutate/exec approval gates, and keep protected diagnostics behind the local session boundary.
  - Progress: added `lib/assistantChatActions.ts` as the shared action/recovery model, extended the dispatch planner with `actionModel` and forced answer/route modes, wired Home chat, HQ chronicle, and CommandBar to render the same answer/open/retry/provider/reset actions, updated CommandBar route actions to sync the shell tab store before navigation, upgraded HQ readiness to merge planner state with `/api/auth-diagnostics`, `/api/ollama/catalog`, and `/api/agent-health`, and expanded `eval:agent-runtime:ci` coverage for planner modes, shared action affordances, diagnostics, and structured recovery. `npm run type-check`, `npx tsc --noEmit`, `npm run eval:agent-runtime:ci`, and `npm run verify` passed. `npm run runtime:launch:3100` timed out in the known wrapper path after starting the runtime; follow-up HTTP checks returned 200 for `/api/health`, `/hq?focus=hq-chronicle`, and `/command?focus=agent-health`, while protected `/api/agent-health` and `/api/ollama/catalog` correctly returned 401 from raw shell without a browser session cookie.

- [x] AI-CHAT-CAPABILITY-UPGRADE — Make the assistant function as a tab-aware operator layer across HQ chronicle, Home chat, and the floating CommandBar while preserving local-first/free-first behavior and review-gated tools.
  - Current pass: add a shared assistant dispatch planner, fix `/api/ai` and `/api/tools` route-policy blockers so handlers can enforce their own local/tool safety, restore missing `/api/agent-health` and `/api/ollama/catalog` diagnostics, add HQ assistant readiness, and replace generic chat failures with actionable recovery copy.
  - Guardrail: do not change the private RPG world, do not add paid-provider dependency, do not loosen mutate/exec approval gates, and do not bypass existing session/network/connector policies.
  - Progress: added `lib/assistantDispatch.ts` as the shared tab-aware planner, wired Home chat, HQ chronicle, and CommandBar through it, changed `/api/ai` and `/api/tools` route policy to authenticated `local_only` so handlers can enforce provider/tool gates, restored `/api/agent-health` and `/api/ollama/catalog`, added HQ assistant readiness chips, and upgraded chat failure copy for Ollama/session/network/high-risk/connector/isolation failures. `npm run type-check`, `npx tsc --noEmit`, `npm run eval:agent-runtime:ci`, and `npm run verify` passed. Live checks returned 200 for `/api/health`, `/hq?focus=hq-chronicle`, and `/command?focus=agent-health`; `/api/agent-health` and `/api/ollama/catalog` correctly returned 401 from this shell because no `NEXUS_TOKEN` or browser session cookie is exported.

- [x] HOMEFRONT-LANDING-SCENE-BOX-REMOVAL — Remove the oversized scenario explanation box from the Homefront hero so the animal/rodent and breach idea is carried by the background home/drone scene instead of a floating card.
  - Current pass: delete the `landing-scenario-reel` panel, its scenario cards, and its control strip from the first viewport while preserving the approved home image, patrol drone, left-click drone signal, headline, CTA, and no-automatic-emergency-call boundary.
  - Guardrail: no asset replacement, no RPG changes, no restored heavy overlays, and no new explanatory box in the hero.
  - Progress: removed `GUARDIAN_SCENARIO_REEL`, the scenario state handlers, and the visible card/control JSX from `HomefrontCommandBackdrop`; updated focused landing coverage to assert those scenario panel test IDs are absent while the hero copy still carries the animal/rodent false-alarm language. `npx tsc --noEmit`, `npm run lint`, `npm run verify`, and `npm run build` passed; the first build attempt hit the known Windows `.next` lock until `npm run runtime:stop:3100`, then the clean retry passed. `npm run runtime:launch:3100` still timed out in its known wrapper path, but `/api/health` returned 200 on `127.0.0.1:3100`.

- [x] HOMEFRONT-LANDING-DRONE-CLICK-SIGNAL — Add a small left-click interaction to the public Homefront hero so the approved drone responds with a restrained signal acknowledgement.
  - Current pass: add pointer handling to the hero backdrop, trigger a short drone acknowledgement state, render one small click-position pulse, and update focused landing coverage.
  - Guardrail: keep the photoreal home and drone assets unchanged; do not restore heavy effects, video overlays, scan grids, beams, marker chips, or public RPG positioning.
  - Progress: added a left-click handler to `HomefrontCommandBackdrop` that ignores button/link clicks, records the click position, briefly sets the drone into `data-signal-state="acknowledged"`, and renders a single fading pulse at the click point. The drone keeps its existing patrol route and approved `homefront-drone-patrol.webp` asset, while the home keeps `homefront-guardian-hero.webp`. Added `specs/features/homefront-landing-drone-click-signal.md` and focused landing coverage for the acknowledgement state, pulse, and continued absence of the removed hero effect layers. `npx tsc --noEmit`, `npm run lint`, `npm run verify`, and `npm run build` passed; the first build attempt hit the known Windows `.next` lock until `npm run runtime:stop:3100`, then the clean retry passed. `npm run runtime:launch:3100` still timed out in its known wrapper path, but `/api/health` returned 200 on `127.0.0.1:3100`.

- [x] HOMEFRONT-LANDING-HERO-EFFECT-CLEANUP — Keep the approved photoreal Homefront home and patrol drone in place while removing the extra hero special-effects layer that was making the scene feel noisy.
  - Current pass: strip only the visual effects from the first-viewport guardian backdrop; preserve the home image, drone asset, patrol route, headline, CTA, scenario boundary copy, and private RPG boundary.
  - Guardrail: no asset replacement, no copy rewrite, no RPG changes, no remote media dependency, no autonomous harm, face recognition, citizen scoring, police/military framing, or automatic emergency-call implication.
  - Progress: removed the hero-only capability video overlay, command-motion/glass-sweep layer, scan bands, pulsing rings, marker chips, warning beam, and tiny camera-ready indicator from `components/landing/LandingPage.tsx`. Converted the static home backdrop to `next/image` while keeping `homefront-guardian-hero.webp`, kept `homefront-drone-patrol.webp` as the separate moving drone, and updated focused landing coverage to assert the effect layers stay absent while the home/drone assets remain mounted. `npx tsc --noEmit`, `npm run lint`, `npm run verify`, and `npm run build` passed; the first build attempt hit the known Windows `.next` lock until `npm run runtime:stop:3100`, then the clean retry passed. `npm run runtime:launch:3100` still timed out in its known wrapper path, but `/api/health`, `/`, `/images/homefront-guardian-hero.webp`, and `/images/homefront-drone-patrol.webp` returned 200 on `127.0.0.1:3100`.

- [x] HOMEFRONT-LANDING-PHOTOREAL-HOME-PERIMETER — Replace the remaining outline-style Homefront house backdrop with a photoreal premium home/perimeter source that the separate drone object can patrol around.
  - Current pass: generate a real residential night-perimeter image with dark copy space, save it as a local source, make the Homefront media generator prefer it for the active hero and capability reel, and keep the drone as an independent hover object.
  - Guardrail: no RPG changes, no remote media dependency, no baked HUD/text, no people/animals, no autonomous harm, face recognition, citizen scoring, police/military framing, or automatic emergency-call implication.
  - Progress: generated and copied `public/images/homefront-guardian-home-photoreal-source.png`, kept a preview derivative at `public/images/homefront-guardian-home-photoreal.webp`, updated `scripts/generate-homefront-guardian-hero.mjs` so future runs prefer that home source over the fallback SVG outline, and regenerated `public/images/homefront-guardian-hero.webp`, `public/images/homefront-guardian-hero-fallback.webp`, `public/images/homefront-drone-patrol-fallback.webp`, `public/images/homefront-drone-patrol.webp`, and `public/videos/homefront-capability-reel.webm`. Direct asset inspection confirms the active hero now uses a photoreal modern home with no baked text, no people, no animals, and the drone remains a separate generated object. `npm run type-check`, `npm run lint`, `npm run verify`, and `npm run build` passed; live `3100` checks returned 200 for `/api/health`, `/`, `/images/homefront-guardian-hero.webp`, `/images/homefront-guardian-home-photoreal.webp`, `/images/homefront-drone-patrol.webp`, and `/videos/homefront-capability-reel.webm`. `npx playwright screenshot` remains locally blocked by Windows worker `spawn EPERM`, so visual proof used direct asset inspection plus route/asset HTTP checks.

- [x] HOMEFRONT-LANDING-PREMIUM-DRONE-OBJECT-V2 — Continue replacing the old/abstract Homefront drone image with a photoreal premium patrol-drone object that no longer reads childlike or illustrated in the landing hero.
  - Current pass: generate a true photoreal drone option, remove the chroma-key background, add it as a local project source, make the generator prefer that source, then regenerate the active WebP and capability reel from the same local media pipeline.
  - Guardrail: keep this as a visual asset-quality pass only; no RPG changes, no remote media dependency, no autonomous harm, face recognition, citizen scoring, police/military framing, or automatic emergency-call implication.
  - Progress: generated a photoreal graphite patrol drone with carbon arms, real rotors, landing skids, and a camera/gimbal stack; removed the chroma-key matte into `public/images/homefront-drone-patrol-photoreal-source.png`; kept a preview derivative at `public/images/homefront-drone-patrol-photoreal.webp`; updated `scripts/generate-homefront-guardian-hero.mjs` so future runs prefer the photoreal source for the fallback drone; and regenerated `public/images/homefront-drone-patrol-fallback.webp`, `public/images/homefront-drone-patrol.webp`, `public/images/homefront-guardian-hero.webp`, `public/images/homefront-guardian-hero-fallback.webp`, and `public/videos/homefront-capability-reel.webm`. `npm run type-check`, `npm run lint`, `npm run verify`, and `npm run build` passed; live `3100` checks returned 200 for `/api/health`, `/`, `/images/homefront-drone-patrol.webp`, and `/videos/homefront-capability-reel.webm`.

- [x] HOMEFRONT-LANDING-PREMIUM-DRONE-OBJECT — Replace the remaining abstract Homefront drone object with a premium, recognizable patrol-drone raster that stays in the local media generation pipeline.
  - Current pass: upgrade the scripted drone fallback from a symbolic top-down glyph into a camera-forward patrol drone with rotors, body shading, lens/gimbal detail, landing skids, status lights, and motion streaks; regenerate the active WebP and capability reel; and tune the landing/shell frame sizes so the object reads as a real product capability.
  - Guardrail: keep the Homefront scene review-first and nonviolent; no autonomous harm, face recognition, citizen scoring, police/military framing, automatic emergency calls, remote media dependency, or public RPG positioning.
  - Progress: updated `scripts/generate-homefront-guardian-hero.mjs` to emit the premium drone source, regenerated `public/images/homefront-drone-patrol-fallback.webp`, `public/images/homefront-drone-patrol.webp`, `public/images/homefront-guardian-hero.webp`, and `public/videos/homefront-capability-reel.webm`, widened the landing hero and capability-spine drone frames, and widened the shared shell drone backdrop. `npm run type-check`, `npm run lint`, `npm run verify`, and `npm run build` passed; live `3100` checks returned 200 for `/api/health`, `/`, `/images/homefront-drone-patrol.webp`, and `/videos/homefront-capability-reel.webm`; browser proof confirmed the landing drone loads `/images/homefront-drone-patrol.webp` at 790x445 natural size, renders as a 362x247 desktop object and 205x136 mobile object, the capability video is ready, runtime overlay is absent, horizontal overflow is zero, and console/page/local request errors are empty.

- [x] HOMEFRONT-LANDING-CLEAN-HERO-MEDIA — Remove baked background words and the lingering dog/animal silhouette from the Homefront landing hero.
  - Current pass: regenerate the local Homefront hero image and capability reel without readable HUD text, pet-safe copy, dog/animal silhouettes, or decorative text rails behind the main headline.
  - Guardrail: the scenario text belongs in the playable React scene controls, not baked into media; keep the no-automatic-emergency-call boundary in normal UI copy.
  - Progress: regenerated `public/images/homefront-guardian-hero.webp`, `public/images/homefront-guardian-hero-fallback.webp`, `public/images/homefront-drone-patrol-fallback.webp`, `public/images/homefront-drone-patrol.webp`, and `public/videos/homefront-capability-reel.webm` from local scripts with no baked pet/dog/animal text and no visible dog/animal silhouette. Removed the first-viewport decorative response rail and scenario rail, converted the camera-ready label into a non-text indicator, moved the playable scenario surface to the right visual field, and lowered the hero video opacity so background media no longer competes with foreground copy. `npm run type-check`, `npm run lint`, `npm run verify`, and `npm run build` passed; live `3100` checks returned 200 for `/api/health`, `/`, `/hq?focus=hq-chronicle`, and `/videos/homefront-capability-reel.webm`; browser proof confirmed both scenario controls still work, the removed text rails are absent, known baked-text strings are absent from the DOM, the scenario panel no longer overlaps the headline or CTA, public RPG text is absent, runtime overlay is absent, horizontal overflow is zero, and console/page/local request errors are empty.

- [x] HOMEFRONT-LANDING-PLAYABLE-SCENARIO-BACKGROUND — Replace the passive dog/pet-feeling hero cue with a playable background scenario surface that runs Mario's two authored Homefront examples: animal/rodent false alarm cleared, then unwanted person breach deterred by operator-approved warning and proof recording.
  - Current pass: make the hero background scenario-selectable and replayable, remove vague dog/pet emphasis, and keep the emergency-call boundary explicit.
  - Guardrail: no actual 911 call, no autonomous harm, no face recognition, no citizen scoring, no police/military framing, no remote media dependency, and no public RPG positioning.
  - Progress: replaced the vague pet/dog-feeling hero cue with a playable two-scene background controller. Scene 01 now runs an animal/rodent false-alarm flow with drone inspection, false-alarm classification, and record-only outcome; Scene 02 now runs an unwanted-person perimeter breach with drone follow, operator-approved warning script, person leaving, proof recording, and explicit no-automatic-emergency-call copy. The scenario controls can play false alarm, play breach warning, and replay the active scene. Focused coverage now asserts the playable controls and both authored flows. `npm run type-check`, `npm run lint`, `npm run verify`, and `npm run build` passed; live `3100` checks returned 200 for `/api/health`, `/`, `/hq?focus=hq-chronicle`, and `/videos/homefront-capability-reel.webm`; browser proof confirmed both controls click, both scenes swap/replay, no-call/911 boundary copy is present during breach, public RPG text is absent, no runtime overlay is present, horizontal overflow is zero, and console/page/local request errors are empty. Focused Playwright runner remains environment-blocked before execution by Windows worker `spawn EPERM`.

- [x] HOMEFRONT-LANDING-SCENARIO-REEL — Turn the public hero's pet/perimeter idea into an animated two-scene surveillance reel: harmless animal-trigger cleared, then unwanted perimeter breach reviewed, drone warning issued, subject leaves, and proof recorded.
  - Current pass: add a cinematic animated scenario layer to the landing hero/background using local media and Framer Motion, keep it readable as product behavior, and update focused landing coverage.
  - Guardrail: deterrence and operator approval only; no autonomous harm, face recognition, citizen scoring, police/military framing, automatic emergency call, remote media dependency, or public RPG positioning.
  - Progress: added a two-scene animated hero scenario reel with separate animal-clear and perimeter-breach cards, moving animal and unwanted-entry markers, drone warning beam, scenario rail, and response boundary. The breach scene can show an operator-approved warning script that says the operator may contact 911, but the landing now explicitly states that Homefront does not place emergency calls automatically and the breach card says no emergency call is placed by Homefront. Focused landing coverage now asserts the scenario reel, animal card, breach card, warning-script text, and no-call boundary. `npm run type-check`, `npm run lint`, `npm run verify`, and `npm run build` passed; live `3100` checks returned 200 for `/api/health`, `/`, `/hq?focus=hq-chronicle`, and `/videos/homefront-capability-reel.webm`; browser proof confirmed the scenario markers/cards/beam are visible, the no-call boundary is present, public RPG text is absent, no runtime overlay is present, horizontal overflow is zero, and console/page/local request errors are empty. Focused Playwright remains environment-blocked before execution by Windows worker `spawn EPERM`.

- [x] HOMEFRONT-LANDING-CAPABILITY-SPINE — Expand the public Homefront landing with a premium core-functions layer that shows what the system watches, how it reasons, what it recommends, and what gets recorded before an operator acts.
  - Current pass: add an interactive capability spine after Guardian Protocol, cover Perimeter/Command/Intel/Cyber/Recon/Vault without exposing the private RPG lane, and make the thought process visible through the existing local Homefront media.
  - Guardrail: keep surveillance/drone language review-first and nonviolent; no autonomous harm, face recognition, citizen scoring, police/military framing, remote media dependency, or public RPG positioning.
  - Progress: added the new interactive Capability Spine section between Guardian Protocol and Doctrine with switchable Perimeter/Command/Intel/Cyber/Recon/Vault panels, local capability-reel media, drone overlay, "How Homefront thinks" chain, and explicit operator-owned/no-autonomous-escalation boundary copy. Focused landing coverage now asserts the section, switcher, and thought chain. `npm run type-check`, `npm run lint`, `npm run verify`, and `npm run build` passed; live `3100` checks returned 200 for `/api/health`, `/`, `/hq?focus=hq-chronicle`, and `/videos/homefront-capability-reel.webm`; browser proof confirmed the switcher works, no public RPG text appears, no runtime overlay is present, no horizontal overflow exists, and console/page/local request errors are empty. The focused Playwright test is still environment-blocked before execution by Windows worker `spawn EPERM`.

- [x] HOMEFRONT-LANDING-CAPABILITY-MEDIA-REEL — Upgrade the public Homefront hero from a still backdrop into a premium capability showcase with photoreal local home/drone media plus motion/video layers that demonstrate patrol, pet-safe classification, operator approval, and incident memory.
  - Current pass: use local generated photoreal assets, build a capability reel/background loop, wire it into the landing and shared Homefront shell where it fits, and keep the private RPG world untouched.
  - Guardrail: no autonomous harm, face recognition, citizen scoring, police/military framing, remote media dependency, or RPG public positioning.
  - Progress: generated photoreal local Homefront media from approved AI-created sources into `public/images/homefront-guardian-hero.webp`, `public/images/homefront-drone-patrol.webp`, and `public/videos/homefront-capability-reel.webm`; added `scripts/generate-homefront-capability-media.mjs` so the reel can be regenerated locally without FFmpeg, Playwright, or remote video; wired the WebM into the public hero background, the Guardian Protocol media panel, and the default/HQ shared shell backdrop while keeping Aether Reliquary/RPG surfaces private and untouched. `npm run type-check`, `npm run verify`, and `npm run build` passed. Live `3100` checks returned 200 for `/api/health`, `/`, `/hq`, the WebM, the hero image, and the drone image; the public HTML contains the capability video/image sources, keeps old agency copy absent, and keeps public RPG copy absent. Focused Playwright remains locally blocked by Windows worker `spawn EPERM`.

- [x] HOMEFRONT-LANDING-REAL-DRONE-HOME-VISUAL — Replace the abstract drone glyph and line-art house feel with concrete local raster assets: a recognizable premium patrol drone and a more believable night home/perimeter scene.
  - Current pass: generate a dedicated drone image asset, regenerate the home hero with stronger real-world house/readability cues, swap the CSS drone marker for the image asset, and verify in-browser.
  - Guardrail: keep the scene review-first and non-violent; no face recognition, citizen scoring, autonomous attack language, remote media, or public RPG positioning.
  - Progress: generated `public/images/homefront-drone-patrol.webp` and regenerated `public/images/homefront-guardian-hero.webp`; replaced the landing's abstract CSS drone marker with the drone image; updated root social metadata to the Homefront perimeter scene; and wired the same local home/perimeter scene plus animated drone layer into the default/HQ shell backdrop without touching the private RPG playfield. `npm run type-check`, `npm run verify`, and `npm run build` passed. Live `3100` checks returned 200 for `/api/health`, `/`, and `/hq`; Node REPL browser proof confirmed the landing drone is an IMG loading `/images/homefront-drone-patrol.webp`, the hero image loads at 1920px, drone motion changes over time, old agency copy stays gone, there is no runtime overlay, no horizontal overflow, no console/page errors, and no failed local requests. Terminal Playwright remains polluted by stale `.worktrees` duplicate Playwright installs, and protected HQ browser proof could not be cookie-seeded from this shell because the local auth token is configured in the runtime but not exported to the Codex shell.

- [x] HOMEFRONT-LANDING-GUARDIAN-PROTOCOL — Carry the new cinematic perimeter idea below the hero as a concrete operating sequence so the page shows how existing cameras, local AI, operator approval, and proof memory work together.
  - Current pass: add a visual protocol section directly after the hero, keep it premium and sparse, make the response ladder feel live, and update focused landing coverage.
  - Guardrail: no autonomous harm, facial recognition, citizen scoring, fake deployment claims, remote media dependency, or public RPG positioning.
  - Progress: added the new Guardian Protocol section immediately after the hero, reusing the local cinematic perimeter image as a low-opacity motion layer and adding live perimeter feeds, pet-safe classification, unknown-approach review, operator-owned response posture, and a decision-trail timeline. Focused landing coverage now asserts the protocol section and operator-owned language. `npm run type-check`, `npm run verify`, `npm run build`, live `3100` `/api/health` and `/` checks, and Node REPL browser proof passed; the browser proof confirmed the section is visible, the hero image still animates, old agency copy stayed gone, and there were zero console errors, page errors, failed local requests, runtime overlay, or horizontal overflow.

- [x] HOMEFRONT-LANDING-CINEMATIC-GUARDIAN-REWORK — Replace the amateur-feeling line-art guardian hero with a media-grade cinematic surveillance scene that feels like a real Homefront product moment.
  - Current pass: create a local raster hero asset, move away from debug-style SVG panels, keep overlays sparse and premium, preserve the operator-approved response story, and verify the first viewport in-browser.
  - Guardrail: do not use unrelated city/agency/nature imagery, do not add remote media dependency, do not expose the private `/hq` RPG lane, and do not imply autonomous harm, facial recognition, or citizen scoring.
  - Progress: regenerated the local hero raster as a darker cinematic property-perimeter still, removed the hidden SVG map/debug camera labels from the hero component, added a moving patrol beam over the image-led scene, and reshaped the response/event readouts into non-clipping premium HUD rails. `npm run type-check`, `npm run verify`, `npm run build` retry, live `3100` `/api/health` and `/` checks, and Node REPL browser proof passed; the browser proof confirmed the 1920x1080 local hero image mounted, drone/image transforms moved, response and event rails stayed inside the 1440x900 viewport, old agency copy stayed gone, and there were zero console errors, page errors, failed local requests, runtime overlay, or horizontal overflow. Windows `spawn EPERM` still appeared on first build/browser-shell attempts, then the documented retry/Node REPL proof path passed.

- [x] HOMEFRONT-LANDING-GUARDIAN-SCENARIO — Reframe the public hero from abstract command-room motion into a Homefront guardian scenario: existing cameras, future patrol drones, local AI classification, pet-safe/non-threat handling, operator-approved escalation, and incident memory.
  - Current pass: replace the hero backdrop and headline copy with a cinematic property-perimeter scene that shows camera cones, drone patrol motion, event classification, and a progressive response ladder without adding remote media or touching the private `/hq` RPG lane.
  - Guardrail: no autonomous harm, facial recognition, citizen scoring, or public RPG positioning; the response ladder must stay situational, review-first, and useful for preexisting camera systems.
  - Progress: replaced the public hero with the Homefront guardian scenario: "Homefront Guards The Perimeter" copy, existing-camera/future-patrol positioning, animated property perimeter, camera coverage cones, moving drone patrol, event posture, RTSP/ONVIF readiness, and an operator-approved response ladder. Added the focused feature spec and landing coverage hooks while keeping `/hq` and the private RPG lane untouched. `npm run type-check`, `npm run verify`, `npm run build`, live `3100` `/api/health` and `/` checks, and Node REPL browser proof passed; the browser proof confirmed the guardian scene mounted, the drone transform/opacity changed over time, the old agency hero copy stayed gone, and there were no console errors, page errors, failed local requests, or runtime overlay. The first post-change `npm run build` retry briefly hit Windows `spawn EPERM`, then a clean retry passed; `npm run runtime:launch:3100` still timed out in the known wait path while leaving a healthy port.

- [x] HOMEFRONT-LANDING-CINEMATIC-MOTION — Restore premium liveness to the new Homefront command-room hero so it feels animated and alive instead of a static line-art backdrop.
  - Current pass: keep the local command-room concept, add layered cinematic movement, moving light/radar sweeps, animated route traces, node activity, and live readout motion while honoring reduced-motion mode.
  - Guardrail: do not reintroduce unrelated nature/agency video, do not add new remote media dependencies, and do not expose or reposition the private `/hq` RPG lane.
  - Progress: added premium motion back into the local Homefront hero with drifting command/map plates, glass light sweeps, radar-style rotation, vertical scan wash, animated SVG route traces, pulsing command nodes, and live proof-readout activity. Focused landing coverage now asserts the animated command-motion layer while reduced-motion coverage keeps the global reduced profile and layout constraints. `npm run type-check`, `npm run verify`, `npm run build`, live `3100` health and `/` checks, local hero asset checks, and Node REPL browser proof passed; the browser proof sampled moving layer transforms/opacity over time and confirmed no console errors, page errors, failed local requests, runtime overlay, or old remote hero video.

- [x] HOMEFRONT-LANDING-COMMAND-BACKGROUND — Replace the public landing hero's old cinematic nature/agency video with a Homefront-native command-room background.
  - Current pass: remove the unrelated botanical hero video, build a full-bleed local-first operations scene from existing project route/command visuals, keep the public story focused on Homefront command intelligence, and verify with the runtime finalize loop.
  - Guardrail: do not expose or reposition the private `/hq` RPG lane; do not add new remote media dependencies; keep the hero readable and product-aligned.
  - Progress: replaced the public landing hero's old remote botanical/video backdrop with a Homefront-native command-room scene built from local tactical/map assets, route nodes, proof readouts, scan motion, and reduced-motion-safe static layers. Updated focused landing coverage for the new backdrop and reduced hero video count. `npm run type-check`, `npm run verify`, `npm run build`, managed `3100` runtime checks for `/api/health`, `/`, `/resources?view=playbooks&playbook=runtime-finalize-loop`, local hero asset checks, and a Node REPL browser probe passed with no console errors, page errors, failed local requests, or unhandled rejections. Terminal Playwright still hits the known Windows `spawn EPERM`, so Node REPL browser proof was used for visual/runtime acceptance.

- [x] HOMEFRONT-RUNTIME-FINALIZE-PLAYBOOK — Turn the newly proven bug-check/finalize workflow into a visible protected Resources lane.
  - Current pass: add a native engineering playbook and compact Resources rail for the update proof loop so future local work can move from implementation to type/verify/build/runtime/browser-error checks before push or USB transfer.
  - Guardrail: keep this as workflow guidance inside the non-RPG Homefront shell; do not touch `/hq` RPG playfield or read local auth secrets just to force a protected browser session.
  - Progress: added the `runtime-finalize-loop` playbook with type-check, verify, clean build, direct `3100` restart, route checks, browser console/pageerror/requestfailed/unhandled-rejection probe, and handoff checks; added aliases for finalize/quality-finalize; surfaced the lane in the Resources support rail; pointed COMMAND/Resources action guidance at the loop; and added focused tab-surface coverage for the exact playbook URL. `npm run type-check`, `npm run verify`, `npm run build`, live `3100` health, and HTTP checks for `/api/health`, `/resources?view=playbooks&playbook=runtime-finalize-loop`, `/resources?view=playbooks`, `/command?focus=agent-health`, and `/` passed. Headless browser probes were quiet; protected route content redirected to the public access form because no auth token was exported in this shell.

- [x] HOMEFRONT-RUNTIME-FINALIZE-LOOP — Make visual/runtime updates finish with a repeatable bug-finding lane before calling them done.
  - Current pass: diagnose the Next dev `[object Event]` overlay after premium landing/media updates, fix the underlying resource/runtime issues, and record the proof loop so future updates check browser errors instead of relying on HTTP 200 alone.
  - Progress: aligned CSP with the premium landing's real font/video hosts, including Google Fonts, CloudFront video, Mux HLS, and Mux redirect hosts; suppressed the intentional `<html>` boot-attribute hydration mismatch; restarted the direct `3100` runtime; confirmed 200 responses for `/api/health`, `/`, `/hq?focus=hq-chronicle`, `/command`, `/intel?view=news`, and `/resources?view=sources`; and ran headless browser probes with zero console errors, page errors, failed requests, or unhandled rejections on the public/redirect route path. Clean build proof passed after stopping the live dev runtime to avoid `.next` collision.

- [x] HOMEFRONT-INFORMATION-ACTION-CONTROL — Make the protected website manage route purpose, information priority, and next actions more efficiently across non-RPG surfaces.
  - Current pass: add a compact shared purpose/action control layer backed by existing surface capability data so each route explains what it is for, what belongs there, what to do next, and which signal posture matters without adding a new dashboard or touching the private RPG playfield.
  - Guardrail: keep `/hq` RPG world visually and structurally unchanged; do not turn every surface into another summary dashboard; keep actions bounded to existing routes and exact-session links.
  - Progress: added the shared `homefront-action-control` rail to authenticated non-HQ shell routes using `lib/surfaceCapabilities.ts` for route mission, best-use signals, jump actions, and subsection links. Updated focused tab-surface coverage while leaving `/hq` and the RPG playfield untouched. `npm run type-check`, `npm run verify`, `npm run build`, and live `3100` checks for `/api/health`, `/hq?focus=hq-chronicle`, `/command`, `/intel?view=news`, and `/resources?view=sources` passed after restarting the direct dev runtime through the persistent Node REPL; cookie-authenticated HTML probing was unavailable because `NEXUS_TOKEN` was not exported in this shell.

- [x] HOMEFRONT-PREMIUM-SHELL-COHESION — Carry the premium live landing visual system into the authenticated website shell itself while leaving the private RPG world/game box unchanged.
  - Current pass: make the logged-in shell feel like the same liquid-glass/editorial Homefront system as the landing page, reduce the older tactical-dashboard stiffness in shared chrome, and fix the noisy shell bootstrap recovery overlay when the hydrated page is actually healthy.
  - Guardrail: do not reframe Aether Reliquary as public product positioning and do not restyle the RPG playfield/game box in this pass.
  - Progress: applied the premium Homefront visual layer to authenticated shared shell chrome, top navigation, route heads, non-HQ work panels, source-intake rails, and bootstrap recovery behavior while leaving `/hq` and the RPG playfield classes untouched. Fixed `/resources?view=sources` canonical routing so Source Intelligence no longer normalizes back to Finder. Added `npm run dev:direct:3100` as a foreground Windows fallback for local proof when the Next CLI worker fork hits `spawn EPERM`. `npm run type-check` and `npm run verify` passed; foreground direct dev proof returned 200 for `/api/health`, `/resources?view=sources`, and `/intel?view=news`, while Playwright remains locally blocked by Windows `spawn EPERM`.

- [x] REPO-HYGIENE-BRANCH-AUDIT — Classify the crowded local/remote branch set before any cleanup so we know which refs are active, merged, archive-only, worktree-bound, or safe-delete candidates.
  - Current pass: document why the branches exist, list what can be removed only after worktree/PR checks, and keep cleanup commands review-gated because branch deletion can discard rollback context.
  - Progress: added `docs/repo-hygiene/branch-cleanup-audit-2026-05-03.md` with current branch/worktree classification, local merged candidates, remote merged candidates, cleanup order, and review-gated commands. Added the read-only `npm run repo:branches:audit` PowerShell report command so the same branch categories can be refreshed without deleting refs. No branch deletion was performed because the current branch has uncommitted work, several refs are worktree-bound, and remote cleanup should follow a fresh `git fetch --prune` plus GitHub PR check from Mario's working Git shell.
- [x] HOMEFRONT-SOURCE-INTELLIGENCE-PASS — Adapt the new external resource batch into Homefront's public and protected source-intelligence language without vendoring code, adding offensive automation, or exposing the private game lane.
  - Current pass: complete the source-intelligence work as a native Resources chamber backed by one shared registry, so the public landing, authenticated shell rail, and operator workbench all show the same intake lanes, guardrails, and active queue without duplicating copy.
  - Guardrail: treat outside repos as bounded references, keep RECON/CYBER passive-first, keep private art tooling out of public product positioning, and do not add unverified runtime dependencies.
  - Progress: completed the Homefront source-intelligence pass by consolidating landing/shell/Resources copy into `lib/homefrontSourceIntelligence.ts`, adding the native `/resources?view=sources` workbench lane, wiring the chamber into Resources navigation/specs/capability maps, and extending focused tab-surface coverage. `npm run type-check`, `npm run verify`, `npm run runtime:launch:3100` health follow-up, and `NEXUS_RELEASE_BASE_URL=http://127.0.0.1:3100 npm run route:integrity` passed; direct protected HTML only shows the auth shell before client hydration, so visual acceptance should use the in-app browser or Playwright when Windows workers are available.
- [x] HOMEFRONT-SYSTEM-COHESION-PASS — Adapt the premium Homefront vision across the public landing and authenticated non-HQ shell so the product feels like one professional local-first command system before any Git push.
  - Current pass: add a shared operating-contract layer, tighten shell/landing copy around the same surface model, preserve the private `/hq` RPG boundary, and keep all work local for USB transfer readiness.
  - Guardrail: improve cohesion and professionalism without turning surfaces into sales pages, adding fake proof, or changing RPG placement.
  - Progress: added the shared Homefront operating-contract rail to authenticated non-HQ shell surfaces, mirrored the same command-contract model on the public landing, updated focused landing/tab regression expectations, and wrote the feature spec. `npm run type-check`, `npm run verify`, `npm run build`, live `3100` health, public `/` contract HTML, authenticated `/command` contract HTML via local token exchange, and `NEXUS_RELEASE_BASE_URL=http://127.0.0.1:3100 npm run route:integrity` passed; focused Playwright browser proof remains locally blocked by Windows worker `spawn EPERM`.
- [x] LANDING-STORY-DEPTH-PASS — Fill the public Homefront landing space with deeper, current product story instead of stale filler: add command-system architecture, agent bench, operator workflow, and stronger proof/boundary language while keeping `/hq` and RPG World private.
  - Current pass: deepen the existing premium landing with useful project-specific sections, update focused landing regression coverage, and verify with type/quality/build plus live route proof before pushing through the PR path.
  - Guardrail: keep the landing as Homefront command intelligence, not a salesy agency page, invented customer proof, or public RPG positioning.
  - Progress: added a richer public landing story pass with expanded live-command rows, system architecture, build ledger, broader real-surface previews, named agent bench, operator flow, focused landing regression expectations, and a feature spec. `npm run type-check`, `npm run verify`, `npm run build`, live `3100` health, public `/` HTML checks, and `NEXUS_RELEASE_BASE_URL=http://127.0.0.1:3100 npm run route:integrity` passed; the focused Playwright landing spec remains locally blocked by Windows worker `spawn EPERM`.
- [x] PREMIUM-LIVE-RPG-VISUAL-EXPANSION — Expand the current premium Homefront live vision across authenticated non-HQ shell surfaces, and add an Aether Reliquary visual replacement lane that keeps rejected/outdated prologue imagery retired while queuing high-fidelity replacements for `/hq`.
  - Current pass: add a compact live-vision strip to the protected shell threshold, create a validated RPG visual replacement contract for retired prologue art, surface the queue in `/hq` Production/Assets, update docs/spec coverage, and push through the PR path.
  - Guardrail: keep `/hq` RPG private/separate from public Homefront positioning, do not promote the rejected Bellroot glyph/vector sheets, do not add paid or unreviewed asset dependencies, and do not move the RPG world out of its current place yet.
  - Progress: shipped the non-HQ live-vision shell strip and RPG visual replacement lane with ten queued Bellroot targets, three retired rejected sheets, approved fallback validation, `/hq` Production/Assets surfacing, docs/spec coverage, and focused tests. `npm run arpg:visual-replacements:check`, `npm run type-check`, `npm run verify`, `npm run build`, `npm run tabs:e2e`, and `NEXUS_RELEASE_BASE_URL=http://127.0.0.1:3100 npm run route:integrity` passed; `npm run hq:e2e` timed out locally and the targeted authenticated HQ browser check hit Windows Playwright `spawn EPERM`.
- [x] LOGGED-IN-HOMEFRONT-LIVE-SIGNALS — Turn the protected Homefront threshold from route-aware static copy into a live operating readout with runtime health, current route/focus, session posture, network/readiness proof, and silent fallback behavior while keeping `/hq` and RPG World excluded.
  - Current pass: add the live-shell spec, wire client-side protected shell signals through existing `/api/health` and `/api/status`, update regression assertions, and verify before pushing through the PR path.
  - Progress: added the live client readout to the shared protected shell with runtime age/status, URL focus, network mode, readiness posture, session proof chips, and silent degraded fallbacks; updated tab-surface assertions for live state/session/focus proof; verified `npm run type-check`, `npm run verify`, `npm run build`, `npm run tabs:e2e`, live `3100` `/api/health`, authenticated `/api/status`, COMMAND/INTEL/ALPHA/CYBER/RECON/VAULT/RESOURCES/SECURITY/SKILLS/VEHICLE threshold HTML, and the `/hq?focus=hq-chronicle` exclusion boundary. `npm run runtime:launch:3100` still times out in the known Windows wrapper way after starting a healthy runtime.
- [x] LOGGED-IN-HOMEFRONT-THRESHOLD — Carry the premium Homefront landing concept into authenticated non-RPG shell surfaces as a route-aware operating threshold: compact live-surface signals, local-first proof, and surface-specific work context while leaving `/hq` and the RPG World lane separate.
  - Current pass: add the shared shell component, style it with the existing dark glass command-room language, update tab-surface coverage, and verify with type/verify/build plus live authenticated route checks.
  - Progress: added the shared `homefront-command-threshold` to authenticated non-HQ shell tabs with surface-specific signals and proof chips, extended tab-surface assertions, and verified `npm run type-check`, `npm run verify`, `npm run build`, live `3100` health, authenticated COMMAND/INTEL/ALPHA/CYBER/RECON/VAULT/RESOURCES/SECURITY/SKILLS route HTML, and the `/hq?focus=hq-chronicle` exclusion boundary. `npm run runtime:launch:3100` still times out in the known Windows wrapper way after starting a healthy runtime.
- [x] AUTH-SHELL-RELOAD-LOOP-FIX — Stop the authenticated shell from entering an infinite `__shellHeal` reload loop after landing login, then outline the Homefront visual-language path for logged-in non-RPG surfaces.
  - Current pass: find the root cause of the changing `__shellHeal` query param, make shell recovery one-shot instead of loop-prone, preserve the private RPG boundary, and verify with type/build/live route checks.
  - Progress: fixed the bootstrap shell guard so it waits longer for heavy authenticated hydration and refuses to auto-reload again when `__shellHeal` is already present. `npm run type-check`, `npm run verify`, `npm run build`, live `3100` health, authenticated `/hq?focus=hq-chronicle&__shellHeal=123`, and authenticated `/command` checks passed.
- [x] LANDING-LIVE-COMMAND-THRESHOLD — Implement the one-step massive Homefront landing upgrade: live command preview, interactive real-surface showcase, proof wall, and access ceremony while preserving the current premium layout, active nav, no-sales posture, and private `/hq` RPG boundary.
  - Current pass: add substantial liveness and product depth to the public landing in one implementation pass; avoid public RPG positioning; verify with type/verify/build and live `3100` route checks.
  - Progress: shipped the one-step landing upgrade with a read-only live command preview, rotating/clickable surface showcase for COMMAND/INTEL/CYBER/VAULT/RESOURCES, a readiness proof wall, and a staged access ceremony. Added focused landing E2E assertions. `npm run type-check`, `npm run verify`, `npm run build`, live `3100` health, and public `/` HTTP checks passed; local Playwright worker launch remains blocked by Windows `spawn EPERM`.
- [x] LANDING-NAV-LIVENESS — Make the premium Homefront landing navigation feel alive without changing the approved layout: section-aware active highlighting, subtle scroll progress, focused hover states, and regression coverage that keeps the public positioning separate from the private `/hq` RPG lane.
  - Current pass: keep the floating pill nav shape, add section tracking for Home/Doctrine/Lanes/Posture/Access, add restrained motion/progress feedback, and verify the public page on the live `3100` runtime.
  - Progress: added hydrated section tracking for the landing nav, a spring-driven active pill, a subtle scroll progress glint, and focused E2E assertions for section highlighting. `npm run type-check`, `npm run verify`, `npm run build`, live `3100` health, and public `/` HTTP checks passed; local Playwright/browser automation still fails on Windows `spawn EPERM`, so interactive browser proof remains manual in the in-app browser.
- [x] LANDING-VISUAL-CONTINUITY — Continue the premium Homefront landing pass by finishing project-specific landing copy, metadata, live proof, and carrying the same local-first command-room visual language into shared tab/shell surfaces where it improves orientation without pulling the private `/hq` RPG lane into public positioning.
  - Current pass: audit landing remnants, shared shell chrome, and route metadata; apply concise Homefront visual language to appropriate tabs and shared components; keep product surfaces operational rather than salesy; verify with type/build and live `3100` route checks.
  - Progress: completed the project-specific Homefront landing cleanup, removed the leftover agency visual hooks from the public page, aligned the auth redirect stub, and added a compact Homefront continuity strip to shared protected shell tabs while keeping `/hq` private and separate. `npm run type-check`, `npm run verify`, `npm run build`, live `3100` health, public `/`, authenticated protected tab checks, and authenticated `/hq` boundary proof passed; `npm run runtime:launch:3100` still reports the known wrapper timeout even though the runtime is healthy.
  - Correction: Mario clarified that the RPG world is personal/private and will move elsewhere later, so the public landing/metadata/handoff language should focus on Homefront/Nexus command intelligence instead of presenting Aether Reliquary as product identity.
- [x] MW6FR8-HQ-RUNTIME-OVERLAY-FIX — Fix the live `/hq` runtime blockers seen in the in-app browser: remove the game-size hydration mismatch from viewport-derived text, guard Phaser camera resize until the main camera exists, then rebuild/relaunch so the game screen is visible again.
  - Current pass: patch the shell height readouts and Phaser viewport sync path only; do not widen into new game content, assets, route changes, or layout redesign.
  - Guardrail: keep the fix local-first and route-stable, preserve the current game window controls, and verify with type/build plus live `3100` route health.
  - Progress: closed in the blocker pass. The viewport-derived game-size readouts are mount-gated, Phaser viewport sync guards `cameras.main`, `npm run build`, `npm run audit:full`, and live `3100` checks for `/api/health`, `/hq`, `/hq?focus=hq-chronicle`, and `/resources?view=impact&impactMode=graph` passed; `npm run hq:e2e` remains blocked locally by Windows `spawn EPERM`, so the accepted proof lane is build/verify plus live route health until a fresh shell can run browser workers.
- [x] MW6FR9-ARPG-PILGRIM-ROWS-REST-ROAD-RUNTIME — Complete the fourth Veyrhold district loop by making Pilgrim Rows interactive: add rest/checkpoint options, road-rumor choices, recovery/travel-prep rewards, validation, and compact `/hq` Map/Kit/Journal proof so the first town has inn and road-prep depth alongside Oathmarket, Warden's Steps, and Bellroot Commons.
  - Current pass: extend Veyrhold town content with Pilgrim Rows rest options and road rumors; validate item/NPC/service/reward/story references; surface the loop through existing local-first inventory/story/reputation actions.
  - Guardrail: keep this as first-town inn and road-prep depth, not a full camping system, overworld travel engine, cloud save, multiplayer, paid asset lane, or new `/game` route.
  - Progress: added three Pilgrim Rows rest/checkpoint options and three Sister Vael road rumors with recovery rewards, route-prep tags, story flags, and reputation deltas; surfaced the loop in `/hq` Map, Kit, and Journal; updated completion/docs/tests. `npm run arpg:content:check`, `npm run arpg:production:check`, `npm run arpg:release:check`, `npm run type-check`, `npm run build`, `npm run verify`, live `3100` `/api/health`/`/hq` checks, and `NEXUS_RELEASE_BASE_URL=http://127.0.0.1:3100 npm run route:integrity` passed. `npm run runtime:launch:3100` still timed out in the known Windows wait path but left a healthy runtime.
- [x] MW6FR7-ARPG-BELLROOT-COMMONS-ALCHEMY-MYSTERY-RUNTIME — Push the next first-town update by making Bellroot Commons interactive: add safe alchemy brews, Ilo-led oath-lamp readings, recovery/mystery rewards, validation, and compact `/hq` Map/Kit/Journal proof so the first town has a softer support/story loop alongside Oathmarket economy and Warden's Steps armor.
  - Current pass: extend the Veyrhold town content with Bellroot Commons brew recipes and lamp mystery readings; validate item/NPC/service/reward/story references; surface the loop through existing local-first inventory/story/reputation actions.
  - Guardrail: keep this as first-town alchemy and city-mystery depth, not a full potion economy, final prologue-art replacement, cloud save, multiplayer, paid asset lane, or new `/game` route.
  - Progress: added three Bellroot Commons safe brews and three Ilo oath-lamp readings with item rewards, condition tags, mystery hooks, story flags, and reputation deltas; surfaced the loop in `/hq` Map, Kit, and Journal. `npm run arpg:content:check`, `npm run arpg:production:check`, `npm run arpg:release:check`, `npm run type-check`, `npm run build`, and `npm run verify` passed; `npm run hq:e2e` timed out locally waiting for the Playwright web server, while `3100` health and target route HTTP checks returned 200.
- [x] MW6FR6-ARPG-WARDENS-STEPS-FORGE-ARMOR-RUNTIME — Push the next first-town update by making Warden's Steps interactive: add forge armor fittings for helm, armor, gloves, and boots, add a civic oath-board choice, surface quality/upgrade identity in `/hq` Map and Armory, validate all references, and keep Veyrhold as the focused first release town rather than opening the broader 12-city game at once.
  - Current pass: extend the Veyrhold town content with Warden's Steps armor fitting orders and a first civic oath contract; validate starter gear, NPC, service, reward, quality path, and story-flag references; surface the forge path through existing local-first inventory/reputation/story actions.
  - Guardrail: keep this as first-town armor progression and job clarity, not a full blacksmith engine, final bespoke armor art pass, cloud save, multiplayer, paid asset lane, or new `/game` route.
  - Progress: added four Warden's Steps armor fittings for helm, armor, gloves, and boots plus three civic oath contracts with rewards, reputation deltas, story flags, quality-path copy, and starter item links; surfaced the forge/contracts in `/hq` Map and mirrored fittings in `/hq` Armory. `npm run arpg:content:check`, `npm run arpg:production:check`, `npm run arpg:release:check`, `npm run type-check`, `npm run build`, and `npm run verify` passed; `npm run hq:e2e` timed out locally waiting for the Playwright web server, while `3100` health and target route HTTP checks returned 200.
- [x] MW6FR5-ARPG-OATHMARKET-VENDOR-JOB-RUNTIME — Push the first deeper Veyrhold district loop by making Oathmarket interactive: add starter vendor wares, a ledger-choice job, reward/comparison hints, validation, and compact `/hq` Map/Kit proof so the first town has a small playable economy beat instead of only district visit cards.
  - Current pass: extend the Veyrhold town content with Oathmarket vendor wares and choice outcomes; validate references; surface the wares and choice flow through existing local-first inventory/reputation actions.
  - Guardrail: keep this as the first district economy/job loop, not a full vendor engine, auction house, paid asset lane, cloud save, multiplayer, or a new `/game` route.
  - Progress: added four Oathmarket starter wares, city-scrip pricing, accessory comparison copy, and three ledger-choice outcomes with rewards/reputation flags; surfaced the exchange in `/hq` Map and starter wares in Kit/Inventory. `npm run arpg:content:check`, `npm run arpg:production:check`, `npm run arpg:release:check`, `npm run type-check`, `npm run build`, and `npm run verify` passed; `npm run hq:e2e` timed out locally with Playwright `EPIPE`, while `3100` health and target route HTTP checks returned 200.
- [x] MW6FR4-ARPG-VEYRHOLD-DISTRICT-HUB-RUNTIME — Push the next first-town update by turning Veyrhold into a clearer visitable town hub: add four district map nodes with NPC/service/job/reward links, district visit actions, validation, and compact `/hq` Map proof so the town feels like the first playable location rather than separate service/job lists.
  - Current pass: extend `lib/arpgTownServicesContent.json/.ts` with district hub nodes, validate node references, and surface the nodes in the existing Map drawer with local-first visit/reward actions.
  - Guardrail: keep this as first-town runtime clarity, not bespoke city tilemaps, final NPC portrait art, audio, cloud saves, accounts, multiplayer, or a new `/game` route.
  - Progress: added four validated Veyrhold district hub nodes tied to NPCs, services, mini-quests, reward items, and visit flags; `/hq` Map now renders a Town hub board with visit actions that select the district, record local-first progress, and grant first rewards once. `npm run arpg:content:check`, `npm run arpg:production:check`, `npm run arpg:release:check`, `npm run type-check`, `npm run build`, and `npm run verify` passed; `npm run hq:e2e` timed out locally, while `3100` health and target route HTTP checks returned 200.
- [x] MW6FR3-ARPG-VEYRHOLD-NPCS-MINIQUESTS — Push the next unimplemented first-town update: add named Veyrhold NPCs, district mini-quests, service outcome cards, early town rewards, and compact `/hq` People/Journal/Map proof so the first release town has people, jobs, and reward clarity instead of only service buttons.
  - Current pass: extend the Veyrhold town-services content with NPCs, mini-quests, and service outcomes; validate references; surface them through existing local-first drawers without adding `/game`, cloud saves, multiplayer, or unapproved art.
  - Guardrail: keep this as town-depth content and UI proof, not a final bespoke map/audio/art pass.
  - Progress: added five named Veyrhold locals, four district mini-quests, and five service outcome cards to `lib/arpgTownServicesContent.json/.ts`; extended validation for NPC/service/quest/reward references; surfaced town jobs in `/hq` Map, service outcomes in Journal, and locals in People; updated completion/Resources tracking and focused E2E assertions. `npm run arpg:content:check`, `npm run arpg:production:check`, `npm run arpg:release:check`, `npm run type-check`, `npm run build`, and `npm run verify` passed; `npm run hq:e2e` timed out locally, while `3100` health and target route HTTP checks returned 200.
- [x] MW6FR2-ARPG-VEYRHOLD-TOWN-SERVICES — Deepen the first open town into a release-grade playable hub: add Veyrhold town services, district hooks, starter armor/accessory progression, blacksmithing/alchemy/market/inn/oath-board affordances, and compact `/hq` proof while preserving the first-release scope, local-first saves, legal asset posture, reduced motion, and no `/game` route.
  - Current pass: author typed Veyrhold service content, expose the services in the Map/Armory flow, make gear progression show helmet/armor/gloves/boots plus necklace/ring acquisition paths, and keep broad city expansion locked for later MW6 slices.
  - Guardrail: do not pretend Veyrhold has final bespoke maps/art/audio yet; this pass is hub mechanics and release clarity using existing approved illustrated assets and content contracts.
  - Progress: added `lib/arpgTownServicesContent.json/.ts`, validated Veyrhold's five services and four district hooks, expanded the core equipment model with gloves, two ring slots, and amulet starter items, surfaced town services and starter armor/accessory paths in `/hq` Map plus Armory, and added focused E2E assertions. `npm run arpg:content:check`, `npm run arpg:production:check`, `npm run arpg:release:check`, `npm run type-check`, `npm run build`, and `npm run verify` passed; `npm run hq:e2e` still times out locally, while `3100` health and target route HTTP checks returned 200.
- [x] MW6FR1-ARPG-FIRST-TOWN-RELEASE-SLICE — Shape the first release of `Aether Reliquary` around one intentional open town instead of exposing the full 12-city plan too early: complete the Bellroot intro into the north-gate route, open Veyrhold as the first town, show its four districts as playable town cards, keep other cities locked as future-act previews, and preserve `/hq`, command input, local-first saves, legal asset posture, reduced motion, and no `/game` route.
  - Current pass: gate `/hq` Map/Adventure travel around Bellroot -> Veyrhold, make Veyrhold's four sub-cities feel like the first release town map, and convert the remaining world content into locked preview copy until future MW6 city slices open it.
  - Guardrail: do not claim all 12 cities are playable yet, do not add fake art, do not widen into cloud/account/multiplayer systems, and do not remove the broader MW6 production registry that future slices will use.
  - Progress: implemented the `/hq` Map first-release town card, locked/route-ready/town-open states, Veyrhold-only town map, four district cards, locked future-city previews, Resources completion tracking, and focused E2E assertions. `npm run arpg:content:check`, `npm run arpg:production:check`, `npm run arpg:release:check`, `npm run type-check`, `npm run build`, and `npm run verify` passed; `npm run hq:e2e` timed out and the focused map retry hit local Windows Playwright `spawn EPERM`, while `3100` health and `/hq` route HTTP checks returned 200. Triaged complete on 2026-05-07 because later MW6FR2-FR9 town runtime slices already build on this foundation and the only unresolved proof was the known Windows browser-worker lane.
- [ ] MW6V-ENEMY-BOSS-HIFI-STORY-INTRO — Promote the `enemy-boss-hifi-cards` brief into an approved high-fidelity illustrated 2D runtime batch for Hollow Sentry, Ashling Scout, Rune Husk, The Brass Warden, Glass Gnawer, Ember Mote, Veyrhold Champion, and The Hollow Regent seed, then make the Bellroot opening play as a non-combat story sequence before the Warden's Antechamber unlocks fighting.
  - Current pass: add prompt/provenance records, normalize the generated source sheet into runtime PNG output, prefer the approved enemy/boss cards in `/hq` Adventure/Target/Journal/Assets surfaces, and preserve player-created identity with no forced name, gender, or "Main" label.
  - Guardrail: no flat vector/glyph replacement, no pixel-art pivot, no untracked generated placeholder, no `/game` route, no cloud/account behavior, and no combat-first opening before the Bellroot intro flow is complete.
  - Progress: generated and normalized the high-fidelity enemy/boss card sheet, added manifest/bench/provenance records, wired `/hq` enemy/codex surfaces to the approved sheet, added Bellroot intro progression before combat unlock, and recorded the armor/accessory tier expansion on the arsenal lane. `npm run arpg:illustrated:generate`, visual/content/asset/production/release gates, `npm run type-check`, `npm run build`, and `npm run verify` passed; `npm run hq:e2e` remains blocked locally by Windows `spawn EPERM`, while `3100` health and target route HTTP checks returned 200.
- [ ] MW6V-ARPG-PROLOGUE-VISUAL-ASSETS — Create and wire the next high-quality illustrated 2D prologue asset batch for `Aether Reliquary`: Bellroot Vestibule location art, Warden's Antechamber card, Ilo/keeper portraits, Descent Ledger, oath-lamp, oracle cradle, Gate Monolith, Loom-Shard, and Quiet Forge icons, all through the existing asset bench with prompt/provenance records, manifest entries, runtime PNG output, and `/hq` Adventure/Journal/People visibility.
  - Current pass: keep the chosen art direction as illustrated 2D PNG/WebP sheets for browser-game cards/icons, not pixel art and not 3D-first; extend the existing `sharp` normalization pipeline and avoid untracked/generated placeholders.
  - Correction: Mario rejected the flat vector/glyph-style prologue sheets as not appropriate game art. Demote those sheets to rejected/reference-only, remove their runtime preview usage, add art-direction guardrails against similar assets, and keep the next art pass focused on higher-fidelity painted/rendered browser RPG imagery.
  - Progress: guardrail implemented and verified with `npm run arpg:illustrated:generate`, asset/content/production/release checks, `npm run type-check`, `npm run build`, and `npm run verify`; `hq:e2e` still times out locally, so the replacement-art task stays open until high-fidelity prologue visuals are created and browser proof is clean.
- [ ] MW6V/W-ARPG-ARSENAL-VISUAL-ITEMIZATION — Make the Aether Reliquary weapon system feel like a real RPG loop by adding illustrated 2D weapon-family assets, quality overlays, named weapon cards, drop/upgrade VFX cues, deterministic weapon templates, quality attributes, comparison/equip/upgrade/salvage behavior, and `/hq` runtime surfaces while preserving the Phaser playfield, command input, local-first saves, legal asset validation, and no `/game` route.
  - Current pass: extend the existing armory/economy registry rather than creating a parallel system; add an illustrated arsenal asset bench batch; wire weapon-quality visuals into loadout, Kit, Armory, loot, and upgrade flows; add validation/test coverage and keep generator/provenance records intact.
  - Expansion requirement: grow the same quality/tier logic across armor and accessories, including helmet, armor, gloves, boots, necklaces, and rings; low-tier gear should read bland and practical, while unique, relic, ancient, mythic, and ultimate-grade items get visibly distinct silhouettes/materials plus dungeon, quest, blacksmithing, and alchemy acquisition paths.
  - Progress: added the arsenal registry, 21 weapon-family templates, 7 quality budgets, named weapon samples, deterministic comparison/drop/upgrade helpers, project-original arsenal source sheets, generated runtime PNG sheets, manifest/bench records, `/hq` Kit/Armory/loadout visuals, and E2E assertions. `npm run arpg:illustrated:generate`, asset/content/production/release gates, `npm run type-check`, `npm run build`, and `npm run verify` passed; `npm run hq:e2e` is still not accepted because the local Playwright run hangs/timeouts and focused browser launch currently hits Windows `spawn EPERM`, so keep this item open until browser proof is clean.
- [ ] MW6V-REAL-ASSET-INTAKE — Optional/reference import bridge, validation support, and `/hq` Assets drawer readiness are verified; keep this open until official CC0/approved pack files are placed in `assets/arpg/intake/raw/`, imported from `assets/arpg/intake/work/<candidate-id>/`, manifest-tracked, and visible as a real model or model-derived preview.
- [ ] MW6V-ILLUSTRATED-PLAYFIELD-CONSISTENCY — Bring the live `/hq` Aether Reliquary playfield into the same illustrated 2D art direction as the approved Hero Kit character/outfit/gear assets: reduce or replace procedural-looking markers, make enemy/player/loot cues use matching illustrated treatments where approved art exists, keep Phaser gameplay intact, and preserve command input, local-first saves, route stability, reduced-motion support, and compact game-first layout.
  - Current pass: diagnose the mixed visual-language issue Mario flagged in-browser, then implement a targeted art-consistency bridge rather than adding new unapproved assets or fake replacement art.
  - Progress: Phaser now loads the approved illustrated Hero Kit outfit, enemy-card, item, and location sheets directly into the live playfield, while keeping procedural fallbacks for missing assets; `npm run build`, `npm run verify`, and live `3100` health/browser checks passed.
- [ ] MW6V-HQ-GAME-FOCUS-LAYOUT — Repair the `/hq` Aether Reliquary spacing issue by adding an explicit Game/Chat focus switch, making reset/lock split controls functional and obvious, and removing non-game chrome from the active game sizing lane while preserving command input, Phaser gameplay, route stability, and local-first preferences.
  - Progress: implemented explicit Game/Chat focus mode, game-only layout controls, restored `/api/health` on `3100`, and browser-checked Game/Chat separation plus reset/lock behavior. Keep open until Playwright/build proof is unblocked from local `spawn EPERM`/runtime-build collision.
  - Current pass: collapsed dashboard-sized HUD chrome into a playfield-first Game mode, moved layout lock/reset into the top game dock, removed the non-game footer rail from the active play surface, added responsive Phaser resize/camera framing, and verified with `npm run verify` plus in-app browser proof.
  - Progress: added a lock-aware manual game-window resize handle with persisted custom height, keyboard resizing, pixel readout, custom-size chip, and reset-to-XL behavior. `npm run type-check` and `npm run build` passed; `npm run hq:e2e` timed out and the focused Playwright rerun hit local Windows `spawn EPERM`, but live `3100` health and in-app browser DOM proof showed the new separator/readout on `/hq?focus=hq-chronicle`.
  - Current fix: made the manual game-window sizing path intuitive by preventing silent no-op drags when size is locked, lifting the custom height cap well above the constrained browser viewport, adding always-visible top-dock drag/readout controls plus a normal Size slider and Less/More fallbacks, and fixing the root layout bug where the stage only set `flex-basis` instead of real `height`. Rebuilt and proved in the live in-app browser that the Size slider plus top drag-chip keyboard controls change `/hq` height from `700PX` to `756PX` and the playfield now expands into the resized stage instead of leaving a blank band. Added focused E2E coverage for the slider path; Playwright remains locally blocked by Windows `spawn EPERM`.
