# AGENTS.md

Read `REPO_CONTEXT.md` before work involving this source.

Keep divergent ideation explicit and capped at the existing three Council calls. A merge may add one pinned JANSKY call. Do not import the upstream runtime, auto-trigger expensive calls, expose hidden reasoning, or add a provider dependency.
