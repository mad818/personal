# Nexus Prime System State

This file is the canonical source of current reality for Nexus.

Use it for:

- latest shipped tranche
- active blockers
- release posture
- known environment issues
- the active Next Up queue

Do not use it for:

- long historical batch logs
- generalized engineering rules
- stable product intent

## Latest Shipped

- SYSTEM-STATE-LATEST-COMPACTION is locally complete as a context-efficiency correction to the canonical project spine. `Latest Shipped` now keeps only a bounded newest-first window instead of injecting a 203-entry, 243,475-character historical log through full-state and `slice=latest` project-context reads; older shipment detail remains recoverable from Git history rather than a second live archive. The documentation gate rejects more than 12 top-level shipped entries or more than 24,000 section characters while preserving newest-completed identifier parity. The actual context extractor now returns 12 entries in 16,843 characters, and the entire canonical state fell from 267,715 to 41,103 characters—an 84.6% reduction—without losing blockers, release posture, environment issues, verification, Next Up, or compatibility sections. Benefit: agents and operators load current evidence instead of spending context on obsolete batch history. Focused documentation, extraction, handoff, publication, and boundary checks passed, and the canonical verifier passed in 188.8 seconds. No route, API/runtime behavior, provider, dependency, phone/PWA, or RPG implementation changed.
- CURRENT-SYSTEM-STATE-RECENCY is locally complete as a correction to the canonical documentation truth chain. `docs/SYSTEM_STATE.md` is now part of the current-surface framework audit, and `docs:stack:check` extracts the first top-level completed identifier from `tasks/todo.md` → `## Next Up` and requires the first `## Latest Shipped` bullet to match it; unchecked tasks and indented subtasks are excluded by runtime fixture. The missing focus/motion/divergent-Council shipment is now recorded with its actual benefits, boundaries, proof, and local-only push posture. Benefit: a green documentation gate can no longer coexist with a stale canonical latest-shipped record. Focused documentation and instruction checks, handoff freshness, publication safety, diff checks, the tracked-plus-untracked zero-phone/PWA/RPG path audit, and canonical verification in 195.2 seconds pass; no route, runtime, provider, dependency, or application source changed.
- FOCUS-MOTION-DIVERGENT-COUNCIL is locally complete as a bounded assimilation of `emilkowalski/skills`, `ayghri/i-have-adhd`, and `UditAkhourii/adhd` into existing Nexus seams. A project-owned motion decision contract now assigns frequency/purpose/input/surface budgets, rejects broad high-confidence transition anti-patterns, and preserves reduced-motion behavior; the opt-in Direct persona now leads with action, bounds lists, exposes current progress, and ends with one next move without inferring a diagnosis; and explicit divergent Council requests reuse exactly three isolated frames plus one optional pinned JANSKY critic instead of importing the upstream larger multi-call runtime. Benefits: operational motion is quieter, response resumption costs are lower, and wider ideation has a visible usage ceiling. Scoped repo analyses, complete source-parity records, Company Map benefits/exclusions, focused runtime/static gates, TypeScript, lint, formatting, publication/security, canonical verification in 212.6 seconds, all 39 production pages in 63.7 seconds, performance budgets, and a tracked-plus-untracked zero-phone/PWA/RPG path audit passed. Commit `d65d3de` is local because GitHub port 443 remains unreachable.
- SENTIMENT-FEED-TRUTH is locally complete as a truthful-availability and request-consolidation correction to the active COMMAND sentiment surfaces. `/api/fear-greed` now makes one fixed Alternative.me request for 30 bounded entries, normalizes integer `0` through `100` values, classifications, and timestamps into a Nexus-owned contract, caps reads at 128 KiB with an eight-second timeout, caches one verified snapshot for an hour, rate-limits the protected no-store route, and returns a fixed safe non-2xx failure without upstream details. One dedicated hook validates HTTP and payload success, updates both supported sentiment store shapes from the same snapshot, records feed status, ignores stale completions, and preserves the last verified value; the competing global-data request is removed. COMMAND no longer substitutes `50` for missing sentiment, excludes unknown sentiment from the system score, offers accessible retained/unavailable feedback and retry, and shows only the verified current Market sample rather than synthetic hourly sentiment history. Benefits: an outage cannot masquerade as neutral evidence, duplicate provider traffic and divergent client state are removed, the last trustworthy reading remains visibly useful, and future boundary or contract drift fails deterministic validation. Focused runtime/static fixtures, explicit TypeScript, zero-warning lint, formatting, rate-limit/security boundaries, publication safety, the 200.5-second canonical verifier, and the 86.2-second production build across all 39 pages passed without live provider traffic. No phone/PWA acceptance or RPG path changed.
- CORE-MARKET-FEED-TRUTH is locally complete as a truthful-availability and input-containment correction to the active crypto-price and prediction-market surfaces. `/api/prices` and `/api/polymarket` now return protected no-store, rate-limited Nexus contracts backed by fixed provider URLs, 12-second timeouts, 512 KiB response ceilings, and normalized finite records; price mode and coin IDs are closed, deduplicated, and capped at 25, while `COINGECKO_KEY` stays server-only. Crypto market quotes and chart history settle independently, validate `response.ok` plus their payload contract before replacing store data, retain the last verified results, ignore stale completions, and publish fixed feed status with accessible ALPHA retry. Polymarket no longer turns missing or malformed outcome evidence into a fabricated neutral `50%`; invalid events are omitted server-side, total failure is a safe non-2xx response, and the panel retains verified markets while exposing accessible loading, retained, unavailable, and retry states. Benefits: provider outages cannot masquerade as successful emptiness, one failed price source no longer erases the other, malformed evidence cannot become a trading signal, and query/provider details stay bounded behind Nexus. Focused runtime/static fixtures, explicit TypeScript, zero-warning lint, formatting, rate-limit/security boundaries, publication safety, the 196.8-second canonical verifier, and the 92.3-second production build passed without live provider traffic. No phone/PWA acceptance or RPG path changed.
- MARKET-RATES-SERVER-TRUTH is locally complete as a privacy and truthful-availability correction to the active FX and commodity panel. The browser now calls only protected same-origin `/api/fx` and `/api/commodities` routes through `apiFetch`; the server owns every fixed provider URL and `FRED_KEY`, validates positive finite quotes and timestamps, caps upstream reads at 256 KiB with ten-second timeouts, rate-limits both routes, and returns fixed safe non-2xx failures when no verified data survives. Metals and configured FRED energy series settle independently, source states identify partial, unconfigured, and unavailable coverage, and the client retains the last verified FX or commodity data when a refresh fails while exposing accessible loading, partial, retained, unavailable, and retry states. Five obsolete provider hosts are removed from normal browser `connect-src`, and the legacy persisted FRED-key field remains scrubbed without a client default. Benefits: keys and provider topology stay behind Nexus, one outage no longer erases a healthy source or masquerades as verified emptiness, stale successful data remains visibly useful, and CSP/contract drift now fails canonical validation. Focused runtime/static fixtures, explicit TypeScript, zero-warning lint, formatting, CSP, protected-route/security boundaries, publication safety, the 195.3-second canonical verifier, and the 87.4-second production build passed without live provider traffic. No phone/PWA acceptance or RPG path changed.
- RECON-SERVER-CONNECTOR-BOUNDARY is locally complete as a privacy and credential-containment correction to the active OSINT suite. `ReconLookup` and `PassiveDnsPanel` now call only the protected same-origin `POST /api/recon/lookup` route through `apiFetch`; the server accepts a closed 15-operation contract, validates target shape before any fetch, selects every fixed provider URL, enforces a ten-second timeout and 512 KiB response ceiling, rate-limits requests, and returns only fixed invalid/key/rate-limit/unavailable failures. Optional HIBP, VirusTotal, and Shodan keys are read only from server environment, their obsolete client defaults are removed, and thirteen unused third-party hosts are gone from normal browser `connect-src`. Existing RDAP, DNS, certificates, geolocation, subdomain, DNS-security, email, username, breach, threat-intel, passive-DNS, and reverse-IP panels remain; HIBP 404 still means a verified empty breach result. Benefits: lookup targets and credentials no longer fan out from browser code, providers cannot be chosen as an arbitrary proxy destination, oversized or malformed upstreams fail safely, and future client/CSP drift fails canonical validation. Focused runtime/static fixtures, explicit TypeScript, zero-warning lint, active-source formatting, CSP, route/security boundaries, publication safety, the 195.9-second canonical verifier, and the 89.4-second production build across all 39 pages passed without live provider traffic. No phone/PWA acceptance or RPG path changed.
- OPERATIONAL-READ-STATE-TRUTH is locally complete across the CISA connector boundary and three user-facing diagnostic/security reads. `/api/cisa-kev` no longer turns upstream HTTP, network, malformed JSON, or invalid entry shape into HTTP 200 plus an empty catalog; it returns a fixed safe 502 without raw exception disclosure, so cyber sweeps can classify the source as unavailable. CISA Feed, unified CYBER triage, and COMMAND Agent Health now validate successful payloads before replacing current data, retain the last verified result during refresh or failure, distinguish verified empty from unavailable, ignore stale completions, and provide accessible status/error feedback with local retry. Benefits: an outage can no longer look like zero known exploited vulnerabilities or zero health metrics, partial triage remains useful, and successful data presentation/sorting stays intact. Proof: focused HTTP/JSON/payload/network runtime fixtures, static route/UI contract, explicit TypeScript, zero-warning lint, complete active-source formatting, surface/accessibility checks, the 186.3-second canonical verifier, and the 89.7-second production build across all 39 pages passed; zero phone/PWA acceptance and RPG paths changed.
- SETTINGS-SERVER-STATUS-TRUTH is locally complete as a narrow correction to the Settings drawer's server-read boundary. API-key rows now show checking, verified, or unknown state and render `set` / `not set` only after `GET /api/settings` returns HTTP success plus a non-empty boolean status map. Drawer-open and post-save refreshes share one non-throwing loader; request sequencing prevents stale completions, closing resets the visible posture, a failed follow-up read does not convert a successful write into failure, and an accessible alert provides local retry. The loader returns fixed safe failure copy for HTTP, network, JSON, and payload-shape failures without exposing key values. Benefits: unavailable local runtime no longer makes every key look definitively absent, saved configuration remains truthful, and future regression is part of canonical verification. Proof: focused static/runtime fixtures, explicit TypeScript, zero-warning lint, complete active-source formatting, the 300.8-second canonical verifier, and the 115.3-second production build across all 39 pages passed; zero phone/PWA acceptance and RPG paths changed.
- ST3GG-MEDIA-TAIL-DEFENSE is locally complete as a bounded extension of the existing browser-local RECON binary triage. Nexus now identifies bytes after a structurally bounded PNG IEND, JPEG EOI, or final PDF EOF marker and reports only category, offset, trailing-byte count, and an optional nested-format signature. Clean PNG/JPEG files and PDF trailing whitespace remain quiet; malformed structures fail closed without scanning or decoding appended content. The indicator reaches visible notes, copied triage, and operator-approved VAULT evidence, while raw files and trailing bytes never leave the browser. ST3GG 3.0.0 remains an AGPL external reference; Nexus independently implements only the cheap format-terminator lesson and excludes creation, extraction, decoding, brute force, statistical steganalysis, metadata parsing, polyglot generation, network channels, AI agents, providers, dependencies, and copied code. Benefits: a common hidden-tail/polyglot signal is now concrete and explainable, RECON can preserve bounded evidence without retaining suspicious content, and deeper forensic claims remain explicitly review-gated. Proof: focused clean/malformed/appended runtime fixtures, static boundary wiring, Company Map, source parity, TypeScript, formatting, zero-warning lint, publication, security, the 196.0-second canonical verifier, and the 86.4-second production build across all 39 pages passed; zero phone/PWA and RPG implementation paths changed.
- GLOSSOPETRAE-HIDDEN-CHANNEL-DEFENSE is locally complete as a defensive extension of the elder-plinius portfolio intake. The AgentShield skill-review lane now rejects Unicode tag, bidi-control, zero-width/format, and BMP or supplementary-plane private-use channels with bounded printable findings while preserving ordinary multilingual text, combining marks, variation selectors, joiner-based emoji, and a leading BOM. The mandatory deterministic scanner covers all 25 tracked `SKILL.md` and `GUIDE.md` files across current Codex, legacy, and project-local skill roots without following symlinks. GLOSSOPETRAE 3.1.0 is recorded as an AGPL external reference; Nexus independently implements only the defensive tokenizer-visibility lesson, exposes its benefit and limits in Research and Legal & Trust, and excludes its language engine, payload creation, decoding, covert channels, tokenizer exploitation, provider experiments, and copied code. During production proof, the standard Next build twice compiled and collected page data before its minified-server prerender worker exited silently at 0/39; debug prerender isolated that optimized path, disabling server minification made the ordinary build complete all 39 pages, and the toolchain gate protects the proven Windows setting. Benefits: skill instructions can no longer rely on CSS or common Unicode invisibility channels to smuggle hidden content, the actual Codex skill root is covered, normal international text remains usable, and production build proof is stable again. Proof: focused detector/scanner, Company Map, source parity, AgentShield, type, lint, formatting, publication, security, and queue checks passed; canonical verification passed in 197.5 seconds; the standard production build passed in 81.3 seconds; zero phone/PWA and RPG paths changed.
- ORBIT-FEYNMAN-QUEUE-TRUTH is complete as a read-only correction to the next-work selector after the recurring-watch tranche. `FEYNMAN-SOURCE-PARITY` now carries the human-owned `blocked_external` queue posture because its only three pending rows require an approved real sandbox or measurable experiment target that is absent from this workspace; an ordinary subprocess or keyword-derived score is not treated as replication or measured autoresearch. ORBIT recognizes only the narrow `blocked_external` and `blocked_manual` evidence markers after RPG exclusion, gives each a distinct reason code, and never infers local readiness or task completion from them. The real queue now reports 18 top-level pending tasks as 0 actionable, 7 blocked/manual, and 11 RPG-excluded, with physical phone/iPad acceptance as the first truthful unblock. The completed Feynman recurring watch remains available as a human-approved, daily-cached public arXiv check that bypasses the generic AI path, stores bounded local material-change receipts, and runs only while Nexus is open. Benefits: future sessions stop cycling into unsafe/fake execution work, an empty local-ready queue is reported honestly, and the next useful action is explicit without spending model tokens on a false start. Proof: focused ORBIT static/runtime fixtures, human/JSON CLI receipts, toolchain, publication safety, security scan, and the 204.5-second canonical verifier passed; application sources did not change, so the passing `32f0afa` production build remains the current build proof; handoff/diff and zero-RPG audits pass with the final commit.

## Active Blockers

- This Codex shell currently cannot reach GitHub over port 443, so `npm run git:safe -- push` failed before this tranche could publish. Use `npm run repo:sync:status` to inspect ahead/dirty state without network, then run `npm run git:safe -- push` from normal PowerShell when GitHub connectivity is available; do not claim the branch is synced until that succeeds.
- Phone/PWA acceptance still requires a physical phone or iPad on the same trusted network even though the receipt engine, assistant proof bridge, live status panel, capture-status artifact, direct-HQ QR handoff, one-command local preflight, no-network repo sync status, and latest-capture report are now implemented: run `npm run repo:sync:status`, run `npm run ops:preflight`, scan the `Scan direct HQ` QR or open the printed LAN HQ URL, log in with `NEXUS_TOKEN`, let the Free Local Readiness panel create the mobile receipt, send `ping`, ask one local Ollama prompt, watch live receipt proof turn ready, install the PWA, rerun capture, then run `npm run phone:acceptance:report` until `acceptanceReady: true`.
- The wide readiness rollup is not release-candidate ready yet because `docs/metrics/readiness-rollup-2026-05-09T22-29-00-772Z.json` links a blocked phone acceptance artifact, release diagnostics used local fallback with routes down, Docker is unavailable, and Dependabot alert package metadata is not reachable from this shell.
- Local Docker proof is still blocked because Docker is not currently available on this machine.
- The target-runtime gate now reads repo-root `.env.local`, but the current project-local config still does not define `NEXUS_RELEASE_BASE_URL`, so the combined `FD2`/`FD3` proof remains blocked until the real Coolify host is added there.
- `MW6V-REAL-ASSET-INTAKE` remains intentionally blocked until approved local CC0/CC-BY/commercial-license asset packs exist and are routed through the manifest/credit intake lane.

## Release Posture

- Current stage: late internal beta / release-candidate hardening for the web lane.
- Current active priority: keep the newly shipped smoothness/operational-light layer stable while closing the remaining proof lanes: physical phone/PWA acceptance, Dependabot classification, Docker/staged-host release proof, and GitHub publish from a network-enabled shell.
- Current wide rollup status: blocked, not release-candidate ready, with sanitized evidence in `docs/metrics/readiness-rollup-2026-05-09T22-29-00-772Z.json`.
- Highest-value milestone: first web deployment proof, not another major feature tranche.
- First release remains web-only and GA-scope only: `/hq`, `/command`, `/intel`, `/alpha`, `/cyber`, `/recon`, `/vault`, `/resources`.
- Desktop remains the second release wave after the web lane is proven in staged hosting.

## Known Environment Issues

- `handoff:pull` now uses `scripts/git-with-acl-repair.ps1` so the known `.git` DENY ACL is removed before Git runs. Normal PowerShell previously proved GitHub connectivity and push, but this Codex shell currently cannot reach GitHub port 443; inside the Codex sandbox, `npm run repo:sync:health` may still show the sandbox-applied DENY between tool calls, so use `npm run git:safe -- <git args>` for Git operations and retry publish from normal PowerShell when network connectivity is available.
- The original checkout at `<repo-root>` is back on `main` and is now the canonical local coding/runtime folder again. The previous dirty root state is preserved on `codex/root-preserve-before-main-restore-2026-04-21`, and the former dedicated `main` worktree now lives on `codex/mainline-runtime-archive-2026-04-21` for rollback/reference.
- The merged `main` runtime launcher prefers `.next/standalone` and mirrors `public/` plus `.next/static/` into it before boot; if a local Next build does not emit standalone output or a `BUILD_ID`, `scripts/start-runtime.mjs` now falls back to a repo-root Next runtime so local review on `http://localhost:3100` can still become healthy.
- In this shell harness, `npm run runtime:launch:3100` can time out even when the managed runtime is already healthy; check `http://127.0.0.1:3100/api/health` plus `.nexus-runtime.pid` before assuming the launch failed.
- Direct background dev-server launch may be blocked by shell policy in Codex; if `runtime:launch:3100` is killed by a tool timeout, rerun `npm run dev` or `npm run runtime:launch:3100` from normal PowerShell for live phone/iPad acceptance.
- Local browser automation can still hit Windows `spawn EPERM` at either the Playwright worker or Edge launch layer after a successful prior run; this reproduced on 2026-05-02 during `npm run hq:e2e` after build/audit/live routes passed and again during the operational-lights hydrated-browser check. When it happens, record the blocker, keep the managed `3100` runtime/source proof, and rerun the browser suites from a fresh shell before using the result as a product regression signal.
- The canonical root-main review flow is now `npm run build` followed by `npm run runtime:launch:3100` from `<repo-root>`; any merged-main visual change still needs that rebuild/relaunch cycle before the IAB reflects it.
- Docker CLI is unavailable on this machine, so `FD2` cannot be proven locally here yet.
- Target-runtime proof now auto-loads repo-root `.env.local` for `NEXUS_RELEASE_BASE_URL` and `NEXUS_TOKEN`, so `npm run launch:gate:target` no longer requires inline env export when the local operator file is populated.
- The repo-local deployment docs intentionally use placeholder hostnames such as `your-host.example`, `target-host.example`, and `<staging-host>`; no real staging hostname is committed under `docs/`, `scripts/`, `.github/`, or env examples, and the current `.env.local` still needs the operator's real Coolify domain.
- The preserved-branch candidate now has a fresh `.nexus-release-boundary.json` capture on `codex/preserve-main-2026-04-11`, and `npm run release:boundary` plus `npm run launch:gate` both pass again on that captured state.
- Local release proof surfaced and closed two deployment-gate regressions during this session: the forecast and scheduler-efficiency runtime-eval endpoints now have explicit route-policy coverage, and their protected `run` handlers now emit direct no-store protected responses.
- Some local shells still do not expose a working `vitest` binary even though repo-level verification and browser harnesses are green.
- Authenticated Playwright suites should still run sequentially on Windows because they share the `.next-e2e` dist dir, but `type-check` no longer depends on that transient folder and `clean-next` now retries removal before failing.
- In the current Codex sandbox, npm package fetches for the MW4 PixiJS/GSAP dependency set fail with registry `EACCES`; install `pixi.js`, `@pixi/react`, `gsap`, and `@gsap/react` from a network-enabled shell before converting the shipped Canvas/Web Animations fallback into a bundled Pixi/GSAP runtime.

## VX1 Verification Checklist

- Targeted proof: `npm exec -- vitest run __tests__/voiceLab.test.ts __tests__/projectArchitecture.test.ts __tests__/aiAnonymization.test.ts __tests__/vulnerabilityReview.test.ts __tests__/assistantCanonicalRegistry.test.ts __tests__/exactSessionLinks.test.ts`
- Baseline proof: `npm run type-check`
- Surface proof: `npm run hq:e2e`, `npm run route:e2e`, and `npm run tabs:e2e` run sequentially on Windows
- Voice Lab truth: `/resources?view=voice-lab&voiceProject=...` must reopen the exact project, browser speech must remain usable when the local runtime is down, and runtime-backed render/export must never leave stale success state behind after degradation
- Architecture truth: `/api/project?section=graph|ownership|hotspots|security` must stay local-only, normalize repo-relative paths correctly, and keep coupling plus sink/secret heuristics readable inside the compact Resources workbench
- Privacy-shield truth: `/api/ai` anonymization must default to cloud-bound sensitive turns only, keep local providers raw unless explicitly forced, and always de-anonymize operator-visible output before it reaches HQ/CYBER/RECON surfaces
- Vuln-review truth: `workflowId=vuln-review` must keep filing durable `/cyber` artifacts into VAULT compiled pages with exact reopen and a visible repair-lane handoff
- AI-exposure-review truth: `workflowId=ai-exposure-review` must stay passive-first and advisory-only, file durable `/recon` or `/cyber` artifacts into VAULT compiled pages with exact reopen, and never widen into active scanning, endpoint probing, or exploit guidance

## Next Up

- Active queue source: `tasks/todo.md`, `docs/plans/unfinished-ideas-triage-2026-05-07.md`, and `docs/repo-hygiene/local-change-set-map-2026-05-08.md` now define the current completion order; current priority is project security, vulnerability protection, dependency auditability, infrastructure validation, and optimization rather than RPG or drone expansion.
- Security infrastructure hardening is now shipped locally with latest proof in `docs/metrics/infra-hardening-2026-05-09T22-28-42-970Z.json`: keep using `npm run infra:hardening:audit` for sanitized evidence and `npm run verify` for the full hardening gate. The next security move is GitHub Dependabot metadata classification, then one minimal dependency upgrade batch at a time with `npm run verify` after each batch.
- Free local phone acceptance is next: desktop proof already showed the LAN HQ URL and local routes working, and the protected receipt API, assistant bridge, live status panel, capture-status artifact, direct-HQ QR handoff, `npm run ops:preflight`, `npm run repo:sync:status`, and `npm run phone:acceptance:report` commands are now wired. From the phone/iPad, run `npm run repo:sync:status` plus the preflight command first, use the Free Local Readiness checklist in HQ or COMMAND, scan the `Scan direct HQ` QR or copy the direct HQ URL, log in with `NEXUS_TOKEN`, let `/api/phone-acceptance/receipt` record the mobile/browser receipt, send `ping`, ask one local Ollama prompt so the shared assistant turn receipt marks the proof booleans, confirm the live receipt proof reads complete, install the PWA from the phone browser, rerun `npm run phone:acceptance:capture`, and use `npm run phone:acceptance:report` until `receiptLiveStatus.acceptanceReady` and `acceptanceReady` are both true.
- Local AI offline operations now have authenticated desktop proof, visible receipt support, and `npm run offline:local:report` for a no-network summary of the latest sanitized rollup plus phone acceptance artifacts; the remaining acceptance step is manual phone/browser validation: send `ping`, then use `Check local AI` or ask one Ollama prompt, confirm the receipt shows local/free posture plus `No file changes`, rerun capture, then rerun the local AI report.
- Git permission recovery is closed for the working flow: use `npm run git:safe -- <git args>` when the Codex sandbox shows `.git` ACL friction, and keep branch cleanup review-gated.
- Dependency-security audit now has real paginated imported metadata, archive manifest quarantine, and the first root runtime patch: use `docs/metrics/dependabot-security-audit-2026-05-25T05-52-26-760Z.json` and `docs/metrics/dependency-risk-posture-2026-07-15T02-56-38-897Z.json` as the current starters. Wait for GitHub to rescan the renamed `archive/` manifests and the `next@15.5.18` patch before treating stale imported Next/archive alerts as active work. The next patch move is the smallest remaining active root-app runtime batch (`postcss` first, then `prismjs` if still open), followed by `npm run dependency:risk:posture` and `npm run verify`. Do not mix package upgrades into phone/local AI acceptance unless a dependency blocks verification.
- Private RPG closure is deferred for now while local AI/phone operations are the active priority; when resumed, keep Aether Reliquary private and route-stable.
- Homefront vehicle/drone readiness is complete for the current no-hardware phase; future work should wait for real hardware or new simulated scenario requirements, and must remain passive/readiness-only with no flight-critical control path.
- AI operator workflow gaps are closed locally; future assistant work should build on `lib/assistantOperatorWorkflow.ts`, the shared chat action model, and the existing `ProposedEditPanel` approval surface instead of adding another mutation path.
- Release engineering stays blocked externally, but the exact FD2/CP2 diagnostics and rollback runbook now exists; the next release move requires the real staged Coolify/VPS hostname, token, and Docker proof before any FD2 completion claim.
- Cleanup follow-through: retire the older replay worktree queue (`m2`/`m3`/`m4` and the temporary UXA3 review worktree) only after there is no remaining need for side-by-side rollback or visual-reference inspection.
- Mainline replay status: `main` now contains the M2/M3/M4 stack plus UXA3 and the S1 runtime/security replay, so no more clean-replay slices are required before normal product work resumes.
- FD2 remains the next external release gate; staged Coolify/VPS proof is still blocked on the real staged hostname being present in repo-root `.env.local`.
- Historical replay reference remains in `docs/plans/m2-fresh-mainline-replay-stack-2026-04-18.md`; the older `m2`/`m3`/`m4` and `uxs45` replay worktrees now serve as rollback/reference material only until they are explicitly retired.
- The successful replay order remains the historical record for this migration: shared shell and taste primitives first, HQ plus route-interior completion second, then trust/auth/protected-action substrate third, followed by UXA3 plus the S1 runtime/security replay.
- UXS4 is now a shipped visual contract, so follow-on UI work should preserve `docs/NEXUS_TASTE_CONTRACT.md` plus `lib/nexusTasteContract.ts` as the taste source of truth unless a future tranche explicitly replaces them.
- Project-local rule remains in force until staging is real: upstream repos are idea sources only, and implementation still lands inside Nexus routes, contracts, state, and docs under `<repo-root>`.
- Repo-boundary rule now remains a shipped contract for the GitHub-repo lane: public GitHub repos only, explicit operator action only, metadata-first only, durable output to VAULT only, and no private repo support, token UX, GitHub write flows, or background operator loops.
- VX1 ship guard: keep Voice Lab, project architecture, privacy shield, and vuln-review inside their current Nexus seams and contracts unless a new tranche explicitly widens them; no new top-level tabs, no provider bypasses, and no remote code-upload service.
- FD2 through FD5 and A7b remain blocked exactly as they are; the shipped UX tranches do not change staged-host, deployment, or forecast-execution posture.
- [x] XR1 — Comprehensive assimilation program: keep Nexus native to its existing shell, routes, governance posture, and trust boundaries while mapping the current external source set into three sequenced subprograms instead of vendoring third-party products
- [x] MR1 — Market Research Workbench: adapt the DefiLeo trader-journaling cue plus reference inputs such as `public-apis`, `ai-engineering-from-scratch`, and the existing `timesfm_companion` contract into governed thesis review, loss review, and emotion-aware market continuity across `/hq`, `/alpha`, and `/vault` without adding autonomous trading or a new top-level route
- [x] MO1 — Memory + OSINT Casefile Loop: adapt `claude-obsidian`, `D4rk_Intel-OSINT-Investigative-Toolkit`, and `public-apis` as native patterns for VAULT memory stewardship plus passive-first RECON/CYBER case progression without bundling third-party binaries, widening the shell into a tool catalog, or relaxing the current advisory boundary
- [x] VR1 — Vehicle + Radar Readiness: adapt `PLFM_RADAR` and reference-only learning patterns from `ai-engineering-from-scratch` into `/vehicle` readiness, onboarding, artifact packaging, and future radar session continuity without adding RF control, flight-critical behavior, or new public routes
- [x] XR2 — Keep the next tranche project-local while polishing reuse, metadata stewardship, exact durable-artifact reopen, and continuity handoffs for the shipped XR1 surfaces without adding new tabs, routes, or external repo dependencies
- [x] MR2 — Market review continuity polish: populate source metadata, reuse strongest prior reviews directly in ALPHA, and reopen exact prior reviews in VAULT without widening into forecast execution
- [x] MR3 — Market Postmortem Coach: extend the existing market-review lane with durable repeat / avoid / next-rule lesson capture across `/hq`, `/alpha`, and `/vault` so exact reopen preserves operator discipline without widening into forecast execution or a new dashboard
- [x] MO2 — Memory + OSINT stewardship polish: reuse strongest prior casefiles, file explicit source/evidence metadata, deepen VAULT cues, and reopen exact casefiles while keeping RECON and CYBER passive-first
- [x] VR2 — Vehicle + radar continuity polish: keep `nexus-vehicle-session-v1` backward compatible while improving radar-readiness guidance, imported bundle review, and exact VAULT handoffs from the existing vehicle artifact lane
- [x] RI1 — RECON Repo Intel: ship a protected public-GitHub metadata brief under `/api/repo-intel`, expose it as a compact RECON OSINT module and `recon-repo-intel` exact-session focus, wire an `analyze_repo` tool into the active agent runtime, and stage GitHub/dependency/competitor repo assessment through HQ without widening the shell into a new tab or code-ingestion surface
- [x] RI2 — GitHub Repo Assimilation Completion: finish the public-safe repo loop by adding explicit repo assimilation through RECON and HQ, exposing an `assimilate_repo` tool, filing durable repo-assimilation briefs into VAULT compiled pages, and upgrading ORBIT handoff to use the saved assimilation brief instead of only the lighter RI1 metadata profile
- [x] RI3 — Public Repo Compare Matrix: add explicit 2–3 repo comparison inside the existing RECON repo-intel lane, expose `compare_repos`, support `/compare-repos`, file durable `repo-compare` briefs into VAULT, and hand ORBIT the saved comparison brief instead of only single-repo intel
- [x] DR1 — NOVA Deep Research Engine: expose a real bounded `deep_research` tool through `/api/tools`, align `/deepresearch` guidance and INTEL/NOVA staging to that runtime, and keep explicit deep-research briefs filing durable `research-workflow` pages into VAULT without widening into a new research product surface
- [x] PC1 — Persona Council Completion: finish the older multi-persona HQ lane by routing Council mode through the existing AI/runtime spine, preserving single-persona prompts for normal dispatch, rendering trustworthy side-by-side council answers with explicit pick-or-merge actions, and adding the missing contract coverage so the feature stops being presentation-only scaffolding
- [x] SO1 — Switch Operator + Provider Resilience Completion: add explicit `/operator` one-shot orchestration, provider-health exact-session focus, shared COMMAND/HQ resilience posture, and actual-provider runtime reporting without inventing scheduler automation or a second provider-routing system
- [x] DU1 — Dynamic UI Contract Completion: finish the existing rules-and-alerts lane by making signal evaluation shell-wide, making dismissals event-aware instead of permanent, rendering the missing top-rail header indicator contract, and proving the behavior with dedicated dynamic-UI coverage before promoting the lane beyond experimental status
- [x] RG1 — Reasoning RAG + VAULT Article Index Completion: keep keyword routing as the cheap fast path, add bounded async reasoning fallback for low-confidence HQ retrieval, enrich saved VAULT clips with claim/entity indexes through the existing AI spine, and reuse those cues across HQ, VAULT, and the memory spine without adding a vector DB, a new public route, or an always-on background process
- [x] VG2 — VAULT Archive Compounding, Capture, and Weekly Synthesis: add local semantic cross-linking and backlink health, explicit dismissible `Save to VAULT` capture prompts for qualifying HQ answers, explicit `/weekly` archive synthesis that files durable `vault-weekly` briefs, and richer Obsidian-style export with frontmatter and wikilinks without widening Nexus into autosave, schedulers, or external graph services
- [x] UX2 — Visual Redesign Across the Core App: redesign the shared shell, HQ flagship composition, and all core tabs into a denser, graphics-heavy, premium operator system without changing routes, exact-session behavior, or assistant workflows
- [x] UX4 — Monochrome Tactical War-Room Redesign: pivot the shell and HQ from the older brass sanctum language into a unified monochrome tactical room with image-led route plates, flatter command-header framing, sharper functional surface naming, and one coherent war-room grammar across the core tabs without changing routes, workflow IDs, or continuity contracts
- [x] UX5 — Creative Work-First Tactical Redesign: absorbed into the shipped UX6 Satellite Ops redesign so the older box-removal and work-first HQ goals now live inside one bespoke whole-app visual system instead of a separate intermediate pass
- [x] UX6 — Bespoke Satellite Ops Redesign: replace the visible UX5 shell objects entirely with a bespoke Satellite Ops command environment built from route-specific operations plates, a new command header, reactive tactical motion, a custom HQ operating table, and a whole-app rail-based visual system that no longer reads like reused shell primitives
- [x] UX7 — Full Satellite Ops Migration and Interactive Experience: finish the redesign as a true full migration by replacing the remaining legacy route module/card wrappers with bespoke workplane/rail/continuity primitives, rebuilding HQ’s 3D scene into a real Satellite Ops command room, and adding richer stateful interaction so no obvious sanctum-era or boxed-dashboard objects remain in first-view route compositions
- [x] VX1 — Local Voice, Privacy Shield, and Repo Security Intelligence: keep Resources Voice Lab trustworthy across runtime-backed and browser-only paths, harden the local Impact graph/ownership/hotspot/security workbench, prove cloud-bound privacy shield behavior inside `/api/ai`, and ship the defensive `vuln-review` workflow with durable VAULT continuity
- [ ] FD2 — Blocked on the real staged Coolify hostname in repo-root `.env.local`; once present, stand up the first Coolify/VPS staging deployment from the repo-root `Dockerfile` with conservative web-self-hosted defaults and the preserved candidate branch `codex/preserve-main-2026-04-11`
- [ ] FD3 — Blocked on FD2 host config; run `npm run launch:gate:target` against that exact host after repo-root `.env.local` carries the real staged `NEXUS_RELEASE_BASE_URL` and local `NEXUS_TOKEN`
- [ ] FD4 — Blocked on staged-host proof; capture remote `/api/diagnostics`, GA route/manual smoke, and the focused A6 CYBER/SKILLS/HQ/COMMAND staging pass on the staged host
- [ ] FD5 — Blocked on staged-host proof; document the first production rollback target and promotion checklist before going live
- [x] A6 — Add shared governance metadata (risk tier, approval requirement, domain tags, operator-only/automation posture) and cyber pack baseline across skills, workflow packs, assistant capabilities, readiness lanes, and CYBER support rails
- [ ] A7b — Blocked forecast execution lane: compare a TimesFM companion provider against the native baseline only after decision-lift measurement and staging proof are stable

## Compatibility Tranche

- `CLAUDE.md`, `tasks/todo.md`, `tasks/lessons.md`, and `docs/AGENT_HANDOFF.md` remain in the repo for compatibility only.
- Historical execution detail remains in `docs/plans/`, `docs/ideas/`, and git history.
