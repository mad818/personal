interface:
  display_name: "To Tickets"
  short_description: "Split approved work into dependency-aware tracer tasks."
  default_prompt: "Use $to-tickets to split this plan into dependency-aware local tasks."
policy:
  allow_implicit_invocation: false
