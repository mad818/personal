# Nexus Prime — project npm config
#
# include=dev prevents npm from pruning devDependencies when NODE_ENV=production
# is set at the system level (common on Windows developer machines).
# Without this, tailwindcss, postcss, autoprefixer and all other devDeps get silently
# removed on the next `npm install`, breaking the build.
#
include=dev
