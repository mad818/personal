name: Path Safety

on:
  pull_request:
  push:
    branches:
      - main
      - master

jobs:
  check-path-collisions:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 24

      - name: Detect path collisions
        run: node scripts/check-path-collisions.js
