<!-- Archive/reference only. This file is no longer part of live context assembly.
     The canonical context spine is AGENTS.md + docs/SYSTEM_STATE.md + docs/STANDARDS.md + docs/PROJECT_BIBLE.md. -->

**Latest Claude Scholar assimilation**

- [docs/plans/claude-scholar-assimilation-2026-04-11.md](https://github.com/mad818/personal/blob/main/docs/plans/claude-scholar-assimilation-2026-04-11.md) — adapted Claude Scholar’s stage-based research lifecycle, filesystem-first project memory, and conservative source-aware workflow discipline into Nexus without adding new GA tabs.
- [lib/learningMissions.ts](https://github.com/mad818/personal/blob/main/lib/learningMissions.ts), [lib/researchSources.ts](https://github.com/mad818/personal/blob/main/lib/researchSources.ts), [components/home/office/hqAssistantContext.ts](https://github.com/mad818/personal/blob/main/components/home/office/hqAssistantContext.ts), and [components/home/office/OfficeCommandCenter.tsx](https://github.com/mad818/personal/blob/main/components/home/office/OfficeCommandCenter.tsx) — HQ now classifies research/study turns into workflow-pack stages, stages one strongest continuation, and mines the right local compartment for source review, synthesis, and study follow-through instead of treating everything as plain guided learning.
- [lib/projectMemory.ts](https://github.com/mad818/personal/blob/main/lib/projectMemory.ts) and [lib/memorySpineStore.ts](https://github.com/mad818/personal/blob/main/lib/memorySpineStore.ts) — Nexus now maintains `.nexus/project-memory/registry.json`, `current.md`, `daily/YYYY-MM-DD.md`, `source-inventory.md`, and `synthesis-map.md`, then folds those repo-bound summaries back into the local memory spine for retrieval.
- [lib/memoryMining.ts](https://github.com/mad818/personal/blob/main/lib/memoryMining.ts), [app/api/memory/mine/route.ts](https://github.com/mad818/personal/blob/main/app/api/memory/mine/route.ts), [components/vault/MemoryPalacePanel.tsx](https://github.com/mad818/personal/blob/main/components/vault/MemoryPalacePanel.tsx), [app/vault/page.tsx](https://github.com/mad818/personal/blob/main/app/vault/page.tsx), [lib/assistantCanonicalRegistry.ts](https://github.com/mad818/personal/blob/main/lib/assistantCanonicalRegistry.ts), and [lib/assistantSessionRegistry.ts](https://github.com/mad818/personal/blob/main/lib/assistantSessionRegistry.ts) — VAULT now supports `research` and `study` memory compartments, plus canonical `vault-memory-research` and `vault-memory-study` exact sessions under the existing route/query contract.
- [lib/artifactContinuity.ts](https://github.com/mad818/personal/blob/main/lib/artifactContinuity.ts), [lib/artifactPromotion.ts](https://github.com/mad818/personal/blob/main/lib/artifactPromotion.ts), [lib/memoryPagesStore.ts](https://github.com/mad818/personal/blob/main/lib/memoryPagesStore.ts), [app/api/memory/pages/route.ts](https://github.com/mad818/personal/blob/main/app/api/memory/pages/route.ts), [components/vault/DocumentIntakePanel.tsx](https://github.com/mad818/personal/blob/main/components/vault/DocumentIntakePanel.tsx), and [components/home/office/officeCommandCenterPostRun.ts](https://github.com/mad818/personal/blob/main/components/home/office/officeCommandCenterPostRun.ts) — compiled pages and durable artifacts now preserve workflow-pack, repo-memory, source-type, and evidence-strength metadata so research/study promotion and reopen behavior stays deterministic and source-aware.
- [components/resources/StudyWorkbenchConsole.tsx](https://github.com/mad818/personal/blob/main/components/resources/StudyWorkbenchConsole.tsx) and [app/skills/page.tsx](https://github.com/mad818/personal/blob/main/app/skills/page.tsx) — Resources and Skills now expose the Claude-Scholar-inspired operator jobs directly: frame the question, review sources, synthesize evidence, and open the exact study workspace, all without adding a new scholar tab.
- [__tests__/learningMissions.test.ts](https://github.com/mad818/personal/blob/main/__tests__/learningMissions.test.ts), [__tests__/memoryMining.test.ts](https://github.com/mad818/personal/blob/main/__tests__/memoryMining.test.ts), [tests/e2e/route-contract.spec.ts](https://github.com/mad818/personal/blob/main/tests/e2e/route-contract.spec.ts), and [tests/e2e/tab-surfaces.spec.ts](https://github.com/mad818/personal/blob/main/tests/e2e/tab-surfaces.spec.ts) — coverage now proves research workflow-pack routing, research/study compartment mining, and the new exact sessions under the stabilized authenticated-shell Playwright baseline.
- `npm run type-check`, `npm run verify`, `npm run route:e2e`, `npm run hq:e2e`, and `npm run tabs:e2e` all passed after the tranche. `npm run handoff:pull` remains blocked on this machine by the known GitHub credential issue (`SEC_E_NO_CREDENTIALS`).

**Latest DeepTutor + MemPalace assimilation**

- [docs/plans/deeptutor-mempalace-assimilation-2026-04-11.md](https://github.com/mad818/personal/blob/main/docs/plans/deeptutor-mempalace-assimilation-2026-04-11.md) — assimilated guided-learning and local-first memory-mining ideas from DeepTutor and MemPalace without adding new GA tabs.
- [lib/learningMissions.ts](https://github.com/mad818/personal/blob/main/lib/learningMissions.ts), [lib/memoryMining.ts](https://github.com/mad818/personal/blob/main/lib/memoryMining.ts), and [app/api/memory/mine/route.ts](https://github.com/mad818/personal/blob/main/app/api/memory/mine/route.ts) — Nexus now has shared learning-mission contracts, tutor profiles, memory compartments, and a local-only mined-memory route backed by the existing memory spine.
- [components/home/office/hqAnswerStyle.ts](https://github.com/mad818/personal/blob/main/components/home/office/hqAnswerStyle.ts), [components/home/office/hqAssistantContext.ts](https://github.com/mad818/personal/blob/main/components/home/office/hqAssistantContext.ts), and [components/home/office/OfficeCommandCenter.tsx](https://github.com/mad818/personal/blob/main/components/home/office/OfficeCommandCenter.tsx) — HQ now detects guided-learning turns, stages tutor-backed exact workspaces, mines local memory when it materially helps, and keeps the resulting study cues compact inside the existing assistant-first flow.
- [lib/artifactContinuity.ts](https://github.com/mad818/personal/blob/main/lib/artifactContinuity.ts), [lib/artifactPromotion.ts](https://github.com/mad818/personal/blob/main/lib/artifactPromotion.ts), [lib/memoryPagesStore.ts](https://github.com/mad818/personal/blob/main/lib/memoryPagesStore.ts), [components/vault/CompiledMemoryPagesPanel.tsx](https://github.com/mad818/personal/blob/main/components/vault/CompiledMemoryPagesPanel.tsx), [components/vault/MemoryPalacePanel.tsx](https://github.com/mad818/personal/blob/main/components/vault/MemoryPalacePanel.tsx), and [app/vault/page.tsx](https://github.com/mad818/personal/blob/main/app/vault/page.tsx) — VAULT now supports learning notes, study briefs, review sheets, quiz sets, and focused `project` / `conversation` / `general` memory-palace compartments through the same continuity and promotion system already used by the archive.
- [components/resources/StudyWorkbenchConsole.tsx](https://github.com/mad818/personal/blob/main/components/resources/StudyWorkbenchConsole.tsx), [components/resources/ResourcesWorkbench.tsx](https://github.com/mad818/personal/blob/main/components/resources/ResourcesWorkbench.tsx), [app/resources/page.tsx](https://github.com/mad818/personal/blob/main/app/resources/page.tsx), and [app/skills/page.tsx](https://github.com/mad818/personal/blob/main/app/skills/page.tsx) — Resources now has a study lane for learn/review/practice jobs, and the internal Skills surface now exposes guided-learning controls and memory-compartment openings instead of requiring a separate tutor product.
- [__tests__/learningMissions.test.ts](https://github.com/mad818/personal/blob/main/__tests__/learningMissions.test.ts), [__tests__/memoryMining.test.ts](https://github.com/mad818/personal/blob/main/__tests__/memoryMining.test.ts), [__tests__/artifactContinuity.test.ts](https://github.com/mad818/personal/blob/main/__tests__/artifactContinuity.test.ts), [__tests__/artifactPromotion.test.ts](https://github.com/mad818/personal/blob/main/__tests__/artifactPromotion.test.ts), [tests/e2e/hq-shell.spec.ts](https://github.com/mad818/personal/blob/main/tests/e2e/hq-shell.spec.ts), [tests/e2e/route-contract.spec.ts](https://github.com/mad818/personal/blob/main/tests/e2e/route-contract.spec.ts), and [tests/e2e/tab-surfaces.spec.ts](https://github.com/mad818/personal/blob/main/tests/e2e/tab-surfaces.spec.ts) — deterministic learning/memory tests landed, and browser coverage was extended for the new HQ learning, Resources study, and VAULT memory-palace lanes.
- `npm run type-check` and `npm run verify` passed after the batch. The remaining browser blocker from that tranche is now closed by [docs/plans/dtm5-auth-shell-baseline-recovery-2026-04-11.md](https://github.com/mad818/personal/blob/main/docs/plans/dtm5-auth-shell-baseline-recovery-2026-04-11.md): `auth:e2e`, `hq:e2e`, `route:e2e`, and `tabs:e2e` are green again under the isolated authenticated-shell baseline.

**Latest DTM5 auth/shell baseline recovery**

- [docs/plans/dtm5-auth-shell-baseline-recovery-2026-04-11.md](https://github.com/mad818/personal/blob/main/docs/plans/dtm5-auth-shell-baseline-recovery-2026-04-11.md) — closed the Playwright auth/toprail blocker by separating native auth coverage from deterministic authenticated-shell coverage, replacing `networkidle` assumptions with shell sentinels, and isolating the E2E runtime in `.next-e2e` on port `3100`.
- [playwright.auth.config.ts](https://github.com/mad818/personal/blob/main/playwright.auth.config.ts) — the DTM5 harness now runs against a deterministic built runtime instead of the hot-reloading dev shell, and it uses an isolated `NEXUS_NEXT_DIST_DIR=.next-e2e` so browser suites no longer collide with the operator's live `.next` tree.
- [tests/e2e/support/authenticatedShell.ts](https://github.com/mad818/personal/blob/main/tests/e2e/support/authenticatedShell.ts) — added shared authenticated-shell seeding for the Nexus session cookie plus `sessionStorage`, canonical URL normalization, shell-heal-safe `gotoShell(...)`, readiness diagnostics, and tolerant anchor-text matching for pages that legitimately repeat module titles.
- [tests/e2e/auth.spec.ts](https://github.com/mad818/personal/blob/main/tests/e2e/auth.spec.ts), [tests/e2e/hq-shell.spec.ts](https://github.com/mad818/personal/blob/main/tests/e2e/hq-shell.spec.ts), [tests/e2e/route-contract.spec.ts](https://github.com/mad818/personal/blob/main/tests/e2e/route-contract.spec.ts), and [tests/e2e/tab-surfaces.spec.ts](https://github.com/mad818/personal/blob/main/tests/e2e/tab-surfaces.spec.ts) — real auth UI checks now stay in `auth.spec.ts`, the shell/route/tab suites boot from deterministic authenticated state, canonical Resources and learning/memory routes are asserted against the assistant registry contract, and stale loading/no-data assumptions no longer mask auth-shell failures.
- `npm run auth:e2e`, `npm run hq:e2e`, `npm run route:e2e`, `npm run tabs:e2e`, `npm run type-check`, and `npm run verify` all passed after the recovery, so `DTM5` is closed and the next major milestone returns to the deployment track starting at `FD1C`.

**Latest data loader auto-heal**

- [docs/plans/data-loader-auto-heal-batch1-2026-04-10.md](https://github.com/mad818/personal/blob/main/docs/plans/data-loader-auto-heal-batch1-2026-04-10.md) — fixed the shared “boxes look stuck loading” failure after local runtime recovery by aligning standalone startup policy with `.env.local` and adding an immediate loader retry after runtime-policy cookies refresh.
- [scripts/start-runtime.mjs](https://github.com/mad818/personal/blob/main/scripts/start-runtime.mjs) — standalone runtime startup now preloads `.env.local` before spawning the production server, so middleware reads the saved `NEXUS_NETWORK_MODE` immediately instead of silently falling back to production `isolated` posture.
- [components/ui/RuntimePolicyCookieSync.tsx](https://github.com/mad818/personal/blob/main/components/ui/RuntimePolicyCookieSync.tsx), [components/ui/GlobalDataLoader.tsx](https://github.com/mad818/personal/blob/main/components/ui/GlobalDataLoader.tsx), [components/ui/DataLoader.tsx](https://github.com/mad818/personal/blob/main/components/ui/DataLoader.tsx), and [lib/runtimePolicyEvents.ts](https://github.com/mad818/personal/blob/main/lib/runtimePolicyEvents.ts) — once `/api/settings` refreshes middleware-visible policy cookies, the shared global and visible-polling loaders now retry immediately instead of waiting for the next timer/visibility event.
- [scripts/runtime-auto-heal.mjs](https://github.com/mad818/personal/blob/main/scripts/runtime-auto-heal.mjs) — fixed the Windows detached-launch edge where the healer awaited a PowerShell wrapper process after `unref()`, which caused unsettled top-level await warnings during relaunch.
- `npm run type-check` passed, authenticated `/api/prices`, `/api/news`, `/api/cves`, `/api/weather`, and `/api/fear-greed` now return `200`, and authenticated `/api/settings` reports `NEXUS_NETWORK_MODE=internal`. The later DTM5 recovery also brought `tabs:e2e` back to green, so the runtime/data path and the browser baseline are now aligned again.

**Latest runtime auto-heal**

- [docs/plans/runtime-auto-heal-batch1-2026-04-10.md](https://github.com/mad818/personal/blob/main/docs/plans/runtime-auto-heal-batch1-2026-04-10.md) — fixed the local `ERR_CONNECTION_REFUSED` recovery lane so operators can heal a dead web runtime safely instead of refreshing a dead tab.
- [scripts/runtime-auto-heal.mjs](https://github.com/mad818/personal/blob/main/scripts/runtime-auto-heal.mjs) and [package.json](https://github.com/mad818/personal/blob/main/package.json) — added supported `npm run runtime:heal` and `npm run hq:open` entrypoints, including a Windows-safe detached background launch path that keeps the standalone runtime alive after the heal command exits.
- [docs/deployment/README.md](https://github.com/mad818/personal/blob/main/docs/deployment/README.md) and [docs/deployment/web-operator-runbook.md](https://github.com/mad818/personal/blob/main/docs/deployment/web-operator-runbook.md) — deployment/operator docs now treat runtime healing as a first-class recovery action and explain the exact command flow for local HQ recovery.
- `npm run type-check`, `npm run runtime:heal`, live `http://127.0.0.1:3000/api/health`, and live `http://127.0.0.1:3000/hq` all passed after the fix. `npm run handoff:pull` is still blocked on this machine by the known GitHub credential issue (`SEC_E_NO_CREDENTIALS`).

**Latest post-redesign behavior cleanup**

- [docs/plans/post-redesign-behavior-cleanup-batch1-2026-04-10.md](https://github.com/mad818/personal/blob/main/docs/plans/post-redesign-behavior-cleanup-batch1-2026-04-10.md) — completed the next behavior-first tranche after the surface redesign by expanding durable continuity, adding duplicate-safe higher-order promotion, and finishing the last density cleanup across HQ, scheduler, VAULT, and Resources.
- [lib/artifactContinuity.ts](https://github.com/mad818/personal/blob/main/lib/artifactContinuity.ts), [lib/artifactPromotion.ts](https://github.com/mad818/personal/blob/main/lib/artifactPromotion.ts), [lib/secondBrainExport.ts](https://github.com/mad818/personal/blob/main/lib/secondBrainExport.ts), [app/vault/page.tsx](https://github.com/mad818/personal/blob/main/app/vault/page.tsx), and [components/vault/VaultExport.tsx](https://github.com/mad818/personal/blob/main/components/vault/VaultExport.tsx) — durable artifacts now carry route/workflow/mission continuity hints, second-brain export ranks related notes by continuity before source coincidence, and export heartbeat/index notes now track both reverse-engineering and research continuity.
- [components/vault/CompiledMemoryPagesPanel.tsx](https://github.com/mad818/personal/blob/main/components/vault/CompiledMemoryPagesPanel.tsx) — VAULT promotion now routes through one deterministic promotion service, checks a broader durable page slice before creating a higher-order brief, supports both RE and research promotions, and surfaces continuity-ranked related durable notes in the page detail view.
- [components/ui/ActionSessionCluster.tsx](https://github.com/mad818/personal/blob/main/components/ui/ActionSessionCluster.tsx), [components/ui/CronSchedulerJobsSection.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerJobsSection.tsx), [components/resources/PlaybooksConsole.tsx](https://github.com/mad818/personal/blob/main/components/resources/PlaybooksConsole.tsx), [components/resources/SpecDrivenConsole.tsx](https://github.com/mad818/personal/blob/main/components/resources/SpecDrivenConsole.tsx), [components/resources/SystemDesignConsole.tsx](https://github.com/mad818/personal/blob/main/components/resources/SystemDesignConsole.tsx), [components/resources/SurfaceCapabilitiesConsole.tsx](https://github.com/mad818/personal/blob/main/components/resources/SurfaceCapabilitiesConsole.tsx), [components/resources/ProjectImpactConsole.tsx](https://github.com/mad818/personal/blob/main/components/resources/ProjectImpactConsole.tsx), [components/vault/VaultStewardshipPanel.tsx](https://github.com/mad818/personal/blob/main/components/vault/VaultStewardshipPanel.tsx), [components/vault/VaultGraphFocusPanel.tsx](https://github.com/mad818/personal/blob/main/components/vault/VaultGraphFocusPanel.tsx), and [components/home/office/HQTerminalSection.tsx](https://github.com/mad818/personal/blob/main/components/home/office/HQTerminalSection.tsx) — dense action clusters now default to one strongest visible continuation, destructive/export controls moved behind disclosure in scheduler and Resources, and HQ dropped the last standing helper copy that duplicated the chronicle guidance.
- [__tests__/artifactContinuity.test.ts](https://github.com/mad818/personal/blob/main/__tests__/artifactContinuity.test.ts), [__tests__/artifactPromotion.test.ts](https://github.com/mad818/personal/blob/main/__tests__/artifactPromotion.test.ts), [__tests__/secondBrainExport.test.ts](https://github.com/mad818/personal/blob/main/__tests__/secondBrainExport.test.ts), and [tests/e2e/tab-surfaces.spec.ts](https://github.com/mad818/personal/blob/main/tests/e2e/tab-surfaces.spec.ts) — the new coverage checks richer continuity derivation, continuity-first related-note ranking, duplicate-safe RE/research promotion behavior, second-brain export continuity indexes, and disclosure-based density cleanup on Resources. `type-check`, `verify`, `build`, `route:e2e`, `hq:e2e`, and `tabs:e2e` passed after the batch. `test:unit` remains blocked on this machine because the local `vitest` binary is missing.

**Latest assistant-first surface redesign**

- [docs/plans/assistant-first-surface-redesign-batch2-2026-04-10.md](https://github.com/mad818/personal/blob/main/docs/plans/assistant-first-surface-redesign-batch2-2026-04-10.md) — completed the remaining redesign waves by carrying the assistant-first module system through COMMAND, INTEL, ALPHA, CYBER, RECON, and VAULT, then closing the final density/guidance audit.
- [lib/surfaceRedesignRegistry.ts](https://github.com/mad818/personal/blob/main/lib/surfaceRedesignRegistry.ts), [components/ui/SurfaceModuleSection.tsx](https://github.com/mad818/personal/blob/main/components/ui/SurfaceModuleSection.tsx), [components/ui/AssistantGuidanceStack.tsx](https://github.com/mad818/personal/blob/main/components/ui/AssistantGuidanceStack.tsx), [app/command/page.tsx](https://github.com/mad818/personal/blob/main/app/command/page.tsx), [app/intel/page.tsx](https://github.com/mad818/personal/blob/main/app/intel/page.tsx), [app/alpha/page.tsx](https://github.com/mad818/personal/blob/main/app/alpha/page.tsx), [app/cyber/page.tsx](https://github.com/mad818/personal/blob/main/app/cyber/page.tsx), [app/recon/page.tsx](https://github.com/mad818/personal/blob/main/app/recon/page.tsx), and [app/vault/page.tsx](https://github.com/mad818/personal/blob/main/app/vault/page.tsx) — segmented pages now resolve through registry-led module cards instead of standalone top-level panel titles, VAULT list/graph modes are reorganized around `Memory Brief`, `Archive Workbench`, `Durable Artifacts`, and `Relations`, and shared `assistantGuidance[]` now has a reusable renderer outside HQ.
- [__tests__/surfaceRedesignRegistry.test.ts](https://github.com/mad818/personal/blob/main/__tests__/surfaceRedesignRegistry.test.ts) and [tasks/todo.md](https://github.com/mad818/personal/blob/main/tasks/todo.md) — regression coverage now checks segmented module overrides, and the redesign backlog slices `SR1D`, `SR1E`, and `BF4C2` are marked complete.
- [docs/plans/assistant-first-surface-redesign-batch1-2026-04-10.md](https://github.com/mad818/personal/blob/main/docs/plans/assistant-first-surface-redesign-batch1-2026-04-10.md) — started the system-level redesign of the 8 GA tabs by introducing a shared assistant-first module registry, a shared module card primitive, and the first two reference-surface rewrites.
- [lib/surfaceRedesignRegistry.ts](https://github.com/mad818/personal/blob/main/lib/surfaceRedesignRegistry.ts), [components/ui/SurfaceModuleCard.tsx](https://github.com/mad818/personal/blob/main/components/ui/SurfaceModuleCard.tsx), [components/home/office/HQStrategiumDeck.tsx](https://github.com/mad818/personal/blob/main/components/home/office/HQStrategiumDeck.tsx), [components/home/office/HQTerminalSection.tsx](https://github.com/mad818/personal/blob/main/components/home/office/HQTerminalSection.tsx), [app/resources/page.tsx](https://github.com/mad818/personal/blob/main/app/resources/page.tsx), and [components/resources/ResourcesWorkbench.tsx](https://github.com/mad818/personal/blob/main/components/resources/ResourcesWorkbench.tsx) — the redesign language is now registry-driven instead of component-local, HQ’s strategium is merged into `Mission Brief`, `Next Move`, and `Runtime & Continuity`, the chronicle demotes standing workflow helper copy, and Resources now orients around `How This Helps` plus the four assistant-led workbench jobs instead of stacked posture cards.
- [__tests__/surfaceRedesignRegistry.test.ts](https://github.com/mad818/personal/blob/main/__tests__/surfaceRedesignRegistry.test.ts) — regression coverage now proves that every covered visible box maps to a valid redesign module and that Resources workbench views stay aligned with the new job model.

**Latest release boundary safeguard**

- [docs/plans/release-boundary-batch1-2026-04-10.md](https://github.com/mad818/personal/blob/main/docs/plans/release-boundary-batch1-2026-04-10.md) — closed the dirty-worktree ambiguity from the first-deployment audit by adding an explicit local release-boundary snapshot/check flow before any deployment proof or staged smoke.
- [scripts/release-boundary.ps1](https://github.com/mad818/personal/blob/main/scripts/release-boundary.ps1), [package.json](https://github.com/mad818/personal/blob/main/package.json), [scripts/launch-gate.ps1](https://github.com/mad818/personal/blob/main/scripts/launch-gate.ps1), [scripts/launch-gate-target.ps1](https://github.com/mad818/personal/blob/main/scripts/launch-gate-target.ps1), [docs/deployment/README.md](https://github.com/mad818/personal/blob/main/docs/deployment/README.md), [docs/deployment/release-readiness-checklist.md](https://github.com/mad818/personal/blob/main/docs/deployment/release-readiness-checklist.md), and [docs/deployment/web-operator-runbook.md](https://github.com/mad818/personal/blob/main/docs/deployment/web-operator-runbook.md) — local release proof now requires either a clean worktree or a captured `.nexus-release-boundary.json` snapshot, and both grouped launch gates refuse to proceed when the tree drifts beyond that explicit boundary.

**Latest release gate cleanup**

- [docs/plans/release-gate-batch1-2026-04-10.md](https://github.com/mad818/personal/blob/main/docs/plans/release-gate-batch1-2026-04-10.md) — turned the cleaned release path into one explicit local candidate gate plus one explicit target-runtime gate.
- [package.json](https://github.com/mad818/personal/blob/main/package.json), [docs/deployment/README.md](https://github.com/mad818/personal/blob/main/docs/deployment/README.md), [docs/deployment/release-readiness-checklist.md](https://github.com/mad818/personal/blob/main/docs/deployment/release-readiness-checklist.md), and [docs/deployment/web-operator-runbook.md](https://github.com/mad818/personal/blob/main/docs/deployment/web-operator-runbook.md) — `npm run launch:gate` now groups `verify`, `eval:agent-runtime:ci`, and `runtime:fresh-proof` for local release-candidate proof, while `npm run launch:gate:target` groups runtime consistency, route integrity, release smoke, and auth regression for an explicitly chosen container or staged host.

**Latest control-plane cleanup**

- [docs/plans/control-plane-cleanup-batch1-2026-04-10.md](https://github.com/mad818/personal/blob/main/docs/plans/control-plane-cleanup-batch1-2026-04-10.md) — normalized the control plane before first deployment proof by cleaning backlog drift, fixing repo-native audit drift, and requiring explicit runtime targets for release smoke, auth, and route-integrity commands.
- [scripts/runtime-target.mjs](https://github.com/mad818/personal/blob/main/scripts/runtime-target.mjs), [scripts/route-integrity.mjs](https://github.com/mad818/personal/blob/main/scripts/route-integrity.mjs), [scripts/release-smoke.mjs](https://github.com/mad818/personal/blob/main/scripts/release-smoke.mjs), [scripts/auth-regression.mjs](https://github.com/mad818/personal/blob/main/scripts/auth-regression.mjs), and [scripts/runtime-consistency.mjs](https://github.com/mad818/personal/blob/main/scripts/runtime-consistency.mjs) — runtime-targeted release checks now require `NEXUS_RELEASE_BASE_URL` or an intentional `NEXUS_ASSUME_LOCAL_RUNTIME=true` opt-in instead of silently assuming `127.0.0.1:3000`.
- [scripts/check-stale-todos.mjs](https://github.com/mad818/personal/blob/main/scripts/check-stale-todos.mjs), [scripts/audit.js](https://github.com/mad818/personal/blob/main/scripts/audit.js), and [tasks/todo.md](https://github.com/mad818/personal/blob/main/tasks/todo.md) — the stale checker now uses Windows-safe git invocation plus recent patch content, the full audit calls the correct path-collision script again, and the backlog now tracks the remaining assistant-first and first-deployment work without duplicate umbrella noise.

**Latest assistant-first tranche**

- [docs/plans/assistant-first-batch6-2026-04-10.md](https://github.com/mad818/personal/blob/main/docs/plans/assistant-first-batch6-2026-04-10.md) — next assistant-first tranche covering shared assistant guidance, canonical route/resource normalization, and passive continuity metadata for durable archive artifacts.
- [lib/assistantGuidance.ts](https://github.com/mad818/personal/blob/main/lib/assistantGuidance.ts), [components/home/office/types.ts](https://github.com/mad818/personal/blob/main/components/home/office/types.ts), [components/home/office/hqAssistantContext.ts](https://github.com/mad818/personal/blob/main/components/home/office/hqAssistantContext.ts), [components/home/office/OfficeCommandCenter.tsx](https://github.com/mad818/personal/blob/main/components/home/office/OfficeCommandCenter.tsx), and [components/home/office/HQTerminalSection.tsx](https://github.com/mad818/personal/blob/main/components/home/office/HQTerminalSection.tsx) — HQ guidance now flows through one deduped `assistantGuidance[]` contract instead of four separate cue fields, so state-led inline guidance can grow without duplicating message shape or chronicle rendering logic.
- [lib/assistantCanonicalRegistry.ts](https://github.com/mad818/personal/blob/main/lib/assistantCanonicalRegistry.ts), [lib/assistantCapabilityRegistry.ts](https://github.com/mad818/personal/blob/main/lib/assistantCapabilityRegistry.ts), [lib/exactSessionLinks.ts](https://github.com/mad818/personal/blob/main/lib/exactSessionLinks.ts), and [lib/assistantSessionRecovery.ts](https://github.com/mad818/personal/blob/main/lib/assistantSessionRecovery.ts) — canonical route aliases, segmented `focus/view` ownership, Resources id normalization, VAULT repair normalization, and exact-session recovery now share one registry layer instead of drifting across separate helper-local constant sets.
- [lib/artifactContinuity.ts](https://github.com/mad818/personal/blob/main/lib/artifactContinuity.ts), [lib/memoryPagesStore.ts](https://github.com/mad818/personal/blob/main/lib/memoryPagesStore.ts), [components/vault/vaultGraphPageUtils.ts](https://github.com/mad818/personal/blob/main/components/vault/vaultGraphPageUtils.ts), and [components/vault/CompiledMemoryPagesPanel.tsx](https://github.com/mad818/personal/blob/main/components/vault/CompiledMemoryPagesPanel.tsx) — durable compiled pages now derive a shared continuity payload (`artifactClass`, `continuityId`, `continuityTag`, `promotionKind`, `qualitySignals`) so reverse-engineering and research artifacts use one passive linking vocabulary, and RE brief promotion reopens existing higher-order artifacts by continuity identity before creating duplicates.
- [__tests__/assistantGuidance.test.ts](https://github.com/mad818/personal/blob/main/__tests__/assistantGuidance.test.ts), [__tests__/assistantCanonicalRegistry.test.ts](https://github.com/mad818/personal/blob/main/__tests__/assistantCanonicalRegistry.test.ts), [__tests__/artifactContinuity.test.ts](https://github.com/mad818/personal/blob/main/__tests__/artifactContinuity.test.ts), [tests/e2e/route-contract.spec.ts](https://github.com/mad818/personal/blob/main/tests/e2e/route-contract.spec.ts), and [scripts/eval-agent-runtime.js](https://github.com/mad818/personal/blob/main/scripts/eval-agent-runtime.js) — coverage now checks shared guidance priority/dedupe, canonical resource/vault normalization, durable continuity metadata, stale resource alias repair, and the assistant runtime’s new guidance/registry invariants directly.
- [docs/plans/assistant-first-batch5-2026-04-10.md](https://github.com/mad818/personal/blob/main/docs/plans/assistant-first-batch5-2026-04-10.md) — next assistant-first tranche covering shared exact-session recovery, auth-safe continuation, quieter Finder openings, passive archive cues, and risky-work execution attachment.
- [lib/assistantSessionRecovery.ts](https://github.com/mad818/personal/blob/main/lib/assistantSessionRecovery.ts), [lib/assistantSessionMemory.ts](https://github.com/mad818/personal/blob/main/lib/assistantSessionMemory.ts), [components/ui/PreparedWorkspaceAutoHeal.tsx](https://github.com/mad818/personal/blob/main/components/ui/PreparedWorkspaceAutoHeal.tsx), [components/auth/AuthGate.tsx](https://github.com/mad818/personal/blob/main/components/auth/AuthGate.tsx), and [components/resources/SessionFinderConsole.tsx](https://github.com/mad818/personal/blob/main/components/resources/SessionFinderConsole.tsx) — exact-session transport now shares one precedence contract across prepared workspaces, unfinished-session recovery, route-default exact fallbacks, auth return, and Finder openings instead of maintaining separate recovery logic per surface.
- [lib/assistantExecutionSignals.ts](https://github.com/mad818/personal/blob/main/lib/assistantExecutionSignals.ts), [components/home/office/hqAssistantContext.ts](https://github.com/mad818/personal/blob/main/components/home/office/hqAssistantContext.ts), [components/home/office/OfficeCommandCenter.tsx](https://github.com/mad818/personal/blob/main/components/home/office/OfficeCommandCenter.tsx), [components/home/office/HQTerminalSection.tsx](https://github.com/mad818/personal/blob/main/components/home/office/HQTerminalSection.tsx), and [components/home/office/types.ts](https://github.com/mad818/personal/blob/main/components/home/office/types.ts) — HQ now surfaces compact archive continuity and execution-context cues only when they materially help the current turn, and risky work can bias the prepared workspace toward Impact/System/Spec/Playbook context instead of a generic route default.
- [__tests__/assistantSessionMemory.test.ts](https://github.com/mad818/personal/blob/main/__tests__/assistantSessionMemory.test.ts), [__tests__/assistantSessionRecovery.test.ts](https://github.com/mad818/personal/blob/main/__tests__/assistantSessionRecovery.test.ts), [__tests__/assistantExecutionSignals.test.ts](https://github.com/mad818/personal/blob/main/__tests__/assistantExecutionSignals.test.ts), [tests/e2e/route-contract.spec.ts](https://github.com/mad818/personal/blob/main/tests/e2e/route-contract.spec.ts), [tests/e2e/hq-shell.spec.ts](https://github.com/mad818/personal/blob/main/tests/e2e/hq-shell.spec.ts), and [scripts/eval-agent-runtime.js](https://github.com/mad818/personal/blob/main/scripts/eval-agent-runtime.js) — regression coverage now includes auth-return exact-session restoration, capability-aware route recovery, and assistant archive/execution cues, while the eval harness checks the new recovery and cue layers directly.

**Memory spine foundation (advanced plan + first implementation slice)**

- [docs/plans/nexus-memory-spine-plan-2026-04-06.md](https://github.com/mad818/personal/blob/main/docs/plans/nexus-memory-spine-plan-2026-04-06.md) — advanced rollout plan for mapping Karpathy/Nick/StixDB-style `raw -> knowledge -> outputs` memory onto Nexus.
- [lib/memorySpine.ts](https://github.com/mad818/personal/blob/main/lib/memorySpine.ts) — pure shared derivation and search layer that normalizes saved articles, agent learnings, agent run artifacts, and mode briefings into one searchable memory contract.
- [components/vault/MemorySpineOverview.tsx](https://github.com/mad818/personal/blob/main/components/vault/MemorySpineOverview.tsx) — first visible VAULT surface for searching across raw artifacts, compiled knowledge, and outputs in one place.
- [app/vault/page.tsx](https://github.com/mad818/personal/blob/main/app/vault/page.tsx) — VAULT list and graph modes now both mount the new Memory Spine panel ahead of folders/librarian workflows.
- [__tests__/memorySpine.test.ts](https://github.com/mad818/personal/blob/main/__tests__/memorySpine.test.ts) — focused coverage for memory snapshot counts, layer filtering, and search ordering.
- [lib/memorySpineStore.ts](https://github.com/mad818/personal/blob/main/lib/memorySpineStore.ts) — server-side local-only durability layer that merges browser-synced clips/outputs with on-disk agent learnings into one reusable memory snapshot.
- [lib/memoryPagesStore.ts](https://github.com/mad818/personal/blob/main/lib/memoryPagesStore.ts) — local-only compiled memory page store for workflow artifacts and manually filed pages, with safe metadata feeding back into the memory spine.
- [components/ui/MemorySpineSync.tsx](https://github.com/mad818/personal/blob/main/components/ui/MemorySpineSync.tsx) — silent free-first sync that writes browser-owned memory sources into a local file for HQ and scheduler reuse.
- [app/api/memory/stats/route.ts](https://github.com/mad818/personal/blob/main/app/api/memory/stats/route.ts) and [app/api/memory/search/route.ts](https://github.com/mad818/personal/blob/main/app/api/memory/search/route.ts) — protected local memory APIs for reusable stats and search without adding any paid service or external database.
- [app/api/memory/pages/route.ts](https://github.com/mad818/personal/blob/main/app/api/memory/pages/route.ts) — protected local read/write route for compiled memory pages; list responses omit raw content and return only safe metadata/previews.
- [components/command/MemorySpineStatusCard.tsx](https://github.com/mad818/personal/blob/main/components/command/MemorySpineStatusCard.tsx) — COMMAND-side visibility into local-only memory readiness, sync freshness, and free-first posture.
- [lib/memorySpine.ts](https://github.com/mad818/personal/blob/main/lib/memorySpine.ts) — now classifies items as `safe`, `internal`, or `restricted`, redacts sensitive text patterns, and withholds restricted artifacts from broad memory search by default.
- [components/vault/CompiledMemoryPagesPanel.tsx](https://github.com/mad818/personal/blob/main/components/vault/CompiledMemoryPagesPanel.tsx) and [app/vault/page.tsx](https://github.com/mad818/personal/blob/main/app/vault/page.tsx) — VAULT now shows durable compiled pages alongside the memory spine and saved clips.
- [components/home/office/OfficeCommandCenter.tsx](https://github.com/mad818/personal/blob/main/components/home/office/OfficeCommandCenter.tsx) — successful HQ workflow commands now auto-write compiled memory pages with workflow, route, topic, and agent metadata.
- [components/ui/CronSchedulerRunner.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerRunner.tsx) — scheduled jobs targeting `vault` or `review` now also write compiled memory pages, not only registry artifacts.
- [lib/memoryPagesStore.ts](https://github.com/mad818/personal/blob/main/lib/memoryPagesStore.ts) and [components/vault/CompiledMemoryPagesPanel.tsx](https://github.com/mad818/personal/blob/main/components/vault/CompiledMemoryPagesPanel.tsx) — compiled-page detail reads now fetch through the protected route and still withhold restricted page bodies even on direct access.
- [lib/memoryAdapters.ts](https://github.com/mad818/personal/blob/main/lib/memoryAdapters.ts) and [app/api/memory/eval/route.ts](https://github.com/mad818/personal/blob/main/app/api/memory/eval/route.ts) — optional StixDB-style sidecar evaluation now sits behind a loopback-only, query-only adapter contract that never auto-syncs restricted Nexus artifacts out to third-party memory infrastructure.
- [app/api/memory/stats/route.ts](https://github.com/mad818/personal/blob/main/app/api/memory/stats/route.ts) and [components/command/MemorySpineStatusCard.tsx](https://github.com/mad818/personal/blob/main/components/command/MemorySpineStatusCard.tsx) — COMMAND now shows native-vs-sidecar memory posture explicitly so the default local boundary stays visible even when sidecar evaluation is configured.
- [lib/memoryAnswers.ts](https://github.com/mad818/personal/blob/main/lib/memoryAnswers.ts) and [app/api/memory/ask/route.ts](https://github.com/mad818/personal/blob/main/app/api/memory/ask/route.ts) — local memory ask now returns deterministic answer, confidence, citations, and related items from sanitized Nexus-native memory first; optional sidecar comparison only appears when explicitly requested and never takes over the answer boundary.
- [components/vault/MemoryAskPanel.tsx](https://github.com/mad818/personal/blob/main/components/vault/MemoryAskPanel.tsx) and [app/vault/page.tsx](https://github.com/mad818/personal/blob/main/app/vault/page.tsx) — VAULT now includes a compact operator-facing memory query panel in both list and graph modes so `/api/memory/ask` is usable directly from the archive surface with layer scoping, explicit compare mode, and local citations.
- [components/vault/MemoryAskPanel.tsx](https://github.com/mad818/personal/blob/main/components/vault/MemoryAskPanel.tsx) and [app/command/page.tsx](https://github.com/mad818/personal/blob/main/app/command/page.tsx) — the same native memory ask panel now appears in COMMAND with mission-lane copy, so operators can query local memory and inspect citations during live work without leaving the command surface.
- [components/home/office/OfficeCommandCenter.tsx](https://github.com/mad818/personal/blob/main/components/home/office/OfficeCommandCenter.tsx), [components/vault/MemoryAskPanel.tsx](https://github.com/mad818/personal/blob/main/components/vault/MemoryAskPanel.tsx), and [app/command/page.tsx](https://github.com/mad818/personal/blob/main/app/command/page.tsx) — HQ can now promote the current live prompt into COMMAND’s native memory lane with one click, and the COMMAND-side memory ask panel accepts routed prefills and auto-runs the local query on arrival.
- [components/home/office/AskMemoryFromReplyButton.tsx](https://github.com/mad818/personal/blob/main/components/home/office/AskMemoryFromReplyButton.tsx), [components/home/office/types.ts](https://github.com/mad818/personal/blob/main/components/home/office/types.ts), and [components/home/office/OfficeCommandCenter.tsx](https://github.com/mad818/personal/blob/main/components/home/office/OfficeCommandCenter.tsx) — completed HQ chronicle replies now retain their originating operator query and expose an inline “Ask memory” action beside playback, so useful answers can be promoted straight into COMMAND’s native memory lane without retyping while keeping the same local-only memory boundary.
- [lib/memorySpine.ts](https://github.com/mad818/personal/blob/main/lib/memorySpine.ts), [lib/memoryPagesStore.ts](https://github.com/mad818/personal/blob/main/lib/memoryPagesStore.ts), [app/api/memory/pages/route.ts](https://github.com/mad818/personal/blob/main/app/api/memory/pages/route.ts), and [components/home/office/FileBackButton.tsx](https://github.com/mad818/personal/blob/main/components/home/office/FileBackButton.tsx) — HQ reply filing now supports editable tags and explicit visibility intent, but the server clamps requested visibility against auto-detected sensitivity so manual filing can raise protection without ever downgrading restricted/internal content, and the local VAULT preview uses sanitized metadata rather than raw reply text.

**Ecosystem assimilation (docs-only, no vendored third-party code)**

- [docs/ideas/assimilated-ecosystem.md](https://github.com/mad818/personal/blob/main/docs/ideas/assimilated-ecosystem.md) — maps external repos (PM Skills, Coolify, autoresearch, UncommonRoute, Onyx, Sentrux, etc.) to Nexus patterns.
- [docs/ideas/external-links-mapping.md](https://github.com/mad818/personal/blob/main/docs/ideas/external-links-mapping.md) — broader link inventory.
- [docs/ideas/README.md](https://github.com/mad818/personal/blob/main/docs/ideas/README.md) — ideas index.
- [docs/plans/feynman-pentagi-rtk-assimilation-batch1-2026-04-06.md](https://github.com/mad818/personal/blob/main/docs/plans/feynman-pentagi-rtk-assimilation-batch1-2026-04-06.md) — structured HQ workflow-command assimilation from Feynman, Pentagi, and RTK patterns.

**Latest command assimilation**

- [components/home/office/workflowCommands.ts](https://github.com/mad818/personal/blob/main/components/home/office/workflowCommands.ts) — reusable slash-command contract for deep research, lit review, compare, brief, threat hunt, and evidence pack workflows.
- [components/home/office/OfficeCommandCenter.tsx](https://github.com/mad818/personal/blob/main/components/home/office/OfficeCommandCenter.tsx) — HQ dispatch now recognizes workflow commands, routes them to the right front, and appends compact structured directives.
- [lib/developerResources.ts](https://github.com/mad818/personal/blob/main/lib/developerResources.ts) — Field Manual now includes Feynman, Pentagi, and RTK as curated ecosystem references.
- [docs/plans/awesome-claude-code-assimilation-batch1-2026-04-06.md](https://github.com/mad818/personal/blob/main/docs/plans/awesome-claude-code-assimilation-batch1-2026-04-06.md), [components/command/RuntimeEfficiencyCard.tsx](https://github.com/mad818/personal/blob/main/components/command/RuntimeEfficiencyCard.tsx), [components/home/office/workflowCommands.ts](https://github.com/mad818/personal/blob/main/components/home/office/workflowCommands.ts), [components/command/WorkflowCommandOpsCard.tsx](https://github.com/mad818/personal/blob/main/components/command/WorkflowCommandOpsCard.tsx), and [app/command/page.tsx](https://github.com/mad818/personal/blob/main/app/command/page.tsx) — assimilated the strongest `awesome-claude-code` patterns into Nexus by turning runtime efficiency into an actionable local usage monitor and by surfacing HQ slash-command governance plus active hook posture directly in COMMAND, without importing any external framework or widening the product boundary.
- [docs/plans/awesome-claude-code-assimilation-batch2-2026-04-06.md](https://github.com/mad818/personal/blob/main/docs/plans/awesome-claude-code-assimilation-batch2-2026-04-06.md), [lib/schedulerGovernance.ts](https://github.com/mad818/personal/blob/main/lib/schedulerGovernance.ts), [components/ui/SchedulerGovernanceCard.tsx](https://github.com/mad818/personal/blob/main/components/ui/SchedulerGovernanceCard.tsx), [components/ui/CronSchedulerPanel.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerPanel.tsx), [components/home/office/workflowCommands.ts](https://github.com/mad818/personal/blob/main/components/home/office/workflowCommands.ts), and [components/command/WorkflowCommandOpsCard.tsx](https://github.com/mad818/personal/blob/main/components/command/WorkflowCommandOpsCard.tsx) — extended the same assimilation into scheduler posture by adding non-interactive usage-monitor recommendations, explicit automation-candidate versus review-only workflow guidance, and visible governance around durable outputs and approval modes before mission-template automation is expanded.
- [docs/plans/awesome-claude-code-assimilation-batch3-2026-04-06.md](https://github.com/mad818/personal/blob/main/docs/plans/awesome-claude-code-assimilation-batch3-2026-04-06.md), [components/home/office/workflowCommands.ts](https://github.com/mad818/personal/blob/main/components/home/office/workflowCommands.ts), and [components/ui/CronSchedulerPanel.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerPanel.tsx) — pushed the assimilation one step further by giving safe HQ workflows scheduler-template defaults, so `/deepresearch`, `/lit-review`, `/compare`, and `/brief` can prefill human-gated scheduled missions from the Cron Scheduler drawer while `/threat-hunt` and `/evidence-pack` stay visible but explicitly review-only.
- [docs/plans/awesome-claude-code-assimilation-batch4-2026-04-06.md](https://github.com/mad818/personal/blob/main/docs/plans/awesome-claude-code-assimilation-batch4-2026-04-06.md), [components/home/office/workflowCommands.ts](https://github.com/mad818/personal/blob/main/components/home/office/workflowCommands.ts), [components/ui/CronSchedulerPanel.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerPanel.tsx), and [components/ui/CronSchedulerRunner.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerRunner.tsx) — completed the next auditability step by resolving scheduled mission lineage from the workflow catalog, surfacing command/source/automation posture directly in the scheduler job list, and persisting workflow id / label / automation-posture tags into scheduler-created registry items and compiled memory pages.
- [docs/plans/awesome-claude-code-assimilation-batch5-2026-04-06.md](https://github.com/mad818/personal/blob/main/docs/plans/awesome-claude-code-assimilation-batch5-2026-04-06.md), [store/useStore.ts](https://github.com/mad818/personal/blob/main/store/useStore.ts), [lib/schedulerGovernance.ts](https://github.com/mad818/personal/blob/main/lib/schedulerGovernance.ts), [components/ui/SchedulerGovernanceCard.tsx](https://github.com/mad818/personal/blob/main/components/ui/SchedulerGovernanceCard.tsx), [components/ui/CronSchedulerRunner.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerRunner.tsx), and [components/ui/CronSchedulerPanel.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerPanel.tsx) — carried the `awesome-claude-code` usage-monitor idea into scheduled missions by persisting local efficiency snapshots after each non-interactive run, summarizing measured prompt load plus low-cacheability gaps in scheduler governance, and surfacing per-job prompt/cacheability posture directly in the Cron Scheduler drawer.

**Efficiency hardening**

- [lib/ai.ts](https://github.com/mad818/personal/blob/main/lib/ai.ts), [components/ui/CronSchedulerRunner.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerRunner.tsx), [lib/schedulerGovernance.ts](https://github.com/mad818/personal/blob/main/lib/schedulerGovernance.ts), [components/ui/SchedulerGovernanceCard.tsx](https://github.com/mad818/personal/blob/main/components/ui/SchedulerGovernanceCard.tsx), and [components/ui/CronSchedulerPanel.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerPanel.tsx) — advanced `UCE4/A5` by splitting scheduled mission prompts into a stable cacheable prefix plus a volatile task delta, teaching Anthropic-compatible non-interactive runs to cache that user-side prefix block, switching equivalent scheduled jobs onto a shared minute-window single-flight key, and exposing cache strategy / dedupe posture in the scheduler’s stored efficiency snapshots and governance UI.
- [lib/ai.ts](https://github.com/mad818/personal/blob/main/lib/ai.ts), [components/ui/CronSchedulerRunner.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerRunner.tsx), [store/useStore.ts](https://github.com/mad818/personal/blob/main/store/useStore.ts), [lib/schedulerGovernance.ts](https://github.com/mad818/personal/blob/main/lib/schedulerGovernance.ts), [components/ui/SchedulerGovernanceCard.tsx](https://github.com/mad818/personal/blob/main/components/ui/SchedulerGovernanceCard.tsx), and [components/ui/CronSchedulerPanel.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerPanel.tsx) — completed the next `A5` step with a safe provider-agnostic scheduler batch lane: eligible review-gated missions that fire in the same window can now share one structured non-interactive request with per-job results, batched runs are recorded in the scheduler efficiency snapshot, and governance/UI now show batch usage alongside cache strategy and dedupe posture. The remaining open piece is provider-native Anthropic batch usage plus explicit cache hit/miss counters.
- [app/api/ai/route.ts](https://github.com/mad818/personal/blob/main/app/api/ai/route.ts), [lib/ai.ts](https://github.com/mad818/personal/blob/main/lib/ai.ts), [store/useStore.ts](https://github.com/mad818/personal/blob/main/store/useStore.ts), [lib/schedulerGovernance.ts](https://github.com/mad818/personal/blob/main/lib/schedulerGovernance.ts), [components/ui/SchedulerGovernanceCard.tsx](https://github.com/mad818/personal/blob/main/components/ui/SchedulerGovernanceCard.tsx), and [components/ui/CronSchedulerPanel.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerPanel.tsx) — closed the cache-observability half of `A5` by forwarding safe Anthropic prompt-cache usage headers through `/api/ai`, teaching the non-interactive AI wrapper to return cache metadata, and recording/surfacing cache hits versus cache warmups directly in scheduled-run efficiency snapshots and scheduler governance. The main remaining open piece is provider-native Anthropic batch usage.
- [lib/anthropicBatches.ts](https://github.com/mad818/personal/blob/main/lib/anthropicBatches.ts), [app/api/ai/batches/route.ts](https://github.com/mad818/personal/blob/main/app/api/ai/batches/route.ts), [app/api/ai/batches/[batchId]/route.ts](https://github.com/mad818/personal/blob/main/app/api/ai/batches/%5BbatchId%5D/route.ts), [lib/ai.ts](https://github.com/mad818/personal/blob/main/lib/ai.ts), [components/ui/CronSchedulerRunner.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerRunner.tsx), [components/ui/CronSchedulerPanel.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerPanel.tsx), [lib/schedulerGovernance.ts](https://github.com/mad818/personal/blob/main/lib/schedulerGovernance.ts), [components/ui/SchedulerGovernanceCard.tsx](https://github.com/mad818/personal/blob/main/components/ui/SchedulerGovernanceCard.tsx), [store/useStore.ts](https://github.com/mad818/personal/blob/main/store/useStore.ts), [lib/security/routePolicy.ts](https://github.com/mad818/personal/blob/main/lib/security/routePolicy.ts), and [.env.example](https://github.com/mad818/personal/blob/main/.env.example) — completed the guarded native-batch half of `A5`: scheduled jobs can now optionally submit eligible Anthropic mission groups to a protected local queued batch lane when `NEXUS_ENABLE_ANTHROPIC_NATIVE_BATCHES=true` and paid Anthropic access is available, queued jobs are tracked without re-submitting every scheduler tick, completed native batches flow back through the same artifact/memory pipeline, prompt accounting now includes the shared cached user prefix more accurately, and the existing internal batch lane remains the default fallback so free-first installs stay unchanged.
- [store/useStore.ts](https://github.com/mad818/personal/blob/main/store/useStore.ts), [lib/schedulerGovernance.ts](https://github.com/mad818/personal/blob/main/lib/schedulerGovernance.ts), [components/ui/SchedulerGovernanceCard.tsx](https://github.com/mad818/personal/blob/main/components/ui/SchedulerGovernanceCard.tsx), [components/ui/CronSchedulerRunner.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerRunner.tsx), and [components/ui/CronSchedulerPanel.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerPanel.tsx) — reinforced the queued native batch lane so it cannot silently stall forever: queued jobs now track poll-failure health, the scheduler automatically releases a queue after repeated poll failures instead of leaving it stuck indefinitely, governance surfaces “queued trouble” explicitly, and the scheduler drawer now shows queued age/retry count plus a manual `Clear queue` escape hatch for operators.
- [lib/anthropicBatches.ts](https://github.com/mad818/personal/blob/main/lib/anthropicBatches.ts), [app/api/ai/batches/route.ts](https://github.com/mad818/personal/blob/main/app/api/ai/batches/route.ts), [lib/ai.ts](https://github.com/mad818/personal/blob/main/lib/ai.ts), and [components/ui/CronSchedulerPanel.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerPanel.tsx) — added an explicit protected local posture read for the optional Anthropic native-batch lane and surfaced it directly in the scheduler drawer, so operators can see before any mission runs whether this install is truly native-ready or still on the default free-first internal batching path, without exposing secrets or widening the product boundary.
- [components/ui/CronSchedulerRunner.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerRunner.tsx) — scheduler-created registry items and compiled memory pages now persist explicit execution-origin metadata (`single_run`, `internal_batch`, or `provider_native_batch`) in tags, notes, and source labels, so later audits can attribute artifacts to the correct execution lane without relying on the live scheduler UI state.
- [store/useStore.ts](https://github.com/mad818/personal/blob/main/store/useStore.ts), [components/ui/CronSchedulerRunner.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerRunner.tsx), and [components/ui/CronSchedulerPanel.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerPanel.tsx) — each scheduled job now retains the last successful artifact origin and target on the job itself, and the scheduler drawer surfaces that as a compact “Last artifact” hint, so operators can see the most recent writeback lane without opening VAULT or registry artifacts first.
- [components/ui/CronSchedulerRunner.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerRunner.tsx) — scheduler runtime logs and notifications now carry the same execution-lane wording used by artifact notes and drawer hints (`single run`, `internal batch`, `provider-native batch`), so the live operator feed matches the persisted audit trail instead of leaving lane attribution implicit.
- [store/useStore.ts](https://github.com/mad818/personal/blob/main/store/useStore.ts), [components/ui/CronSchedulerRunner.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerRunner.tsx), and [components/ui/CronSchedulerPanel.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerPanel.tsx) — scheduled jobs now also retain the last completed execution lane even when no artifact is written, and the drawer only shows that compact “Last lane” hint when it adds signal beyond the artifact-origin pill, so `notify` / `none` / failed runs do not lose lane attribution after the live feed scrolls away.
- [store/useStore.ts](https://github.com/mad818/personal/blob/main/store/useStore.ts), [components/ui/CronSchedulerRunner.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerRunner.tsx), and [components/ui/CronSchedulerPanel.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerPanel.tsx) — scheduled jobs now keep a small recent execution ledger in local persisted state, and the drawer renders that as a compact recent-runs audit trail with status, lane, artifact posture, cache strategy, and summary for the last few executions without introducing a new route or widening the product boundary.
- [lib/schedulerGovernance.ts](https://github.com/mad818/personal/blob/main/lib/schedulerGovernance.ts) and [components/ui/CronSchedulerPanel.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerPanel.tsx) — scheduler governance now exposes one centralized local audit-export payload and matching `Copy audit` / `Export audit` drawer actions, so the same lane/governance vocabulary shown in the UI can leave the drawer as JSON without any new route or backend dependency.
- [lib/schedulerGovernance.ts](https://github.com/mad818/personal/blob/main/lib/schedulerGovernance.ts) and [components/ui/CronSchedulerPanel.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerPanel.tsx) — that same centralized scheduler-audit payload now supports explicit per-job scope metadata, and each job card exposes `Copy job audit` / `Export job audit` actions so a single mission’s history can leave the drawer without exporting the full scheduler bundle.
- [lib/schedulerGovernance.ts](https://github.com/mad818/personal/blob/main/lib/schedulerGovernance.ts) and [components/ui/CronSchedulerPanel.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerPanel.tsx) — scheduler audit now has one shared filter vocabulary for lane, outcome, and time window, and the drawer applies those compact filters to both recent-run UI and full/per-job JSON exports so copied artifacts stay aligned with what the operator is looking at.
- [lib/schedulerGovernance.ts](https://github.com/mad818/personal/blob/main/lib/schedulerGovernance.ts) and [components/ui/CronSchedulerPanel.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerPanel.tsx) — the scheduler drawer now restores the last selected audit filters from validated local browser storage, so reloads and drawer reopen keep the same narrowed lane/outcome/time slice without adding any backend state or a second filter model.
- [lib/schedulerGovernance.ts](https://github.com/mad818/personal/blob/main/lib/schedulerGovernance.ts) and [components/ui/CronSchedulerPanel.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerPanel.tsx) — operators can now save up to six named scheduler audit views in local browser storage, then re-apply or remove those saved slices directly in the drawer without adding any backend dependency or drifting away from the shared lane/outcome/time contract.
- [lib/schedulerGovernance.ts](https://github.com/mad818/personal/blob/main/lib/schedulerGovernance.ts) and [components/ui/CronSchedulerPanel.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerPanel.tsx) — those saved scheduler audit views can now also be exported and re-imported as a validated local JSON bundle, with name-based merge behavior and the same six-view cap, so operators can carry useful slices between machines without adding any backend dependency or alternate schema.
- [lib/schedulerGovernance.ts](https://github.com/mad818/personal/blob/main/lib/schedulerGovernance.ts) and [components/ui/CronSchedulerPanel.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerPanel.tsx) — saved audit view imports now pause at a compact preview first, showing which names will be added, which existing names will be replaced, and whether the six-view cap will trim older entries before the operator confirms the merge.
- [components/ui/CronSchedulerPanel.tsx](https://github.com/mad818/personal/blob/main/components/ui/CronSchedulerPanel.tsx) — that same saved audit view import lane now also accepts pasted JSON directly in the drawer and feeds it through the same validated preview-and-confirm flow as file imports, so operators can reuse slices without touching the filesystem.
- [lib/memoryPagesStore.ts](https://github.com/mad818/personal/blob/main/lib/memoryPagesStore.ts) and [components/vault/CompiledMemoryPagesPanel.tsx](https://github.com/mad818/personal/blob/main/components/vault/CompiledMemoryPagesPanel.tsx) — compiled memory pages now derive local research signals like source counts, citation cues, document hints, section structure, and referenced domains; restricted pages keep those details withheld by default, while the VAULT panel surfaces the safe/internal signals so research artifacts are easier to trust and compare at a glance.
- [lib/memoryPagesStore.ts](https://github.com/mad818/personal/blob/main/lib/memoryPagesStore.ts), [app/api/memory/pages/route.ts](https://github.com/mad818/personal/blob/main/app/api/memory/pages/route.ts), [components/vault/DocumentIntakePanel.tsx](https://github.com/mad818/personal/blob/main/components/vault/DocumentIntakePanel.tsx), and [components/vault/CompiledMemoryPagesPanel.tsx](https://github.com/mad818/personal/blob/main/components/vault/CompiledMemoryPagesPanel.tsx) — VAULT now has a local-only OCR-ready document intake lane for pasted extracted text, compiled memory pages accept sanitized document metadata like origin label, mime type, and page count, restricted pages keep that metadata withheld by default, and the compiled-page surface refreshes immediately after intake without any upload or external OCR dependency.
- [app/vault/page.tsx](https://github.com/mad818/personal/blob/main/app/vault/page.tsx) — the VAULT graph now pulls compiled memory page summaries from the protected local route and synthesizes them alongside saved articles, while restricted compiled pages stay represented as durable nodes without contributing rich summary text, tags, document metadata, or research-signal-driven graph edges.
- [components/vault/VaultGraphFocusPanel.tsx](https://github.com/mad818/personal/blob/main/components/vault/VaultGraphFocusPanel.tsx) and [app/vault/page.tsx](https://github.com/mad818/personal/blob/main/app/vault/page.tsx) — VAULT graph mode now includes a local-only focus panel for clicked nodes, with saved clips rendered directly from local store state and compiled pages resolved through the protected memory detail route, while restricted compiled pages still withhold body content and sensitive derived details by default.
- [components/vault/VaultGraphFocusPanel.tsx](https://github.com/mad818/personal/blob/main/components/vault/VaultGraphFocusPanel.tsx) and [app/vault/page.tsx](https://github.com/mad818/personal/blob/main/app/vault/page.tsx) — that same graph focus panel now also exposes linked-context summaries from the current graph edges, sorted by strength, while any relationship involving a restricted compiled page is downgraded to a generic protected-linkage reason instead of surfacing detailed edge explanations.
- [components/home/office/types.ts](https://github.com/mad818/personal/blob/main/components/home/office/types.ts) and [app/vault/page.tsx](https://github.com/mad818/personal/blob/main/app/vault/page.tsx) — VAULT graph mode now applies compact source and visibility filters on top of the local graph, using small `originKind` / `visibility` hints on graph nodes so operators can isolate saved clips, compiled pages, safe-only views, or sensitive topology without changing the protected local graph boundary or exposing additional detail.
- [app/vault/page.tsx](https://github.com/mad818/personal/blob/main/app/vault/page.tsx) — VAULT graph mode now also shows compact topology stats and legends for the active filtered view, including visible nodes, edges, clusters, orphans, node-type mix, and visibility mix, all derived only from the currently visible local graph so no hidden-node counts leak through the summary layer.
- [app/vault/page.tsx](https://github.com/mad818/personal/blob/main/app/vault/page.tsx) — VAULT graph mode now also includes one-click local presets for balanced, safe-only, compiled research, and restricted topology views, and it restores the last used source/visibility filter pair from validated browser storage without adding any backend state or changing the protected local graph boundary.
- [app/vault/page.tsx](https://github.com/mad818/personal/blob/main/app/vault/page.tsx) — VAULT graph mode now also includes a local-only `Reset view` action that clears the saved browser-side graph filter state, returns the graph to the balanced default, and clears the current node selection without adding any backend state or changing the protected local graph boundary.
- [app/vault/page.tsx](https://github.com/mad818/personal/blob/main/app/vault/page.tsx) — VAULT graph mode now also displays a compact active-view label derived from the existing source/visibility filter state, so the operator can immediately see whether the current graph is the balanced default, a named preset, or a custom filtered state without adding any backend state or second filter model.
- [app/vault/page.tsx](https://github.com/mad818/personal/blob/main/app/vault/page.tsx) — VAULT graph mode now also includes a local-only `Copy view summary` action that copies the active view label, filter state, visible topology counts, node-type mix, and visibility mix from the currently visible filtered graph, with lightweight client-side feedback and no backend persistence or hidden-graph leakage.
- [app/vault/page.tsx](https://github.com/mad818/personal/blob/main/app/vault/page.tsx) — VAULT graph mode now also includes a local-only `Copy visible nodes` action that exports only the currently visible filtered node set with each node’s visible title, type, visibility, and origin kind, plus the active view label and visible-node count, without exposing hidden-node state or adding any backend persistence.

**Self-hosting & containers**

- [docs/deployment/coolify.md](https://github.com/mad818/personal/blob/main/docs/deployment/coolify.md) — Coolify vs Nixpacks vs Dockerfile.
- [docs/deployment/README.md](https://github.com/mad818/personal/blob/main/docs/deployment/README.md) — deployment index + `docker build` one-liner.
- [Dockerfile](https://github.com/mad818/personal/blob/main/Dockerfile) — multi-stage Node 20 Alpine, Next **standalone** (`node server.js`, port 3000).
- [.dockerignore](https://github.com/mad818/personal/blob/main/.dockerignore) — keeps build context lean.
- [next.config.js](https://github.com/mad818/personal/blob/main/next.config.js) — `output: 'standalone'` for the Docker image.

**Environment & IDE routers**

- [.env.example](https://github.com/mad818/personal/blob/main/.env.example) — optional block for **UncommonRoute** / **factory-cursor-bridge** (`OPENAI_BASE_URL` / `ANTHROPIC_BASE_URL` in Cursor, not Nexus server).

**Repo entrypoints updated**

- [README.md](https://github.com/mad818/personal/blob/main/README.md) — Self-hosting section, project tree includes `Dockerfile`, `docs/deployment/`, `docs/ideas/assimilated-ecosystem.md`.
- [CLAUDE.md](https://github.com/mad818/personal/blob/main/CLAUDE.md) — `docs/` paths + assimilated ecosystem pointer.
- [docs/release-support-matrix.md](https://github.com/mad818/personal/blob/main/docs/release-support-matrix.md) — GA vs beta vs internal product surface policy for this cycle.
- [docs/deployment/release-readiness-checklist.md](https://github.com/mad818/personal/blob/main/docs/deployment/release-readiness-checklist.md) — shared web/desktop release gate checklist.

**Handoff automation**

- [scripts/generate-handoff.js](https://github.com/mad818/personal/blob/main/scripts/generate-handoff.js) — **branch** tree + commit history links from `origin`, plus this file appended into `docs/AGENT_HANDOFF.md` and compatibility copies (no per-commit SHA — avoids handoff sync oscillation).

**PM operator + cockpit (planned)**

- [docs/pm-operator-model.md](https://github.com/mad818/personal/blob/main/docs/pm-operator-model.md) — you = PM, in-app/IDE agents = engineering squad; what “always running” means.
- [docs/pm-cockpit-plan.md](https://github.com/mad818/personal/blob/main/docs/pm-cockpit-plan.md) — phased plan: health strip → checklist UI → optional CI hooks; includes **HTTP 500 triage** (identify route, 401 vs 503 vs 500, check dev server logs).

Use **`blob/main/...`** links above for stable file URLs. For the exact revision after a push, see [commit history](https://github.com/mad818/personal/commits/main) or run `git log -1` locally.

- 2026-04-07: Landed `docs/plans/multi-sector-operator-export-batch1-2026-04-07.md`. COMMAND now has local-only `Copy summary` actions in `RuntimeEfficiencyCard` and `WorkflowCommandOpsCard` for operator handoff of visible efficiency/governance posture, and VAULT graph mode now has a local-only `Download visible nodes JSON` action that exports only the currently visible filtered node metadata plus active view/filter context.
- 2026-04-07: Landed `docs/plans/offline-readiness-batch1-2026-04-07.md`. Nexus now has a shared browser/runtime offline-readiness layer, internet-backed background polling pauses while the browser is offline and resumes on reconnect, and HQ, COMMAND, and VAULT now surface compact local-first readiness guidance so operators can tell what still works when internet drops versus when the local runtime itself is unavailable.
- 2026-04-07: Landed `docs/plans/offline-readiness-batch2-2026-04-07.md`. Nexus now tracks shared feed freshness in store for prices, news, CVEs, threat intel, and SEC filings; COMMAND KPIs and AI Briefing, CYBER triage, and INTEL SEC filings now expose clearer fresh-vs-last-known-local posture; and the SEC filings surface preserves the last good local result set instead of clearing itself on refresh failure.
- 2026-04-07: Landed `docs/plans/offline-readiness-batch3-2026-04-07.md`. Nexus now tracks freshness for weather and flights, INTEL Flight Tracker preserves the last good airborne set on refresh failure and reconnects cleanly after offline starts, OPS map layers keep last-known good overlays instead of repainting from failed fetches, and the Flight Tracker / Ops Map / Sensor Dashboard / Security Alerts surfaces now expose the same compact fresh-versus-local-copy posture across degraded connectivity.
- 2026-04-07: Landed `docs/plans/offline-readiness-batch4-2026-04-07.md`. Nexus now tracks freshness for conflict and Polymarket, News Feed surfaces the same fresh-versus-local-copy posture as the rest of the app, Conflict Feed and Geo Heatmap clearly preserve and explain their last-known local conflict fallback, and the Polymarket panel now distinguishes fresh remote odds from retained local state while disabling pointless manual refresh attempts during offline sessions.
- 2026-04-07: Landed `docs/plans/offline-readiness-batch5-2026-04-07.md`. Nexus now tracks freshness for market rates and CISA KEV, MarketRates and CISAFeed clearly distinguish fresh remote state from retained local state while disabling pointless offline refresh attempts, and CVEFeed now surfaces the shared CVE freshness contract directly inside the detailed cyber lane so the panel matches KPI and triage trust language.
- 2026-04-07: Landed `docs/plans/offline-readiness-batch6-2026-04-07.md`. `NetworkHealth` now explains when results are retained last-run snapshots during offline/degraded sessions and shows per-target last-check timing, while `SystemStatusFooter` is now internet-aware, only advances its refresh timestamp on the real refresh-complete event, and distinguishes connected data sources from last-known local copies in the footer indicators.
- 2026-04-07: Landed `docs/plans/offline-readiness-batch7-2026-04-07.md`. `HeadersAudit` and `PassiveDnsPanel` now preserve the last successful RECON result across failed reruns, both surfaces clearly explain when they are showing retained local results during offline/degraded sessions, and live lookup actions now disable while the browser is offline.
- 2026-04-07: Landed `docs/plans/recon-boundary-batch1-2026-04-07.md`. RECON now has protected local proxy routes for passive DNS and Tor exit-node checks, `PassiveDnsPanel` and `OpsecPanel` no longer call those third parties directly from the browser, the new routes are registered under connector policy, and the OPSEC Tor lane now reports unavailable/degraded instead of silently treating failures as “not on Tor.”
- 2026-04-07: Landed `docs/plans/recon-boundary-batch2-2026-04-07.md`. RECON now has a protected local lookup route for the highest-risk BYOK lanes, `ReconLookup` no longer calls HIBP / VirusTotal / Shodan directly from the browser, those keys now exist in the server-side settings contract and `.env.example`, and the lookup surface now pauses cleanly offline while retaining prior successful panel results when individual scans fail.
- 2026-04-07: Landed `docs/plans/auth-connect-resilience-batch1-2026-04-07.md`. `AuthGate` now keeps the native `/auth/connect` path as the primary submit lane, but automatically falls back to the local `/api/token` exchange if the browser submit stalls, surfaces clearer inline status for empty-token and unreachable-runtime cases, and avoids leaving the operator on a stuck “Connecting” button state.
- 2026-04-07: Landed `docs/plans/free-first-audit-batch1-2026-04-07.md`. Free-first AI posture is now normalized through a shared provider helper, the persisted default AI lane is back on `ollama`, invalid or hidden paid-provider values auto-correct to a safe local lane, Settings no longer accepts arbitrary provider text, and stale `settings.apiKey` gates were removed from draft finalization plus the affected COMMAND/INTEL AI helper panels so local and server-backed free-first lanes no longer look broken.
- 2026-04-07: Landed `docs/plans/shell-health-batch1-2026-04-07.md`. The authenticated root layout now mounts `ShellHealthGuard`, which detects unstyled or stalled shells after login, auto-reloads once instead of silently leaving the operator on a broken black screen, and then switches into an emergency recovery mode with targeted local view reset if the shell still does not recover. `auth:e2e` and `hq:e2e` now also assert computed toprail styling and HQ shell presence after login, and the live runtime checks returned `200` for both `/` and `/hq`.
- 2026-04-07: Landed `docs/plans/shell-state-guard-batch2-2026-04-07.md`. The root layout now injects a boot-time persisted-browser-state repair script before hydration, invalid `nexus-settings` and stale graph/scheduler caches are sanitized or cleared instead of poisoning the authenticated shell, and a compact recovery notice appears when the app auto-heals local shell state. `auth:e2e` now also poisons browser storage on purpose and proves the styled HQ shell still recovers cleanly, while `type-check`, `verify`, live `127.0.0.1:3000` reachability, and authenticated browser flows all passed after the repair layer landed.
- 2026-04-07: Landed `docs/plans/critical-boundary-batch1-2026-04-07.md`. RECON lookup panels now route through a protected local lookup endpoint instead of browser-direct RDAP / DNS / cert / geo / username / BYOK calls, Guardian and FRED key usage are server-owned again through `/api/news` and `/api/commodities`, persisted `nexus-settings` now auto-strip legacy sensitive key fields on write and boot, and the OPS fire layer now self-corrects between live and sample hotspot posture without relying on stale client secrets. `type-check`, `verify`, `auth:e2e`, and live `127.0.0.1:3000` / `/hq` / `/recon` reachability all passed after the batch.
- 2026-04-07: Landed `docs/plans/critical-boundary-batch2-2026-04-07.md`. COMMAND network checks now run through a protected local route instead of browser-direct fetches, custom targets auto-normalize and reject malformed or duplicate input, external checks respect isolated/offline posture while local `/api/*` checks remain usable, and `localApiKey` is now explicitly labeled session-only in Settings so the UI matches the browser secret-hygiene model. `type-check`, `verify`, `auth:e2e`, and live `127.0.0.1:3000` / `/command` reachability all passed after the batch.
- 2026-04-07: Landed `docs/plans/critical-workbench-fetch-batch1-2026-04-07.md`. `GeoDeltaPanel`, `RegistryConsole`, `BlacksiteLab`, `WorkflowForge`, `MemorySpineStatusCard`, and `SweepEnginePanel` now use the authenticated local fetch wrapper instead of raw browser fetches for protected local routes, mutation/read failures preserve retained local state instead of collapsing into silent empties, and each touched panel now surfaces a compact degraded notice when the protected route or stream is temporarily unavailable. `type-check`, `verify`, `auth:e2e`, and live `127.0.0.1:3000` / `/command` / `/intel` reachability all passed after the batch.
- 2026-04-07: Landed `docs/plans/drone-ops-cohesion-batch1-2026-04-07.md`. Vehicle Lab now includes a Drone Ops Launchpad that turns bench progress, passive bridge posture, compliance review, local memory, and Vault packaging into one operator flow; drone/F450/Pixhawk/MAVLink route intent now lands on Vehicle Lab by default while FAA/compliance prompts stay on CYBER’s drone lane; and `DroneCompliancePanel` now uses the authenticated local fetch wrapper, preserves the last successful compliance review on transient failures, and accepts launchpad-prefill query params. `type-check`, `verify`, `auth:e2e`, and live `127.0.0.1:3000` / `/vehicle` / `/cyber?view=drone` reachability all passed after the batch.
- 2026-04-07: Landed `docs/plans/drone-hardware-readiness-batch2-2026-04-07.md`. Vehicle Lab now includes a persistent, self-healing future Pixhawk / ArduPilot connector onboarding profile, a copyable passive bridge stub command plus `scripts/vehicle-bridge-stub.mjs` and `docs/deployment/vehicle-passive-bridge-stub.md`, a local JSON flight-session bundle import/export lane with Vault filing, and a dedicated first-hardware-day checklist plus recovery flows for the arrival-day bridge session. `type-check`, `verify`, `auth:e2e`, and live `127.0.0.1:3000` / `/vehicle` / `/vault` reachability all passed after the batch.
- 2026-04-07: Landed `docs/plans/critical-consistency-batch1-2026-04-07.md`. The agent runtime now reads live Zustand settings instead of stale `localStorage` state, `apiFetch(...)` no longer hands concurrent GET callers the same consumed `Response`, and the highest-signal remaining VAULT / COMMAND / SECURITY / lessons panels now use the authenticated local fetch wrapper instead of raw browser `fetch(...)`. This closes a cross-cutting trust gap around stale settings drift, protected-route auth consistency, and half-broken shared route reads. `npm run build`, `npm run type-check`, `npm run verify`, `npm run auth:e2e`, and live `127.0.0.1:3000` / `/vault` / `/command` reachability all passed after the batch.
- 2026-04-07: Landed `docs/plans/critical-consistency-batch2-2026-04-07.md`. The remaining important RECON / system / HQ / local-memory sync callers now use `apiFetch(...)`, `MemorySpineSync` now compares a lightweight metadata key instead of stringifying the whole persistence payload every render, and the vehicle readiness self-heal lane now uses explicit structural checklist/profile comparisons instead of `JSON.stringify(...)` equality. This keeps the free-first/local-first boundary intact while removing more silent auth drift and hot-path churn before the bigger file decompositions. `type-check`, `verify`, `auth:e2e`, and live `127.0.0.1:3000` / `/vehicle` / `/recon` reachability passed after the batch.
- 2026-04-07: Landed `docs/plans/audit-priority-batch3-2026-04-07.md`. `AuthGate` diagnostics probing now uses `apiFetch(...)`, which closes the last auth-boundary raw-fetch special case, and `OfficeCommandCenter.tsx` started its decomposition by moving pure constants, animation CSS, prompt presets, and stateless helpers into `components/home/office/officeCommandCenterConfig.ts`. This does not change HQ behavior, but it lowers the blast radius for the next cleanup pass on one of the app’s biggest files.

---

## Handoff template — use this at end of every work session

Agents and IDE sessions should close with this structure in their final message:

```
## HANDOFF — [agent name] — [date]

### COMPLETED
- [task] — [file changed] — [what it does]

### ARTIFACTS
- [file path] — [one-line description]

### VERIFICATION
- [ ] tsc --noEmit passes
- [ ] npm run verify passes
- [ ] Patched sections re-read and confirmed correct

### KNOWN ISSUES
- [anything left broken or incomplete — none if clean]

### NEXT
- [suggested next task] — owner: [agent or Mario]
```

This template feeds `docs/AGENT_HANDOFF.md` via `npm run handoff:write`, along with tool-compatible copies for Claude, Codex, and Cursor.
If nothing broke and verify is green, state that explicitly — "No known issues."
- 2026-04-07: Continued `docs/plans/audit-priority-batch3-2026-04-07.md`. `AuthGate` now uses `apiFetch(...)` for diagnostics probing, and `OfficeCommandCenter.tsx` shed another large pure block into `components/home/office/officeCommandCenterStrategium.ts` so Strategium/front/tempo/mission derivation no longer lives inline with the live HQ send loop. `npm run type-check` and `npm run verify` pass after the split, and live reachability returned `200` for `/`, `/hq`, and `/vehicle`.
- 2026-04-08: Landed `docs/plans/audit-priority-batch4-2026-04-08.md`. The shared no-store cache helper now lives in `lib/cacheHeaders.ts`, so `lib/authSession.ts` and the Edge middleware path no longer import Node-only runtime identity code through `lib/runtimeIdentity.ts`. Clean `next build` now passes again without the old Edge-runtime warnings, `auth:e2e` stays green after the build gate, and live reachability returned `200` for `/`, `/hq`, and `/command`.
- 2026-04-08: Landed `docs/plans/audit-priority-batch5-2026-04-08.md`. `VehicleArtifactManifestCard` no longer serializes the full future flight-session bundle during render just to keep copy/download buttons ready; session bundle JSON is now generated only on demand when the operator clicks copy/download, which reduces hot-path churn as telemetry history grows while leaving the local export/import flow unchanged.
- 2026-04-08: Landed `docs/plans/audit-priority-batch6-2026-04-08.md`. `OfficeCommandCenter.tsx` now delegates the bulky successful-run tail into `components/home/office/officeCommandCenterPostRun.ts`, moving passive memory note assembly, successful learnings writeback, workflow artifact filing, session summary formatting, and lesson proposal generation out of the main HQ send loop. `type-check`, `verify`, `hq:e2e`, and live reachability all stayed green after the split.
- 2026-04-08: Landed `docs/plans/audit-priority-batch7-2026-04-08.md`. The scheduler drawer now collapses its most text-heavy guidance into reusable expandable operator notes, pure scheduler presets/storage/formatters moved into `components/ui/cronSchedulerPanelUtils.ts`, and pure office-room scene types/palettes/transforms/camera presets moved into `components/home/office/officeRoom3DScene.ts`. This keeps the free-first/local-first posture unchanged while making `CronSchedulerPanel.tsx` less overloaded and reducing inline config burden inside `OfficeRoom3D.tsx`. `build`, `type-check`, `verify`, `hq:e2e`, and live reachability all stayed green after the batch.
- 2026-04-08: Landed `docs/plans/audit-priority-batch8-2026-04-08.md`. The scheduler drawer now has real section components in `components/ui/CronSchedulerWorkflowTemplatesSection.tsx` and `components/ui/CronSchedulerGovernanceSection.tsx`, and the HQ shell now renders its prelude/posture through `components/home/office/HQPreludePostureSection.tsx` plus `HQPreludePostureGrid.tsx`. This shrinks both `CronSchedulerPanel.tsx` and `OfficeCommandCenter.tsx` without changing behavior, keeps the free-first/local-first boundary intact, and leaves the next cleanup pass with cleaner UI seams. `type-check`, `verify`, `build`, and `hq:e2e` passed; live `127.0.0.1:3000` and `/hq` returned `200` after a clean dev-server restart.
- 2026-04-08: Landed `docs/plans/audit-priority-batch9-2026-04-08.md`. Local Playwright auth/HQ runs now default to the real `127.0.0.1:3000` runtime instead of a second `3100` dev server, `scripts/dev-server.mjs` now uses retry-aware cleanup with warning-only fallback for locked `.next` and runtime-identity state, and the scheduler auto-ops preview now lives in `components/ui/CronSchedulerAutoOpsSection.tsx`. While verifying that batch, a real same-origin auth bug surfaced in dev: `next.config.js` had `allowedDevOrigins` in full-URL form and `lib/authSession.ts` was rebuilding redirects from `req.nextUrl.origin`, so auth on `127.0.0.1` half-broke by mixing `127.0.0.1` and `localhost`. Both were corrected, and `build`, `verify`, `auth:e2e`, `hq:e2e`, and live `127.0.0.1:3000` / `/hq` checks are green again.
- 2026-04-08: Landed `docs/plans/audit-priority-batch10-2026-04-08.md`. `scripts/build-runtime.mjs` now hard-blocks `npm run build` whenever a live app is active on `127.0.0.1:3000`, and `scripts/dev-server.mjs` now writes a dedicated `.nexus-dev-runtime.json` marker so the guard catches plain `npm run dev` sessions instead of only the standalone runtime. This closes the last dev-runtime poisoning gap that was letting `next build` wipe `.next` out from under the live browser session. Guarded `build`, `verify`, `auth:e2e`, `hq:e2e`, and live `127.0.0.1:3000` / `/hq` / `/vehicle` plus `_next/static/chunks/main-app.js` reachability are green after a clean dev restart.
- 2026-04-08: Continued `docs/plans/audit-priority-batch11-2026-04-08.md`. The HQ terminal/composer lane now lives in `components/home/office/HQTerminalSection.tsx`, the scheduler sorted-job list now lives in `components/ui/CronSchedulerJobsSection.tsx`, and both tabs now use the compact operator-note pattern to keep helper copy present without leaving the surfaces overloaded with text. `type-check`, `verify`, and `hq:e2e` passed after the split, and live `127.0.0.1:3000` / `/hq` / `/vehicle` reachability stayed green.
- 2026-04-08: Landed `docs/plans/audit-priority-batch12-2026-04-08.md`. The HQ console shell now lives in `components/home/office/HQConsoleShellSection.tsx`, which pulls the header status strip, office stage, and splitter controls out of `OfficeCommandCenter.tsx`, and the scheduler job-creation block now lives in `components/ui/CronSchedulerComposerSection.tsx`. The scheduler composer also uses a compact operator-note to keep guidance available without adding more visible text weight. `type-check`, `verify`, and `hq:e2e` passed after the extraction, and live `127.0.0.1:3000` / `/hq` / `/vehicle` reachability stayed green.
- 2026-04-08: Landed `docs/plans/audit-priority-batch13-2026-04-08.md`. Scheduler governance is now split across `components/ui/CronSchedulerGovernanceStatusSection.tsx`, `CronSchedulerSavedViewsSection.tsx`, and `CronSchedulerAuditFiltersSection.tsx`, which takes posture/actions, saved-view import/export flows, and audit filters out of `CronSchedulerGovernanceSection.tsx`. HQ shell control behavior now lives in `components/home/office/useOfficeConsoleShellControls.ts`, so keyboard shortcuts and split-resize logic no longer sit inline inside `OfficeCommandCenter.tsx`. `type-check`, `verify`, and `hq:e2e` passed after the split, and live `127.0.0.1:3000` / `/hq` / `/vehicle` reachability stayed green.
- 2026-04-08: Landed `docs/plans/audit-priority-batch14-2026-04-08.md`. The remaining `/meta` JANSKY special-case now lives in `components/home/office/officeCommandCenterMeta.ts`, so learnings fetch, prompt assembly, AI proposal parsing, and pending-edit shaping are no longer inline inside the main HQ `send()` callback. `OfficeCommandCenter.tsx` keeps UI ownership of the state transitions and queued-edit enqueue, while `type-check`, `verify`, `hq:e2e`, and live `127.0.0.1:3000` / `/hq` / `/vehicle` reachability stayed green after the split.
- 2026-04-08: Landed `docs/plans/audit-priority-batch15-2026-04-08.md`. The VAULT graph page’s shared summary/filter contract now lives in `components/vault/vaultGraphPageUtils.ts`, which also removes the duplicate compiled-memory page summary interface from `VaultGraphFocusPanel.tsx`. The graph toolbar, presets, filters, export actions, and legend now live in `components/vault/VaultGraphControlsSection.tsx`, so `app/vault/page.tsx` is more route-focused and less overloaded with inline graph chrome. `type-check`, `verify`, and live `127.0.0.1:3000` / `/vault` / `/hq` reachability stayed green after the extraction.
- 2026-04-08: Landed `docs/plans/ux-direction-batch1-2026-04-08.md`. Captured a Nexus-specific UX/design brief based on the linked `My Brain Is Full Crew`, `taste-skill`, and `xyOps` references. The doc turns those patterns into Nexus-native guidance: preserve tabs and local-first posture, add a mission-oriented layer over the existing IA, keep using progressive disclosure to reduce text overload, standardize “continue mission” actions across tabs, and use the included rewritten prompt for future UI improvement batches instead of the generic modern-website brief. Local runtime checks for `/`, `/hq`, and `/vault` stayed green after the documentation batch.
- 2026-04-08: Landed `docs/plans/ux-direction-batch2-2026-04-08.md`. HQ now has a compact mission-oriented entry layer through `components/home/office/HQMissionRailSection.tsx`, adding `Observe / Investigate / Automate / Archive / Launch` directly into the prelude without removing the existing tab model. `HQPreludePostureSection.tsx` now routes those actions into COMMAND, the current investigation theater, scheduler state, VAULT, and VEHICLE, so HQ is a clearer “start here” surface while staying local-first and operator-focused. `type-check`, `verify`, `hq:e2e`, and live `127.0.0.1:3000` / `/hq` checks stayed green after the batch.
- 2026-04-08: Landed `docs/plans/ux-direction-batch3-2026-04-08.md`. HQ mission launches now carry shared `mission` / `from` / `source` handoff params through `lib/missionHandoff.ts`, and destination surfaces use `components/ui/MissionHandoffStrip.tsx` to explain why the operator arrived there without adding a second navigation system. `OfficeCommandCenter.tsx` now stamps the HQ rail routes consistently, and `COMMAND`, `INTEL`, `CYBER`, `VAULT`, and `VEHICLE` each surface a compact continue-mission strip with return-to-HQ affordance and next-step guidance. `type-check`, `verify`, `hq:e2e`, and live `127.0.0.1:3000` / `/hq` / `/command` / `/vault` / `/vehicle` / `/intel` / `/cyber` checks all stayed green after the batch.
- 2026-04-08: Landed `docs/plans/ux-direction-batch4-2026-04-08.md`. HQ replies and VAULT focus/detail now carry post-action continuation chips through `components/ui/MissionContinuationActions.tsx`, which reuses the centralized route-intent map plus new helpers in `lib/missionHandoff.ts` to offer compact next steps instead of another wall of controls. `AskMemoryFromReplyButton.tsx` now folds reply-level memory routing and continue-in-surface behavior into one lightweight action row, and `VaultGraphFocusPanel.tsx` now lets saved clips and compiled pages jump back into memory, HQ, or the most relevant investigation lane without exposing restricted bodies. `type-check`, `verify`, `hq:e2e`, and live `127.0.0.1:3000` / `/hq` / `/vault` / `/command` checks all stayed green after the batch.
- 2026-04-08: Landed `docs/plans/ux-direction-batch5-2026-04-08.md`. The shared continuation layer now accepts explicit route hints and extra targets, so scheduler results and compiled-memory pages can keep the operator moving without new control walls. `CronSchedulerJobsSection.tsx` now offers compact next-step chips from the job prompt, workflow route, and VAULT artifact target, while `CompiledMemoryPagesPanel.tsx` and `VaultGraphFocusPanel.tsx` use the same continuation model to reopen memory, the right working lane, or HQ from durable artifacts. `type-check`, `verify`, `hq:e2e`, and live `127.0.0.1:3000` / `/hq` / `/vault` / `/command` checks all stayed green after the batch.
- 2026-04-08: Landed `docs/plans/ux-direction-batch6-2026-04-08.md`. Scheduler job cards and compiled-memory page cards now carry less always-visible text because the strongest next actions are already explicit. `CronSchedulerJobsSection.tsx` now truncates long mission briefs and automation guidance in the default card state, while `CompiledMemoryPagesPanel.tsx` now leads with summary, compact research/document metadata, continuation chips, and source provenance, moving the noisier content preview posture behind the existing `Read page` expansion path. `type-check`, `verify`, `hq:e2e`, and live `127.0.0.1:3000` / `/hq` / `/command` / `/vault` checks all stayed green after the batch.
- 2026-04-08: Landed `docs/plans/ux-direction-batch7-2026-04-08.md`. Repeated scheduler and VAULT intro guidance is now lighter because those surfaces already have enough structure to stand on their own. `CronSchedulerWorkflowTemplatesSection.tsx` now replaces the repeated review-only prose stack with one caution note plus concise hoverable chips, `CronSchedulerGovernanceStatusSection.tsx` trims the governance explanation down to one note plus a short inline hint, and `MemorySpineOverview.tsx` now presents readiness and restricted-artifact posture as a single compact operator note instead of stacked callouts. `type-check`, `verify`, `hq:e2e`, and live `127.0.0.1:3000` / `/hq` / `/command` / `/vault` checks all stayed green after the batch.
- 2026-04-08: Landed `docs/plans/ux-direction-batch8-2026-04-08.md`. VAULT list and graph modes now each explain themselves once through `components/vault/VaultModeOrientationSection.tsx`, using compact action anchors instead of repeating the same guidance in route-level `SectionLabel` detail text. `app/vault/page.tsx` now relies on that mode-level orientation block plus shorter section labels, which keeps the archive shell easier to scan without removing any surface or changing the local-first workflow. `type-check`, `verify`, `hq:e2e`, and live `127.0.0.1:3000` / `/hq` / `/command` / `/vault` checks all stayed green after the batch.
- 2026-04-08: Landed `docs/plans/render-assimilation-batch1-2026-04-08.md`. Assimilated the strongest useful idea from `mfranzon/render` without adopting a new dependency: Vehicle session manifests now reserve optional local slots for a future `.glb` preview asset and its parametric/source file through `lib/vehicle/types.ts` and `lib/vehicle/readiness.ts`. `components/vehicle/VehicleArtifactManifestCard.tsx` now surfaces a compact 3D artifact readiness lane, so future drone mounts, brackets, enclosure studies, or other fit-check previews have an obvious home in the existing local bundle/export workflow before hardware or model files actually arrive. `type-check`, `verify`, and live `127.0.0.1:3000` / `/vehicle` / `/vault` checks all stayed green after the batch.
- 2026-04-08: Landed `docs/plans/render-assimilation-batch2-2026-04-08.md`. `lib/vehicle/hardwareReadiness.ts` now includes a deterministic local render-brief generator for likely first drone-part targets such as camera mounts, companion enclosures, telemetry masts, battery brackets, and landing-gear accessories. `components/vehicle/VehicleArtifactManifestCard.tsx` now exposes that brief generator directly in Vehicle Lab with target selection, optional constraint input, and copy/download actions, so Nexus can hand off a good local-first design brief to future CAD/render tools without pretending the aircraft or model files already exist. `type-check`, `verify`, and live `127.0.0.1:3000` / `/vehicle` / `/vault` checks all stayed green after the batch.
- 2026-04-08: Landed `docs/plans/render-assimilation-batch3-2026-04-08.md`. The future-hardware render brief is now a first-class local artifact instead of a dead-end export: `lib/vehicle/hardwareReadiness.ts` now builds a compiled-memory-ready Vault draft for the current target and operator goal, and `components/vehicle/VehicleArtifactManifestCard.tsx` now files that brief directly into VAULT through `/api/memory/pages`, with local status feedback and a `nexus-memory-pages-updated` refresh signal so the archive surfaces see the new page immediately. `type-check`, `verify`, and live `127.0.0.1:3000` / `/vehicle` / `/vault` checks stayed green after the batch.
- 2026-04-08: Landed `docs/plans/render-assimilation-batch4-2026-04-08.md`. Vehicle Lab filing actions no longer dead-end at `Filed`: `components/vehicle/VehicleArtifactManifestCard.tsx` now reuses the shared `MissionContinuationActions` pattern after successful session-summary, imported-bundle, and render-brief filing, so future hardware prep can jump straight into VAULT, memory, or back into VEHICLE without inventing a second navigation system or implying live-flight authority. `type-check`, `verify`, and live `127.0.0.1:3000` / `/hq` / `/vehicle` / `/vault` checks stayed green after the batch.
- 2026-04-08: Landed `docs/plans/render-assimilation-batch5-2026-04-08.md`. VAULT now treats vehicle render briefs as a visibly distinct artifact kind instead of generic compiled notes: `components/vault/CompiledMemoryPagesPanel.tsx` now detects the existing `vehicle-render-brief` / `cad-prep` / `/vehicle` contract, applies a future-hardware visual treatment, and surfaces compact render-target and non-flight-critical badges so filed design briefs stand out at a glance while staying inside the same local compiled-memory model. `type-check`, `verify`, and live `127.0.0.1:3000` / `/hq` / `/vault` / `/internal/vehicle` checks stayed green after the batch.
- 2026-04-08: Landed `docs/plans/code-review-graph-assimilation-batch1-2026-04-08.md`. Nexus now borrows the most useful idea from `code-review-graph` without adopting its full parser/index stack: `lib/projectImpact.ts` computes a local import-based blast-radius estimate, `/api/project?section=impact` exposes that through the existing protected project route, and the Resources workbench now has an `Impact` tab with `components/resources/ProjectImpactConsole.tsx` for direct imports, importers, likely touched files, and a copyable review pack. This stays fully local-only and free-first while giving the internal workbench a fast “what will this touch?” lane. `type-check`, `verify`, and live `127.0.0.1:3000` / `/resources` checks stayed green after the batch.
- 2026-04-08: Landed `docs/plans/system-design-batch1-2026-04-08.md`. Resources now includes a `System Design` lane built around real Nexus subsystem maps in `lib/systemDesignMaps.ts`, covering ownership, boundaries, entry points, dependencies, failure modes, guardrails, and impact seed files for HQ flow, AI runtime, memory spine, scheduler governance, RECON, and vehicle readiness. `components/resources/SystemDesignConsole.tsx` gives each subsystem a compact architecture card with one-click handoff into the `Impact` lane, and `components/resources/ProjectImpactConsole.tsx` now honors routed file prefills so architecture maps can reopen targeted blast-radius analysis directly. `type-check`, `verify`, and live `127.0.0.1:3000` / `/resources` checks stayed green after the batch.
- 2026-04-08: Landed `docs/plans/system-design-batch2-2026-04-08.md`. The new Resources architecture console now doubles as an engineering audit lane: `lib/systemDesignMaps.ts` includes subsystem-specific performance hotspots, worthwhile micro-optimizations, and security audit checks, and `components/resources/SystemDesignConsole.tsx` renders those as compact review cards instead of long prose. I also recorded a new process lesson in `tasks/lessons.md`: long-running sessions should refresh handoff context and continue in a new task before context quality drift turns into avoidable mistakes. `type-check`, `verify`, and live `127.0.0.1:3000` / `/resources` checks stayed green after the batch.
- 2026-04-08: Landed `docs/plans/claude-howto-assimilation-batch1-2026-04-08.md`. Borrowed the strongest idea from `claude-howto` without turning Nexus into a Claude-only feature catalog: `lib/engineeringPlaybooks.ts` now defines compact, task-first engineering playbooks for high-risk review, safe refactors, security boundary audits, release hardening, and subsystem onboarding. `components/resources/PlaybooksConsole.tsx` and `components/resources/ResourcesWorkbench.tsx` add a new `Playbooks` lane that links directly into the existing `System Design` and `Impact` tabs, so workflow, architecture, and blast radius stay connected during real repo work. `type-check`, `verify`, and live `127.0.0.1:3000` / `/resources` checks stayed green after the batch.
- 2026-04-08: Landed `docs/plans/claude-howto-assimilation-batch2-2026-04-08.md`. The new Resources playbooks are now operator-usable working sessions instead of static cards: `lib/engineeringPlaybooks.ts` now carries explicit follow-on action links plus a shared brief generator, and `components/resources/PlaybooksConsole.tsx` now exposes local-only `Copy brief` / `Download brief` actions alongside one-click jump-offs into the right Nexus surfaces like `System Design`, `Impact`, `COMMAND`, `HQ`, `RECON`, and `VAULT`. That keeps the `claude-howto` assimilation practical and local-first by turning guidance into action without inventing parallel tooling. `type-check`, `verify`, and live `127.0.0.1:3000` / `/resources` / `/command` checks stayed green after the batch.
- 2026-04-08: Landed `docs/plans/claude-howto-assimilation-batch3-2026-04-08.md`. The playbooks lane now lands closer to the real working panel instead of dropping the operator at the top of a broad route: `components/ui/SurfaceFocusStrip.tsx` plus `hooks/useSurfaceFocusScroll.ts` create a small reusable focused-session pattern, and `app/command/page.tsx`, `app/recon/page.tsx`, and `app/vault/page.tsx` now honor focused query params that explain the session and scroll to the relevant panel. `lib/engineeringPlaybooks.ts` was updated so the strongest follow-on actions now use focused links like runtime efficiency, OPSEC checks, and memory spine instead of only broad route drops. `type-check`, `verify`, and live `127.0.0.1:3000` / `/resources` / `/command` / `/recon` / `/vault` checks stayed green after the batch.
- 2026-04-08: Landed `docs/plans/shell-bootstrap-recovery-batch3-2026-04-08.md`. The broken “stars plus raw links” shell now has both a runtime fix and a true bootstrap self-heal. `scripts/stage-runtime-assets.mjs` is wired into both `build` and `start`, so the standalone server always stages `.next/static` and `public` into `.next/standalone` before serving; that closes the root cause where HTML referenced hashed CSS/JS the live runtime did not actually have. On the browser side, `lib/shellBootstrapGuard.ts` now uses safe DOM creation, cache-busting reloads, real static-asset failure marking, and a final “must have hydrated” check so a styled-but-half-dead shell is treated as unhealthy. The inline guard emission bug was also fixed by replacing the broken emitted regex with plain string checks, and `tests/e2e/hq-shell.spec.ts` now uses a fresh authenticated browser context to prove the recovery overlay appears when shell assets fail. `build`, `verify`, `hq:e2e`, and live `127.0.0.1:3000` / `/hq` / `/vault` / `/internal/vehicle` plus hashed asset `200` checks are green after the batch.
- 2026-04-08: Landed `docs/plans/surface-capabilities-batch1-2026-04-09.md`. Resources now has a new `Surfaces` lane built from `lib/surfaceCapabilities.ts` and `components/resources/SurfaceCapabilitiesConsole.tsx`, turning Nexus’s major routes into a compact capability audit with mission summaries, strongest abilities, key subsections, free-first/offline posture, and “strengthen next” guidance. `components/resources/ResourcesWorkbench.tsx`, `app/resources/page.tsx`, and the `resourcesWorkbenchView` store contract were updated so the new lane reads as a first-class workbench view instead of an extra document. `type-check`, `verify`, `handoff:write`, and live `127.0.0.1:3000` / `/resources` / `/resources?view=surfaces` checks are green after the batch.
- 2026-04-09: Landed `docs/plans/surface-focus-batch1-2026-04-09.md`. The focused-session landing pattern now extends beyond COMMAND / RECON / VAULT into `INTEL`, `CYBER`, `ALPHA`, and `VEHICLE`: each touched route now understands a small `focus` contract, shows a compact `SurfaceFocusStrip` that explains why the operator landed there, and scrolls to stable panel ids instead of dumping focused links at the top of the page. `lib/surfaceCapabilities.ts` now uses those deeper links for the highest-signal subsections, and the segmented routes self-correct to the right sub-view when a focus param implies a different tab than the raw `view` param. `type-check`, `verify`, `handoff:write`, and live `127.0.0.1:3000` / `/resources?view=surfaces` / `/intel?view=sweeps&focus=intel-sweeps` / `/cyber?view=drone&focus=cyber-drone` / `/alpha?view=scanner&focus=alpha-scanner` / `/vehicle?focus=vehicle-artifact-convention` checks are green after the batch.
- 2026-04-09: Landed `docs/plans/surface-focus-batch2-2026-04-09.md`. The focused-session landing pattern now reaches the remaining high-signal support/internal routes: `app/security/page.tsx` and `app/skills/page.tsx` both understand `focus` params, self-correct segmented views from those focus targets, show compact `SurfaceFocusStrip` guidance, and scroll to stable panel ids for doctrine, AI surface, physical ops, Workflow Forge, Blacksite, System Brain, and Library sections. `components/ui/MissionHandoffStrip.tsx` now recognizes SECURITY and SKILLS as valid handoff surfaces, and `lib/surfaceCapabilities.ts` now uses the deeper SECURITY and SKILLS links so the Resources surface audit lands closer to real work. `type-check`, `verify`, `handoff:write`, and live `127.0.0.1:3000` / `/security?view=ai&focus=security-ai-surface` / `/skills?view=blacksite&focus=skills-blacksite` checks are green after the batch.
- 2026-04-09: Landed `docs/plans/obsidian-mind-assimilation-batch1-2026-04-09.md`. Borrowed the strongest `obsidian-mind` idea in a Nexus-native way: VAULT now treats archive stewardship as a first-class surface instead of assuming the graph stays healthy on its own. `lib/vaultStewardship.ts` turns existing local graph/lint signals into operator-readable archive-health posture, `components/vault/VaultStewardshipPanel.tsx` surfaces linked coverage, tag coverage, route coverage, orphans, stale items, and thin-topic drift, and `app/vault/page.tsx` now gives that panel a real `vault-stewardship` focus target in both list and graph modes. `lib/surfaceCapabilities.ts` also exposes the new stewardship lane through Resources so the archive can be opened directly in maintenance posture. `type-check`, `verify`, `handoff:write`, and live `127.0.0.1:3000` / `/vault?focus=vault-stewardship` / `/resources?view=surfaces` checks are green after the batch.
- 2026-04-09: Landed `docs/plans/ai-hallucination-hardening-batch1-2026-04-09.md`. Added a shared truth-boundary contract in `lib/aiTruthBoundary.ts`, injected it into `lib/ai.ts` so every main system prompt now forbids fabricated citations/tool results and requires observed-versus-inferred uncertainty posture, and reinforced the specialist agent layer in `components/home/office/prompts.ts` with a compact truthfulness override. The Resources engineering lane now treats hallucination mitigation as an explicit practice: `lib/systemDesignMaps.ts` hardens the AI runtime map with hallucination-related failure modes and guardrails, and `lib/engineeringPlaybooks.ts` adds a dedicated `Hallucination hardening audit` playbook. `type-check`, `verify`, `handoff:write`, and live `127.0.0.1:3000` / `/resources?view=system&system=ai-runtime-boundary` / `/resources?view=playbooks&playbook=hallucination-hardening` / `/command?focus=runtime-efficiency` checks are green after the batch. Targeted Vitest coverage was added in `__tests__/ai.test.ts`, but the machine’s current npm/dev-dependency state still lacks a runnable local `vitest` binary, so the repo-level verification remained the authoritative proof for this batch.
- 2026-04-09: Landed `docs/plans/ai-hallucination-hardening-batch2-2026-04-09.md`. Closed the remaining direct-call drift in the AI runtime by adding a dedicated `buildDirectCallSystemPrompt(...)` lane in `lib/ai.ts` and a `DIRECT-CALL BOUNDARY` block in `lib/aiTruthBoundary.ts`, so `callAI(...)` can no longer imply searches, file reads, or live verification it did not actually perform. Both cloud and local direct-call paths now receive that shared system prompt, which means compact panels like briefing/framework flows inherit the same anti-fabrication and uncertainty posture as the main HQ runtime instead of running as raw completions. I also hardened the legacy `components/home/AgentOffice.tsx` prompt path with the shared truthfulness override so it cannot drift behind the modern HQ surface. `type-check`, `verify`, `handoff:write`, and live `127.0.0.1:3000` / `/hq` / `/command` / `/resources?view=playbooks&playbook=hallucination-hardening` checks are green after the batch.
- 2026-04-09: Landed `docs/plans/ai-hallucination-hardening-batch3-2026-04-09.md`. The anti-hallucination boundary is now visible on high-risk direct-call answer panels instead of living only inside prompts: `lib/aiStructuredEvidence.ts` adds a shared JSON extraction and observed/inferred/verify-next parsing layer, `components/ui/EvidencePosturePanel.tsx` renders that posture compactly, and both `components/command/AIBriefing.tsx` and `components/intel/StrategyFrameworks.tsx` now request structured evidence-aware output and surface it directly when the model complies, while safely falling back to raw text when it does not. I also added targeted parsing coverage in `__tests__/ai.test.ts`. `type-check`, `verify`, `handoff:write`, and live `127.0.0.1:3000` / `/command` / `/intel` / `/resources?view=playbooks&playbook=hallucination-hardening` checks are green after the batch.
- 2026-04-09: Landed `docs/plans/ai-hallucination-hardening-batch4-2026-04-09.md`. The visible evidence-posture pattern now reaches the highest-risk action-oriented COMMAND panels instead of stopping at briefing/strategy summaries. `lib/aiStructuredEvidence.ts` now supports optional action lists and numeric scores inside the same shared JSON contract, `components/command/JobRiskAnalyzer.tsx` now separates rubric-based observed facts from inferred risk and verify-next checks while preserving a numeric risk score, and both `components/command/BusinessBuilder.tsx` and `components/command/FocusPanel.tsx` now render compact `Observed / Inferred / Verify next` posture plus concrete action lists when the model returns structured guidance. I also extended `__tests__/ai.test.ts` to cover the richer parser contract. `type-check`, `verify`, `handoff:write`, and live `127.0.0.1:3000` / `/hq` / `/command` / `/resources?view=playbooks&playbook=hallucination-hardening` checks are green after the batch.
- 2026-04-10: Expanded `docs/plans/behavior-first-assimilation-plan-2026-04-10.md` from a short correction into a larger execution program. The plan now treats external idea assimilation as a behavior-first effort with explicit phases, anti-patterns, architectural touchpoints, large-batch breakdowns, validation metrics, and exit criteria. `tasks/todo.md` now reflects the deeper queue with BF1A-BF5C sub-batches so the next implementation work can target conversational HQ, verified live retrieval, passive second-brain shaping, quieter continuation, and spec-anchored execution without drifting back into button-heavy UI changes.
- 2026-04-10: Completed the first real BF6 safeguards/self-heal slice from `docs/plans/session-integrity-batch1-2026-04-10.md`. `lib/exactSessionLinks.ts` plus `hooks/useSessionHrefAutoHeal.ts` now form a shared canonical-session contract that repairs stale Resources, VAULT, HQ, and segmented-route links immediately instead of letting each console silently default to the wrong card. I also restored the broken associations that had drifted out of the live registries: `lib/engineeringPlaybooks.ts` now includes the playbook ids the app actually links to, `lib/specDrivenDevelopment.ts` again includes the `reverse-engineering-memory` starter referenced elsewhere, and the Resources consoles now read from normalized session params instead of raw search params plus ad hoc corrective effects. `type-check`, `route:e2e`, `build`, `verify`, `handoff:write`, and live `127.0.0.1:3000` checks on repaired stale routes are green after the batch.
- 2026-04-09: Landed `docs/plans/ai-hallucination-hardening-batch5-2026-04-09.md`. The visible evidence-posture pattern now reaches the remaining compact high-trust AI surfaces instead of stopping at COMMAND planning panels. `components/alpha/BuyBot.tsx` now requests structured trade rationale output, preserves `Observed / Inferred / Verify next` posture in both live signals and saved history, and keeps compact trade-action steps visible alongside the rationale. `components/home/office/officeCommandCenterMeta.ts` now uses a structured JSON contract for `/meta` prompt-improvement proposals, falls back to the legacy parser when needed, and returns visible observed/inferred/verify-next sections with the queued edit so HQ meta-analysis no longer looks like an opaque prose suggestion. `type-check`, `verify`, `handoff:write`, and live `127.0.0.1:3000` / `/hq` / `/alpha` / `/resources?view=playbooks&playbook=hallucination-hardening` checks are green after the batch.
- 2026-04-09: Landed `docs/plans/repo-idea-audit-batch1-2026-04-09.md`. After re-auditing the idea-source repos, the clearest remaining product gap was not another explanation surface but a missing repair loop: Nexus could describe archive problems but not open the exact fix view. `components/vault/VaultStewardshipPanel.tsx` now offers one-click repair actions for route-less compiled pages, untagged compiled pages, and orphan graph recovery; `components/vault/CompiledMemoryPagesPanel.tsx` now supports repair-oriented filtered views through `compiledFilter` route params with contextual repair callouts; and `app/vault/page.tsx` now treats orphan recovery and compiled-page repair as first-class focused sessions instead of generic route landings. I also updated `lib/surfaceCapabilities.ts` so the Resources surface audit can jump straight into those repair views. `type-check`, `verify`, `handoff:write`, and live `127.0.0.1:3000` / `/vault?focus=vault-compiled-pages&compiledFilter=route-less` / `/vault?focus=vault-compiled-pages&compiledFilter=untagged` / `/vault?focus=vault-graph-focus&graphAudit=orphans` checks are green after the batch.
- 2026-04-09: Landed `docs/plans/repo-idea-audit-batch2-2026-04-09.md`. Resources now treats architecture and surface audits as launch points instead of static guidance: `lib/systemDesignMaps.ts` carries exact remediation sessions for each subsystem, `components/resources/SystemDesignConsole.tsx` renders those repair links directly, and `lib/surfaceCapabilities.ts` plus `components/resources/SurfaceCapabilitiesConsole.tsx` now expose route-specific improvement launch points alongside the textual “strengthen next” guidance. While doing that audit follow-through, I also corrected the stale VEHICLE system-map link from `vehicle-readiness` to the real `vehicle-bridge` id, and refreshed `ResourcesWorkbench` / `app/resources/page.tsx` copy so the route now explicitly promises audit-to-repair continuity. `type-check`, `verify`, `handoff:write`, and live `127.0.0.1:3000` / `/resources?view=system&system=memory-spine` / `/resources?view=surfaces&surface=vault` / `/vehicle?focus=vehicle-connector-onboarding` / `/security?view=ai&focus=security-ai-surface` checks are green after the batch.
- 2026-04-09: Landed `docs/plans/repo-idea-audit-batch3-2026-04-09.md`. Playbooks now follow the same audit-to-repair discipline as the other Resources consoles: `components/home/office/OfficeCommandCenter.tsx` understands focused HQ sessions for strategium, console shell, chronicle, and scheduler repairs; `components/ui/CronSchedulerPanel.tsx` now supports exact landing on composer, governance, and active-job sections with its own focused-session strip; `components/auth/AuthGate.tsx` now preserves focused-session query params across login instead of collapsing deep links back to plain `/hq`; and `lib/engineeringPlaybooks.ts` now routes the strongest refactor and release-hardening jump-offs into those exact HQ and scheduler repair sessions instead of only broad `/hq` drops. `type-check`, `verify`, `hq:e2e`, `handoff:write`, and live `127.0.0.1:3000` / `/resources?view=playbooks` / `/hq?focus=hq-chronicle` / `/hq?focus=hq-console-shell` / `/hq?focus=hq-scheduler-governance` checks are green after the batch.
- 2026-04-09: Landed `docs/plans/repo-idea-audit-batch4-2026-04-09.md`. Playbooks now push the audit-to-repair pattern deeper into the app instead of stopping at route-level landings: `lib/engineeringPlaybooks.ts` retargets the strongest workflow cards to existing exact-panel sessions in COMMAND, VAULT, and RECON, including runtime posture, compiled pages, stewardship, OPSEC, and headers-audit work views. `components/resources/PlaybooksConsole.tsx` now marks those links as `Exact panel` actions so operators can distinguish direct repair sessions from broader route analysis at a glance. `type-check`, `verify`, `handoff:write`, and live `127.0.0.1:3000` / `/resources?view=playbooks` / `/command?focus=runtime-efficiency` / `/vault?focus=vault-stewardship` / `/vault?focus=vault-compiled-pages` / `/recon?view=headers&focus=recon-headers` checks are green after the batch.
- 2026-04-09: Landed `docs/plans/repo-idea-audit-batch5-2026-04-09.md`. `System Design` and `Surfaces` now follow the same audit-to-repair precision as Playbooks instead of mixing exact repair sessions with unlabeled broad route drops. `lib/exactSessionLinks.ts` centralizes the focused-session heuristic, `components/resources/SystemDesignConsole.tsx` and `components/resources/SurfaceCapabilitiesConsole.tsx` now visibly mark `Exact panel` versus `Route` actions, and the broadest remaining HQ and scheduler links were retargeted to `/hq?focus=hq-chronicle` and `/hq?focus=hq-scheduler-governance` through `lib/systemDesignMaps.ts` and `lib/surfaceCapabilities.ts`. `type-check`, `verify`, `handoff:write`, and live `127.0.0.1:3000` / `/resources?view=system` / `/resources?view=surfaces` / `/hq?focus=hq-chronicle` / `/hq?focus=hq-scheduler-governance` checks are green after the batch.
- 2026-04-09: Landed `docs/plans/repo-idea-audit-batch6-2026-04-09.md`. `Impact` now participates in the same diagnose-to-fix pattern instead of stopping at a file list: `lib/impactRepairSessions.ts` maps the highest-value Nexus files to existing exact repair sessions, `components/resources/ProjectImpactConsole.tsx` now surfaces those likely repair sessions directly from the target and review-pack files, and `components/resources/ResourcesWorkbench.tsx` explains that the impact lane can now point straight at the right repair panel when one exists. I also aligned `components/vault/VaultStewardshipPanel.tsx` with the same `Exact panel` language used by the newer Resources consoles, so archive repair actions read like first-class exact sessions instead of anonymous buttons. `type-check`, `verify`, `handoff:write`, and live `127.0.0.1:3000` / `/resources?view=impact` / file-prefilled impact URLs / `/vault?focus=vault-stewardship` checks are green after the batch.
- 2026-04-09: Landed `docs/plans/repo-idea-audit-batch7-2026-04-09.md`. The audit-to-repair flow now uses one quieter shared exact-session action cluster instead of drifting across consoles: `components/ui/ActionSessionCluster.tsx` now defaults to lower-noise rendering without raw hrefs, `components/resources/ProjectImpactConsole.tsx` now uses that shared cluster for likely repair sessions derived from the target and review-pack files, and `components/vault/VaultStewardshipPanel.tsx` now uses the same component for archive repair actions. This keeps Playbooks, System Design, Surfaces, Impact, and VAULT stewardship aligned around the same diagnose-to-fix interaction model while reducing duplicate UI maintenance. `type-check`, `verify`, `handoff:write`, and live `127.0.0.1:3000` / `/resources?view=playbooks` / `/resources?view=system` / `/resources?view=surfaces` / `/resources?view=impact` / `/vault?focus=vault-stewardship` checks are green after the batch.
- 2026-04-09: Landed `docs/plans/repo-idea-audit-batch8-2026-04-09.md`. The remaining subsection and repair cards now participate in the same lighter exact-session pattern instead of carrying their own small action UIs. `components/resources/SurfaceCapabilitiesConsole.tsx` now treats subsection cards as descriptive lane summaries and uses one shared exact-session cluster to open those subsection targets, which reduces button clutter while preserving direct access. `components/vault/CompiledMemoryPagesPanel.tsx` now gives the route-less and untagged repair filters their own compact repair-session cluster, so operators can move straight from the problem view into stewardship, adjacent repair lanes, or graph recovery without backing out first. `type-check`, `verify`, `handoff:write`, and live `127.0.0.1:3000` / `/resources?view=surfaces&surface=vault` / `/vault?focus=vault-compiled-pages&compiledFilter=route-less` / `/vault?focus=vault-compiled-pages&compiledFilter=untagged` checks are green after the batch.
- 2026-04-09: Landed `docs/plans/repo-idea-audit-batch9-2026-04-09.md`. The exact-session repair pattern now reaches deeper architecture and archive drill-down surfaces instead of stopping at the outer audit consoles. `components/resources/SystemDesignConsole.tsx` now uses the shared action-session cluster for impact seeds, so subsystem maps send you into local blast-radius analysis through the same consistent launcher pattern as the rest of Resources. `components/vault/VaultGraphFocusPanel.tsx` now adds compact repair-session actions when the selected graph artifact is isolated, route-less, or untagged, which means graph drill-down can now move directly into orphan recovery, stewardship, or compiled-page repair sessions from the selected artifact itself. `type-check`, `verify`, `handoff:write`, and live `127.0.0.1:3000` / `/resources?view=system&system=memory-spine` / `/vault?focus=vault-graph-focus&graphAudit=orphans` / `/vault?focus=vault-compiled-pages&compiledFilter=route-less` checks are green after the batch.
- 2026-04-09: Landed `docs/plans/repo-idea-audit-batch10-2026-04-09.md`. The audit details themselves are now more interactive instead of acting like static reference text. `components/resources/SystemDesignConsole.tsx` now turns read-first files into direct local impact-analysis launchers through the shared action-session cluster, so architecture orientation can move straight into blast-radius work. `components/vault/VaultGraphFocusPanel.tsx` now lets linked-context items be inspected directly from inside graph drill-down, and `app/vault/page.tsx` now wires the focus panel to update the selected node from those in-panel pivots. `type-check`, `verify`, `handoff:write`, and live `127.0.0.1:3000` / `/resources?view=system&system=memory-spine` / `/vault?focus=vault-graph-focus&graphAudit=orphans` checks are green after the batch.
- 2026-04-09: Landed `docs/plans/obliteratus-assimilation-batch1-2026-04-09.md`. Assimilated the strongest safe idea from `elder-plinius/OBLITERATUS`: make hardening observable, staged, and actionable. `lib/aiHardeningCoverage.ts` now defines a local AI hardening loop plus tracked coverage for the highest-risk Nexus answer surfaces, and `components/security/AIHardeningCoveragePanel.tsx` renders that inside `SECURITY > AI Surface` with visible `Visible evidence posture` versus `Boundary only` status and exact repair-session launchers. This turns hallucination hardening into a live operator console instead of only a hidden prompt rule or playbook note. `type-check`, `verify`, `handoff:write`, and live `127.0.0.1:3000` / `/security?view=ai&focus=security-ai-surface` / `/resources?view=playbooks&playbook=hallucination-hardening` checks are green after the batch.
- 2026-04-09: Landed `docs/plans/obliteratus-assimilation-batch2-2026-04-09.md`. Closed the biggest remaining gap exposed by the new AI hardening coverage map: HQ chronicle replies can now surface visible `Observed / Inferred / Verify next` posture instead of only inheriting the truth boundary silently. `lib/aiTruthBoundary.ts` now defines a reusable compact evidence-footer contract, `components/home/office/prompts.ts` injects it into HQ agent runs, `lib/aiStructuredEvidence.ts` now parses markdown/plain-text evidence footers, and `components/home/office/HQTerminalSection.tsx` renders that posture through the shared evidence panel while stripping the raw footer from the main chronicle bubble. `lib/aiHardeningCoverage.ts` was updated so HQ chronicle is now tracked as visible-evidence posture instead of a boundary-only gap. `type-check`, `verify`, `handoff:write`, and live `127.0.0.1:3000` / `/hq?focus=hq-chronicle` / `/security?view=ai&focus=security-ai-surface` / `/resources?view=playbooks&playbook=hallucination-hardening` checks are green after the batch.
- 2026-04-09: Landed `docs/plans/spec-driven-development-batch1-2026-04-09.md`. Spec-driven development is now a first-class local workflow inside Resources instead of an implicit habit. `lib/specDrivenDevelopment.ts` defines reusable spec-first starters for high-risk changes, safe refactors, security boundaries, archive repair, and future-hardware work, each with problem/non-goal/constraint/acceptance/verification sections plus exact jump-offs into live Nexus surfaces. `components/resources/SpecDrivenConsole.tsx` powers a new `Resources > Specs` lane, `components/resources/ResourcesWorkbench.tsx` and `store/useStore.ts` now support the new view, `lib/engineeringPlaybooks.ts` now includes a `spec-driven-development` playbook, and `lib/surfaceCapabilities.ts` plus `app/resources/page.tsx` now acknowledge the spec-first lane in the Resources audit posture. `type-check`, `verify`, `handoff:write`, and live `127.0.0.1:3000` / `/resources` / `/resources?view=specs` / `/resources?view=playbooks&playbook=spec-driven-development` checks are green after the batch.
- 2026-04-09: Landed `docs/plans/fff-assimilation-batch1-2026-04-09.md`. Assimilated the strongest useful idea from `fff.nvim` as a Nexus-native fast session finder rather than a file-picker clone. `lib/sessionFinder.ts` now builds a local catalog from Playbooks, Specs, System Design maps, Surface audits, and exact repair-session actions, then ranks them with exact-session bias plus local recency memory from browser storage. `components/resources/SessionFinderConsole.tsx` powers a new `Resources > Finder` lane, `components/resources/ResourcesWorkbench.tsx`, `store/useStore.ts`, `app/resources/page.tsx`, and `lib/surfaceCapabilities.ts` now treat the finder as a first-class part of the workbench, and the live route lets operators jump to exact repair panels like `/hq?focus=hq-chronicle` faster than scanning every audit console manually. `type-check`, `verify`, `handoff:write`, and live `127.0.0.1:3000` / `/resources` / `/resources?view=finder` / `/hq?focus=hq-chronicle` checks are green after the batch.
- 2026-04-09: Landed `docs/plans/fff-assimilation-batch2-2026-04-09.md`. Finder now carries seeded working context instead of stopping at broad overview screens. `lib/sessionFinder.ts` adds an optional working-context contract for playbook, spec, system, and surface results, choosing the best next spec starter, impact seed, or exact repair panel from the metadata Nexus already had. `components/resources/SessionFinderConsole.tsx` now lets operators open either the overview result or the strongest seeded next session, and `components/resources/ResourcesWorkbench.tsx` plus `app/resources/page.tsx` now describe Finder as a route-to-work accelerator instead of only a fast route picker. `type-check`, `verify`, `handoff:write`, and live `127.0.0.1:3000` / `/resources?view=finder` / a seeded Impact route / a seeded Specs route checks are green after the batch.
- 2026-04-09: Landed `docs/plans/groq-cloud-chat-recovery-batch1-2026-04-09.md`. Normal cloud inference on `/api/ai` now follows connector posture instead of the same `high_risk` gate as action routes, Settings now marks cloud providers as `blocked by mode` instead of `ready` when `isolated` posture prevents chat, placeholder values no longer count as configured API keys, and standalone/runtime env reads now resolve `.env.local` from the real repo root instead of `.next/standalone`. `lib/agent.ts` also now returns a specific cloud-policy self-heal message when Ollama is unavailable and `/api/ai` is blocked. `type-check`, `verify`, `handoff:write`, `build`, and live `127.0.0.1:3000` / `/hq` checks passed after the batch. The remaining operator step is explicit now: paste a real Groq key again if the old standalone runtime had swallowed it, and switch Network Mode away from `isolated` to use cloud chat.
- 2026-04-09: Completed the remaining Groq/browser-policy gap from `docs/plans/groq-cloud-chat-recovery-batch1-2026-04-09.md`. `lib/security/runtimePolicyCookies.ts`, `app/api/settings/route.ts`, `middleware.ts`, and `components/ui/RuntimePolicyCookieSync.tsx` now keep middleware-visible network posture in sync with saved Settings, which means changing Network Mode to `internal` or `connected` immediately stops stale browser sessions from blocking `/api/ai` as `isolated`. Live proof is now real: authenticated `GET /api/settings` emits the policy cookies, and authenticated `POST /api/ai` succeeds against Groq with `X-Model: qwen/qwen3-32b` instead of returning `Blocked by network policy`.
- 2026-04-09: Landed `docs/plans/claude-code-obsidian-second-brain-batch1-2026-04-09.md`. The verified X link resolved to an AI Edge article titled `Claude Code + Obsidian Ultimate Guide (build an AI second brain)`, and the strongest Nexus-native parts are now implemented without cloning another app. `lib/secondBrainExport.ts` plus `components/vault/VaultExport.tsx` now build an Obsidian-ready markdown second-brain pack from saved clips and compiled pages, including YAML frontmatter, wiki-style related links, a durable index note, a heartbeat checklist note, and local folder export when the browser supports the File System Access API (with combined-markdown download fallback). `lib/specDrivenDevelopment.ts` now includes a `second-brain-system` spec starter, `lib/engineeringPlaybooks.ts` adds a `second-brain-heartbeat` playbook, and `components/ui/cronSchedulerPanelUtils.ts` adds a scheduler-ready `Second brain heartbeat` mission template so archive upkeep can recur intentionally instead of depending on memory. `type-check`, `verify`, `handoff:write`, `build`, and live `127.0.0.1:3000` / `/vault` / `/resources?view=specs&spec=second-brain-system` / `/resources?view=playbooks&playbook=second-brain-heartbeat` checks are green after the batch.
- 2026-04-09: Landed `docs/plans/ghidra-assimilation-batch1-2026-04-09.md`. Borrowed the strongest portable idea from `nationalsecurityagency/ghidra` without pretending Nexus is a browser decompiler: `lib/binaryTriage.ts` now provides a local-only suspicious-file triage contract for format detection, entropy, printable strings, IOC extraction, and operator notes; `components/recon/BinaryTriagePanel.tsx` exposes that as an in-browser reverse-engineering prep lane that never uploads the sample; and `app/recon/page.tsx` now gives RECON a focused `binary` session alongside OSINT, headers, metadata, and OPSEC. `lib/surfaceCapabilities.ts` was also updated so Resources advertises the new reverse-engineering prep lane honestly and can deep-link straight into `/recon?view=binary&focus=recon-binary`. `type-check`, `verify`, `handoff:write`, and live `127.0.0.1:3000` / `/hq` / `/recon?view=binary&focus=recon-binary` / `/resources?view=surfaces&surface=recon` checks are green after the batch.
- 2026-04-09: Landed `docs/plans/claude-code-obsidian-ghidra-batch1-2026-04-09.md`. The second-brain and Ghidra ideas now meet in a durable reverse-engineering memory loop instead of two separate features. `components/recon/BinaryTriagePanel.tsx` now files binary triage reports into VAULT through the compiled-memory contract and reuses the shared continuation actions so RECON can keep moving into VAULT or memory follow-up after filing. `lib/secondBrainExport.ts` now recognizes those filed reports as first-class `Reverse Engineering Prep` notes in the Obsidian-ready markdown pack, adds reverse-engineering counts to the export summary, and surfaces them in the index and heartbeat notes. `components/vault/VaultExport.tsx` now reports those reverse-engineering note counts during export, and `components/vault/CompiledMemoryPagesPanel.tsx` now gives binary triage artifacts their own visual treatment so they stand out from generic compiled pages inside VAULT. `type-check`, `verify`, `handoff:write`, and live `127.0.0.1:3000` / `/recon?view=binary&focus=recon-binary` / `/vault?focus=vault-compiled-pages` / `/resources?view=playbooks&playbook=second-brain-heartbeat` / `/resources?view=surfaces&surface=recon` checks are green after the batch.
- 2026-04-09: Landed `docs/plans/claude-code-obsidian-ghidra-batch2-2026-04-09.md`. Reverse-engineering follow-through is now a first-class Resources workflow instead of an implicit side path. `lib/specDrivenDevelopment.ts` now has a dedicated `reverse-engineering-memory` spec starter for local-only sample boundaries, durable triage artifacts, and RECON-to-VAULT continuity. `lib/engineeringPlaybooks.ts` now has a matching `reverse-engineering-follow-through` playbook that connects RECON binary triage, VAULT archive review, and second-brain upkeep as one guided workflow. `lib/sessionFinder.ts` was also improved so spec entries prefer exact-session working context when available, which means the new reverse-engineering spec can jump straight into `/recon?view=binary&focus=recon-binary` instead of always defaulting to Impact-first follow-up. `type-check`, `verify`, `handoff:write`, and live `127.0.0.1:3000` / `/resources?view=specs&spec=reverse-engineering-memory` / `/resources?view=playbooks&playbook=reverse-engineering-follow-through` / `/resources?view=finder` / `/recon?view=binary&focus=recon-binary` checks are green after the batch.
- 2026-04-09: Landed `docs/plans/claude-code-obsidian-ghidra-batch3-2026-04-09.md`. Reverse-engineering prep is now maintained like a real archive lane instead of a one-off durable note class. `lib/binaryTriage.ts` now exports a shared reverse-engineering artifact detector so VAULT, exports, and upkeep cues agree on what counts as binary triage memory. `components/vault/CompiledMemoryPagesPanel.tsx` now supports a dedicated `compiledFilter=reverse-engineering` repair session with exact follow-up actions, `lib/vaultStewardship.ts` plus `components/vault/VaultStewardshipPanel.tsx` now track reverse-engineering prep posture and recommend the strongest next exact repair session from current archive state in an `autoskills`-style way, and `lib/secondBrainExport.ts` now surfaces route/tag upkeep for reverse-engineering notes explicitly inside the heartbeat note. I also extended `lib/engineeringPlaybooks.ts` so both the second-brain heartbeat and reverse-engineering follow-through playbooks can jump directly into the new RE maintenance lane. `type-check`, `verify`, `handoff:write`, and live `127.0.0.1:3000` / `/vault?focus=vault-stewardship` / `/vault?focus=vault-compiled-pages&compiledFilter=reverse-engineering` / `/resources?view=playbooks&playbook=second-brain-heartbeat` / `/recon?view=binary&focus=recon-binary` checks are green after the batch.
- 2026-04-09: Landed `docs/plans/claude-code-obsidian-ghidra-batch4-2026-04-09.md`. Reverse-engineering upkeep is now easier to reopen from both sides of the loop. `lib/secondBrainExport.ts` now includes a dedicated `02 Reverse Engineering Prep Index.md` note in the Obsidian-ready pack, with reverse-engineering counts, maintenance cues, and direct Nexus reopen links for RECON triage, the VAULT RE repair lane, and second-brain heartbeat review. `lib/sessionFinder.ts` now exposes a first-class `Reverse-engineering maintenance` Finder result with exact working-context jump into `/vault?focus=vault-compiled-pages&compiledFilter=reverse-engineering`, so the workflow is easier to find than relying on playbook/spec discovery alone. `type-check`, `verify`, `handoff:write`, and live `127.0.0.1:3000` / `/resources?view=finder` / `/resources?view=playbooks&playbook=reverse-engineering-follow-through` / `/vault?focus=vault-compiled-pages&compiledFilter=reverse-engineering` / `/recon?view=binary&focus=recon-binary` checks are green after the batch.
- 2026-04-09: Landed `docs/plans/claude-code-obsidian-ghidra-batch5-2026-04-09.md`. Durable reverse-engineering prep can now be promoted into a higher-order analyst brief without an AI dependency. `lib/binaryTriage.ts` now builds deterministic reverse-engineering brief drafts from filed triage notes, `components/vault/CompiledMemoryPagesPanel.tsx` now exposes a duplicate-safe `Promote to brief` path for reverse-engineering prep and visually distinguishes promoted briefs from raw triage, `lib/secondBrainExport.ts` now exports those promoted briefs as their own note class with a dedicated `Reverse Engineering Briefs Index`, and `lib/vaultStewardship.ts` plus `components/vault/VaultStewardshipPanel.tsx` now surface promoted brief counts alongside the raw prep lane. `type-check`, `verify`, `handoff:write`, and live `127.0.0.1:3000` / `/vault?focus=vault-compiled-pages&compiledFilter=reverse-engineering` / `/recon?view=binary&focus=recon-binary` / `/resources?view=playbooks&playbook=reverse-engineering-follow-through` checks are green after the batch.
- 2026-04-09: Landed `docs/plans/hq-chronicle-recovery-batch1-2026-04-09.md`. HQ chronicle now self-heals the two user-facing failures that were showing up together: `lib/aiThinkingTrace.ts` extracts raw `<think>` blocks, `lib/agent.ts` now sanitizes final answers at the runtime boundary and reroutes leaked reasoning into a compact agent-runtime step instead of the visible chat bubble, and `components/home/office/HQTerminalSection.tsx` applies the same cleanup as a final render-side guard so malformed or older replies still render safely. I also fixed the chat layout contract in `components/home/office/OfficeCommandCenter.tsx` and `components/home/office/HQTerminalSection.tsx` so the chronicle viewport owns scrolling while the lesson/composer area stays pinned; the old `scrollIntoView()` behavior was replaced with explicit viewport scrolling to stop the whole page from drifting after the first response. `tests/e2e/hq-shell.spec.ts` now stubs a raw `<think>` reply and proves both that the composer stays visible and that the chronicle only shows the final answer. `type-check`, `verify`, `hq:e2e`, `handoff:write`, `build`, and live `127.0.0.1:3000` / `/hq` checks are green after the batch.
- 2026-04-10: Landed `docs/plans/ollama-groq-connectivity-batch2-2026-04-10.md`. Live diagnosis showed the operator machine had Ollama reachable on `localhost:11434` with installed models like `gemma4:latest`, but Nexus could still fall through to blocked cloud chat because a stale persisted local model name (`qwen2.5:7b`) was being treated like a runtime outage, and the saved server network posture in `.env.local` was still `isolated`. `lib/ollamaModelResolver.ts` now centralizes local model-catalog reads, string/object Ollama error parsing, stale-model detection, and compatible fallback selection. `lib/agent.ts` now retries with a detected installed model before declaring Ollama unavailable, persists the repaired local model when the configured local lane was stale, and distinguishes missing-model failures from true runtime reachability failures in operator messaging. `lib/ai.ts` now applies the same self-heal to direct-call and streaming local AI surfaces, so compact AI panels stop silently failing on the same stale model drift. `components/settings/SettingsDrawer.tsx` now shows the real local Ollama runtime posture, detected installed models, a `Use detected model` repair action, and stronger saved-vs-unsaved server mode guidance so cloud readiness is tied to the actually saved server policy instead of the unsaved select state in the drawer. I also aligned the saved runtime mode with the user’s stated `internal` preference in `.env.local`, added coverage for the resolver and local-model recovery path in `__tests__/ollamaModelResolver.test.ts` and `__tests__/ai.test.ts`, and recorded two new general rules in `tasks/lessons.md`: missing-model drift must self-heal before declaring Ollama down, and mixed local/server settings surfaces must mark unsaved server changes clearly. `type-check`, `verify`, `build`, `hq:e2e`, and live `127.0.0.1:3000` / `/hq` checks are green after the batch. Targeted Vitest files were added but the machine still lacks a runnable local `vitest` binary, so repo-level verification plus browser/runtime checks remained the authoritative proof.
- 2026-04-10: Landed `docs/plans/session-integrity-batch1-2026-04-10.md`. Exact-session continuity now has a canonical link-association layer in `lib/exactSessionLinks.ts`, including stale path aliases (`/home` -> `/hq`, `/internal/vehicle` -> `/vehicle`), Resources view inference/corrections, route-aware `focus` to `view` ownership for segmented tabs, and VAULT repair-filter / graph-audit normalization. `hooks/useSessionHrefAutoHeal.ts` now quietly repairs malformed or stale session URLs on the main route shells and HQ without adding more UI, while `components/ui/ActionSessionCluster.tsx` and `components/resources/SessionFinderConsole.tsx` normalize launch targets before navigation so old links and new launchers resolve to the same working session. I also expanded the behavior-first roadmap with a dedicated BF6 safeguards/self-heal workstream and added a new general lesson: link repair rules must stay centralized instead of drifting into one-off console logic. `type-check`, `verify`, `handoff:write`, and live repair-route checks stayed green after the batch.
- 2026-04-10: Landed `docs/plans/behavior-first-bf1-batch1-2026-04-10.md`. HQ now classifies prompts before generation through `components/home/office/hqAnswerStyle.ts`, so simple chat, product help, repo work, live/current questions, and structured workflow turns no longer all share the same memo-style operator reply posture. `components/home/office/OfficeCommandCenter.tsx` now scopes context injection by answer style, retries live/current questions once with a retrieval-forcing directive when no verified retrieval step occurs, and marks the answer unverified instead of pretending local context was enough. `components/home/office/HQTerminalSection.tsx` now suppresses evidence posture for low-friction chat while still stripping inline evidence blocks from the main bubble, and `tests/e2e/hq-shell.spec.ts` plus `__tests__/hqAnswerStyle.test.ts` cover memo-style reply self-heal plus the new routing rules. `verify` and `hq:e2e` are green after the batch; `type-check` required a clean `.next/types` rebuild before final confirmation because the existing runtime had stale generated type entries again.
- 2026-04-10: Landed `docs/plans/assistant-first-batch1-2026-04-10.md`. HQ now has a quiet assistant-orchestration layer instead of treating Resources as a manual prerequisite. `components/home/office/hqAssistantContext.ts` adds ranked internal context resolution across Surfaces, System Design, Specs, Playbooks, and Impact, choosing only the strongest attachments plus one prepared workspace target for the current turn. `lib/assistantSessionRegistry.ts` centralizes assistant-facing exact sessions and canonical prepared-workspace targets, `store/useStore.ts` persists a normalized prepared-workspace session with intent/source metadata, and `components/ui/PreparedWorkspaceAutoHeal.tsx` upgrades broad route opens into staged exact sessions when the prepared target is still fresh. `components/home/office/OfficeCommandCenter.tsx` now feeds the assistant context into the prompt quietly, stages one strongest workspace after the reply instead of forcing route jumps mid-turn, and stores the prepared workspace on the chronicle message. `components/home/office/HQTerminalSection.tsx` now surfaces that as a compact `Workspace prepared` continuation instead of another control row. Regression coverage now includes prepared-session auto-heal through auth in `tests/e2e/route-contract.spec.ts`. `type-check`, `verify`, `hq:e2e`, `route:e2e`, and `build` all passed after the batch.
- 2026-04-10: Landed `docs/plans/assistant-first-batch2-2026-04-10.md`. Assistant-first continuity now survives beyond the immediate reply. `lib/assistantSessionMemory.ts` adds canonical unfinished-session memory with pruning, dedupe, route-aware ranking, and intent-sensitive boosts. `store/useStore.ts` now persists `unfinishedSessions[]` plus `rememberUnfinishedSession(...)` and `touchUnfinishedSession(...)`, with migration-time pruning so stale memory decays safely. `components/home/office/hqAssistantContext.ts` now prefers a strong unfinished exact session before inventing a fresh prepared workspace, `components/home/office/OfficeCommandCenter.tsx` now remembers high-confidence prepared sessions as unfinished work after successful turns, and `components/ui/PreparedWorkspaceAutoHeal.tsx` now upgrades broad route opens from either a fresh prepared workspace or the strongest unfinished exact session for that route. `tests/e2e/route-contract.spec.ts` now proves that stored unfinished RE work self-heals a broad `/recon` open into `/recon?view=binary&focus=recon-binary`. `type-check`, `verify`, `route:e2e`, `hq:e2e`, and `build` all passed after the batch.
- 2026-04-10: Landed `docs/plans/assistant-first-batch3-2026-04-10.md`. Phase 3 assistant-first behavior is now real in HQ without adding new top-level UI. `lib/assistantCapabilityRegistry.ts` centralizes assistant-facing route aliases, segmented exact-session view rules, and capability-to-surface/system/spec/playbook associations so assistant routing stops drifting across separate helpers. `lib/assistantIndexedRetrieval.ts` turns existing Surfaces, System Design, Specs, Playbooks, and Impact metadata into indexed context documents, and `components/home/office/hqAssistantContext.ts` now uses that ranked retrieval layer to attach only the strongest internal scaffolding quietly. `app/api/assistant/retrieve/route.ts` plus `lib/assistantLiveRetrieval.ts` add a protected feed-first verification path for latest/current HQ turns, using internal market/news/cyber routes first and a degraded open-web fallback before allowing unverified copy. `components/home/office/hqAnswerStyle.ts` now distinguishes `assistant` / `evidence` / `workflow` reply kinds, `components/home/office/OfficeCommandCenter.tsx` preflights live retrieval and feeds it into the agent prompt before retrying tool verification, and `components/home/office/HQTerminalSection.tsx` now suppresses repeated prompt/workflow rows after a real conversation starts and hides persisted tool trace on assistant-first replies. `lib/exactSessionLinks.ts` now consumes canonical route/view mappings from the capability registry, `scripts/eval-agent-runtime.js` now verifies the new assistant response/registry/retrieval layers, and new unit coverage was added in `__tests__/assistantCapabilityRegistry.test.ts`, `__tests__/assistantIndexedRetrieval.test.ts`, and `__tests__/hqAnswerStyle.test.ts`. `type-check`, `verify`, `eval:agent-runtime:ci`, `route:e2e`, `hq:e2e`, and `build` all passed after the batch; the one correction during verification was procedural, not code-related: Playwright and build/runtime tasks must stay serial because they share `.next` and port/runtime state.
