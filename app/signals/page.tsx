// ── signals/page ────────────────────────────────────────────
// SIGNALS tab: live news feed with bias filter, threads, topic clustering.

import { redirect } from 'next/navigation'

export default function SignalsPage() {
  redirect('/intel')
}
